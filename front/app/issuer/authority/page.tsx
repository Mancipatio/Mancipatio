// Operational authority and upgrade-authority wallets need no global Admin
// record before accepting/bootstrap. Each action verifies its own chain proof.
export { default } from "@/app/admin/platform/page";
