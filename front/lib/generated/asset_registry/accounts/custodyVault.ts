/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getRealizeActionDecoder,
  getRealizeActionEncoder,
  getVaultStateDecoder,
  getVaultStateEncoder,
  getVaultTypeDecoder,
  getVaultTypeEncoder,
  type RealizeAction,
  type RealizeActionArgs,
  type VaultState,
  type VaultStateArgs,
  type VaultType,
  type VaultTypeArgs,
} from "../types";

export const CUSTODY_VAULT_DISCRIMINATOR = new Uint8Array([
  9, 225, 23, 25, 23, 223, 65, 72,
]);

export function getCustodyVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CUSTODY_VAULT_DISCRIMINATOR,
  );
}

export type CustodyVault = {
  discriminator: ReadonlyUint8Array;
  shareClass: Address;
  mint: Address;
  /**
   * Token-2022 escrow account holding the custodied tokens; its authority is
   * this `CustodyVault` PDA.
   */
  escrow: Address;
  vaultId: bigint;
  /** Who may `trigger` the vault. */
  authority: Address;
  vaultType: VaultType;
  realizeAction: RealizeAction;
  /** Expected custodied amount (informational; escrow balance is source of truth). */
  amount: bigint;
  state: VaultState;
  /** Unix ts after which the vault may be reverted. */
  deadline: bigint;
  /** SHA-256 of the off-chain agreement / conversion document. */
  metadataHash: ReadonlyUint8Array;
  /**
   * Recipient of `return_custody_vault` (DeliveryEscrow only).
   * `Pubkey::default()` when unused.
   */
  beneficiary: Address;
  version: number;
  bump: number;
  /**
   * **Deposit ledger** — the running total the beneficiary has put into this
   * escrow through `deposit_to_custody_vault` (the only instruction that
   * increments it). It is what makes `return_custody_vault`'s KYC exemption
   * evidence-based instead of assumed: a return of at most `deposited` is a
   * refund of the beneficiary's OWN units and needs no receiver KYC (a
   * lapsed passport must never strand a holder's deposit), while anything
   * beyond it did not come from the beneficiary — the only other way units
   * can land in the escrow is a raw `transfer_checked` from elsewhere (e.g.
   * freshly emitted treasury units) — and is released only to a receiver
   * whose `KycEntry` passes. Appended last so the account's existing byte
   * layout is unchanged up to `bump`.
   */
  deposited: bigint;
};

export type CustodyVaultArgs = {
  shareClass: Address;
  mint: Address;
  /**
   * Token-2022 escrow account holding the custodied tokens; its authority is
   * this `CustodyVault` PDA.
   */
  escrow: Address;
  vaultId: number | bigint;
  /** Who may `trigger` the vault. */
  authority: Address;
  vaultType: VaultTypeArgs;
  realizeAction: RealizeActionArgs;
  /** Expected custodied amount (informational; escrow balance is source of truth). */
  amount: number | bigint;
  state: VaultStateArgs;
  /** Unix ts after which the vault may be reverted. */
  deadline: number | bigint;
  /** SHA-256 of the off-chain agreement / conversion document. */
  metadataHash: ReadonlyUint8Array;
  /**
   * Recipient of `return_custody_vault` (DeliveryEscrow only).
   * `Pubkey::default()` when unused.
   */
  beneficiary: Address;
  version: number;
  bump: number;
  /**
   * **Deposit ledger** — the running total the beneficiary has put into this
   * escrow through `deposit_to_custody_vault` (the only instruction that
   * increments it). It is what makes `return_custody_vault`'s KYC exemption
   * evidence-based instead of assumed: a return of at most `deposited` is a
   * refund of the beneficiary's OWN units and needs no receiver KYC (a
   * lapsed passport must never strand a holder's deposit), while anything
   * beyond it did not come from the beneficiary — the only other way units
   * can land in the escrow is a raw `transfer_checked` from elsewhere (e.g.
   * freshly emitted treasury units) — and is released only to a receiver
   * whose `KycEntry` passes. Appended last so the account's existing byte
   * layout is unchanged up to `bump`.
   */
  deposited: number | bigint;
};

/** Gets the encoder for {@link CustodyVaultArgs} account data. */
export function getCustodyVaultEncoder(): FixedSizeEncoder<CustodyVaultArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["shareClass", getAddressEncoder()],
      ["mint", getAddressEncoder()],
      ["escrow", getAddressEncoder()],
      ["vaultId", getU64Encoder()],
      ["authority", getAddressEncoder()],
      ["vaultType", getVaultTypeEncoder()],
      ["realizeAction", getRealizeActionEncoder()],
      ["amount", getU64Encoder()],
      ["state", getVaultStateEncoder()],
      ["deadline", getI64Encoder()],
      ["metadataHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["beneficiary", getAddressEncoder()],
      ["version", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["deposited", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CUSTODY_VAULT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link CustodyVault} account data. */
export function getCustodyVaultDecoder(): FixedSizeDecoder<CustodyVault> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["shareClass", getAddressDecoder()],
    ["mint", getAddressDecoder()],
    ["escrow", getAddressDecoder()],
    ["vaultId", getU64Decoder()],
    ["authority", getAddressDecoder()],
    ["vaultType", getVaultTypeDecoder()],
    ["realizeAction", getRealizeActionDecoder()],
    ["amount", getU64Decoder()],
    ["state", getVaultStateDecoder()],
    ["deadline", getI64Decoder()],
    ["metadataHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["beneficiary", getAddressDecoder()],
    ["version", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["deposited", getU64Decoder()],
  ]);
}

/** Gets the codec for {@link CustodyVault} account data. */
export function getCustodyVaultCodec(): FixedSizeCodec<
  CustodyVaultArgs,
  CustodyVault
> {
  return combineCodec(getCustodyVaultEncoder(), getCustodyVaultDecoder());
}

export function decodeCustodyVault<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<CustodyVault, TAddress>;
export function decodeCustodyVault<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<CustodyVault, TAddress>;
export function decodeCustodyVault<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<CustodyVault, TAddress> | MaybeAccount<CustodyVault, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getCustodyVaultDecoder(),
  );
}

export async function fetchCustodyVault<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<CustodyVault, TAddress>> {
  const maybeAccount = await fetchMaybeCustodyVault(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeCustodyVault<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<CustodyVault, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeCustodyVault(maybeAccount);
}

export async function fetchAllCustodyVault(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<CustodyVault>[]> {
  const maybeAccounts = await fetchAllMaybeCustodyVault(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeCustodyVault(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<CustodyVault>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeCustodyVault(maybeAccount));
}

export function getCustodyVaultSize(): number {
  return 237;
}
