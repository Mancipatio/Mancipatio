/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const ISSUER_RECOVERY_DISCRIMINATOR = new Uint8Array([
  48, 126, 235, 154, 174, 129, 80, 187,
]);

export function getIssuerRecoveryDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ISSUER_RECOVERY_DISCRIMINATOR,
  );
}

export type IssuerRecovery = {
  discriminator: ReadonlyUint8Array;
  /** Byte 8: the Issuer PDA. */
  issuer: Address;
  /**
   * Byte 40: `issuer.authority` when proposed; execute requires it to still
   * be the live authority (a rotation in between makes the recovery stale).
   */
  currentAuthority: Address;
  /** Byte 72: the only key that can execute (it signs). */
  newAuthority: Address;
  /**
   * Byte 104: the proposing super admin; execute requires it to still be
   * `platform.admin`. Receives every refund (this rent and the old grant's).
   */
  proposedBy: Address;
  /** Byte 136. */
  proposedAt: bigint;
  /** Byte 144: executable at or after this unix ts. */
  eta: bigint;
  /** Byte 152: `eta + ISSUER_RECOVERY_EXECUTION_WINDOW`; executable strictly before it. */
  expiresAt: bigint;
  /** Byte 160: `STATE_VERSION`. */
  version: number;
  /** Byte 161. */
  bump: number;
};

export type IssuerRecoveryArgs = {
  /** Byte 8: the Issuer PDA. */
  issuer: Address;
  /**
   * Byte 40: `issuer.authority` when proposed; execute requires it to still
   * be the live authority (a rotation in between makes the recovery stale).
   */
  currentAuthority: Address;
  /** Byte 72: the only key that can execute (it signs). */
  newAuthority: Address;
  /**
   * Byte 104: the proposing super admin; execute requires it to still be
   * `platform.admin`. Receives every refund (this rent and the old grant's).
   */
  proposedBy: Address;
  /** Byte 136. */
  proposedAt: number | bigint;
  /** Byte 144: executable at or after this unix ts. */
  eta: number | bigint;
  /** Byte 152: `eta + ISSUER_RECOVERY_EXECUTION_WINDOW`; executable strictly before it. */
  expiresAt: number | bigint;
  /** Byte 160: `STATE_VERSION`. */
  version: number;
  /** Byte 161. */
  bump: number;
};

/** Gets the encoder for {@link IssuerRecoveryArgs} account data. */
export function getIssuerRecoveryEncoder(): FixedSizeEncoder<IssuerRecoveryArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["issuer", getAddressEncoder()],
      ["currentAuthority", getAddressEncoder()],
      ["newAuthority", getAddressEncoder()],
      ["proposedBy", getAddressEncoder()],
      ["proposedAt", getI64Encoder()],
      ["eta", getI64Encoder()],
      ["expiresAt", getI64Encoder()],
      ["version", getU8Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ISSUER_RECOVERY_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link IssuerRecovery} account data. */
export function getIssuerRecoveryDecoder(): FixedSizeDecoder<IssuerRecovery> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["issuer", getAddressDecoder()],
    ["currentAuthority", getAddressDecoder()],
    ["newAuthority", getAddressDecoder()],
    ["proposedBy", getAddressDecoder()],
    ["proposedAt", getI64Decoder()],
    ["eta", getI64Decoder()],
    ["expiresAt", getI64Decoder()],
    ["version", getU8Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link IssuerRecovery} account data. */
export function getIssuerRecoveryCodec(): FixedSizeCodec<
  IssuerRecoveryArgs,
  IssuerRecovery
> {
  return combineCodec(getIssuerRecoveryEncoder(), getIssuerRecoveryDecoder());
}

export function decodeIssuerRecovery<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<IssuerRecovery, TAddress>;
export function decodeIssuerRecovery<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<IssuerRecovery, TAddress>;
export function decodeIssuerRecovery<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<IssuerRecovery, TAddress> | MaybeAccount<IssuerRecovery, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getIssuerRecoveryDecoder(),
  );
}

export async function fetchIssuerRecovery<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<IssuerRecovery, TAddress>> {
  const maybeAccount = await fetchMaybeIssuerRecovery(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeIssuerRecovery<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<IssuerRecovery, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeIssuerRecovery(maybeAccount);
}

export async function fetchAllIssuerRecovery(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<IssuerRecovery>[]> {
  const maybeAccounts = await fetchAllMaybeIssuerRecovery(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeIssuerRecovery(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<IssuerRecovery>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeIssuerRecovery(maybeAccount),
  );
}

export function getIssuerRecoverySize(): number {
  return 162;
}
