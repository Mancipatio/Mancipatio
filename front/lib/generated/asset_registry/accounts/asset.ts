/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAssetStatusDecoder,
  getAssetStatusEncoder,
  getAssetTypeDecoder,
  getAssetTypeEncoder,
  getJurisdictionRulesDecoder,
  getJurisdictionRulesEncoder,
  type AssetStatus,
  type AssetStatusArgs,
  type AssetType,
  type AssetTypeArgs,
  type JurisdictionRules,
  type JurisdictionRulesArgs,
} from "../types";

export const ASSET_DISCRIMINATOR = new Uint8Array([
  234, 180, 241, 252, 139, 224, 160, 8,
]);

export function getAssetDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ASSET_DISCRIMINATOR);
}

export type Asset = {
  discriminator: ReadonlyUint8Array;
  issuer: Address;
  assetId: string;
  assetType: AssetType;
  name: string;
  symbolPrefix: string;
  /** SHA-256 of the off-chain legal documentation (term sheet, prospectus…). */
  legalDocHash: ReadonlyUint8Array;
  jurisdictionRules: JurisdictionRules;
  status: AssetStatus;
  shareClassesCount: number;
  /**
   * `None` = use the global KYC registry; `Some` = stricter per-issuer
   * registry (extra restrictions only, never a relaxation). docs/01 §9.
   *
   * NOTE: Token-2022 permanent-delegate clawback is **always-on** — Mancipatio
   * is the permanent delegate on every share-class mint (clawback / regulatory
   * orders / forced delivery), applied at mint creation, not a per-asset flag
   * (Answers for Mladen §2).
   */
  extraKycRegistry: Option<Address>;
  version: number;
  bump: number;
};

export type AssetArgs = {
  issuer: Address;
  assetId: string;
  assetType: AssetTypeArgs;
  name: string;
  symbolPrefix: string;
  /** SHA-256 of the off-chain legal documentation (term sheet, prospectus…). */
  legalDocHash: ReadonlyUint8Array;
  jurisdictionRules: JurisdictionRulesArgs;
  status: AssetStatusArgs;
  shareClassesCount: number;
  /**
   * `None` = use the global KYC registry; `Some` = stricter per-issuer
   * registry (extra restrictions only, never a relaxation). docs/01 §9.
   *
   * NOTE: Token-2022 permanent-delegate clawback is **always-on** — Mancipatio
   * is the permanent delegate on every share-class mint (clawback / regulatory
   * orders / forced delivery), applied at mint creation, not a per-asset flag
   * (Answers for Mladen §2).
   */
  extraKycRegistry: OptionOrNullable<Address>;
  version: number;
  bump: number;
};

/** Gets the encoder for {@link AssetArgs} account data. */
export function getAssetEncoder(): Encoder<AssetArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["issuer", getAddressEncoder()],
      ["assetId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["assetType", getAssetTypeEncoder()],
      ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["symbolPrefix", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["legalDocHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["jurisdictionRules", getJurisdictionRulesEncoder()],
      ["status", getAssetStatusEncoder()],
      ["shareClassesCount", getU8Encoder()],
      ["extraKycRegistry", getOptionEncoder(getAddressEncoder())],
      ["version", getU8Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ASSET_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Asset} account data. */
export function getAssetDecoder(): Decoder<Asset> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["issuer", getAddressDecoder()],
    ["assetId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["assetType", getAssetTypeDecoder()],
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["symbolPrefix", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["legalDocHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["jurisdictionRules", getJurisdictionRulesDecoder()],
    ["status", getAssetStatusDecoder()],
    ["shareClassesCount", getU8Decoder()],
    ["extraKycRegistry", getOptionDecoder(getAddressDecoder())],
    ["version", getU8Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Asset} account data. */
export function getAssetCodec(): Codec<AssetArgs, Asset> {
  return combineCodec(getAssetEncoder(), getAssetDecoder());
}

export function decodeAsset<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Asset, TAddress>;
export function decodeAsset<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Asset, TAddress>;
export function decodeAsset<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Asset, TAddress> | MaybeAccount<Asset, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getAssetDecoder(),
  );
}

export async function fetchAsset<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Asset, TAddress>> {
  const maybeAccount = await fetchMaybeAsset(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeAsset<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Asset, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeAsset(maybeAccount);
}

export async function fetchAllAsset(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Asset>[]> {
  const maybeAccounts = await fetchAllMaybeAsset(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeAsset(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Asset>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeAsset(maybeAccount));
}
