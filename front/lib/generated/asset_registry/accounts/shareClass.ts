/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getShareClassTypeDecoder,
  getShareClassTypeEncoder,
  type ShareClassType,
  type ShareClassTypeArgs,
} from "../types";

export const SHARE_CLASS_DISCRIMINATOR = new Uint8Array([
  169, 98, 255, 104, 159, 47, 105, 6,
]);

export function getShareClassDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SHARE_CLASS_DISCRIMINATOR);
}

export type ShareClass = {
  discriminator: ReadonlyUint8Array;
  asset: Address;
  /**
   * Token-2022 mint. `Pubkey::default()` until `initialize_share_class_mint`
   * (next increment) creates the mint with the transfer-hook extension.
   */
  mint: Address;
  classIndex: number;
  classType: ShareClassType;
  /** Bitfield of `RIGHT_*` flags (docs/01 §3). */
  rightsBitfield: number;
  /** Liquidation-preference multiplier in bps (10000 = 1.0x, 15000 = 1.5x). */
  liqPrefMultiplierBps: number;
  /** Lower number = more senior in the liquidation waterfall. */
  liqSeniority: number;
  /** Votes per token unit. */
  votingWeight: number;
  /** Share class this one converts into, if any. */
  convertibleTo: Option<Address>;
  /**
   * Supply cap: lifetime for PhysicalGood, circulating for other assets.
   * `None` = uncapped (never permitted for PhysicalGood).
   */
  maxSupply: Option<bigint>;
  circulatingSupply: bigint;
  lockedSupply: bigint;
  /** docs/01 §9 Q6: post-launch minting (dilution) is gated behind this flag. */
  mintablePostLaunch: boolean;
  /** Set once the Token-2022 mint has been created for this class. */
  mintInitialized: boolean;
  /** Once locked, no further minting unless `mintable_post_launch` is set. */
  supplyLocked: boolean;
  version: number;
  bump: number;
  /** Cumulative issued units; burns never reduce this counter (v2). */
  lifetimeMinted: bigint;
  /** PhysicalGood caps are cumulative; other assets retain circulating caps. */
  cumulativeCap: boolean;
};

export type ShareClassArgs = {
  asset: Address;
  /**
   * Token-2022 mint. `Pubkey::default()` until `initialize_share_class_mint`
   * (next increment) creates the mint with the transfer-hook extension.
   */
  mint: Address;
  classIndex: number;
  classType: ShareClassTypeArgs;
  /** Bitfield of `RIGHT_*` flags (docs/01 §3). */
  rightsBitfield: number;
  /** Liquidation-preference multiplier in bps (10000 = 1.0x, 15000 = 1.5x). */
  liqPrefMultiplierBps: number;
  /** Lower number = more senior in the liquidation waterfall. */
  liqSeniority: number;
  /** Votes per token unit. */
  votingWeight: number;
  /** Share class this one converts into, if any. */
  convertibleTo: OptionOrNullable<Address>;
  /**
   * Supply cap: lifetime for PhysicalGood, circulating for other assets.
   * `None` = uncapped (never permitted for PhysicalGood).
   */
  maxSupply: OptionOrNullable<number | bigint>;
  circulatingSupply: number | bigint;
  lockedSupply: number | bigint;
  /** docs/01 §9 Q6: post-launch minting (dilution) is gated behind this flag. */
  mintablePostLaunch: boolean;
  /** Set once the Token-2022 mint has been created for this class. */
  mintInitialized: boolean;
  /** Once locked, no further minting unless `mintable_post_launch` is set. */
  supplyLocked: boolean;
  version: number;
  bump: number;
  /** Cumulative issued units; burns never reduce this counter (v2). */
  lifetimeMinted: number | bigint;
  /** PhysicalGood caps are cumulative; other assets retain circulating caps. */
  cumulativeCap: boolean;
};

/** Gets the encoder for {@link ShareClassArgs} account data. */
export function getShareClassEncoder(): Encoder<ShareClassArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["asset", getAddressEncoder()],
      ["mint", getAddressEncoder()],
      ["classIndex", getU8Encoder()],
      ["classType", getShareClassTypeEncoder()],
      ["rightsBitfield", getU8Encoder()],
      ["liqPrefMultiplierBps", getU16Encoder()],
      ["liqSeniority", getU8Encoder()],
      ["votingWeight", getU32Encoder()],
      ["convertibleTo", getOptionEncoder(getAddressEncoder())],
      ["maxSupply", getOptionEncoder(getU64Encoder())],
      ["circulatingSupply", getU64Encoder()],
      ["lockedSupply", getU64Encoder()],
      ["mintablePostLaunch", getBooleanEncoder()],
      ["mintInitialized", getBooleanEncoder()],
      ["supplyLocked", getBooleanEncoder()],
      ["version", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["lifetimeMinted", getU64Encoder()],
      ["cumulativeCap", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SHARE_CLASS_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link ShareClass} account data. */
export function getShareClassDecoder(): Decoder<ShareClass> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["asset", getAddressDecoder()],
    ["mint", getAddressDecoder()],
    ["classIndex", getU8Decoder()],
    ["classType", getShareClassTypeDecoder()],
    ["rightsBitfield", getU8Decoder()],
    ["liqPrefMultiplierBps", getU16Decoder()],
    ["liqSeniority", getU8Decoder()],
    ["votingWeight", getU32Decoder()],
    ["convertibleTo", getOptionDecoder(getAddressDecoder())],
    ["maxSupply", getOptionDecoder(getU64Decoder())],
    ["circulatingSupply", getU64Decoder()],
    ["lockedSupply", getU64Decoder()],
    ["mintablePostLaunch", getBooleanDecoder()],
    ["mintInitialized", getBooleanDecoder()],
    ["supplyLocked", getBooleanDecoder()],
    ["version", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["lifetimeMinted", getU64Decoder()],
    ["cumulativeCap", getBooleanDecoder()],
  ]);
}

/** Gets the codec for {@link ShareClass} account data. */
export function getShareClassCodec(): Codec<ShareClassArgs, ShareClass> {
  return combineCodec(getShareClassEncoder(), getShareClassDecoder());
}

export function decodeShareClass<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<ShareClass, TAddress>;
export function decodeShareClass<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<ShareClass, TAddress>;
export function decodeShareClass<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<ShareClass, TAddress> | MaybeAccount<ShareClass, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getShareClassDecoder(),
  );
}

export async function fetchShareClass<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<ShareClass, TAddress>> {
  const maybeAccount = await fetchMaybeShareClass(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeShareClass<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<ShareClass, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeShareClass(maybeAccount);
}

export async function fetchAllShareClass(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<ShareClass>[]> {
  const maybeAccounts = await fetchAllMaybeShareClass(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeShareClass(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<ShareClass>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeShareClass(maybeAccount));
}
