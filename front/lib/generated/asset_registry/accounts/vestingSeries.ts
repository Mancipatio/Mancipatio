/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getVestingDeliveryModeDecoder,
  getVestingDeliveryModeEncoder,
  getVestingSeriesStatusDecoder,
  getVestingSeriesStatusEncoder,
  getVestingTimingModeDecoder,
  getVestingTimingModeEncoder,
  getVestingTrancheDecoder,
  getVestingTrancheEncoder,
  type VestingDeliveryMode,
  type VestingDeliveryModeArgs,
  type VestingSeriesStatus,
  type VestingSeriesStatusArgs,
  type VestingTimingMode,
  type VestingTimingModeArgs,
  type VestingTranche,
  type VestingTrancheArgs,
} from "../types";

export const VESTING_SERIES_DISCRIMINATOR = new Uint8Array([
  249, 151, 82, 176, 235, 77, 68, 180,
]);

export function getVestingSeriesDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VESTING_SERIES_DISCRIMINATOR,
  );
}

export type VestingSeries = {
  discriminator: ReadonlyUint8Array;
  /** The client wallet — approval / recovery / cancellation authority. */
  authority: Address;
  /** The token being vested (any SPL / Token-2022 mint). */
  tokenMint: Address;
  /** Escrow token account holding the deposits; authority is this PDA. */
  escrow: Address;
  seriesId: bigint;
  /** Sum of all position allocations. Frozen when Draft is finalized to Active. */
  totalAllocated: bigint;
  /** Deposit ledger — only `deposit_to_vesting_escrow` writes it. */
  deposited: bigint;
  /** Cumulative released to recipients (claims + pushes). */
  totalReleased: bigint;
  timingMode: VestingTimingMode;
  deliveryMode: VestingDeliveryMode;
  status: VestingSeriesStatus;
  /**
   * Approval-mode window (seconds) after which an un-approved vested
   * tranche becomes deliverable anyway. Fixed at creation; 0 in Auto mode.
   */
  approvalWindowSecs: bigint;
  /**
   * Fixed forever at creation: the client may re-point a position to a
   * replacement wallet (same recipient, lost keys).
   */
  recoveryEnabled: boolean;
  /**
   * Starts as chosen at creation; ON can be irrevocably turned OFF
   * (`disable_vesting_cancellation`), never the reverse.
   */
  cancellationEnabled: boolean;
  /**
   * % (bps of each allocation) recipients keep when cancelled before
   * anything vested. Fixed at creation; 0 = "recipient gets nothing".
   */
  preCliffBps: number;
  /** Bitmask of client-approved tranches (Approval mode; bit i = tranche i). */
  approvedMask: bigint;
  cancelledAt: bigint;
  /**
   * Vested cumulative frozen at cancellation (or the pre-cliff amount) —
   * the post-cancel entitlement basis.
   */
  finalCumulative: bigint;
  positionsCount: number;
  createdAt: bigint;
  version: number;
  bump: number;
  /**
   * The schedule — ascending `unlock_ts`, every `amount > 0`. The final
   * tranche's cumulative must equal `total_allocated` once all positions
   * are added (enforced at finalization, release and active cancellation).
   */
  tranches: Array<VestingTranche>;
};

export type VestingSeriesArgs = {
  /** The client wallet — approval / recovery / cancellation authority. */
  authority: Address;
  /** The token being vested (any SPL / Token-2022 mint). */
  tokenMint: Address;
  /** Escrow token account holding the deposits; authority is this PDA. */
  escrow: Address;
  seriesId: number | bigint;
  /** Sum of all position allocations. Frozen when Draft is finalized to Active. */
  totalAllocated: number | bigint;
  /** Deposit ledger — only `deposit_to_vesting_escrow` writes it. */
  deposited: number | bigint;
  /** Cumulative released to recipients (claims + pushes). */
  totalReleased: number | bigint;
  timingMode: VestingTimingModeArgs;
  deliveryMode: VestingDeliveryModeArgs;
  status: VestingSeriesStatusArgs;
  /**
   * Approval-mode window (seconds) after which an un-approved vested
   * tranche becomes deliverable anyway. Fixed at creation; 0 in Auto mode.
   */
  approvalWindowSecs: number | bigint;
  /**
   * Fixed forever at creation: the client may re-point a position to a
   * replacement wallet (same recipient, lost keys).
   */
  recoveryEnabled: boolean;
  /**
   * Starts as chosen at creation; ON can be irrevocably turned OFF
   * (`disable_vesting_cancellation`), never the reverse.
   */
  cancellationEnabled: boolean;
  /**
   * % (bps of each allocation) recipients keep when cancelled before
   * anything vested. Fixed at creation; 0 = "recipient gets nothing".
   */
  preCliffBps: number;
  /** Bitmask of client-approved tranches (Approval mode; bit i = tranche i). */
  approvedMask: number | bigint;
  cancelledAt: number | bigint;
  /**
   * Vested cumulative frozen at cancellation (or the pre-cliff amount) —
   * the post-cancel entitlement basis.
   */
  finalCumulative: number | bigint;
  positionsCount: number;
  createdAt: number | bigint;
  version: number;
  bump: number;
  /**
   * The schedule — ascending `unlock_ts`, every `amount > 0`. The final
   * tranche's cumulative must equal `total_allocated` once all positions
   * are added (enforced at finalization, release and active cancellation).
   */
  tranches: Array<VestingTrancheArgs>;
};

/** Gets the encoder for {@link VestingSeriesArgs} account data. */
export function getVestingSeriesEncoder(): Encoder<VestingSeriesArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["tokenMint", getAddressEncoder()],
      ["escrow", getAddressEncoder()],
      ["seriesId", getU64Encoder()],
      ["totalAllocated", getU64Encoder()],
      ["deposited", getU64Encoder()],
      ["totalReleased", getU64Encoder()],
      ["timingMode", getVestingTimingModeEncoder()],
      ["deliveryMode", getVestingDeliveryModeEncoder()],
      ["status", getVestingSeriesStatusEncoder()],
      ["approvalWindowSecs", getI64Encoder()],
      ["recoveryEnabled", getBooleanEncoder()],
      ["cancellationEnabled", getBooleanEncoder()],
      ["preCliffBps", getU16Encoder()],
      ["approvedMask", getU64Encoder()],
      ["cancelledAt", getI64Encoder()],
      ["finalCumulative", getU64Encoder()],
      ["positionsCount", getU32Encoder()],
      ["createdAt", getI64Encoder()],
      ["version", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["tranches", getArrayEncoder(getVestingTrancheEncoder())],
    ]),
    (value) => ({ ...value, discriminator: VESTING_SERIES_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link VestingSeries} account data. */
export function getVestingSeriesDecoder(): Decoder<VestingSeries> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["tokenMint", getAddressDecoder()],
    ["escrow", getAddressDecoder()],
    ["seriesId", getU64Decoder()],
    ["totalAllocated", getU64Decoder()],
    ["deposited", getU64Decoder()],
    ["totalReleased", getU64Decoder()],
    ["timingMode", getVestingTimingModeDecoder()],
    ["deliveryMode", getVestingDeliveryModeDecoder()],
    ["status", getVestingSeriesStatusDecoder()],
    ["approvalWindowSecs", getI64Decoder()],
    ["recoveryEnabled", getBooleanDecoder()],
    ["cancellationEnabled", getBooleanDecoder()],
    ["preCliffBps", getU16Decoder()],
    ["approvedMask", getU64Decoder()],
    ["cancelledAt", getI64Decoder()],
    ["finalCumulative", getU64Decoder()],
    ["positionsCount", getU32Decoder()],
    ["createdAt", getI64Decoder()],
    ["version", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["tranches", getArrayDecoder(getVestingTrancheDecoder())],
  ]);
}

/** Gets the codec for {@link VestingSeries} account data. */
export function getVestingSeriesCodec(): Codec<
  VestingSeriesArgs,
  VestingSeries
> {
  return combineCodec(getVestingSeriesEncoder(), getVestingSeriesDecoder());
}

export function decodeVestingSeries<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<VestingSeries, TAddress>;
export function decodeVestingSeries<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<VestingSeries, TAddress>;
export function decodeVestingSeries<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<VestingSeries, TAddress> | MaybeAccount<VestingSeries, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getVestingSeriesDecoder(),
  );
}

export async function fetchVestingSeries<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<VestingSeries, TAddress>> {
  const maybeAccount = await fetchMaybeVestingSeries(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeVestingSeries<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<VestingSeries, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeVestingSeries(maybeAccount);
}

export async function fetchAllVestingSeries(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<VestingSeries>[]> {
  const maybeAccounts = await fetchAllMaybeVestingSeries(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeVestingSeries(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<VestingSeries>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeVestingSeries(maybeAccount));
}
