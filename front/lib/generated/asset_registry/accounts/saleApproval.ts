/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getRaiseTypeDecoder,
  getRaiseTypeEncoder,
  type RaiseType,
  type RaiseTypeArgs,
} from "../types";

export const SALE_APPROVAL_DISCRIMINATOR = new Uint8Array([
  164, 165, 31, 198, 182, 206, 95, 3,
]);

export function getSaleApprovalDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SALE_APPROVAL_DISCRIMINATOR,
  );
}

export type SaleApproval = {
  discriminator: ReadonlyUint8Array;
  /** Byte 8. */
  shareClass: Address;
  /** Byte 40. */
  saleId: bigint;
  /**
   * Byte 48: the `Issuer` PDA (not its authority), so an approval
   * survives an issuer authority rotation.
   */
  issuer: Address;
  paymentMint: Address;
  /** Payment-mint base units; `price_per_unit * total_for_sale` must not exceed it. */
  maxGrossRaise: bigint;
  minPricePerUnit: bigint;
  maxPricePerUnit: bigint;
  raiseType: RaiseType;
  /** Unix ts; `open_sale` is refused after it. */
  expiresAt: bigint;
  /** sha256 of the canonical reviewed-application snapshot (kept off-chain). */
  applicationHash: ReadonlyUint8Array;
  /** The approving Admin; receives the rent on consume / revoke. */
  approvedBy: Address;
  bump: number;
  version: number;
};

export type SaleApprovalArgs = {
  /** Byte 8. */
  shareClass: Address;
  /** Byte 40. */
  saleId: number | bigint;
  /**
   * Byte 48: the `Issuer` PDA (not its authority), so an approval
   * survives an issuer authority rotation.
   */
  issuer: Address;
  paymentMint: Address;
  /** Payment-mint base units; `price_per_unit * total_for_sale` must not exceed it. */
  maxGrossRaise: number | bigint;
  minPricePerUnit: number | bigint;
  maxPricePerUnit: number | bigint;
  raiseType: RaiseTypeArgs;
  /** Unix ts; `open_sale` is refused after it. */
  expiresAt: number | bigint;
  /** sha256 of the canonical reviewed-application snapshot (kept off-chain). */
  applicationHash: ReadonlyUint8Array;
  /** The approving Admin; receives the rent on consume / revoke. */
  approvedBy: Address;
  bump: number;
  version: number;
};

/** Gets the encoder for {@link SaleApprovalArgs} account data. */
export function getSaleApprovalEncoder(): FixedSizeEncoder<SaleApprovalArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["shareClass", getAddressEncoder()],
      ["saleId", getU64Encoder()],
      ["issuer", getAddressEncoder()],
      ["paymentMint", getAddressEncoder()],
      ["maxGrossRaise", getU64Encoder()],
      ["minPricePerUnit", getU64Encoder()],
      ["maxPricePerUnit", getU64Encoder()],
      ["raiseType", getRaiseTypeEncoder()],
      ["expiresAt", getI64Encoder()],
      ["applicationHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["approvedBy", getAddressEncoder()],
      ["bump", getU8Encoder()],
      ["version", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SALE_APPROVAL_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link SaleApproval} account data. */
export function getSaleApprovalDecoder(): FixedSizeDecoder<SaleApproval> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["shareClass", getAddressDecoder()],
    ["saleId", getU64Decoder()],
    ["issuer", getAddressDecoder()],
    ["paymentMint", getAddressDecoder()],
    ["maxGrossRaise", getU64Decoder()],
    ["minPricePerUnit", getU64Decoder()],
    ["maxPricePerUnit", getU64Decoder()],
    ["raiseType", getRaiseTypeDecoder()],
    ["expiresAt", getI64Decoder()],
    ["applicationHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["approvedBy", getAddressDecoder()],
    ["bump", getU8Decoder()],
    ["version", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link SaleApproval} account data. */
export function getSaleApprovalCodec(): FixedSizeCodec<
  SaleApprovalArgs,
  SaleApproval
> {
  return combineCodec(getSaleApprovalEncoder(), getSaleApprovalDecoder());
}

export function decodeSaleApproval<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<SaleApproval, TAddress>;
export function decodeSaleApproval<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<SaleApproval, TAddress>;
export function decodeSaleApproval<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<SaleApproval, TAddress> | MaybeAccount<SaleApproval, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getSaleApprovalDecoder(),
  );
}

export async function fetchSaleApproval<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<SaleApproval, TAddress>> {
  const maybeAccount = await fetchMaybeSaleApproval(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeSaleApproval<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<SaleApproval, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeSaleApproval(maybeAccount);
}

export async function fetchAllSaleApproval(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<SaleApproval>[]> {
  const maybeAccounts = await fetchAllMaybeSaleApproval(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeSaleApproval(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<SaleApproval>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeSaleApproval(maybeAccount));
}

export function getSaleApprovalSize(): number {
  return 211;
}
