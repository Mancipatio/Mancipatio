/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getDistributionStatusDecoder,
  getDistributionStatusEncoder,
  type DistributionStatus,
  type DistributionStatusArgs,
} from "../types";

export const DISTRIBUTION_DISCRIMINATOR = new Uint8Array([
  176, 85, 17, 11, 13, 194, 18, 1,
]);

export function getDistributionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DISTRIBUTION_DISCRIMINATOR,
  );
}

export type Distribution = {
  discriminator: ReadonlyUint8Array;
  /** Admin that created the distribution. */
  admin: Address;
  /**
   * Wallet that funded the escrow — `close_distribution` may only sweep the
   * remainder to a token account owned by it. The rent (escrow and marker)
   * goes to `admin`, which paid for both (2D).
   */
  funder: Address;
  shareClass: Address;
  /** Share-class Token-2022 mint the distribution relates to (indexing). */
  mint: Address;
  /** Payment mint being distributed (e.g. USDT). */
  paymentMint: Address;
  /** Distribution-PDA-owned escrow holding the payment tokens. */
  escrow: Address;
  /** Total payment-token units funded for this distribution. */
  totalAmount: bigint;
  /**
   * Share supply the off-chain pro-rata was computed against
   * (informational on-chain record).
   */
  snapshotSupply: bigint;
  /** Cumulative payment-token units paid out so far. */
  distributedAmount: bigint;
  /** Cumulative number of payouts executed so far. */
  paidCount: number;
  status: DistributionStatus;
  distributionId: bigint;
  version: number;
  bump: number;
};

export type DistributionArgs = {
  /** Admin that created the distribution. */
  admin: Address;
  /**
   * Wallet that funded the escrow — `close_distribution` may only sweep the
   * remainder to a token account owned by it. The rent (escrow and marker)
   * goes to `admin`, which paid for both (2D).
   */
  funder: Address;
  shareClass: Address;
  /** Share-class Token-2022 mint the distribution relates to (indexing). */
  mint: Address;
  /** Payment mint being distributed (e.g. USDT). */
  paymentMint: Address;
  /** Distribution-PDA-owned escrow holding the payment tokens. */
  escrow: Address;
  /** Total payment-token units funded for this distribution. */
  totalAmount: number | bigint;
  /**
   * Share supply the off-chain pro-rata was computed against
   * (informational on-chain record).
   */
  snapshotSupply: number | bigint;
  /** Cumulative payment-token units paid out so far. */
  distributedAmount: number | bigint;
  /** Cumulative number of payouts executed so far. */
  paidCount: number;
  status: DistributionStatusArgs;
  distributionId: number | bigint;
  version: number;
  bump: number;
};

/** Gets the encoder for {@link DistributionArgs} account data. */
export function getDistributionEncoder(): FixedSizeEncoder<DistributionArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["admin", getAddressEncoder()],
      ["funder", getAddressEncoder()],
      ["shareClass", getAddressEncoder()],
      ["mint", getAddressEncoder()],
      ["paymentMint", getAddressEncoder()],
      ["escrow", getAddressEncoder()],
      ["totalAmount", getU64Encoder()],
      ["snapshotSupply", getU64Encoder()],
      ["distributedAmount", getU64Encoder()],
      ["paidCount", getU32Encoder()],
      ["status", getDistributionStatusEncoder()],
      ["distributionId", getU64Encoder()],
      ["version", getU8Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: DISTRIBUTION_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Distribution} account data. */
export function getDistributionDecoder(): FixedSizeDecoder<Distribution> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["admin", getAddressDecoder()],
    ["funder", getAddressDecoder()],
    ["shareClass", getAddressDecoder()],
    ["mint", getAddressDecoder()],
    ["paymentMint", getAddressDecoder()],
    ["escrow", getAddressDecoder()],
    ["totalAmount", getU64Decoder()],
    ["snapshotSupply", getU64Decoder()],
    ["distributedAmount", getU64Decoder()],
    ["paidCount", getU32Decoder()],
    ["status", getDistributionStatusDecoder()],
    ["distributionId", getU64Decoder()],
    ["version", getU8Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Distribution} account data. */
export function getDistributionCodec(): FixedSizeCodec<
  DistributionArgs,
  Distribution
> {
  return combineCodec(getDistributionEncoder(), getDistributionDecoder());
}

export function decodeDistribution<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Distribution, TAddress>;
export function decodeDistribution<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Distribution, TAddress>;
export function decodeDistribution<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Distribution, TAddress> | MaybeAccount<Distribution, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getDistributionDecoder(),
  );
}

export async function fetchDistribution<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Distribution, TAddress>> {
  const maybeAccount = await fetchMaybeDistribution(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeDistribution<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Distribution, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeDistribution(maybeAccount);
}

export async function fetchAllDistribution(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Distribution>[]> {
  const maybeAccounts = await fetchAllMaybeDistribution(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeDistribution(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Distribution>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeDistribution(maybeAccount));
}

export function getDistributionSize(): number {
  return 239;
}
