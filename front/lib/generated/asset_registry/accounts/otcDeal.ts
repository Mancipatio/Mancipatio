/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getOtcDealStatusDecoder,
  getOtcDealStatusEncoder,
  type OtcDealStatus,
  type OtcDealStatusArgs,
} from "../types";

export const OTC_DEAL_DISCRIMINATOR = new Uint8Array([
  39, 32, 145, 202, 73, 153, 81, 49,
]);

export function getOtcDealDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(OTC_DEAL_DISCRIMINATOR);
}

export type OtcDeal = {
  discriminator: ReadonlyUint8Array;
  /** Admin that created the deal (also the signer of `cancel_otc_deal`). */
  admin: Address;
  buyer: Address;
  seller: Address;
  shareClass: Address;
  /** Share-class Token-2022 mint being sold. */
  mint: Address;
  /** Payment mint the buyer pays in. */
  paymentMint: Address;
  /** Deal-PDA-owned escrow holding the seller's deposited share units. */
  assetEscrow: Address;
  /** Deal-PDA-owned escrow holding the buyer's deposited payment. */
  paymentEscrow: Address;
  /** Share-class units the seller deposits. */
  amount: bigint;
  /** Total price in payment-token base units the buyer deposits. */
  price: bigint;
  assetDeposited: boolean;
  paymentDeposited: boolean;
  status: OtcDealStatus;
  dealId: bigint;
  /**
   * Unix ts after which anyone may `expire_otc_deal` it (deposits are
   * refunded). 0 = never expires.
   */
  expiresAt: bigint;
  version: number;
  bump: number;
  /**
   * **Deposit ledger (asset leg)** — share units the SELLER actually moved
   * into `asset_escrow` through `deposit_otc_asset` (always exactly
   * `amount`; recorded rather than assumed). The refund paths cap the
   * KYC-free payout to the seller at this number: the escrow is an ordinary
   * token account, so anyone — including the admin who named both parties —
   * can raw-`transfer_checked` extra units into it, and sweeping the live
   * balance would hand those units to a wallet the chain never vetted.
   * Appended last so the existing byte layout up to `bump` is unchanged.
   */
  assetDepositedAmount: bigint;
  /**
   * **Deposit ledger (payment leg)** — payment units the BUYER actually
   * moved into `payment_escrow` through `deposit_otc_payment` (always
   * exactly `price`). The payment mint carries no transfer hook, so this is
   * not a KYC control; it stops a refund from paying out value the buyer
   * never deposited.
   */
  paymentDepositedAmount: bigint;
};

export type OtcDealArgs = {
  /** Admin that created the deal (also the signer of `cancel_otc_deal`). */
  admin: Address;
  buyer: Address;
  seller: Address;
  shareClass: Address;
  /** Share-class Token-2022 mint being sold. */
  mint: Address;
  /** Payment mint the buyer pays in. */
  paymentMint: Address;
  /** Deal-PDA-owned escrow holding the seller's deposited share units. */
  assetEscrow: Address;
  /** Deal-PDA-owned escrow holding the buyer's deposited payment. */
  paymentEscrow: Address;
  /** Share-class units the seller deposits. */
  amount: number | bigint;
  /** Total price in payment-token base units the buyer deposits. */
  price: number | bigint;
  assetDeposited: boolean;
  paymentDeposited: boolean;
  status: OtcDealStatusArgs;
  dealId: number | bigint;
  /**
   * Unix ts after which anyone may `expire_otc_deal` it (deposits are
   * refunded). 0 = never expires.
   */
  expiresAt: number | bigint;
  version: number;
  bump: number;
  /**
   * **Deposit ledger (asset leg)** — share units the SELLER actually moved
   * into `asset_escrow` through `deposit_otc_asset` (always exactly
   * `amount`; recorded rather than assumed). The refund paths cap the
   * KYC-free payout to the seller at this number: the escrow is an ordinary
   * token account, so anyone — including the admin who named both parties —
   * can raw-`transfer_checked` extra units into it, and sweeping the live
   * balance would hand those units to a wallet the chain never vetted.
   * Appended last so the existing byte layout up to `bump` is unchanged.
   */
  assetDepositedAmount: number | bigint;
  /**
   * **Deposit ledger (payment leg)** — payment units the BUYER actually
   * moved into `payment_escrow` through `deposit_otc_payment` (always
   * exactly `price`). The payment mint carries no transfer hook, so this is
   * not a KYC control; it stops a refund from paying out value the buyer
   * never deposited.
   */
  paymentDepositedAmount: number | bigint;
};

/** Gets the encoder for {@link OtcDealArgs} account data. */
export function getOtcDealEncoder(): FixedSizeEncoder<OtcDealArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["admin", getAddressEncoder()],
      ["buyer", getAddressEncoder()],
      ["seller", getAddressEncoder()],
      ["shareClass", getAddressEncoder()],
      ["mint", getAddressEncoder()],
      ["paymentMint", getAddressEncoder()],
      ["assetEscrow", getAddressEncoder()],
      ["paymentEscrow", getAddressEncoder()],
      ["amount", getU64Encoder()],
      ["price", getU64Encoder()],
      ["assetDeposited", getBooleanEncoder()],
      ["paymentDeposited", getBooleanEncoder()],
      ["status", getOtcDealStatusEncoder()],
      ["dealId", getU64Encoder()],
      ["expiresAt", getI64Encoder()],
      ["version", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["assetDepositedAmount", getU64Encoder()],
      ["paymentDepositedAmount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: OTC_DEAL_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link OtcDeal} account data. */
export function getOtcDealDecoder(): FixedSizeDecoder<OtcDeal> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["admin", getAddressDecoder()],
    ["buyer", getAddressDecoder()],
    ["seller", getAddressDecoder()],
    ["shareClass", getAddressDecoder()],
    ["mint", getAddressDecoder()],
    ["paymentMint", getAddressDecoder()],
    ["assetEscrow", getAddressDecoder()],
    ["paymentEscrow", getAddressDecoder()],
    ["amount", getU64Decoder()],
    ["price", getU64Decoder()],
    ["assetDeposited", getBooleanDecoder()],
    ["paymentDeposited", getBooleanDecoder()],
    ["status", getOtcDealStatusDecoder()],
    ["dealId", getU64Decoder()],
    ["expiresAt", getI64Decoder()],
    ["version", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["assetDepositedAmount", getU64Decoder()],
    ["paymentDepositedAmount", getU64Decoder()],
  ]);
}

/** Gets the codec for {@link OtcDeal} account data. */
export function getOtcDealCodec(): FixedSizeCodec<OtcDealArgs, OtcDeal> {
  return combineCodec(getOtcDealEncoder(), getOtcDealDecoder());
}

export function decodeOtcDeal<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<OtcDeal, TAddress>;
export function decodeOtcDeal<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<OtcDeal, TAddress>;
export function decodeOtcDeal<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<OtcDeal, TAddress> | MaybeAccount<OtcDeal, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getOtcDealDecoder(),
  );
}

export async function fetchOtcDeal<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<OtcDeal, TAddress>> {
  const maybeAccount = await fetchMaybeOtcDeal(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeOtcDeal<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<OtcDeal, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeOtcDeal(maybeAccount);
}

export async function fetchAllOtcDeal(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<OtcDeal>[]> {
  const maybeAccounts = await fetchAllMaybeOtcDeal(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeOtcDeal(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<OtcDeal>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeOtcDeal(maybeAccount));
}

export function getOtcDealSize(): number {
  return 317;
}
