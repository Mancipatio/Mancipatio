/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const VESTING_POSITION_DISCRIMINATOR = new Uint8Array([
  51, 62, 55, 157, 232, 141, 253, 13,
]);

export function getVestingPositionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VESTING_POSITION_DISCRIMINATOR,
  );
}

export type VestingPosition = {
  discriminator: ReadonlyUint8Array;
  series: Address;
  index: number;
  /** Current recipient wallet — mutated only by recovery. */
  wallet: Address;
  allocation: bigint;
  released: bigint;
  version: number;
  bump: number;
};

export type VestingPositionArgs = {
  series: Address;
  index: number;
  /** Current recipient wallet — mutated only by recovery. */
  wallet: Address;
  allocation: number | bigint;
  released: number | bigint;
  version: number;
  bump: number;
};

/** Gets the encoder for {@link VestingPositionArgs} account data. */
export function getVestingPositionEncoder(): FixedSizeEncoder<VestingPositionArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["series", getAddressEncoder()],
      ["index", getU32Encoder()],
      ["wallet", getAddressEncoder()],
      ["allocation", getU64Encoder()],
      ["released", getU64Encoder()],
      ["version", getU8Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: VESTING_POSITION_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link VestingPosition} account data. */
export function getVestingPositionDecoder(): FixedSizeDecoder<VestingPosition> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["series", getAddressDecoder()],
    ["index", getU32Decoder()],
    ["wallet", getAddressDecoder()],
    ["allocation", getU64Decoder()],
    ["released", getU64Decoder()],
    ["version", getU8Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link VestingPosition} account data. */
export function getVestingPositionCodec(): FixedSizeCodec<
  VestingPositionArgs,
  VestingPosition
> {
  return combineCodec(getVestingPositionEncoder(), getVestingPositionDecoder());
}

export function decodeVestingPosition<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<VestingPosition, TAddress>;
export function decodeVestingPosition<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<VestingPosition, TAddress>;
export function decodeVestingPosition<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<VestingPosition, TAddress>
  | MaybeAccount<VestingPosition, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getVestingPositionDecoder(),
  );
}

export async function fetchVestingPosition<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<VestingPosition, TAddress>> {
  const maybeAccount = await fetchMaybeVestingPosition(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeVestingPosition<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<VestingPosition, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeVestingPosition(maybeAccount);
}

export async function fetchAllVestingPosition(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<VestingPosition>[]> {
  const maybeAccounts = await fetchAllMaybeVestingPosition(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeVestingPosition(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<VestingPosition>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeVestingPosition(maybeAccount),
  );
}

export function getVestingPositionSize(): number {
  return 94;
}
