/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getRaiseTypeDecoder,
  getRaiseTypeEncoder,
  getSaleStatusDecoder,
  getSaleStatusEncoder,
  type RaiseType,
  type RaiseTypeArgs,
  type SaleStatus,
  type SaleStatusArgs,
} from "../types";

export const SALE_DISCRIMINATOR = new Uint8Array([
  202, 64, 232, 171, 178, 172, 34, 183,
]);

export function getSaleDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SALE_DISCRIMINATOR);
}

export type Sale = {
  discriminator: ReadonlyUint8Array;
  shareClass: Address;
  /** Share-class Token-2022 mint being sold. */
  mint: Address;
  /** Payment mint (e.g. USDC) buyers pay in. */
  paymentMint: Address;
  /** Escrow token account (payment mint) collecting proceeds. */
  proceeds: Address;
  /** Issuer authority that opened the sale. */
  authority: Address;
  saleId: bigint;
  /** Payment-token base units per one share-class unit. */
  pricePerUnit: bigint;
  totalForSale: bigint;
  sold: bigint;
  startTs: bigint;
  /** 0 = no end. */
  endTs: bigint;
  status: SaleStatus;
  /** Disbursement type, fixed at sale open (investors see it before buying). */
  raiseType: RaiseType;
  /** Startup: months with no payout before first tranche. */
  cliffMonths: number;
  /** Startup: total vesting horizon in months. */
  vestingMonths: number;
  version: number;
  bump: number;
  /** The `SaleApproval` that `open_sale` consumed (and closed). */
  saleApproval: Address;
  /** Copied from the approval: commitment to the reviewed application. */
  applicationHash: ReadonlyUint8Array;
};

export type SaleArgs = {
  shareClass: Address;
  /** Share-class Token-2022 mint being sold. */
  mint: Address;
  /** Payment mint (e.g. USDC) buyers pay in. */
  paymentMint: Address;
  /** Escrow token account (payment mint) collecting proceeds. */
  proceeds: Address;
  /** Issuer authority that opened the sale. */
  authority: Address;
  saleId: number | bigint;
  /** Payment-token base units per one share-class unit. */
  pricePerUnit: number | bigint;
  totalForSale: number | bigint;
  sold: number | bigint;
  startTs: number | bigint;
  /** 0 = no end. */
  endTs: number | bigint;
  status: SaleStatusArgs;
  /** Disbursement type, fixed at sale open (investors see it before buying). */
  raiseType: RaiseTypeArgs;
  /** Startup: months with no payout before first tranche. */
  cliffMonths: number;
  /** Startup: total vesting horizon in months. */
  vestingMonths: number;
  version: number;
  bump: number;
  /** The `SaleApproval` that `open_sale` consumed (and closed). */
  saleApproval: Address;
  /** Copied from the approval: commitment to the reviewed application. */
  applicationHash: ReadonlyUint8Array;
};

/** Gets the encoder for {@link SaleArgs} account data. */
export function getSaleEncoder(): FixedSizeEncoder<SaleArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["shareClass", getAddressEncoder()],
      ["mint", getAddressEncoder()],
      ["paymentMint", getAddressEncoder()],
      ["proceeds", getAddressEncoder()],
      ["authority", getAddressEncoder()],
      ["saleId", getU64Encoder()],
      ["pricePerUnit", getU64Encoder()],
      ["totalForSale", getU64Encoder()],
      ["sold", getU64Encoder()],
      ["startTs", getI64Encoder()],
      ["endTs", getI64Encoder()],
      ["status", getSaleStatusEncoder()],
      ["raiseType", getRaiseTypeEncoder()],
      ["cliffMonths", getU8Encoder()],
      ["vestingMonths", getU8Encoder()],
      ["version", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["saleApproval", getAddressEncoder()],
      ["applicationHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: SALE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Sale} account data. */
export function getSaleDecoder(): FixedSizeDecoder<Sale> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["shareClass", getAddressDecoder()],
    ["mint", getAddressDecoder()],
    ["paymentMint", getAddressDecoder()],
    ["proceeds", getAddressDecoder()],
    ["authority", getAddressDecoder()],
    ["saleId", getU64Decoder()],
    ["pricePerUnit", getU64Decoder()],
    ["totalForSale", getU64Decoder()],
    ["sold", getU64Decoder()],
    ["startTs", getI64Decoder()],
    ["endTs", getI64Decoder()],
    ["status", getSaleStatusDecoder()],
    ["raiseType", getRaiseTypeDecoder()],
    ["cliffMonths", getU8Decoder()],
    ["vestingMonths", getU8Decoder()],
    ["version", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["saleApproval", getAddressDecoder()],
    ["applicationHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

/** Gets the codec for {@link Sale} account data. */
export function getSaleCodec(): FixedSizeCodec<SaleArgs, Sale> {
  return combineCodec(getSaleEncoder(), getSaleDecoder());
}

export function decodeSale<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Sale, TAddress>;
export function decodeSale<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Sale, TAddress>;
export function decodeSale<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Sale, TAddress> | MaybeAccount<Sale, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getSaleDecoder(),
  );
}

export async function fetchSale<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Sale, TAddress>> {
  const maybeAccount = await fetchMaybeSale(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeSale<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Sale, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeSale(maybeAccount);
}

export async function fetchAllSale(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Sale>[]> {
  const maybeAccounts = await fetchAllMaybeSale(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeSale(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Sale>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeSale(maybeAccount));
}

export function getSaleSize(): number {
  return 286;
}
