/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getKybStatusDecoder,
  getKybStatusEncoder,
  type KybStatus,
  type KybStatusArgs,
} from "../types";

export const ISSUER_DISCRIMINATOR = new Uint8Array([
  216, 19, 83, 230, 108, 53, 80, 14,
]);

export function getIssuerDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ISSUER_DISCRIMINATOR);
}

export type Issuer = {
  discriminator: ReadonlyUint8Array;
  authority: Address;
  /** Client-supplied 32-byte legal-entity identifier (also the PDA seed). */
  legalEntityId: ReadonlyUint8Array;
  /** ISO-3166 numeric country code of incorporation. */
  jurisdiction: number;
  kybStatus: KybStatus;
  /** SHA-256 of the off-chain KYB document bundle. */
  kybDocHash: ReadonlyUint8Array;
  assetsCount: bigint;
  version: number;
  bump: number;
};

export type IssuerArgs = {
  authority: Address;
  /** Client-supplied 32-byte legal-entity identifier (also the PDA seed). */
  legalEntityId: ReadonlyUint8Array;
  /** ISO-3166 numeric country code of incorporation. */
  jurisdiction: number;
  kybStatus: KybStatusArgs;
  /** SHA-256 of the off-chain KYB document bundle. */
  kybDocHash: ReadonlyUint8Array;
  assetsCount: number | bigint;
  version: number;
  bump: number;
};

/** Gets the encoder for {@link IssuerArgs} account data. */
export function getIssuerEncoder(): FixedSizeEncoder<IssuerArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["legalEntityId", fixEncoderSize(getBytesEncoder(), 32)],
      ["jurisdiction", getU16Encoder()],
      ["kybStatus", getKybStatusEncoder()],
      ["kybDocHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["assetsCount", getU64Encoder()],
      ["version", getU8Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ISSUER_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Issuer} account data. */
export function getIssuerDecoder(): FixedSizeDecoder<Issuer> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["legalEntityId", fixDecoderSize(getBytesDecoder(), 32)],
    ["jurisdiction", getU16Decoder()],
    ["kybStatus", getKybStatusDecoder()],
    ["kybDocHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["assetsCount", getU64Decoder()],
    ["version", getU8Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Issuer} account data. */
export function getIssuerCodec(): FixedSizeCodec<IssuerArgs, Issuer> {
  return combineCodec(getIssuerEncoder(), getIssuerDecoder());
}

export function decodeIssuer<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Issuer, TAddress>;
export function decodeIssuer<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Issuer, TAddress>;
export function decodeIssuer<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Issuer, TAddress> | MaybeAccount<Issuer, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getIssuerDecoder(),
  );
}

export async function fetchIssuer<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Issuer, TAddress>> {
  const maybeAccount = await fetchMaybeIssuer(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeIssuer<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Issuer, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeIssuer(maybeAccount);
}

export async function fetchAllIssuer(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Issuer>[]> {
  const maybeAccounts = await fetchAllMaybeIssuer(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeIssuer(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Issuer>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeIssuer(maybeAccount));
}

export function getIssuerSize(): number {
  return 117;
}
