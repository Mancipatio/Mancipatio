/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const VESTING_MILESTONE_DISCRIMINATOR = new Uint8Array([
  178, 45, 138, 76, 141, 49, 162, 138,
]);

export function getVestingMilestoneDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VESTING_MILESTONE_DISCRIMINATOR,
  );
}

export type VestingMilestone = {
  discriminator: ReadonlyUint8Array;
  issuance: Address;
  index: number;
  /** Merkle root of `(claimer, entitlement)` — the holder snapshot. */
  merkleRoot: ReadonlyUint8Array;
  /** Total underlying claimable in this milestone. */
  amountPool: bigint;
  claimed: bigint;
  /** Claims open once `unix_timestamp >= unlock_ts`. */
  unlockTs: bigint;
  version: number;
  bump: number;
};

export type VestingMilestoneArgs = {
  issuance: Address;
  index: number;
  /** Merkle root of `(claimer, entitlement)` — the holder snapshot. */
  merkleRoot: ReadonlyUint8Array;
  /** Total underlying claimable in this milestone. */
  amountPool: number | bigint;
  claimed: number | bigint;
  /** Claims open once `unix_timestamp >= unlock_ts`. */
  unlockTs: number | bigint;
  version: number;
  bump: number;
};

/** Gets the encoder for {@link VestingMilestoneArgs} account data. */
export function getVestingMilestoneEncoder(): FixedSizeEncoder<VestingMilestoneArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["issuance", getAddressEncoder()],
      ["index", getU16Encoder()],
      ["merkleRoot", fixEncoderSize(getBytesEncoder(), 32)],
      ["amountPool", getU64Encoder()],
      ["claimed", getU64Encoder()],
      ["unlockTs", getI64Encoder()],
      ["version", getU8Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: VESTING_MILESTONE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link VestingMilestone} account data. */
export function getVestingMilestoneDecoder(): FixedSizeDecoder<VestingMilestone> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["issuance", getAddressDecoder()],
    ["index", getU16Decoder()],
    ["merkleRoot", fixDecoderSize(getBytesDecoder(), 32)],
    ["amountPool", getU64Decoder()],
    ["claimed", getU64Decoder()],
    ["unlockTs", getI64Decoder()],
    ["version", getU8Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link VestingMilestone} account data. */
export function getVestingMilestoneCodec(): FixedSizeCodec<
  VestingMilestoneArgs,
  VestingMilestone
> {
  return combineCodec(
    getVestingMilestoneEncoder(),
    getVestingMilestoneDecoder(),
  );
}

export function decodeVestingMilestone<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<VestingMilestone, TAddress>;
export function decodeVestingMilestone<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<VestingMilestone, TAddress>;
export function decodeVestingMilestone<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<VestingMilestone, TAddress>
  | MaybeAccount<VestingMilestone, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getVestingMilestoneDecoder(),
  );
}

export async function fetchVestingMilestone<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<VestingMilestone, TAddress>> {
  const maybeAccount = await fetchMaybeVestingMilestone(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeVestingMilestone<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<VestingMilestone, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeVestingMilestone(maybeAccount);
}

export async function fetchAllVestingMilestone(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<VestingMilestone>[]> {
  const maybeAccounts = await fetchAllMaybeVestingMilestone(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeVestingMilestone(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<VestingMilestone>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeVestingMilestone(maybeAccount),
  );
}

export function getVestingMilestoneSize(): number {
  return 100;
}
