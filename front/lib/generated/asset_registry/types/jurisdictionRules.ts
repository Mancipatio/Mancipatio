/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/**
 * Transfer-eligibility policy attached to an `Asset`.
 *
 * **Informational only — NOT enforced on-chain.** No program reads these
 * fields when validating a transfer; actual enforcement is the
 * `transfer_hook` program's KYC registry (sanctions blocklist on every
 * transfer, plus the optional per-mint KYC-gated mode). This struct is a
 * declared policy record that off-chain tooling may display or act on.
 */
export type JurisdictionRules = {
  /** 1024-bit bitmap over ISO-3166 numeric country codes (bit N = country N). */
  allowedCountries: ReadonlyUint8Array;
  /** Maximum distinct holders; 0 = unlimited. */
  maxHolders: number;
  /** Unix ts until which only whitelist transfers are allowed; 0 = none. */
  restrictedPeriodEnd: bigint;
  /** Whether peer-to-peer (non-launchpad / non-OTC) transfers are allowed. */
  allowP2p: boolean;
};

export type JurisdictionRulesArgs = {
  /** 1024-bit bitmap over ISO-3166 numeric country codes (bit N = country N). */
  allowedCountries: ReadonlyUint8Array;
  /** Maximum distinct holders; 0 = unlimited. */
  maxHolders: number;
  /** Unix ts until which only whitelist transfers are allowed; 0 = none. */
  restrictedPeriodEnd: number | bigint;
  /** Whether peer-to-peer (non-launchpad / non-OTC) transfers are allowed. */
  allowP2p: boolean;
};

export function getJurisdictionRulesEncoder(): FixedSizeEncoder<JurisdictionRulesArgs> {
  return getStructEncoder([
    ["allowedCountries", fixEncoderSize(getBytesEncoder(), 128)],
    ["maxHolders", getU32Encoder()],
    ["restrictedPeriodEnd", getI64Encoder()],
    ["allowP2p", getBooleanEncoder()],
  ]);
}

export function getJurisdictionRulesDecoder(): FixedSizeDecoder<JurisdictionRules> {
  return getStructDecoder([
    ["allowedCountries", fixDecoderSize(getBytesDecoder(), 128)],
    ["maxHolders", getU32Decoder()],
    ["restrictedPeriodEnd", getI64Decoder()],
    ["allowP2p", getBooleanDecoder()],
  ]);
}

export function getJurisdictionRulesCodec(): FixedSizeCodec<
  JurisdictionRulesArgs,
  JurisdictionRules
> {
  return combineCodec(
    getJurisdictionRulesEncoder(),
    getJurisdictionRulesDecoder(),
  );
}
