/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  findAdminRecordPda,
  findCloseDistributionEscrowMarkerPda,
} from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CLOSE_DISTRIBUTION_DISCRIMINATOR = new Uint8Array([
  238, 70, 219, 176, 69, 243, 141, 230,
]);

export function getCloseDistributionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_DISTRIBUTION_DISCRIMINATOR,
  );
}

export type CloseDistributionInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAdminRecord extends string | AccountMeta<string> = string,
  TAccountDistribution extends string | AccountMeta<string> = string,
  TAccountPaymentMint extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountRefundAccount extends string | AccountMeta<string> = string,
  TAccountEscrowRentRecipient extends string | AccountMeta<string> = string,
  TAccountEscrowMarker extends string | AccountMeta<string> = string,
  TAccountPaymentTokenProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAdminRecord extends string
        ? ReadonlyAccount<TAccountAdminRecord>
        : TAccountAdminRecord,
      TAccountDistribution extends string
        ? WritableAccount<TAccountDistribution>
        : TAccountDistribution,
      TAccountPaymentMint extends string
        ? ReadonlyAccount<TAccountPaymentMint>
        : TAccountPaymentMint,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountRefundAccount extends string
        ? WritableAccount<TAccountRefundAccount>
        : TAccountRefundAccount,
      TAccountEscrowRentRecipient extends string
        ? WritableAccount<TAccountEscrowRentRecipient>
        : TAccountEscrowRentRecipient,
      TAccountEscrowMarker extends string
        ? WritableAccount<TAccountEscrowMarker>
        : TAccountEscrowMarker,
      TAccountPaymentTokenProgram extends string
        ? ReadonlyAccount<TAccountPaymentTokenProgram>
        : TAccountPaymentTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CloseDistributionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseDistributionInstructionDataArgs = {};

export function getCloseDistributionInstructionDataEncoder(): FixedSizeEncoder<CloseDistributionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_DISTRIBUTION_DISCRIMINATOR }),
  );
}

export function getCloseDistributionInstructionDataDecoder(): FixedSizeDecoder<CloseDistributionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseDistributionInstructionDataCodec(): FixedSizeCodec<
  CloseDistributionInstructionDataArgs,
  CloseDistributionInstructionData
> {
  return combineCodec(
    getCloseDistributionInstructionDataEncoder(),
    getCloseDistributionInstructionDataDecoder(),
  );
}

export type CloseDistributionAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountDistribution extends string = string,
  TAccountPaymentMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountRefundAccount extends string = string,
  TAccountEscrowRentRecipient extends string = string,
  TAccountEscrowMarker extends string = string,
  TAccountPaymentTokenProgram extends string = string,
> = {
  /**
   * Any platform Admin. Receives no rent (2D: all rent goes to
   * `distribution.admin`, whichever Admin closes).
   */
  authority: TransactionSigner<TAccountAuthority>;
  /** Admin gate — only an admin may close distributions. */
  adminRecord?: Address<TAccountAdminRecord>;
  distribution: Address<TAccountDistribution>;
  paymentMint: Address<TAccountPaymentMint>;
  escrow: Address<TAccountEscrow>;
  /**
   * Receives the undistributed remainder — must be owned by the wallet
   * that funded the distribution (`distribution.funder`).
   */
  refundAccount: Address<TAccountRefundAccount>;
  /**
   * lamports — constrained to be `distribution.admin`, the Admin that paid
   * for both at `create_distribution` (2D). The token remainder still goes
   * only to the funder's `refund_account`.
   */
  escrowRentRecipient: Address<TAccountEscrowRentRecipient>;
  /**
   * Escrow marker for the distribution PDA — closed here (rent →
   * `distribution.admin` via `escrow_rent_recipient`); close is the
   * distribution's only terminal path.
   */
  escrowMarker?: Address<TAccountEscrowMarker>;
  paymentTokenProgram: Address<TAccountPaymentTokenProgram>;
};

export async function getCloseDistributionInstructionAsync<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountDistribution extends string,
  TAccountPaymentMint extends string,
  TAccountEscrow extends string,
  TAccountRefundAccount extends string,
  TAccountEscrowRentRecipient extends string,
  TAccountEscrowMarker extends string,
  TAccountPaymentTokenProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: CloseDistributionAsyncInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountDistribution,
    TAccountPaymentMint,
    TAccountEscrow,
    TAccountRefundAccount,
    TAccountEscrowRentRecipient,
    TAccountEscrowMarker,
    TAccountPaymentTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CloseDistributionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountDistribution,
    TAccountPaymentMint,
    TAccountEscrow,
    TAccountRefundAccount,
    TAccountEscrowRentRecipient,
    TAccountEscrowMarker,
    TAccountPaymentTokenProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    distribution: { value: input.distribution ?? null, isWritable: true },
    paymentMint: { value: input.paymentMint ?? null, isWritable: false },
    escrow: { value: input.escrow ?? null, isWritable: true },
    refundAccount: { value: input.refundAccount ?? null, isWritable: true },
    escrowRentRecipient: {
      value: input.escrowRentRecipient ?? null,
      isWritable: true,
    },
    escrowMarker: { value: input.escrowMarker ?? null, isWritable: true },
    paymentTokenProgram: {
      value: input.paymentTokenProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.adminRecord.value) {
    accounts.adminRecord.value = await findAdminRecordPda({
      authority: expectAddress(accounts.authority.value),
    });
  }
  if (!accounts.escrowMarker.value) {
    accounts.escrowMarker.value = await findCloseDistributionEscrowMarkerPda({
      distribution: expectAddress(accounts.distribution.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.distribution),
      getAccountMeta(accounts.paymentMint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.refundAccount),
      getAccountMeta(accounts.escrowRentRecipient),
      getAccountMeta(accounts.escrowMarker),
      getAccountMeta(accounts.paymentTokenProgram),
    ],
    data: getCloseDistributionInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseDistributionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountDistribution,
    TAccountPaymentMint,
    TAccountEscrow,
    TAccountRefundAccount,
    TAccountEscrowRentRecipient,
    TAccountEscrowMarker,
    TAccountPaymentTokenProgram
  >);
}

export type CloseDistributionInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountDistribution extends string = string,
  TAccountPaymentMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountRefundAccount extends string = string,
  TAccountEscrowRentRecipient extends string = string,
  TAccountEscrowMarker extends string = string,
  TAccountPaymentTokenProgram extends string = string,
> = {
  /**
   * Any platform Admin. Receives no rent (2D: all rent goes to
   * `distribution.admin`, whichever Admin closes).
   */
  authority: TransactionSigner<TAccountAuthority>;
  /** Admin gate — only an admin may close distributions. */
  adminRecord: Address<TAccountAdminRecord>;
  distribution: Address<TAccountDistribution>;
  paymentMint: Address<TAccountPaymentMint>;
  escrow: Address<TAccountEscrow>;
  /**
   * Receives the undistributed remainder — must be owned by the wallet
   * that funded the distribution (`distribution.funder`).
   */
  refundAccount: Address<TAccountRefundAccount>;
  /**
   * lamports — constrained to be `distribution.admin`, the Admin that paid
   * for both at `create_distribution` (2D). The token remainder still goes
   * only to the funder's `refund_account`.
   */
  escrowRentRecipient: Address<TAccountEscrowRentRecipient>;
  /**
   * Escrow marker for the distribution PDA — closed here (rent →
   * `distribution.admin` via `escrow_rent_recipient`); close is the
   * distribution's only terminal path.
   */
  escrowMarker: Address<TAccountEscrowMarker>;
  paymentTokenProgram: Address<TAccountPaymentTokenProgram>;
};

export function getCloseDistributionInstruction<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountDistribution extends string,
  TAccountPaymentMint extends string,
  TAccountEscrow extends string,
  TAccountRefundAccount extends string,
  TAccountEscrowRentRecipient extends string,
  TAccountEscrowMarker extends string,
  TAccountPaymentTokenProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: CloseDistributionInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountDistribution,
    TAccountPaymentMint,
    TAccountEscrow,
    TAccountRefundAccount,
    TAccountEscrowRentRecipient,
    TAccountEscrowMarker,
    TAccountPaymentTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CloseDistributionInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAdminRecord,
  TAccountDistribution,
  TAccountPaymentMint,
  TAccountEscrow,
  TAccountRefundAccount,
  TAccountEscrowRentRecipient,
  TAccountEscrowMarker,
  TAccountPaymentTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    distribution: { value: input.distribution ?? null, isWritable: true },
    paymentMint: { value: input.paymentMint ?? null, isWritable: false },
    escrow: { value: input.escrow ?? null, isWritable: true },
    refundAccount: { value: input.refundAccount ?? null, isWritable: true },
    escrowRentRecipient: {
      value: input.escrowRentRecipient ?? null,
      isWritable: true,
    },
    escrowMarker: { value: input.escrowMarker ?? null, isWritable: true },
    paymentTokenProgram: {
      value: input.paymentTokenProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.distribution),
      getAccountMeta(accounts.paymentMint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.refundAccount),
      getAccountMeta(accounts.escrowRentRecipient),
      getAccountMeta(accounts.escrowMarker),
      getAccountMeta(accounts.paymentTokenProgram),
    ],
    data: getCloseDistributionInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseDistributionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountDistribution,
    TAccountPaymentMint,
    TAccountEscrow,
    TAccountRefundAccount,
    TAccountEscrowRentRecipient,
    TAccountEscrowMarker,
    TAccountPaymentTokenProgram
  >);
}

export type ParsedCloseDistributionInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Any platform Admin. Receives no rent (2D: all rent goes to
     * `distribution.admin`, whichever Admin closes).
     */
    authority: TAccountMetas[0];
    /** Admin gate — only an admin may close distributions. */
    adminRecord: TAccountMetas[1];
    distribution: TAccountMetas[2];
    paymentMint: TAccountMetas[3];
    escrow: TAccountMetas[4];
    /**
     * Receives the undistributed remainder — must be owned by the wallet
     * that funded the distribution (`distribution.funder`).
     */
    refundAccount: TAccountMetas[5];
    /**
     * lamports — constrained to be `distribution.admin`, the Admin that paid
     * for both at `create_distribution` (2D). The token remainder still goes
     * only to the funder's `refund_account`.
     */
    escrowRentRecipient: TAccountMetas[6];
    /**
     * Escrow marker for the distribution PDA — closed here (rent →
     * `distribution.admin` via `escrow_rent_recipient`); close is the
     * distribution's only terminal path.
     */
    escrowMarker: TAccountMetas[7];
    paymentTokenProgram: TAccountMetas[8];
  };
  data: CloseDistributionInstructionData;
};

export function parseCloseDistributionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseDistributionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      adminRecord: getNextAccount(),
      distribution: getNextAccount(),
      paymentMint: getNextAccount(),
      escrow: getNextAccount(),
      refundAccount: getNextAccount(),
      escrowRentRecipient: getNextAccount(),
      escrowMarker: getNextAccount(),
      paymentTokenProgram: getNextAccount(),
    },
    data: getCloseDistributionInstructionDataDecoder().decode(instruction.data),
  };
}
