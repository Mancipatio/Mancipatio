/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findPlatformPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SET_ISSUER_PERMISSIONS_DISCRIMINATOR = new Uint8Array([
  65, 58, 151, 175, 34, 194, 81, 70,
]);

export function getSetIssuerPermissionsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_ISSUER_PERMISSIONS_DISCRIMINATOR,
  );
}

export type SetIssuerPermissionsInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountSuperAdmin extends string | AccountMeta<string> = string,
  TAccountPlatform extends string | AccountMeta<string> = string,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TAccountPermissions extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSuperAdmin extends string
        ? WritableSignerAccount<TAccountSuperAdmin> &
            AccountSignerMeta<TAccountSuperAdmin>
        : TAccountSuperAdmin,
      TAccountPlatform extends string
        ? ReadonlyAccount<TAccountPlatform>
        : TAccountPlatform,
      TAccountIssuer extends string
        ? ReadonlyAccount<TAccountIssuer>
        : TAccountIssuer,
      TAccountPermissions extends string
        ? WritableAccount<TAccountPermissions>
        : TAccountPermissions,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SetIssuerPermissionsInstructionData = {
  discriminator: ReadonlyUint8Array;
  capabilities: number;
};

export type SetIssuerPermissionsInstructionDataArgs = { capabilities: number };

export function getSetIssuerPermissionsInstructionDataEncoder(): FixedSizeEncoder<SetIssuerPermissionsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["capabilities", getU8Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: SET_ISSUER_PERMISSIONS_DISCRIMINATOR,
    }),
  );
}

export function getSetIssuerPermissionsInstructionDataDecoder(): FixedSizeDecoder<SetIssuerPermissionsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["capabilities", getU8Decoder()],
  ]);
}

export function getSetIssuerPermissionsInstructionDataCodec(): FixedSizeCodec<
  SetIssuerPermissionsInstructionDataArgs,
  SetIssuerPermissionsInstructionData
> {
  return combineCodec(
    getSetIssuerPermissionsInstructionDataEncoder(),
    getSetIssuerPermissionsInstructionDataDecoder(),
  );
}

export type SetIssuerPermissionsAsyncInput<
  TAccountSuperAdmin extends string = string,
  TAccountPlatform extends string = string,
  TAccountIssuer extends string = string,
  TAccountPermissions extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  superAdmin: TransactionSigner<TAccountSuperAdmin>;
  platform?: Address<TAccountPlatform>;
  issuer: Address<TAccountIssuer>;
  permissions: Address<TAccountPermissions>;
  systemProgram?: Address<TAccountSystemProgram>;
  capabilities: SetIssuerPermissionsInstructionDataArgs["capabilities"];
};

export async function getSetIssuerPermissionsInstructionAsync<
  TAccountSuperAdmin extends string,
  TAccountPlatform extends string,
  TAccountIssuer extends string,
  TAccountPermissions extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: SetIssuerPermissionsAsyncInput<
    TAccountSuperAdmin,
    TAccountPlatform,
    TAccountIssuer,
    TAccountPermissions,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetIssuerPermissionsInstruction<
    TProgramAddress,
    TAccountSuperAdmin,
    TAccountPlatform,
    TAccountIssuer,
    TAccountPermissions,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    superAdmin: { value: input.superAdmin ?? null, isWritable: true },
    platform: { value: input.platform ?? null, isWritable: false },
    issuer: { value: input.issuer ?? null, isWritable: false },
    permissions: { value: input.permissions ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.platform.value) {
    accounts.platform.value = await findPlatformPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.superAdmin),
      getAccountMeta(accounts.platform),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.permissions),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSetIssuerPermissionsInstructionDataEncoder().encode(
      args as SetIssuerPermissionsInstructionDataArgs,
    ),
    programAddress,
  } as SetIssuerPermissionsInstruction<
    TProgramAddress,
    TAccountSuperAdmin,
    TAccountPlatform,
    TAccountIssuer,
    TAccountPermissions,
    TAccountSystemProgram
  >);
}

export type SetIssuerPermissionsInput<
  TAccountSuperAdmin extends string = string,
  TAccountPlatform extends string = string,
  TAccountIssuer extends string = string,
  TAccountPermissions extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  superAdmin: TransactionSigner<TAccountSuperAdmin>;
  platform: Address<TAccountPlatform>;
  issuer: Address<TAccountIssuer>;
  permissions: Address<TAccountPermissions>;
  systemProgram?: Address<TAccountSystemProgram>;
  capabilities: SetIssuerPermissionsInstructionDataArgs["capabilities"];
};

export function getSetIssuerPermissionsInstruction<
  TAccountSuperAdmin extends string,
  TAccountPlatform extends string,
  TAccountIssuer extends string,
  TAccountPermissions extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: SetIssuerPermissionsInput<
    TAccountSuperAdmin,
    TAccountPlatform,
    TAccountIssuer,
    TAccountPermissions,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SetIssuerPermissionsInstruction<
  TProgramAddress,
  TAccountSuperAdmin,
  TAccountPlatform,
  TAccountIssuer,
  TAccountPermissions,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    superAdmin: { value: input.superAdmin ?? null, isWritable: true },
    platform: { value: input.platform ?? null, isWritable: false },
    issuer: { value: input.issuer ?? null, isWritable: false },
    permissions: { value: input.permissions ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.superAdmin),
      getAccountMeta(accounts.platform),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.permissions),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSetIssuerPermissionsInstructionDataEncoder().encode(
      args as SetIssuerPermissionsInstructionDataArgs,
    ),
    programAddress,
  } as SetIssuerPermissionsInstruction<
    TProgramAddress,
    TAccountSuperAdmin,
    TAccountPlatform,
    TAccountIssuer,
    TAccountPermissions,
    TAccountSystemProgram
  >);
}

export type ParsedSetIssuerPermissionsInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    superAdmin: TAccountMetas[0];
    platform: TAccountMetas[1];
    issuer: TAccountMetas[2];
    permissions: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: SetIssuerPermissionsInstructionData;
};

export function parseSetIssuerPermissionsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetIssuerPermissionsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      superAdmin: getNextAccount(),
      platform: getNextAccount(),
      issuer: getNextAccount(),
      permissions: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSetIssuerPermissionsInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
