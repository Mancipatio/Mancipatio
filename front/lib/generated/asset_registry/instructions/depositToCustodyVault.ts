/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findPlatformPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const DEPOSIT_TO_CUSTODY_VAULT_DISCRIMINATOR = new Uint8Array([
  18, 233, 139, 124, 153, 3, 51, 64,
]);

export function getDepositToCustodyVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DEPOSIT_TO_CUSTODY_VAULT_DISCRIMINATOR,
  );
}

export type DepositToCustodyVaultInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountDepositor extends string | AccountMeta<string> = string,
  TAccountShareClass extends string | AccountMeta<string> = string,
  TAccountCustodyVault extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountDepositorShareAccount extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountPlatform extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountDepositor extends string
        ? ReadonlySignerAccount<TAccountDepositor> &
            AccountSignerMeta<TAccountDepositor>
        : TAccountDepositor,
      TAccountShareClass extends string
        ? ReadonlyAccount<TAccountShareClass>
        : TAccountShareClass,
      TAccountCustodyVault extends string
        ? WritableAccount<TAccountCustodyVault>
        : TAccountCustodyVault,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountDepositorShareAccount extends string
        ? WritableAccount<TAccountDepositorShareAccount>
        : TAccountDepositorShareAccount,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountPlatform extends string
        ? ReadonlyAccount<TAccountPlatform>
        : TAccountPlatform,
      ...TRemainingAccounts,
    ]
  >;

export type DepositToCustodyVaultInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type DepositToCustodyVaultInstructionDataArgs = {
  amount: number | bigint;
};

export function getDepositToCustodyVaultInstructionDataEncoder(): FixedSizeEncoder<DepositToCustodyVaultInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: DEPOSIT_TO_CUSTODY_VAULT_DISCRIMINATOR,
    }),
  );
}

export function getDepositToCustodyVaultInstructionDataDecoder(): FixedSizeDecoder<DepositToCustodyVaultInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getDepositToCustodyVaultInstructionDataCodec(): FixedSizeCodec<
  DepositToCustodyVaultInstructionDataArgs,
  DepositToCustodyVaultInstructionData
> {
  return combineCodec(
    getDepositToCustodyVaultInstructionDataEncoder(),
    getDepositToCustodyVaultInstructionDataDecoder(),
  );
}

export type DepositToCustodyVaultAsyncInput<
  TAccountDepositor extends string = string,
  TAccountShareClass extends string = string,
  TAccountCustodyVault extends string = string,
  TAccountMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountDepositorShareAccount extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountPlatform extends string = string,
> = {
  /**
   * The depositing holder — signs the escrow-funding `transfer_checked`.
   * For a `DeliveryEscrow` vault this MUST be the vault's `beneficiary`
   * (checked in the handler): the ledger this instruction writes is the
   * evidence `return_custody_vault` uses to release units KYC-free, so it
   * may only ever record the beneficiary's own units.
   */
  depositor: TransactionSigner<TAccountDepositor>;
  shareClass: Address<TAccountShareClass>;
  custodyVault: Address<TAccountCustodyVault>;
  mint: Address<TAccountMint>;
  escrow: Address<TAccountEscrow>;
  /** The depositor's share-class token account — debited exactly `amount`. */
  depositorShareAccount: Address<TAccountDepositorShareAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
  /**
   * Emergency-pause gate (read-only). Keep LAST among named accounts: old
   * account indices and the remaining-accounts hook tail keep their positions.
   */
  platform?: Address<TAccountPlatform>;
  amount: DepositToCustodyVaultInstructionDataArgs["amount"];
};

export async function getDepositToCustodyVaultInstructionAsync<
  TAccountDepositor extends string,
  TAccountShareClass extends string,
  TAccountCustodyVault extends string,
  TAccountMint extends string,
  TAccountEscrow extends string,
  TAccountDepositorShareAccount extends string,
  TAccountTokenProgram extends string,
  TAccountPlatform extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: DepositToCustodyVaultAsyncInput<
    TAccountDepositor,
    TAccountShareClass,
    TAccountCustodyVault,
    TAccountMint,
    TAccountEscrow,
    TAccountDepositorShareAccount,
    TAccountTokenProgram,
    TAccountPlatform
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DepositToCustodyVaultInstruction<
    TProgramAddress,
    TAccountDepositor,
    TAccountShareClass,
    TAccountCustodyVault,
    TAccountMint,
    TAccountEscrow,
    TAccountDepositorShareAccount,
    TAccountTokenProgram,
    TAccountPlatform
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    depositor: { value: input.depositor ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: false },
    custodyVault: { value: input.custodyVault ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    escrow: { value: input.escrow ?? null, isWritable: true },
    depositorShareAccount: {
      value: input.depositorShareAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    platform: { value: input.platform ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.platform.value) {
    accounts.platform.value = await findPlatformPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.depositor),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.custodyVault),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.depositorShareAccount),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.platform),
    ],
    data: getDepositToCustodyVaultInstructionDataEncoder().encode(
      args as DepositToCustodyVaultInstructionDataArgs,
    ),
    programAddress,
  } as DepositToCustodyVaultInstruction<
    TProgramAddress,
    TAccountDepositor,
    TAccountShareClass,
    TAccountCustodyVault,
    TAccountMint,
    TAccountEscrow,
    TAccountDepositorShareAccount,
    TAccountTokenProgram,
    TAccountPlatform
  >);
}

export type DepositToCustodyVaultInput<
  TAccountDepositor extends string = string,
  TAccountShareClass extends string = string,
  TAccountCustodyVault extends string = string,
  TAccountMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountDepositorShareAccount extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountPlatform extends string = string,
> = {
  /**
   * The depositing holder — signs the escrow-funding `transfer_checked`.
   * For a `DeliveryEscrow` vault this MUST be the vault's `beneficiary`
   * (checked in the handler): the ledger this instruction writes is the
   * evidence `return_custody_vault` uses to release units KYC-free, so it
   * may only ever record the beneficiary's own units.
   */
  depositor: TransactionSigner<TAccountDepositor>;
  shareClass: Address<TAccountShareClass>;
  custodyVault: Address<TAccountCustodyVault>;
  mint: Address<TAccountMint>;
  escrow: Address<TAccountEscrow>;
  /** The depositor's share-class token account — debited exactly `amount`. */
  depositorShareAccount: Address<TAccountDepositorShareAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
  /**
   * Emergency-pause gate (read-only). Keep LAST among named accounts: old
   * account indices and the remaining-accounts hook tail keep their positions.
   */
  platform: Address<TAccountPlatform>;
  amount: DepositToCustodyVaultInstructionDataArgs["amount"];
};

export function getDepositToCustodyVaultInstruction<
  TAccountDepositor extends string,
  TAccountShareClass extends string,
  TAccountCustodyVault extends string,
  TAccountMint extends string,
  TAccountEscrow extends string,
  TAccountDepositorShareAccount extends string,
  TAccountTokenProgram extends string,
  TAccountPlatform extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: DepositToCustodyVaultInput<
    TAccountDepositor,
    TAccountShareClass,
    TAccountCustodyVault,
    TAccountMint,
    TAccountEscrow,
    TAccountDepositorShareAccount,
    TAccountTokenProgram,
    TAccountPlatform
  >,
  config?: { programAddress?: TProgramAddress },
): DepositToCustodyVaultInstruction<
  TProgramAddress,
  TAccountDepositor,
  TAccountShareClass,
  TAccountCustodyVault,
  TAccountMint,
  TAccountEscrow,
  TAccountDepositorShareAccount,
  TAccountTokenProgram,
  TAccountPlatform
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    depositor: { value: input.depositor ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: false },
    custodyVault: { value: input.custodyVault ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    escrow: { value: input.escrow ?? null, isWritable: true },
    depositorShareAccount: {
      value: input.depositorShareAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    platform: { value: input.platform ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.depositor),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.custodyVault),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.depositorShareAccount),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.platform),
    ],
    data: getDepositToCustodyVaultInstructionDataEncoder().encode(
      args as DepositToCustodyVaultInstructionDataArgs,
    ),
    programAddress,
  } as DepositToCustodyVaultInstruction<
    TProgramAddress,
    TAccountDepositor,
    TAccountShareClass,
    TAccountCustodyVault,
    TAccountMint,
    TAccountEscrow,
    TAccountDepositorShareAccount,
    TAccountTokenProgram,
    TAccountPlatform
  >);
}

export type ParsedDepositToCustodyVaultInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * The depositing holder — signs the escrow-funding `transfer_checked`.
     * For a `DeliveryEscrow` vault this MUST be the vault's `beneficiary`
     * (checked in the handler): the ledger this instruction writes is the
     * evidence `return_custody_vault` uses to release units KYC-free, so it
     * may only ever record the beneficiary's own units.
     */
    depositor: TAccountMetas[0];
    shareClass: TAccountMetas[1];
    custodyVault: TAccountMetas[2];
    mint: TAccountMetas[3];
    escrow: TAccountMetas[4];
    /** The depositor's share-class token account — debited exactly `amount`. */
    depositorShareAccount: TAccountMetas[5];
    tokenProgram: TAccountMetas[6];
    /**
     * Emergency-pause gate (read-only). Keep LAST among named accounts: old
     * account indices and the remaining-accounts hook tail keep their positions.
     */
    platform: TAccountMetas[7];
  };
  data: DepositToCustodyVaultInstructionData;
};

export function parseDepositToCustodyVaultInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDepositToCustodyVaultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      depositor: getNextAccount(),
      shareClass: getNextAccount(),
      custodyVault: getNextAccount(),
      mint: getNextAccount(),
      escrow: getNextAccount(),
      depositorShareAccount: getNextAccount(),
      tokenProgram: getNextAccount(),
      platform: getNextAccount(),
    },
    data: getDepositToCustodyVaultInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
