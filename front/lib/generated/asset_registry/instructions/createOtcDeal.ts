/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  findAdminRecordPda,
  findAssetEscrowPda,
  findCancelOtcDealEscrowMarkerPda,
  findDealPda,
  findPaymentEscrowPda,
  findPlatformPda,
} from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CREATE_OTC_DEAL_DISCRIMINATOR = new Uint8Array([
  86, 214, 235, 88, 220, 56, 103, 15,
]);

export function getCreateOtcDealDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_OTC_DEAL_DISCRIMINATOR,
  );
}

export type CreateOtcDealInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAdminRecord extends string | AccountMeta<string> = string,
  TAccountShareClass extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountPaymentMint extends string | AccountMeta<string> = string,
  TAccountDeal extends string | AccountMeta<string> = string,
  TAccountAssetEscrow extends string | AccountMeta<string> = string,
  TAccountPaymentEscrow extends string | AccountMeta<string> = string,
  TAccountEscrowMarker extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountPaymentTokenProgram extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountPlatform extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAdminRecord extends string
        ? ReadonlyAccount<TAccountAdminRecord>
        : TAccountAdminRecord,
      TAccountShareClass extends string
        ? ReadonlyAccount<TAccountShareClass>
        : TAccountShareClass,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountPaymentMint extends string
        ? ReadonlyAccount<TAccountPaymentMint>
        : TAccountPaymentMint,
      TAccountDeal extends string
        ? WritableAccount<TAccountDeal>
        : TAccountDeal,
      TAccountAssetEscrow extends string
        ? WritableAccount<TAccountAssetEscrow>
        : TAccountAssetEscrow,
      TAccountPaymentEscrow extends string
        ? WritableAccount<TAccountPaymentEscrow>
        : TAccountPaymentEscrow,
      TAccountEscrowMarker extends string
        ? WritableAccount<TAccountEscrowMarker>
        : TAccountEscrowMarker,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountPaymentTokenProgram extends string
        ? ReadonlyAccount<TAccountPaymentTokenProgram>
        : TAccountPaymentTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountPlatform extends string
        ? ReadonlyAccount<TAccountPlatform>
        : TAccountPlatform,
      ...TRemainingAccounts,
    ]
  >;

export type CreateOtcDealInstructionData = {
  discriminator: ReadonlyUint8Array;
  dealId: bigint;
  buyer: Address;
  seller: Address;
  amount: bigint;
  price: bigint;
  paymentMint: Address;
  expiresAt: bigint;
};

export type CreateOtcDealInstructionDataArgs = {
  dealId: number | bigint;
  buyer: Address;
  seller: Address;
  amount: number | bigint;
  price: number | bigint;
  paymentMint: Address;
  expiresAt: number | bigint;
};

export function getCreateOtcDealInstructionDataEncoder(): FixedSizeEncoder<CreateOtcDealInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["dealId", getU64Encoder()],
      ["buyer", getAddressEncoder()],
      ["seller", getAddressEncoder()],
      ["amount", getU64Encoder()],
      ["price", getU64Encoder()],
      ["paymentMint", getAddressEncoder()],
      ["expiresAt", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_OTC_DEAL_DISCRIMINATOR }),
  );
}

export function getCreateOtcDealInstructionDataDecoder(): FixedSizeDecoder<CreateOtcDealInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["dealId", getU64Decoder()],
    ["buyer", getAddressDecoder()],
    ["seller", getAddressDecoder()],
    ["amount", getU64Decoder()],
    ["price", getU64Decoder()],
    ["paymentMint", getAddressDecoder()],
    ["expiresAt", getI64Decoder()],
  ]);
}

export function getCreateOtcDealInstructionDataCodec(): FixedSizeCodec<
  CreateOtcDealInstructionDataArgs,
  CreateOtcDealInstructionData
> {
  return combineCodec(
    getCreateOtcDealInstructionDataEncoder(),
    getCreateOtcDealInstructionDataDecoder(),
  );
}

export type CreateOtcDealAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountShareClass extends string = string,
  TAccountMint extends string = string,
  TAccountPaymentMint extends string = string,
  TAccountDeal extends string = string,
  TAccountAssetEscrow extends string = string,
  TAccountPaymentEscrow extends string = string,
  TAccountEscrowMarker extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountPaymentTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountPlatform extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /**
   * Admin gate — only an admin may create OTC deals (the platform opens the
   * deal after the parties have agreed off-chain).
   */
  adminRecord?: Address<TAccountAdminRecord>;
  shareClass: Address<TAccountShareClass>;
  mint: Address<TAccountMint>;
  /** Payment mint the buyer will pay in (plain SPL or Token-2022). */
  paymentMint: Address<TAccountPaymentMint>;
  deal?: Address<TAccountDeal>;
  /** Escrow for the seller's deposited share units; authority is the deal PDA. */
  assetEscrow?: Address<TAccountAssetEscrow>;
  /** Escrow for the buyer's deposited payment; authority is the deal PDA. */
  paymentEscrow?: Address<TAccountPaymentEscrow>;
  /**
   * Marks the deal PDA as a platform escrow authority — the transfer hook
   * exempts escrow legs from receiver-KYC while this exists. Closed on
   * every terminal path (settle / cancel / expire).
   */
  escrowMarker?: Address<TAccountEscrowMarker>;
  tokenProgram?: Address<TAccountTokenProgram>;
  paymentTokenProgram: Address<TAccountPaymentTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  /**
   * Emergency-pause gate (read-only). Keep LAST among named accounts: old
   * account indices and the remaining-accounts hook tail keep their positions.
   */
  platform?: Address<TAccountPlatform>;
  dealId: CreateOtcDealInstructionDataArgs["dealId"];
  buyer: CreateOtcDealInstructionDataArgs["buyer"];
  seller: CreateOtcDealInstructionDataArgs["seller"];
  amount: CreateOtcDealInstructionDataArgs["amount"];
  price: CreateOtcDealInstructionDataArgs["price"];
  paymentMintArg: CreateOtcDealInstructionDataArgs["paymentMint"];
  expiresAt: CreateOtcDealInstructionDataArgs["expiresAt"];
};

export async function getCreateOtcDealInstructionAsync<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountShareClass extends string,
  TAccountMint extends string,
  TAccountPaymentMint extends string,
  TAccountDeal extends string,
  TAccountAssetEscrow extends string,
  TAccountPaymentEscrow extends string,
  TAccountEscrowMarker extends string,
  TAccountTokenProgram extends string,
  TAccountPaymentTokenProgram extends string,
  TAccountSystemProgram extends string,
  TAccountPlatform extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: CreateOtcDealAsyncInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass,
    TAccountMint,
    TAccountPaymentMint,
    TAccountDeal,
    TAccountAssetEscrow,
    TAccountPaymentEscrow,
    TAccountEscrowMarker,
    TAccountTokenProgram,
    TAccountPaymentTokenProgram,
    TAccountSystemProgram,
    TAccountPlatform
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateOtcDealInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass,
    TAccountMint,
    TAccountPaymentMint,
    TAccountDeal,
    TAccountAssetEscrow,
    TAccountPaymentEscrow,
    TAccountEscrowMarker,
    TAccountTokenProgram,
    TAccountPaymentTokenProgram,
    TAccountSystemProgram,
    TAccountPlatform
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    paymentMint: { value: input.paymentMint ?? null, isWritable: false },
    deal: { value: input.deal ?? null, isWritable: true },
    assetEscrow: { value: input.assetEscrow ?? null, isWritable: true },
    paymentEscrow: { value: input.paymentEscrow ?? null, isWritable: true },
    escrowMarker: { value: input.escrowMarker ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    paymentTokenProgram: {
      value: input.paymentTokenProgram ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    platform: { value: input.platform ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input, paymentMint: input.paymentMintArg };

  // Resolve default values.
  if (!accounts.adminRecord.value) {
    accounts.adminRecord.value = await findAdminRecordPda({
      authority: expectAddress(accounts.authority.value),
    });
  }
  if (!accounts.deal.value) {
    accounts.deal.value = await findDealPda({
      shareClass: expectAddress(accounts.shareClass.value),
      dealId: expectSome(args.dealId),
    });
  }
  if (!accounts.assetEscrow.value) {
    accounts.assetEscrow.value = await findAssetEscrowPda({
      deal: expectAddress(accounts.deal.value),
    });
  }
  if (!accounts.paymentEscrow.value) {
    accounts.paymentEscrow.value = await findPaymentEscrowPda({
      deal: expectAddress(accounts.deal.value),
    });
  }
  if (!accounts.escrowMarker.value) {
    accounts.escrowMarker.value = await findCancelOtcDealEscrowMarkerPda({
      deal: expectAddress(accounts.deal.value),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.platform.value) {
    accounts.platform.value = await findPlatformPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.paymentMint),
      getAccountMeta(accounts.deal),
      getAccountMeta(accounts.assetEscrow),
      getAccountMeta(accounts.paymentEscrow),
      getAccountMeta(accounts.escrowMarker),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.paymentTokenProgram),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.platform),
    ],
    data: getCreateOtcDealInstructionDataEncoder().encode(
      args as CreateOtcDealInstructionDataArgs,
    ),
    programAddress,
  } as CreateOtcDealInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass,
    TAccountMint,
    TAccountPaymentMint,
    TAccountDeal,
    TAccountAssetEscrow,
    TAccountPaymentEscrow,
    TAccountEscrowMarker,
    TAccountTokenProgram,
    TAccountPaymentTokenProgram,
    TAccountSystemProgram,
    TAccountPlatform
  >);
}

export type CreateOtcDealInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountShareClass extends string = string,
  TAccountMint extends string = string,
  TAccountPaymentMint extends string = string,
  TAccountDeal extends string = string,
  TAccountAssetEscrow extends string = string,
  TAccountPaymentEscrow extends string = string,
  TAccountEscrowMarker extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountPaymentTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountPlatform extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /**
   * Admin gate — only an admin may create OTC deals (the platform opens the
   * deal after the parties have agreed off-chain).
   */
  adminRecord: Address<TAccountAdminRecord>;
  shareClass: Address<TAccountShareClass>;
  mint: Address<TAccountMint>;
  /** Payment mint the buyer will pay in (plain SPL or Token-2022). */
  paymentMint: Address<TAccountPaymentMint>;
  deal: Address<TAccountDeal>;
  /** Escrow for the seller's deposited share units; authority is the deal PDA. */
  assetEscrow: Address<TAccountAssetEscrow>;
  /** Escrow for the buyer's deposited payment; authority is the deal PDA. */
  paymentEscrow: Address<TAccountPaymentEscrow>;
  /**
   * Marks the deal PDA as a platform escrow authority — the transfer hook
   * exempts escrow legs from receiver-KYC while this exists. Closed on
   * every terminal path (settle / cancel / expire).
   */
  escrowMarker: Address<TAccountEscrowMarker>;
  tokenProgram?: Address<TAccountTokenProgram>;
  paymentTokenProgram: Address<TAccountPaymentTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  /**
   * Emergency-pause gate (read-only). Keep LAST among named accounts: old
   * account indices and the remaining-accounts hook tail keep their positions.
   */
  platform: Address<TAccountPlatform>;
  dealId: CreateOtcDealInstructionDataArgs["dealId"];
  buyer: CreateOtcDealInstructionDataArgs["buyer"];
  seller: CreateOtcDealInstructionDataArgs["seller"];
  amount: CreateOtcDealInstructionDataArgs["amount"];
  price: CreateOtcDealInstructionDataArgs["price"];
  paymentMintArg: CreateOtcDealInstructionDataArgs["paymentMint"];
  expiresAt: CreateOtcDealInstructionDataArgs["expiresAt"];
};

export function getCreateOtcDealInstruction<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountShareClass extends string,
  TAccountMint extends string,
  TAccountPaymentMint extends string,
  TAccountDeal extends string,
  TAccountAssetEscrow extends string,
  TAccountPaymentEscrow extends string,
  TAccountEscrowMarker extends string,
  TAccountTokenProgram extends string,
  TAccountPaymentTokenProgram extends string,
  TAccountSystemProgram extends string,
  TAccountPlatform extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: CreateOtcDealInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass,
    TAccountMint,
    TAccountPaymentMint,
    TAccountDeal,
    TAccountAssetEscrow,
    TAccountPaymentEscrow,
    TAccountEscrowMarker,
    TAccountTokenProgram,
    TAccountPaymentTokenProgram,
    TAccountSystemProgram,
    TAccountPlatform
  >,
  config?: { programAddress?: TProgramAddress },
): CreateOtcDealInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAdminRecord,
  TAccountShareClass,
  TAccountMint,
  TAccountPaymentMint,
  TAccountDeal,
  TAccountAssetEscrow,
  TAccountPaymentEscrow,
  TAccountEscrowMarker,
  TAccountTokenProgram,
  TAccountPaymentTokenProgram,
  TAccountSystemProgram,
  TAccountPlatform
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    paymentMint: { value: input.paymentMint ?? null, isWritable: false },
    deal: { value: input.deal ?? null, isWritable: true },
    assetEscrow: { value: input.assetEscrow ?? null, isWritable: true },
    paymentEscrow: { value: input.paymentEscrow ?? null, isWritable: true },
    escrowMarker: { value: input.escrowMarker ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    paymentTokenProgram: {
      value: input.paymentTokenProgram ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    platform: { value: input.platform ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input, paymentMint: input.paymentMintArg };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.paymentMint),
      getAccountMeta(accounts.deal),
      getAccountMeta(accounts.assetEscrow),
      getAccountMeta(accounts.paymentEscrow),
      getAccountMeta(accounts.escrowMarker),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.paymentTokenProgram),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.platform),
    ],
    data: getCreateOtcDealInstructionDataEncoder().encode(
      args as CreateOtcDealInstructionDataArgs,
    ),
    programAddress,
  } as CreateOtcDealInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass,
    TAccountMint,
    TAccountPaymentMint,
    TAccountDeal,
    TAccountAssetEscrow,
    TAccountPaymentEscrow,
    TAccountEscrowMarker,
    TAccountTokenProgram,
    TAccountPaymentTokenProgram,
    TAccountSystemProgram,
    TAccountPlatform
  >);
}

export type ParsedCreateOtcDealInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    /**
     * Admin gate — only an admin may create OTC deals (the platform opens the
     * deal after the parties have agreed off-chain).
     */
    adminRecord: TAccountMetas[1];
    shareClass: TAccountMetas[2];
    mint: TAccountMetas[3];
    /** Payment mint the buyer will pay in (plain SPL or Token-2022). */
    paymentMint: TAccountMetas[4];
    deal: TAccountMetas[5];
    /** Escrow for the seller's deposited share units; authority is the deal PDA. */
    assetEscrow: TAccountMetas[6];
    /** Escrow for the buyer's deposited payment; authority is the deal PDA. */
    paymentEscrow: TAccountMetas[7];
    /**
     * Marks the deal PDA as a platform escrow authority — the transfer hook
     * exempts escrow legs from receiver-KYC while this exists. Closed on
     * every terminal path (settle / cancel / expire).
     */
    escrowMarker: TAccountMetas[8];
    tokenProgram: TAccountMetas[9];
    paymentTokenProgram: TAccountMetas[10];
    systemProgram: TAccountMetas[11];
    /**
     * Emergency-pause gate (read-only). Keep LAST among named accounts: old
     * account indices and the remaining-accounts hook tail keep their positions.
     */
    platform: TAccountMetas[12];
  };
  data: CreateOtcDealInstructionData;
};

export function parseCreateOtcDealInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateOtcDealInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 13) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      adminRecord: getNextAccount(),
      shareClass: getNextAccount(),
      mint: getNextAccount(),
      paymentMint: getNextAccount(),
      deal: getNextAccount(),
      assetEscrow: getNextAccount(),
      paymentEscrow: getNextAccount(),
      escrowMarker: getNextAccount(),
      tokenProgram: getNextAccount(),
      paymentTokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
      platform: getNextAccount(),
    },
    data: getCreateOtcDealInstructionDataDecoder().decode(instruction.data),
  };
}
