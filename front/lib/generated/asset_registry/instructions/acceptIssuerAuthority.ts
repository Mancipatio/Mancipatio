/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  findAcceptIssuerAuthorityTransferPda,
  findNewAdminRecordPda,
  findNewPermissionsPda,
  findRecoveryPda,
} from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const ACCEPT_ISSUER_AUTHORITY_DISCRIMINATOR = new Uint8Array([
  68, 51, 39, 79, 55, 177, 220, 214,
]);

export function getAcceptIssuerAuthorityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ACCEPT_ISSUER_AUTHORITY_DISCRIMINATOR,
  );
}

export type AcceptIssuerAuthorityInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountNewAuthority extends string | AccountMeta<string> = string,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TAccountTransfer extends string | AccountMeta<string> = string,
  TAccountOldPermissions extends string | AccountMeta<string> = string,
  TAccountNewPermissions extends string | AccountMeta<string> = string,
  TAccountOldAdminRecord extends string | AccountMeta<string> = string,
  TAccountNewAdminRecord extends string | AccountMeta<string> = string,
  TAccountRecovery extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountNewAuthority extends string
        ? WritableSignerAccount<TAccountNewAuthority> &
            AccountSignerMeta<TAccountNewAuthority>
        : TAccountNewAuthority,
      TAccountIssuer extends string
        ? WritableAccount<TAccountIssuer>
        : TAccountIssuer,
      TAccountTransfer extends string
        ? WritableAccount<TAccountTransfer>
        : TAccountTransfer,
      TAccountOldPermissions extends string
        ? WritableAccount<TAccountOldPermissions>
        : TAccountOldPermissions,
      TAccountNewPermissions extends string
        ? WritableAccount<TAccountNewPermissions>
        : TAccountNewPermissions,
      TAccountOldAdminRecord extends string
        ? ReadonlyAccount<TAccountOldAdminRecord>
        : TAccountOldAdminRecord,
      TAccountNewAdminRecord extends string
        ? ReadonlyAccount<TAccountNewAdminRecord>
        : TAccountNewAdminRecord,
      TAccountRecovery extends string
        ? WritableAccount<TAccountRecovery>
        : TAccountRecovery,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AcceptIssuerAuthorityInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AcceptIssuerAuthorityInstructionDataArgs = {};

export function getAcceptIssuerAuthorityInstructionDataEncoder(): FixedSizeEncoder<AcceptIssuerAuthorityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: ACCEPT_ISSUER_AUTHORITY_DISCRIMINATOR,
    }),
  );
}

export function getAcceptIssuerAuthorityInstructionDataDecoder(): FixedSizeDecoder<AcceptIssuerAuthorityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAcceptIssuerAuthorityInstructionDataCodec(): FixedSizeCodec<
  AcceptIssuerAuthorityInstructionDataArgs,
  AcceptIssuerAuthorityInstructionData
> {
  return combineCodec(
    getAcceptIssuerAuthorityInstructionDataEncoder(),
    getAcceptIssuerAuthorityInstructionDataDecoder(),
  );
}

export type AcceptIssuerAuthorityAsyncInput<
  TAccountNewAuthority extends string = string,
  TAccountIssuer extends string = string,
  TAccountTransfer extends string = string,
  TAccountOldPermissions extends string = string,
  TAccountNewPermissions extends string = string,
  TAccountOldAdminRecord extends string = string,
  TAccountNewAdminRecord extends string = string,
  TAccountRecovery extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  newAuthority: TransactionSigner<TAccountNewAuthority>;
  issuer: Address<TAccountIssuer>;
  transfer?: Address<TAccountTransfer>;
  /**
   * The outgoing authority's grant, closed here when present (rent to the
   * acceptor); its capabilities move to `new_permissions`.
   * validated by `util::take_old_grant`. It may not exist.
   */
  oldPermissions: Address<TAccountOldPermissions>;
  newPermissions?: Address<TAccountNewPermissions>;
  /** The outgoing authority's global Admin PDA (usually missing). */
  oldAdminRecord: Address<TAccountOldAdminRecord>;
  /** The incoming key's global Admin PDA (usually missing). */
  newAdminRecord?: Address<TAccountNewAdminRecord>;
  /** The issuer's pending recovery, retired here when present (it may not exist). */
  recovery?: Address<TAccountRecovery>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getAcceptIssuerAuthorityInstructionAsync<
  TAccountNewAuthority extends string,
  TAccountIssuer extends string,
  TAccountTransfer extends string,
  TAccountOldPermissions extends string,
  TAccountNewPermissions extends string,
  TAccountOldAdminRecord extends string,
  TAccountNewAdminRecord extends string,
  TAccountRecovery extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: AcceptIssuerAuthorityAsyncInput<
    TAccountNewAuthority,
    TAccountIssuer,
    TAccountTransfer,
    TAccountOldPermissions,
    TAccountNewPermissions,
    TAccountOldAdminRecord,
    TAccountNewAdminRecord,
    TAccountRecovery,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AcceptIssuerAuthorityInstruction<
    TProgramAddress,
    TAccountNewAuthority,
    TAccountIssuer,
    TAccountTransfer,
    TAccountOldPermissions,
    TAccountNewPermissions,
    TAccountOldAdminRecord,
    TAccountNewAdminRecord,
    TAccountRecovery,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    newAuthority: { value: input.newAuthority ?? null, isWritable: true },
    issuer: { value: input.issuer ?? null, isWritable: true },
    transfer: { value: input.transfer ?? null, isWritable: true },
    oldPermissions: { value: input.oldPermissions ?? null, isWritable: true },
    newPermissions: { value: input.newPermissions ?? null, isWritable: true },
    oldAdminRecord: { value: input.oldAdminRecord ?? null, isWritable: false },
    newAdminRecord: { value: input.newAdminRecord ?? null, isWritable: false },
    recovery: { value: input.recovery ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.transfer.value) {
    accounts.transfer.value = await findAcceptIssuerAuthorityTransferPda({
      issuer: expectAddress(accounts.issuer.value),
    });
  }
  if (!accounts.newPermissions.value) {
    accounts.newPermissions.value = await findNewPermissionsPda({
      issuer: expectAddress(accounts.issuer.value),
      newAuthority: expectAddress(accounts.newAuthority.value),
    });
  }
  if (!accounts.newAdminRecord.value) {
    accounts.newAdminRecord.value = await findNewAdminRecordPda({
      newAuthority: expectAddress(accounts.newAuthority.value),
    });
  }
  if (!accounts.recovery.value) {
    accounts.recovery.value = await findRecoveryPda({
      issuer: expectAddress(accounts.issuer.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.newAuthority),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.transfer),
      getAccountMeta(accounts.oldPermissions),
      getAccountMeta(accounts.newPermissions),
      getAccountMeta(accounts.oldAdminRecord),
      getAccountMeta(accounts.newAdminRecord),
      getAccountMeta(accounts.recovery),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAcceptIssuerAuthorityInstructionDataEncoder().encode({}),
    programAddress,
  } as AcceptIssuerAuthorityInstruction<
    TProgramAddress,
    TAccountNewAuthority,
    TAccountIssuer,
    TAccountTransfer,
    TAccountOldPermissions,
    TAccountNewPermissions,
    TAccountOldAdminRecord,
    TAccountNewAdminRecord,
    TAccountRecovery,
    TAccountSystemProgram
  >);
}

export type AcceptIssuerAuthorityInput<
  TAccountNewAuthority extends string = string,
  TAccountIssuer extends string = string,
  TAccountTransfer extends string = string,
  TAccountOldPermissions extends string = string,
  TAccountNewPermissions extends string = string,
  TAccountOldAdminRecord extends string = string,
  TAccountNewAdminRecord extends string = string,
  TAccountRecovery extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  newAuthority: TransactionSigner<TAccountNewAuthority>;
  issuer: Address<TAccountIssuer>;
  transfer: Address<TAccountTransfer>;
  /**
   * The outgoing authority's grant, closed here when present (rent to the
   * acceptor); its capabilities move to `new_permissions`.
   * validated by `util::take_old_grant`. It may not exist.
   */
  oldPermissions: Address<TAccountOldPermissions>;
  newPermissions: Address<TAccountNewPermissions>;
  /** The outgoing authority's global Admin PDA (usually missing). */
  oldAdminRecord: Address<TAccountOldAdminRecord>;
  /** The incoming key's global Admin PDA (usually missing). */
  newAdminRecord: Address<TAccountNewAdminRecord>;
  /** The issuer's pending recovery, retired here when present (it may not exist). */
  recovery: Address<TAccountRecovery>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getAcceptIssuerAuthorityInstruction<
  TAccountNewAuthority extends string,
  TAccountIssuer extends string,
  TAccountTransfer extends string,
  TAccountOldPermissions extends string,
  TAccountNewPermissions extends string,
  TAccountOldAdminRecord extends string,
  TAccountNewAdminRecord extends string,
  TAccountRecovery extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: AcceptIssuerAuthorityInput<
    TAccountNewAuthority,
    TAccountIssuer,
    TAccountTransfer,
    TAccountOldPermissions,
    TAccountNewPermissions,
    TAccountOldAdminRecord,
    TAccountNewAdminRecord,
    TAccountRecovery,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AcceptIssuerAuthorityInstruction<
  TProgramAddress,
  TAccountNewAuthority,
  TAccountIssuer,
  TAccountTransfer,
  TAccountOldPermissions,
  TAccountNewPermissions,
  TAccountOldAdminRecord,
  TAccountNewAdminRecord,
  TAccountRecovery,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    newAuthority: { value: input.newAuthority ?? null, isWritable: true },
    issuer: { value: input.issuer ?? null, isWritable: true },
    transfer: { value: input.transfer ?? null, isWritable: true },
    oldPermissions: { value: input.oldPermissions ?? null, isWritable: true },
    newPermissions: { value: input.newPermissions ?? null, isWritable: true },
    oldAdminRecord: { value: input.oldAdminRecord ?? null, isWritable: false },
    newAdminRecord: { value: input.newAdminRecord ?? null, isWritable: false },
    recovery: { value: input.recovery ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.newAuthority),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.transfer),
      getAccountMeta(accounts.oldPermissions),
      getAccountMeta(accounts.newPermissions),
      getAccountMeta(accounts.oldAdminRecord),
      getAccountMeta(accounts.newAdminRecord),
      getAccountMeta(accounts.recovery),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAcceptIssuerAuthorityInstructionDataEncoder().encode({}),
    programAddress,
  } as AcceptIssuerAuthorityInstruction<
    TProgramAddress,
    TAccountNewAuthority,
    TAccountIssuer,
    TAccountTransfer,
    TAccountOldPermissions,
    TAccountNewPermissions,
    TAccountOldAdminRecord,
    TAccountNewAdminRecord,
    TAccountRecovery,
    TAccountSystemProgram
  >);
}

export type ParsedAcceptIssuerAuthorityInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    newAuthority: TAccountMetas[0];
    issuer: TAccountMetas[1];
    transfer: TAccountMetas[2];
    /**
     * The outgoing authority's grant, closed here when present (rent to the
     * acceptor); its capabilities move to `new_permissions`.
     * validated by `util::take_old_grant`. It may not exist.
     */
    oldPermissions: TAccountMetas[3];
    newPermissions: TAccountMetas[4];
    /** The outgoing authority's global Admin PDA (usually missing). */
    oldAdminRecord: TAccountMetas[5];
    /** The incoming key's global Admin PDA (usually missing). */
    newAdminRecord: TAccountMetas[6];
    /** The issuer's pending recovery, retired here when present (it may not exist). */
    recovery: TAccountMetas[7];
    systemProgram: TAccountMetas[8];
  };
  data: AcceptIssuerAuthorityInstructionData;
};

export function parseAcceptIssuerAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAcceptIssuerAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      newAuthority: getNextAccount(),
      issuer: getNextAccount(),
      transfer: getNextAccount(),
      oldPermissions: getNextAccount(),
      newPermissions: getNextAccount(),
      oldAdminRecord: getNextAccount(),
      newAdminRecord: getNextAccount(),
      recovery: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAcceptIssuerAuthorityInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
