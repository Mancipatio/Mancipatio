/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findAdminRecordPda, findSaleApprovalPda, findSalePda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";
import {
  getRaiseTypeDecoder,
  getRaiseTypeEncoder,
  type RaiseType,
  type RaiseTypeArgs,
} from "../types";

export const APPROVE_SALE_DISCRIMINATOR = new Uint8Array([
  161, 214, 32, 147, 179, 73, 138, 227,
]);

export function getApproveSaleDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPROVE_SALE_DISCRIMINATOR,
  );
}

export type ApproveSaleInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAdminRecord extends string | AccountMeta<string> = string,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TAccountAsset extends string | AccountMeta<string> = string,
  TAccountShareClass extends string | AccountMeta<string> = string,
  TAccountPaymentMint extends string | AccountMeta<string> = string,
  TAccountSale extends string | AccountMeta<string> = string,
  TAccountSaleApproval extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAdminRecord extends string
        ? ReadonlyAccount<TAccountAdminRecord>
        : TAccountAdminRecord,
      TAccountIssuer extends string
        ? ReadonlyAccount<TAccountIssuer>
        : TAccountIssuer,
      TAccountAsset extends string
        ? ReadonlyAccount<TAccountAsset>
        : TAccountAsset,
      TAccountShareClass extends string
        ? ReadonlyAccount<TAccountShareClass>
        : TAccountShareClass,
      TAccountPaymentMint extends string
        ? ReadonlyAccount<TAccountPaymentMint>
        : TAccountPaymentMint,
      TAccountSale extends string
        ? ReadonlyAccount<TAccountSale>
        : TAccountSale,
      TAccountSaleApproval extends string
        ? WritableAccount<TAccountSaleApproval>
        : TAccountSaleApproval,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ApproveSaleInstructionData = {
  discriminator: ReadonlyUint8Array;
  saleId: bigint;
  maxGrossRaise: bigint;
  minPricePerUnit: bigint;
  maxPricePerUnit: bigint;
  raiseType: RaiseType;
  expiresAt: bigint;
  applicationHash: ReadonlyUint8Array;
};

export type ApproveSaleInstructionDataArgs = {
  saleId: number | bigint;
  maxGrossRaise: number | bigint;
  minPricePerUnit: number | bigint;
  maxPricePerUnit: number | bigint;
  raiseType: RaiseTypeArgs;
  expiresAt: number | bigint;
  applicationHash: ReadonlyUint8Array;
};

export function getApproveSaleInstructionDataEncoder(): FixedSizeEncoder<ApproveSaleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["saleId", getU64Encoder()],
      ["maxGrossRaise", getU64Encoder()],
      ["minPricePerUnit", getU64Encoder()],
      ["maxPricePerUnit", getU64Encoder()],
      ["raiseType", getRaiseTypeEncoder()],
      ["expiresAt", getI64Encoder()],
      ["applicationHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: APPROVE_SALE_DISCRIMINATOR }),
  );
}

export function getApproveSaleInstructionDataDecoder(): FixedSizeDecoder<ApproveSaleInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["saleId", getU64Decoder()],
    ["maxGrossRaise", getU64Decoder()],
    ["minPricePerUnit", getU64Decoder()],
    ["maxPricePerUnit", getU64Decoder()],
    ["raiseType", getRaiseTypeDecoder()],
    ["expiresAt", getI64Decoder()],
    ["applicationHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getApproveSaleInstructionDataCodec(): FixedSizeCodec<
  ApproveSaleInstructionDataArgs,
  ApproveSaleInstructionData
> {
  return combineCodec(
    getApproveSaleInstructionDataEncoder(),
    getApproveSaleInstructionDataDecoder(),
  );
}

export type ApproveSaleAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountIssuer extends string = string,
  TAccountAsset extends string = string,
  TAccountShareClass extends string = string,
  TAccountPaymentMint extends string = string,
  TAccountSale extends string = string,
  TAccountSaleApproval extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The approving Admin; pays the approval's rent and becomes `approved_by`. */
  authority: TransactionSigner<TAccountAuthority>;
  /** Admin gate: only a platform Admin may approve a sale. */
  adminRecord?: Address<TAccountAdminRecord>;
  issuer: Address<TAccountIssuer>;
  asset: Address<TAccountAsset>;
  shareClass: Address<TAccountShareClass>;
  /**
   * The payment mint the approved sale must use (validated in the handler
   * with the same payment-leg rule `open_sale` applies to its escrow).
   */
  paymentMint: Address<TAccountPaymentMint>;
  /**
   * The future `Sale` PDA for this id. It must still be empty, so the
   * approval can actually be consumed.
   */
  sale?: Address<TAccountSale>;
  saleApproval?: Address<TAccountSaleApproval>;
  systemProgram?: Address<TAccountSystemProgram>;
  saleId: ApproveSaleInstructionDataArgs["saleId"];
  maxGrossRaise: ApproveSaleInstructionDataArgs["maxGrossRaise"];
  minPricePerUnit: ApproveSaleInstructionDataArgs["minPricePerUnit"];
  maxPricePerUnit: ApproveSaleInstructionDataArgs["maxPricePerUnit"];
  raiseType: ApproveSaleInstructionDataArgs["raiseType"];
  expiresAt: ApproveSaleInstructionDataArgs["expiresAt"];
  applicationHash: ApproveSaleInstructionDataArgs["applicationHash"];
};

export async function getApproveSaleInstructionAsync<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountIssuer extends string,
  TAccountAsset extends string,
  TAccountShareClass extends string,
  TAccountPaymentMint extends string,
  TAccountSale extends string,
  TAccountSaleApproval extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ApproveSaleAsyncInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountIssuer,
    TAccountAsset,
    TAccountShareClass,
    TAccountPaymentMint,
    TAccountSale,
    TAccountSaleApproval,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ApproveSaleInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountIssuer,
    TAccountAsset,
    TAccountShareClass,
    TAccountPaymentMint,
    TAccountSale,
    TAccountSaleApproval,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    issuer: { value: input.issuer ?? null, isWritable: false },
    asset: { value: input.asset ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: false },
    paymentMint: { value: input.paymentMint ?? null, isWritable: false },
    sale: { value: input.sale ?? null, isWritable: false },
    saleApproval: { value: input.saleApproval ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.adminRecord.value) {
    accounts.adminRecord.value = await findAdminRecordPda({
      authority: expectAddress(accounts.authority.value),
    });
  }
  if (!accounts.sale.value) {
    accounts.sale.value = await findSalePda({
      shareClass: expectAddress(accounts.shareClass.value),
      saleId: expectSome(args.saleId),
    });
  }
  if (!accounts.saleApproval.value) {
    accounts.saleApproval.value = await findSaleApprovalPda({
      shareClass: expectAddress(accounts.shareClass.value),
      saleId: expectSome(args.saleId),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.asset),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.paymentMint),
      getAccountMeta(accounts.sale),
      getAccountMeta(accounts.saleApproval),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getApproveSaleInstructionDataEncoder().encode(
      args as ApproveSaleInstructionDataArgs,
    ),
    programAddress,
  } as ApproveSaleInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountIssuer,
    TAccountAsset,
    TAccountShareClass,
    TAccountPaymentMint,
    TAccountSale,
    TAccountSaleApproval,
    TAccountSystemProgram
  >);
}

export type ApproveSaleInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountIssuer extends string = string,
  TAccountAsset extends string = string,
  TAccountShareClass extends string = string,
  TAccountPaymentMint extends string = string,
  TAccountSale extends string = string,
  TAccountSaleApproval extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The approving Admin; pays the approval's rent and becomes `approved_by`. */
  authority: TransactionSigner<TAccountAuthority>;
  /** Admin gate: only a platform Admin may approve a sale. */
  adminRecord: Address<TAccountAdminRecord>;
  issuer: Address<TAccountIssuer>;
  asset: Address<TAccountAsset>;
  shareClass: Address<TAccountShareClass>;
  /**
   * The payment mint the approved sale must use (validated in the handler
   * with the same payment-leg rule `open_sale` applies to its escrow).
   */
  paymentMint: Address<TAccountPaymentMint>;
  /**
   * The future `Sale` PDA for this id. It must still be empty, so the
   * approval can actually be consumed.
   */
  sale: Address<TAccountSale>;
  saleApproval: Address<TAccountSaleApproval>;
  systemProgram?: Address<TAccountSystemProgram>;
  saleId: ApproveSaleInstructionDataArgs["saleId"];
  maxGrossRaise: ApproveSaleInstructionDataArgs["maxGrossRaise"];
  minPricePerUnit: ApproveSaleInstructionDataArgs["minPricePerUnit"];
  maxPricePerUnit: ApproveSaleInstructionDataArgs["maxPricePerUnit"];
  raiseType: ApproveSaleInstructionDataArgs["raiseType"];
  expiresAt: ApproveSaleInstructionDataArgs["expiresAt"];
  applicationHash: ApproveSaleInstructionDataArgs["applicationHash"];
};

export function getApproveSaleInstruction<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountIssuer extends string,
  TAccountAsset extends string,
  TAccountShareClass extends string,
  TAccountPaymentMint extends string,
  TAccountSale extends string,
  TAccountSaleApproval extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ApproveSaleInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountIssuer,
    TAccountAsset,
    TAccountShareClass,
    TAccountPaymentMint,
    TAccountSale,
    TAccountSaleApproval,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ApproveSaleInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAdminRecord,
  TAccountIssuer,
  TAccountAsset,
  TAccountShareClass,
  TAccountPaymentMint,
  TAccountSale,
  TAccountSaleApproval,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    issuer: { value: input.issuer ?? null, isWritable: false },
    asset: { value: input.asset ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: false },
    paymentMint: { value: input.paymentMint ?? null, isWritable: false },
    sale: { value: input.sale ?? null, isWritable: false },
    saleApproval: { value: input.saleApproval ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.asset),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.paymentMint),
      getAccountMeta(accounts.sale),
      getAccountMeta(accounts.saleApproval),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getApproveSaleInstructionDataEncoder().encode(
      args as ApproveSaleInstructionDataArgs,
    ),
    programAddress,
  } as ApproveSaleInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountIssuer,
    TAccountAsset,
    TAccountShareClass,
    TAccountPaymentMint,
    TAccountSale,
    TAccountSaleApproval,
    TAccountSystemProgram
  >);
}

export type ParsedApproveSaleInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The approving Admin; pays the approval's rent and becomes `approved_by`. */
    authority: TAccountMetas[0];
    /** Admin gate: only a platform Admin may approve a sale. */
    adminRecord: TAccountMetas[1];
    issuer: TAccountMetas[2];
    asset: TAccountMetas[3];
    shareClass: TAccountMetas[4];
    /**
     * The payment mint the approved sale must use (validated in the handler
     * with the same payment-leg rule `open_sale` applies to its escrow).
     */
    paymentMint: TAccountMetas[5];
    /**
     * The future `Sale` PDA for this id. It must still be empty, so the
     * approval can actually be consumed.
     */
    sale: TAccountMetas[6];
    saleApproval: TAccountMetas[7];
    systemProgram: TAccountMetas[8];
  };
  data: ApproveSaleInstructionData;
};

export function parseApproveSaleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedApproveSaleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      adminRecord: getNextAccount(),
      issuer: getNextAccount(),
      asset: getNextAccount(),
      shareClass: getNextAccount(),
      paymentMint: getNextAccount(),
      sale: getNextAccount(),
      saleApproval: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getApproveSaleInstructionDataDecoder().decode(instruction.data),
  };
}
