/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const RECLAIM_RENT_DISCRIMINATOR = new Uint8Array([
  218, 200, 19, 197, 227, 89, 192, 22,
]);

export function getReclaimRentDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RECLAIM_RENT_DISCRIMINATOR,
  );
}

export type ReclaimRentInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountTarget extends string | AccountMeta<string> = string,
  TAccountLinked extends string | AccountMeta<string> = string,
  TAccountLinkedB extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> = string,
  TAccountTokenProgramB extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? ReadonlySignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountOwner extends string
        ? WritableAccount<TAccountOwner>
        : TAccountOwner,
      TAccountTarget extends string
        ? WritableAccount<TAccountTarget>
        : TAccountTarget,
      TAccountLinked extends string
        ? WritableAccount<TAccountLinked>
        : TAccountLinked,
      TAccountLinkedB extends string
        ? WritableAccount<TAccountLinkedB>
        : TAccountLinkedB,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountTokenProgramB extends string
        ? ReadonlyAccount<TAccountTokenProgramB>
        : TAccountTokenProgramB,
      ...TRemainingAccounts,
    ]
  >;

export type ReclaimRentInstructionData = { discriminator: ReadonlyUint8Array };

export type ReclaimRentInstructionDataArgs = {};

export function getReclaimRentInstructionDataEncoder(): FixedSizeEncoder<ReclaimRentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: RECLAIM_RENT_DISCRIMINATOR }),
  );
}

export function getReclaimRentInstructionDataDecoder(): FixedSizeDecoder<ReclaimRentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getReclaimRentInstructionDataCodec(): FixedSizeCodec<
  ReclaimRentInstructionDataArgs,
  ReclaimRentInstructionData
> {
  return combineCodec(
    getReclaimRentInstructionDataEncoder(),
    getReclaimRentInstructionDataDecoder(),
  );
}

export type ReclaimRentInput<
  TAccountCaller extends string = string,
  TAccountOwner extends string = string,
  TAccountTarget extends string = string,
  TAccountLinked extends string = string,
  TAccountLinkedB extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountTokenProgramB extends string = string,
> = {
  /**
   * Offer arm: any signer (the rent is fixed to the maker). Every other
   * arm: must equal `owner`.
   */
  caller: TransactionSigner<TAccountCaller>;
  /**
   * The recorded rent owner: `offer.maker` | `deal.admin` |
   * `vault.authority` | `registry.authority`.
   */
  owner: Address<TAccountOwner>;
  /**
   * The terminal account, owned by this program. Its discriminator selects
   * the arm, which deserializes it with `try_deserialize`; an unknown tag
   * (a tombstone included) is refused.
   */
  target: Address<TAccountTarget>;
  /**
   * `Offer.escrow` | `OtcDeal.asset_escrow` | `CustodyVault.escrow` |
   * `KycEntry.registry`.
   */
  linked: Address<TAccountLinked>;
  /** `OtcDeal.payment_escrow`; `None` for every other arm. */
  linkedB?: Address<TAccountLinkedB>;
  /** Token program owning `linked` (token arms only). */
  tokenProgram?: Address<TAccountTokenProgram>;
  /** Token program owning `linked_b` (OtcDeal arm only). */
  tokenProgramB?: Address<TAccountTokenProgramB>;
};

export function getReclaimRentInstruction<
  TAccountCaller extends string,
  TAccountOwner extends string,
  TAccountTarget extends string,
  TAccountLinked extends string,
  TAccountLinkedB extends string,
  TAccountTokenProgram extends string,
  TAccountTokenProgramB extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ReclaimRentInput<
    TAccountCaller,
    TAccountOwner,
    TAccountTarget,
    TAccountLinked,
    TAccountLinkedB,
    TAccountTokenProgram,
    TAccountTokenProgramB
  >,
  config?: { programAddress?: TProgramAddress },
): ReclaimRentInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountOwner,
  TAccountTarget,
  TAccountLinked,
  TAccountLinkedB,
  TAccountTokenProgram,
  TAccountTokenProgramB
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    owner: { value: input.owner ?? null, isWritable: true },
    target: { value: input.target ?? null, isWritable: true },
    linked: { value: input.linked ?? null, isWritable: true },
    linkedB: { value: input.linkedB ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    tokenProgramB: { value: input.tokenProgramB ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.target),
      getAccountMeta(accounts.linked),
      getAccountMeta(accounts.linkedB),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.tokenProgramB),
    ],
    data: getReclaimRentInstructionDataEncoder().encode({}),
    programAddress,
  } as ReclaimRentInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountOwner,
    TAccountTarget,
    TAccountLinked,
    TAccountLinkedB,
    TAccountTokenProgram,
    TAccountTokenProgramB
  >);
}

export type ParsedReclaimRentInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Offer arm: any signer (the rent is fixed to the maker). Every other
     * arm: must equal `owner`.
     */
    caller: TAccountMetas[0];
    /**
     * The recorded rent owner: `offer.maker` | `deal.admin` |
     * `vault.authority` | `registry.authority`.
     */
    owner: TAccountMetas[1];
    /**
     * The terminal account, owned by this program. Its discriminator selects
     * the arm, which deserializes it with `try_deserialize`; an unknown tag
     * (a tombstone included) is refused.
     */
    target: TAccountMetas[2];
    /**
     * `Offer.escrow` | `OtcDeal.asset_escrow` | `CustodyVault.escrow` |
     * `KycEntry.registry`.
     */
    linked: TAccountMetas[3];
    /** `OtcDeal.payment_escrow`; `None` for every other arm. */
    linkedB?: TAccountMetas[4] | undefined;
    /** Token program owning `linked` (token arms only). */
    tokenProgram?: TAccountMetas[5] | undefined;
    /** Token program owning `linked_b` (OtcDeal arm only). */
    tokenProgramB?: TAccountMetas[6] | undefined;
  };
  data: ReclaimRentInstructionData;
};

export function parseReclaimRentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedReclaimRentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === ASSET_REGISTRY_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      owner: getNextAccount(),
      target: getNextAccount(),
      linked: getNextAccount(),
      linkedB: getNextOptionalAccount(),
      tokenProgram: getNextOptionalAccount(),
      tokenProgramB: getNextOptionalAccount(),
    },
    data: getReclaimRentInstructionDataDecoder().decode(instruction.data),
  };
}
