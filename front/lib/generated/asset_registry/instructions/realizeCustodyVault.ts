/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  findAdminRecordPda,
  findOpenCustodyVaultEscrowMarkerPda,
} from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const REALIZE_CUSTODY_VAULT_DISCRIMINATOR = new Uint8Array([
  54, 190, 128, 13, 20, 18, 143, 2,
]);

export function getRealizeCustodyVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REALIZE_CUSTODY_VAULT_DISCRIMINATOR,
  );
}

export type RealizeCustodyVaultInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountShareClass extends string | AccountMeta<string> = string,
  TAccountCustodyVault extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountEscrowMarker extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountAuthorityAdminRecord extends string | AccountMeta<string> = string,
  TAccountKycRegistry extends string | AccountMeta<string> = string,
  TAccountKycEntry extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountShareClass extends string
        ? WritableAccount<TAccountShareClass>
        : TAccountShareClass,
      TAccountCustodyVault extends string
        ? WritableAccount<TAccountCustodyVault>
        : TAccountCustodyVault,
      TAccountMint extends string
        ? WritableAccount<TAccountMint>
        : TAccountMint,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountEscrowMarker extends string
        ? WritableAccount<TAccountEscrowMarker>
        : TAccountEscrowMarker,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountAuthorityAdminRecord extends string
        ? ReadonlyAccount<TAccountAuthorityAdminRecord>
        : TAccountAuthorityAdminRecord,
      TAccountKycRegistry extends string
        ? ReadonlyAccount<TAccountKycRegistry>
        : TAccountKycRegistry,
      TAccountKycEntry extends string
        ? ReadonlyAccount<TAccountKycEntry>
        : TAccountKycEntry,
      ...TRemainingAccounts,
    ]
  >;

export type RealizeCustodyVaultInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RealizeCustodyVaultInstructionDataArgs = {};

export function getRealizeCustodyVaultInstructionDataEncoder(): FixedSizeEncoder<RealizeCustodyVaultInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: REALIZE_CUSTODY_VAULT_DISCRIMINATOR,
    }),
  );
}

export function getRealizeCustodyVaultInstructionDataDecoder(): FixedSizeDecoder<RealizeCustodyVaultInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRealizeCustodyVaultInstructionDataCodec(): FixedSizeCodec<
  RealizeCustodyVaultInstructionDataArgs,
  RealizeCustodyVaultInstructionData
> {
  return combineCodec(
    getRealizeCustodyVaultInstructionDataEncoder(),
    getRealizeCustodyVaultInstructionDataDecoder(),
  );
}

export type RealizeCustodyVaultAsyncInput<
  TAccountAuthority extends string = string,
  TAccountShareClass extends string = string,
  TAccountCustodyVault extends string = string,
  TAccountMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountEscrowMarker extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountAuthorityAdminRecord extends string = string,
  TAccountKycRegistry extends string = string,
  TAccountKycEntry extends string = string,
> = {
  /** Mut: receives the closed escrow marker's rent. */
  authority: TransactionSigner<TAccountAuthority>;
  shareClass: Address<TAccountShareClass>;
  custodyVault: Address<TAccountCustodyVault>;
  mint: Address<TAccountMint>;
  escrow: Address<TAccountEscrow>;
  /**
   * Escrow marker for the vault PDA — closed here (rent → authority);
   * realize is a terminal path.
   */
  escrowMarker?: Address<TAccountEscrowMarker>;
  tokenProgram?: Address<TAccountTokenProgram>;
  authorityAdminRecord?: Address<TAccountAuthorityAdminRecord>;
  /**
   * DeliveryEscrow only: the registry pinned at open
   * (`custody_vault.kyc_registry`). Ignored for every other type. Appended
   * LAST (2C-3).
   */
  kycRegistry?: Address<TAccountKycRegistry>;
  /**
   * DeliveryEscrow only: the beneficiary's entry,
   * `["kyc", kyc_registry, custody_vault.beneficiary]` — checked in the
   * handler (no Anchor seeds, so clients never auto-fill a non-existent
   * entry and each failure keeps its own error). Ignored otherwise.
   */
  kycEntry?: Address<TAccountKycEntry>;
};

export async function getRealizeCustodyVaultInstructionAsync<
  TAccountAuthority extends string,
  TAccountShareClass extends string,
  TAccountCustodyVault extends string,
  TAccountMint extends string,
  TAccountEscrow extends string,
  TAccountEscrowMarker extends string,
  TAccountTokenProgram extends string,
  TAccountAuthorityAdminRecord extends string,
  TAccountKycRegistry extends string,
  TAccountKycEntry extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: RealizeCustodyVaultAsyncInput<
    TAccountAuthority,
    TAccountShareClass,
    TAccountCustodyVault,
    TAccountMint,
    TAccountEscrow,
    TAccountEscrowMarker,
    TAccountTokenProgram,
    TAccountAuthorityAdminRecord,
    TAccountKycRegistry,
    TAccountKycEntry
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RealizeCustodyVaultInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountShareClass,
    TAccountCustodyVault,
    TAccountMint,
    TAccountEscrow,
    TAccountEscrowMarker,
    TAccountTokenProgram,
    TAccountAuthorityAdminRecord,
    TAccountKycRegistry,
    TAccountKycEntry
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    shareClass: { value: input.shareClass ?? null, isWritable: true },
    custodyVault: { value: input.custodyVault ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    escrowMarker: { value: input.escrowMarker ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    authorityAdminRecord: {
      value: input.authorityAdminRecord ?? null,
      isWritable: false,
    },
    kycRegistry: { value: input.kycRegistry ?? null, isWritable: false },
    kycEntry: { value: input.kycEntry ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.escrowMarker.value) {
    accounts.escrowMarker.value = await findOpenCustodyVaultEscrowMarkerPda({
      custodyVault: expectAddress(accounts.custodyVault.value),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.authorityAdminRecord.value) {
    accounts.authorityAdminRecord.value = await findAdminRecordPda({
      authority: expectAddress(accounts.authority.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.custodyVault),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.escrowMarker),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.authorityAdminRecord),
      getAccountMeta(accounts.kycRegistry),
      getAccountMeta(accounts.kycEntry),
    ],
    data: getRealizeCustodyVaultInstructionDataEncoder().encode({}),
    programAddress,
  } as RealizeCustodyVaultInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountShareClass,
    TAccountCustodyVault,
    TAccountMint,
    TAccountEscrow,
    TAccountEscrowMarker,
    TAccountTokenProgram,
    TAccountAuthorityAdminRecord,
    TAccountKycRegistry,
    TAccountKycEntry
  >);
}

export type RealizeCustodyVaultInput<
  TAccountAuthority extends string = string,
  TAccountShareClass extends string = string,
  TAccountCustodyVault extends string = string,
  TAccountMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountEscrowMarker extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountAuthorityAdminRecord extends string = string,
  TAccountKycRegistry extends string = string,
  TAccountKycEntry extends string = string,
> = {
  /** Mut: receives the closed escrow marker's rent. */
  authority: TransactionSigner<TAccountAuthority>;
  shareClass: Address<TAccountShareClass>;
  custodyVault: Address<TAccountCustodyVault>;
  mint: Address<TAccountMint>;
  escrow: Address<TAccountEscrow>;
  /**
   * Escrow marker for the vault PDA — closed here (rent → authority);
   * realize is a terminal path.
   */
  escrowMarker: Address<TAccountEscrowMarker>;
  tokenProgram?: Address<TAccountTokenProgram>;
  authorityAdminRecord: Address<TAccountAuthorityAdminRecord>;
  /**
   * DeliveryEscrow only: the registry pinned at open
   * (`custody_vault.kyc_registry`). Ignored for every other type. Appended
   * LAST (2C-3).
   */
  kycRegistry?: Address<TAccountKycRegistry>;
  /**
   * DeliveryEscrow only: the beneficiary's entry,
   * `["kyc", kyc_registry, custody_vault.beneficiary]` — checked in the
   * handler (no Anchor seeds, so clients never auto-fill a non-existent
   * entry and each failure keeps its own error). Ignored otherwise.
   */
  kycEntry?: Address<TAccountKycEntry>;
};

export function getRealizeCustodyVaultInstruction<
  TAccountAuthority extends string,
  TAccountShareClass extends string,
  TAccountCustodyVault extends string,
  TAccountMint extends string,
  TAccountEscrow extends string,
  TAccountEscrowMarker extends string,
  TAccountTokenProgram extends string,
  TAccountAuthorityAdminRecord extends string,
  TAccountKycRegistry extends string,
  TAccountKycEntry extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: RealizeCustodyVaultInput<
    TAccountAuthority,
    TAccountShareClass,
    TAccountCustodyVault,
    TAccountMint,
    TAccountEscrow,
    TAccountEscrowMarker,
    TAccountTokenProgram,
    TAccountAuthorityAdminRecord,
    TAccountKycRegistry,
    TAccountKycEntry
  >,
  config?: { programAddress?: TProgramAddress },
): RealizeCustodyVaultInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountShareClass,
  TAccountCustodyVault,
  TAccountMint,
  TAccountEscrow,
  TAccountEscrowMarker,
  TAccountTokenProgram,
  TAccountAuthorityAdminRecord,
  TAccountKycRegistry,
  TAccountKycEntry
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    shareClass: { value: input.shareClass ?? null, isWritable: true },
    custodyVault: { value: input.custodyVault ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    escrowMarker: { value: input.escrowMarker ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    authorityAdminRecord: {
      value: input.authorityAdminRecord ?? null,
      isWritable: false,
    },
    kycRegistry: { value: input.kycRegistry ?? null, isWritable: false },
    kycEntry: { value: input.kycEntry ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.custodyVault),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.escrowMarker),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.authorityAdminRecord),
      getAccountMeta(accounts.kycRegistry),
      getAccountMeta(accounts.kycEntry),
    ],
    data: getRealizeCustodyVaultInstructionDataEncoder().encode({}),
    programAddress,
  } as RealizeCustodyVaultInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountShareClass,
    TAccountCustodyVault,
    TAccountMint,
    TAccountEscrow,
    TAccountEscrowMarker,
    TAccountTokenProgram,
    TAccountAuthorityAdminRecord,
    TAccountKycRegistry,
    TAccountKycEntry
  >);
}

export type ParsedRealizeCustodyVaultInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Mut: receives the closed escrow marker's rent. */
    authority: TAccountMetas[0];
    shareClass: TAccountMetas[1];
    custodyVault: TAccountMetas[2];
    mint: TAccountMetas[3];
    escrow: TAccountMetas[4];
    /**
     * Escrow marker for the vault PDA — closed here (rent → authority);
     * realize is a terminal path.
     */
    escrowMarker: TAccountMetas[5];
    tokenProgram: TAccountMetas[6];
    authorityAdminRecord: TAccountMetas[7];
    /**
     * DeliveryEscrow only: the registry pinned at open
     * (`custody_vault.kyc_registry`). Ignored for every other type. Appended
     * LAST (2C-3).
     */
    kycRegistry?: TAccountMetas[8] | undefined;
    /**
     * DeliveryEscrow only: the beneficiary's entry,
     * `["kyc", kyc_registry, custody_vault.beneficiary]` — checked in the
     * handler (no Anchor seeds, so clients never auto-fill a non-existent
     * entry and each failure keeps its own error). Ignored otherwise.
     */
    kycEntry?: TAccountMetas[9] | undefined;
  };
  data: RealizeCustodyVaultInstructionData;
};

export function parseRealizeCustodyVaultInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRealizeCustodyVaultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 10) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === ASSET_REGISTRY_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      shareClass: getNextAccount(),
      custodyVault: getNextAccount(),
      mint: getNextAccount(),
      escrow: getNextAccount(),
      escrowMarker: getNextAccount(),
      tokenProgram: getNextAccount(),
      authorityAdminRecord: getNextAccount(),
      kycRegistry: getNextOptionalAccount(),
      kycEntry: getNextOptionalAccount(),
    },
    data: getRealizeCustodyVaultInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
