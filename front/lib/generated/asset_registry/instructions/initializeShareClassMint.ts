/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findMintPda, findPlatformPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const INITIALIZE_SHARE_CLASS_MINT_DISCRIMINATOR = new Uint8Array([
  166, 90, 28, 7, 76, 64, 235, 254,
]);

export function getInitializeShareClassMintDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_SHARE_CLASS_MINT_DISCRIMINATOR,
  );
}

export type InitializeShareClassMintInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAdminRecord extends string | AccountMeta<string> = string,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TAccountAsset extends string | AccountMeta<string> = string,
  TAccountShareClass extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountHookConfig extends string | AccountMeta<string> = string,
  TAccountExtraAccountMetaList extends string | AccountMeta<string> = string,
  TAccountTransferHookProgram extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountPlatform extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAdminRecord extends string
        ? ReadonlyAccount<TAccountAdminRecord>
        : TAccountAdminRecord,
      TAccountIssuer extends string
        ? ReadonlyAccount<TAccountIssuer>
        : TAccountIssuer,
      TAccountAsset extends string
        ? ReadonlyAccount<TAccountAsset>
        : TAccountAsset,
      TAccountShareClass extends string
        ? WritableAccount<TAccountShareClass>
        : TAccountShareClass,
      TAccountMint extends string
        ? WritableAccount<TAccountMint>
        : TAccountMint,
      TAccountHookConfig extends string
        ? WritableAccount<TAccountHookConfig>
        : TAccountHookConfig,
      TAccountExtraAccountMetaList extends string
        ? WritableAccount<TAccountExtraAccountMetaList>
        : TAccountExtraAccountMetaList,
      TAccountTransferHookProgram extends string
        ? ReadonlyAccount<TAccountTransferHookProgram>
        : TAccountTransferHookProgram,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountPlatform extends string
        ? ReadonlyAccount<TAccountPlatform>
        : TAccountPlatform,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeShareClassMintInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitializeShareClassMintInstructionDataArgs = {};

export function getInitializeShareClassMintInstructionDataEncoder(): FixedSizeEncoder<InitializeShareClassMintInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_SHARE_CLASS_MINT_DISCRIMINATOR,
    }),
  );
}

export function getInitializeShareClassMintInstructionDataDecoder(): FixedSizeDecoder<InitializeShareClassMintInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitializeShareClassMintInstructionDataCodec(): FixedSizeCodec<
  InitializeShareClassMintInstructionDataArgs,
  InitializeShareClassMintInstructionData
> {
  return combineCodec(
    getInitializeShareClassMintInstructionDataEncoder(),
    getInitializeShareClassMintInstructionDataDecoder(),
  );
}

export type InitializeShareClassMintAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountIssuer extends string = string,
  TAccountAsset extends string = string,
  TAccountShareClass extends string = string,
  TAccountMint extends string = string,
  TAccountHookConfig extends string = string,
  TAccountExtraAccountMetaList extends string = string,
  TAccountTransferHookProgram extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountPlatform extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /** Global Admin or issuer-local MINT capability; the signer must also be the issuer. */
  adminRecord: Address<TAccountAdminRecord>;
  issuer: Address<TAccountIssuer>;
  asset: Address<TAccountAsset>;
  shareClass: Address<TAccountShareClass>;
  /**
   * Token-2022 mint for this share class — a PDA, with the `ShareClass` PDA
   * as mint authority. TransferHook extension wired to the Mancipatio
   * `transfer_hook` program. No `DefaultAccountState=Frozen` — custody, not
   * freeze (docs/01 §6).
   */
  mint?: Address<TAccountMint>;
  /**
   * (`["hook_cfg", mint]` under the transfer_hook program) — created by the
   * hook program via CPI; that program enforces the seeds, and the handler
   * additionally checks the derived address.
   */
  hookConfig: Address<TAccountHookConfig>;
  /**
   * (`["extra-account-metas", mint]` under the transfer_hook program) —
   * created by the hook program via CPI; same validation as `hook_config`.
   */
  extraAccountMetaList: Address<TAccountExtraAccountMetaList>;
  transferHookProgram: Address<TAccountTransferHookProgram>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  /**
   * Emergency-pause gate (read-only). Keep LAST among named accounts: old
   * account indices and the remaining-accounts hook tail keep their positions.
   */
  platform?: Address<TAccountPlatform>;
};

export async function getInitializeShareClassMintInstructionAsync<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountIssuer extends string,
  TAccountAsset extends string,
  TAccountShareClass extends string,
  TAccountMint extends string,
  TAccountHookConfig extends string,
  TAccountExtraAccountMetaList extends string,
  TAccountTransferHookProgram extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TAccountPlatform extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: InitializeShareClassMintAsyncInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountIssuer,
    TAccountAsset,
    TAccountShareClass,
    TAccountMint,
    TAccountHookConfig,
    TAccountExtraAccountMetaList,
    TAccountTransferHookProgram,
    TAccountTokenProgram,
    TAccountSystemProgram,
    TAccountPlatform
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeShareClassMintInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountIssuer,
    TAccountAsset,
    TAccountShareClass,
    TAccountMint,
    TAccountHookConfig,
    TAccountExtraAccountMetaList,
    TAccountTransferHookProgram,
    TAccountTokenProgram,
    TAccountSystemProgram,
    TAccountPlatform
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    issuer: { value: input.issuer ?? null, isWritable: false },
    asset: { value: input.asset ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: true },
    hookConfig: { value: input.hookConfig ?? null, isWritable: true },
    extraAccountMetaList: {
      value: input.extraAccountMetaList ?? null,
      isWritable: true,
    },
    transferHookProgram: {
      value: input.transferHookProgram ?? null,
      isWritable: false,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    platform: { value: input.platform ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.mint.value) {
    accounts.mint.value = await findMintPda({
      shareClass: expectAddress(accounts.shareClass.value),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.platform.value) {
    accounts.platform.value = await findPlatformPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.asset),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.hookConfig),
      getAccountMeta(accounts.extraAccountMetaList),
      getAccountMeta(accounts.transferHookProgram),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.platform),
    ],
    data: getInitializeShareClassMintInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeShareClassMintInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountIssuer,
    TAccountAsset,
    TAccountShareClass,
    TAccountMint,
    TAccountHookConfig,
    TAccountExtraAccountMetaList,
    TAccountTransferHookProgram,
    TAccountTokenProgram,
    TAccountSystemProgram,
    TAccountPlatform
  >);
}

export type InitializeShareClassMintInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountIssuer extends string = string,
  TAccountAsset extends string = string,
  TAccountShareClass extends string = string,
  TAccountMint extends string = string,
  TAccountHookConfig extends string = string,
  TAccountExtraAccountMetaList extends string = string,
  TAccountTransferHookProgram extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountPlatform extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /** Global Admin or issuer-local MINT capability; the signer must also be the issuer. */
  adminRecord: Address<TAccountAdminRecord>;
  issuer: Address<TAccountIssuer>;
  asset: Address<TAccountAsset>;
  shareClass: Address<TAccountShareClass>;
  /**
   * Token-2022 mint for this share class — a PDA, with the `ShareClass` PDA
   * as mint authority. TransferHook extension wired to the Mancipatio
   * `transfer_hook` program. No `DefaultAccountState=Frozen` — custody, not
   * freeze (docs/01 §6).
   */
  mint: Address<TAccountMint>;
  /**
   * (`["hook_cfg", mint]` under the transfer_hook program) — created by the
   * hook program via CPI; that program enforces the seeds, and the handler
   * additionally checks the derived address.
   */
  hookConfig: Address<TAccountHookConfig>;
  /**
   * (`["extra-account-metas", mint]` under the transfer_hook program) —
   * created by the hook program via CPI; same validation as `hook_config`.
   */
  extraAccountMetaList: Address<TAccountExtraAccountMetaList>;
  transferHookProgram: Address<TAccountTransferHookProgram>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  /**
   * Emergency-pause gate (read-only). Keep LAST among named accounts: old
   * account indices and the remaining-accounts hook tail keep their positions.
   */
  platform: Address<TAccountPlatform>;
};

export function getInitializeShareClassMintInstruction<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountIssuer extends string,
  TAccountAsset extends string,
  TAccountShareClass extends string,
  TAccountMint extends string,
  TAccountHookConfig extends string,
  TAccountExtraAccountMetaList extends string,
  TAccountTransferHookProgram extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TAccountPlatform extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: InitializeShareClassMintInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountIssuer,
    TAccountAsset,
    TAccountShareClass,
    TAccountMint,
    TAccountHookConfig,
    TAccountExtraAccountMetaList,
    TAccountTransferHookProgram,
    TAccountTokenProgram,
    TAccountSystemProgram,
    TAccountPlatform
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeShareClassMintInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAdminRecord,
  TAccountIssuer,
  TAccountAsset,
  TAccountShareClass,
  TAccountMint,
  TAccountHookConfig,
  TAccountExtraAccountMetaList,
  TAccountTransferHookProgram,
  TAccountTokenProgram,
  TAccountSystemProgram,
  TAccountPlatform
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    issuer: { value: input.issuer ?? null, isWritable: false },
    asset: { value: input.asset ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: true },
    hookConfig: { value: input.hookConfig ?? null, isWritable: true },
    extraAccountMetaList: {
      value: input.extraAccountMetaList ?? null,
      isWritable: true,
    },
    transferHookProgram: {
      value: input.transferHookProgram ?? null,
      isWritable: false,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    platform: { value: input.platform ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.asset),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.hookConfig),
      getAccountMeta(accounts.extraAccountMetaList),
      getAccountMeta(accounts.transferHookProgram),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.platform),
    ],
    data: getInitializeShareClassMintInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeShareClassMintInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountIssuer,
    TAccountAsset,
    TAccountShareClass,
    TAccountMint,
    TAccountHookConfig,
    TAccountExtraAccountMetaList,
    TAccountTransferHookProgram,
    TAccountTokenProgram,
    TAccountSystemProgram,
    TAccountPlatform
  >);
}

export type ParsedInitializeShareClassMintInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    /** Global Admin or issuer-local MINT capability; the signer must also be the issuer. */
    adminRecord: TAccountMetas[1];
    issuer: TAccountMetas[2];
    asset: TAccountMetas[3];
    shareClass: TAccountMetas[4];
    /**
     * Token-2022 mint for this share class — a PDA, with the `ShareClass` PDA
     * as mint authority. TransferHook extension wired to the Mancipatio
     * `transfer_hook` program. No `DefaultAccountState=Frozen` — custody, not
     * freeze (docs/01 §6).
     */
    mint: TAccountMetas[5];
    /**
     * (`["hook_cfg", mint]` under the transfer_hook program) — created by the
     * hook program via CPI; that program enforces the seeds, and the handler
     * additionally checks the derived address.
     */
    hookConfig: TAccountMetas[6];
    /**
     * (`["extra-account-metas", mint]` under the transfer_hook program) —
     * created by the hook program via CPI; same validation as `hook_config`.
     */
    extraAccountMetaList: TAccountMetas[7];
    transferHookProgram: TAccountMetas[8];
    tokenProgram: TAccountMetas[9];
    systemProgram: TAccountMetas[10];
    /**
     * Emergency-pause gate (read-only). Keep LAST among named accounts: old
     * account indices and the remaining-accounts hook tail keep their positions.
     */
    platform: TAccountMetas[11];
  };
  data: InitializeShareClassMintInstructionData;
};

export function parseInitializeShareClassMintInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeShareClassMintInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 12) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      adminRecord: getNextAccount(),
      issuer: getNextAccount(),
      asset: getNextAccount(),
      shareClass: getNextAccount(),
      mint: getNextAccount(),
      hookConfig: getNextAccount(),
      extraAccountMetaList: getNextAccount(),
      transferHookProgram: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
      platform: getNextAccount(),
    },
    data: getInitializeShareClassMintInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
