/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  findAdminRecordPda,
  findHolderEscrowMarkerPda,
  findKycEntryPda,
} from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CLAWBACK_FROM_HOLDER_DISCRIMINATOR = new Uint8Array([
  29, 189, 234, 75, 240, 45, 236, 165,
]);

export function getClawbackFromHolderDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAWBACK_FROM_HOLDER_DISCRIMINATOR,
  );
}

export type ClawbackFromHolderInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAdminRecord extends string | AccountMeta<string> = string,
  TAccountShareClass extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountHolderShareAccount extends string | AccountMeta<string> = string,
  TAccountHolderEscrowMarker extends string | AccountMeta<string> = string,
  TAccountDestination extends string | AccountMeta<string> = string,
  TAccountCustodyVault extends string | AccountMeta<string> = string,
  TAccountKycRegistry extends string | AccountMeta<string> = string,
  TAccountKycEntry extends string | AccountMeta<string> = string,
  TAccountHookConfig extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAdminRecord extends string
        ? ReadonlyAccount<TAccountAdminRecord>
        : TAccountAdminRecord,
      TAccountShareClass extends string
        ? ReadonlyAccount<TAccountShareClass>
        : TAccountShareClass,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountHolderShareAccount extends string
        ? WritableAccount<TAccountHolderShareAccount>
        : TAccountHolderShareAccount,
      TAccountHolderEscrowMarker extends string
        ? ReadonlyAccount<TAccountHolderEscrowMarker>
        : TAccountHolderEscrowMarker,
      TAccountDestination extends string
        ? WritableAccount<TAccountDestination>
        : TAccountDestination,
      TAccountCustodyVault extends string
        ? ReadonlyAccount<TAccountCustodyVault>
        : TAccountCustodyVault,
      TAccountKycRegistry extends string
        ? ReadonlyAccount<TAccountKycRegistry>
        : TAccountKycRegistry,
      TAccountKycEntry extends string
        ? ReadonlyAccount<TAccountKycEntry>
        : TAccountKycEntry,
      TAccountHookConfig extends string
        ? ReadonlyAccount<TAccountHookConfig>
        : TAccountHookConfig,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ClawbackFromHolderInstructionData = {
  discriminator: ReadonlyUint8Array;
  holder: Address;
  amount: bigint;
};

export type ClawbackFromHolderInstructionDataArgs = {
  holder: Address;
  amount: number | bigint;
};

export function getClawbackFromHolderInstructionDataEncoder(): FixedSizeEncoder<ClawbackFromHolderInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["holder", getAddressEncoder()],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: CLAWBACK_FROM_HOLDER_DISCRIMINATOR,
    }),
  );
}

export function getClawbackFromHolderInstructionDataDecoder(): FixedSizeDecoder<ClawbackFromHolderInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["holder", getAddressDecoder()],
    ["amount", getU64Decoder()],
  ]);
}

export function getClawbackFromHolderInstructionDataCodec(): FixedSizeCodec<
  ClawbackFromHolderInstructionDataArgs,
  ClawbackFromHolderInstructionData
> {
  return combineCodec(
    getClawbackFromHolderInstructionDataEncoder(),
    getClawbackFromHolderInstructionDataDecoder(),
  );
}

export type ClawbackFromHolderAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountShareClass extends string = string,
  TAccountMint extends string = string,
  TAccountHolderShareAccount extends string = string,
  TAccountHolderEscrowMarker extends string = string,
  TAccountDestination extends string = string,
  TAccountCustodyVault extends string = string,
  TAccountKycRegistry extends string = string,
  TAccountKycEntry extends string = string,
  TAccountHookConfig extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /** Admin gate — the signer must hold an `Admin` record. */
  adminRecord?: Address<TAccountAdminRecord>;
  shareClass: Address<TAccountShareClass>;
  mint: Address<TAccountMint>;
  /**
   * The holder's share token account — the clawback source. The `ShareClass`
   * PDA is the mint's Token-2022 `PermanentDelegate` (set at mint creation),
   * so no holder signature is needed.
   */
  holderShareAccount: Address<TAccountHolderShareAccount>;
  /**
   * Proof that `holder` is an ordinary wallet and not one of this program's
   * own escrows. `holder` is a free parameter and `approve_holder` mints a
   * `KycEntry` for ANY pubkey, PDAs included — so without this an admin
   * could approve→revoke an Offer / OtcDeal / CustodyVault PDA and "claw
   * back" its balance into a burn vault, destroying the ledgered deposit of
   * a fully compliant maker, seller or beneficiary. Every such escrow
   * carries an `EscrowMarker` or `EscrowIdentity` at this address, so an INITIALISED account
   * here means the target is program-held property. The address is derived
   * by Anchor, so the caller can neither omit nor substitute it; the
   * constraint demands it be empty (never opened, or already closed).
   */
  holderEscrowMarker?: Address<TAccountHolderEscrowMarker>;
  /**
   * Destination — the escrow of `custody_vault` below, i.e. a burn-only
   * quarantine escrow of THIS share class. The vault PDA owns it and carries
   * an `EscrowMarker`, so the hook's destination-marker exemption keeps the
   * clawback leg from ever being blocked by receiver-KYC.
   */
  destination: Address<TAccountDestination>;
  /**
   * The quarantine vault the seized units land in — a typed, admin-opened
   * `CustodyVault` of this share class, restricted to the one vault shape
   * whose every exit BURNS: `RedemptionQueue` + `BurnAndAttest`
   * (`realize_custody_vault` burns, `revert_custody_vault` burns, and
   * `return_custody_vault` — the only escrow→wallet exit — is reserved for
   * `DeliveryEscrow` vaults and therefore unreachable here). An
   * `EscrowMarker` alone would NOT be containment: `create_offer` mints one
   * permissionlessly and `cancel_offer` pays the full escrow balance back to
   * the maker's wallet.
   */
  custodyVault: Address<TAccountCustodyVault>;
  /**
   * Registry the holder's entry is checked against — pinned by the handler
   * to the registry the mint's hook config points at (clawback is only
   * allowed on `KycGated` mints, so the pin always applies).
   */
  kycRegistry: Address<TAccountKycRegistry>;
  /** The holder's KYC entry — must be `Revoked` or expired (see handler). */
  kycEntry?: Address<TAccountKycEntry>;
  /**
   * the transfer_hook program) — address enforced by the seeds constraint;
   * the handler reads `restriction_mode` / `kyc_registry` by offset
   * (layout mirrored in constants.rs, no crate dependency on the hook).
   * Must exist and say `KycGated`: on an `Open` mint there is no registry
   * to pin `kyc_registry` to, so the whole instruction is refused.
   */
  hookConfig?: Address<TAccountHookConfig>;
  tokenProgram?: Address<TAccountTokenProgram>;
  holder: ClawbackFromHolderInstructionDataArgs["holder"];
  amount: ClawbackFromHolderInstructionDataArgs["amount"];
};

export async function getClawbackFromHolderInstructionAsync<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountShareClass extends string,
  TAccountMint extends string,
  TAccountHolderShareAccount extends string,
  TAccountHolderEscrowMarker extends string,
  TAccountDestination extends string,
  TAccountCustodyVault extends string,
  TAccountKycRegistry extends string,
  TAccountKycEntry extends string,
  TAccountHookConfig extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ClawbackFromHolderAsyncInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass,
    TAccountMint,
    TAccountHolderShareAccount,
    TAccountHolderEscrowMarker,
    TAccountDestination,
    TAccountCustodyVault,
    TAccountKycRegistry,
    TAccountKycEntry,
    TAccountHookConfig,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ClawbackFromHolderInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass,
    TAccountMint,
    TAccountHolderShareAccount,
    TAccountHolderEscrowMarker,
    TAccountDestination,
    TAccountCustodyVault,
    TAccountKycRegistry,
    TAccountKycEntry,
    TAccountHookConfig,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    holderShareAccount: {
      value: input.holderShareAccount ?? null,
      isWritable: true,
    },
    holderEscrowMarker: {
      value: input.holderEscrowMarker ?? null,
      isWritable: false,
    },
    destination: { value: input.destination ?? null, isWritable: true },
    custodyVault: { value: input.custodyVault ?? null, isWritable: false },
    kycRegistry: { value: input.kycRegistry ?? null, isWritable: false },
    kycEntry: { value: input.kycEntry ?? null, isWritable: false },
    hookConfig: { value: input.hookConfig ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.adminRecord.value) {
    accounts.adminRecord.value = await findAdminRecordPda({
      authority: expectAddress(accounts.authority.value),
    });
  }
  if (!accounts.holderEscrowMarker.value) {
    accounts.holderEscrowMarker.value = await findHolderEscrowMarkerPda({
      holder: expectSome(args.holder),
    });
  }
  if (!accounts.kycEntry.value) {
    accounts.kycEntry.value = await findKycEntryPda({
      kycRegistry: expectAddress(accounts.kycRegistry.value),
      holder: expectSome(args.holder),
    });
  }
  if (!accounts.hookConfig.value) {
    accounts.hookConfig.value = await getProgramDerivedAddress({
      programAddress:
        "GBDyesyTr266LqKeFq95r1DeigRyHpfw6ACWdjENHAPy" as Address<"GBDyesyTr266LqKeFq95r1DeigRyHpfw6ACWdjENHAPy">,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([104, 111, 111, 107, 95, 99, 102, 103]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.mint.value)),
      ],
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.holderShareAccount),
      getAccountMeta(accounts.holderEscrowMarker),
      getAccountMeta(accounts.destination),
      getAccountMeta(accounts.custodyVault),
      getAccountMeta(accounts.kycRegistry),
      getAccountMeta(accounts.kycEntry),
      getAccountMeta(accounts.hookConfig),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getClawbackFromHolderInstructionDataEncoder().encode(
      args as ClawbackFromHolderInstructionDataArgs,
    ),
    programAddress,
  } as ClawbackFromHolderInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass,
    TAccountMint,
    TAccountHolderShareAccount,
    TAccountHolderEscrowMarker,
    TAccountDestination,
    TAccountCustodyVault,
    TAccountKycRegistry,
    TAccountKycEntry,
    TAccountHookConfig,
    TAccountTokenProgram
  >);
}

export type ClawbackFromHolderInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountShareClass extends string = string,
  TAccountMint extends string = string,
  TAccountHolderShareAccount extends string = string,
  TAccountHolderEscrowMarker extends string = string,
  TAccountDestination extends string = string,
  TAccountCustodyVault extends string = string,
  TAccountKycRegistry extends string = string,
  TAccountKycEntry extends string = string,
  TAccountHookConfig extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /** Admin gate — the signer must hold an `Admin` record. */
  adminRecord: Address<TAccountAdminRecord>;
  shareClass: Address<TAccountShareClass>;
  mint: Address<TAccountMint>;
  /**
   * The holder's share token account — the clawback source. The `ShareClass`
   * PDA is the mint's Token-2022 `PermanentDelegate` (set at mint creation),
   * so no holder signature is needed.
   */
  holderShareAccount: Address<TAccountHolderShareAccount>;
  /**
   * Proof that `holder` is an ordinary wallet and not one of this program's
   * own escrows. `holder` is a free parameter and `approve_holder` mints a
   * `KycEntry` for ANY pubkey, PDAs included — so without this an admin
   * could approve→revoke an Offer / OtcDeal / CustodyVault PDA and "claw
   * back" its balance into a burn vault, destroying the ledgered deposit of
   * a fully compliant maker, seller or beneficiary. Every such escrow
   * carries an `EscrowMarker` or `EscrowIdentity` at this address, so an INITIALISED account
   * here means the target is program-held property. The address is derived
   * by Anchor, so the caller can neither omit nor substitute it; the
   * constraint demands it be empty (never opened, or already closed).
   */
  holderEscrowMarker: Address<TAccountHolderEscrowMarker>;
  /**
   * Destination — the escrow of `custody_vault` below, i.e. a burn-only
   * quarantine escrow of THIS share class. The vault PDA owns it and carries
   * an `EscrowMarker`, so the hook's destination-marker exemption keeps the
   * clawback leg from ever being blocked by receiver-KYC.
   */
  destination: Address<TAccountDestination>;
  /**
   * The quarantine vault the seized units land in — a typed, admin-opened
   * `CustodyVault` of this share class, restricted to the one vault shape
   * whose every exit BURNS: `RedemptionQueue` + `BurnAndAttest`
   * (`realize_custody_vault` burns, `revert_custody_vault` burns, and
   * `return_custody_vault` — the only escrow→wallet exit — is reserved for
   * `DeliveryEscrow` vaults and therefore unreachable here). An
   * `EscrowMarker` alone would NOT be containment: `create_offer` mints one
   * permissionlessly and `cancel_offer` pays the full escrow balance back to
   * the maker's wallet.
   */
  custodyVault: Address<TAccountCustodyVault>;
  /**
   * Registry the holder's entry is checked against — pinned by the handler
   * to the registry the mint's hook config points at (clawback is only
   * allowed on `KycGated` mints, so the pin always applies).
   */
  kycRegistry: Address<TAccountKycRegistry>;
  /** The holder's KYC entry — must be `Revoked` or expired (see handler). */
  kycEntry: Address<TAccountKycEntry>;
  /**
   * the transfer_hook program) — address enforced by the seeds constraint;
   * the handler reads `restriction_mode` / `kyc_registry` by offset
   * (layout mirrored in constants.rs, no crate dependency on the hook).
   * Must exist and say `KycGated`: on an `Open` mint there is no registry
   * to pin `kyc_registry` to, so the whole instruction is refused.
   */
  hookConfig: Address<TAccountHookConfig>;
  tokenProgram?: Address<TAccountTokenProgram>;
  holder: ClawbackFromHolderInstructionDataArgs["holder"];
  amount: ClawbackFromHolderInstructionDataArgs["amount"];
};

export function getClawbackFromHolderInstruction<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountShareClass extends string,
  TAccountMint extends string,
  TAccountHolderShareAccount extends string,
  TAccountHolderEscrowMarker extends string,
  TAccountDestination extends string,
  TAccountCustodyVault extends string,
  TAccountKycRegistry extends string,
  TAccountKycEntry extends string,
  TAccountHookConfig extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ClawbackFromHolderInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass,
    TAccountMint,
    TAccountHolderShareAccount,
    TAccountHolderEscrowMarker,
    TAccountDestination,
    TAccountCustodyVault,
    TAccountKycRegistry,
    TAccountKycEntry,
    TAccountHookConfig,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ClawbackFromHolderInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAdminRecord,
  TAccountShareClass,
  TAccountMint,
  TAccountHolderShareAccount,
  TAccountHolderEscrowMarker,
  TAccountDestination,
  TAccountCustodyVault,
  TAccountKycRegistry,
  TAccountKycEntry,
  TAccountHookConfig,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    holderShareAccount: {
      value: input.holderShareAccount ?? null,
      isWritable: true,
    },
    holderEscrowMarker: {
      value: input.holderEscrowMarker ?? null,
      isWritable: false,
    },
    destination: { value: input.destination ?? null, isWritable: true },
    custodyVault: { value: input.custodyVault ?? null, isWritable: false },
    kycRegistry: { value: input.kycRegistry ?? null, isWritable: false },
    kycEntry: { value: input.kycEntry ?? null, isWritable: false },
    hookConfig: { value: input.hookConfig ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.holderShareAccount),
      getAccountMeta(accounts.holderEscrowMarker),
      getAccountMeta(accounts.destination),
      getAccountMeta(accounts.custodyVault),
      getAccountMeta(accounts.kycRegistry),
      getAccountMeta(accounts.kycEntry),
      getAccountMeta(accounts.hookConfig),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getClawbackFromHolderInstructionDataEncoder().encode(
      args as ClawbackFromHolderInstructionDataArgs,
    ),
    programAddress,
  } as ClawbackFromHolderInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass,
    TAccountMint,
    TAccountHolderShareAccount,
    TAccountHolderEscrowMarker,
    TAccountDestination,
    TAccountCustodyVault,
    TAccountKycRegistry,
    TAccountKycEntry,
    TAccountHookConfig,
    TAccountTokenProgram
  >);
}

export type ParsedClawbackFromHolderInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    /** Admin gate — the signer must hold an `Admin` record. */
    adminRecord: TAccountMetas[1];
    shareClass: TAccountMetas[2];
    mint: TAccountMetas[3];
    /**
     * The holder's share token account — the clawback source. The `ShareClass`
     * PDA is the mint's Token-2022 `PermanentDelegate` (set at mint creation),
     * so no holder signature is needed.
     */
    holderShareAccount: TAccountMetas[4];
    /**
     * Proof that `holder` is an ordinary wallet and not one of this program's
     * own escrows. `holder` is a free parameter and `approve_holder` mints a
     * `KycEntry` for ANY pubkey, PDAs included — so without this an admin
     * could approve→revoke an Offer / OtcDeal / CustodyVault PDA and "claw
     * back" its balance into a burn vault, destroying the ledgered deposit of
     * a fully compliant maker, seller or beneficiary. Every such escrow
     * carries an `EscrowMarker` or `EscrowIdentity` at this address, so an INITIALISED account
     * here means the target is program-held property. The address is derived
     * by Anchor, so the caller can neither omit nor substitute it; the
     * constraint demands it be empty (never opened, or already closed).
     */
    holderEscrowMarker: TAccountMetas[5];
    /**
     * Destination — the escrow of `custody_vault` below, i.e. a burn-only
     * quarantine escrow of THIS share class. The vault PDA owns it and carries
     * an `EscrowMarker`, so the hook's destination-marker exemption keeps the
     * clawback leg from ever being blocked by receiver-KYC.
     */
    destination: TAccountMetas[6];
    /**
     * The quarantine vault the seized units land in — a typed, admin-opened
     * `CustodyVault` of this share class, restricted to the one vault shape
     * whose every exit BURNS: `RedemptionQueue` + `BurnAndAttest`
     * (`realize_custody_vault` burns, `revert_custody_vault` burns, and
     * `return_custody_vault` — the only escrow→wallet exit — is reserved for
     * `DeliveryEscrow` vaults and therefore unreachable here). An
     * `EscrowMarker` alone would NOT be containment: `create_offer` mints one
     * permissionlessly and `cancel_offer` pays the full escrow balance back to
     * the maker's wallet.
     */
    custodyVault: TAccountMetas[7];
    /**
     * Registry the holder's entry is checked against — pinned by the handler
     * to the registry the mint's hook config points at (clawback is only
     * allowed on `KycGated` mints, so the pin always applies).
     */
    kycRegistry: TAccountMetas[8];
    /** The holder's KYC entry — must be `Revoked` or expired (see handler). */
    kycEntry: TAccountMetas[9];
    /**
     * the transfer_hook program) — address enforced by the seeds constraint;
     * the handler reads `restriction_mode` / `kyc_registry` by offset
     * (layout mirrored in constants.rs, no crate dependency on the hook).
     * Must exist and say `KycGated`: on an `Open` mint there is no registry
     * to pin `kyc_registry` to, so the whole instruction is refused.
     */
    hookConfig: TAccountMetas[10];
    tokenProgram: TAccountMetas[11];
  };
  data: ClawbackFromHolderInstructionData;
};

export function parseClawbackFromHolderInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClawbackFromHolderInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 12) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      adminRecord: getNextAccount(),
      shareClass: getNextAccount(),
      mint: getNextAccount(),
      holderShareAccount: getNextAccount(),
      holderEscrowMarker: getNextAccount(),
      destination: getNextAccount(),
      custodyVault: getNextAccount(),
      kycRegistry: getNextAccount(),
      kycEntry: getNextAccount(),
      hookConfig: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getClawbackFromHolderInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
