/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findAdminRecordPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const OPEN_VAULT_VOTE_DISCRIMINATOR = new Uint8Array([
  16, 87, 164, 104, 47, 93, 71, 153,
]);

export function getOpenVaultVoteDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    OPEN_VAULT_VOTE_DISCRIMINATOR,
  );
}

export type OpenVaultVoteInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAdminRecord extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountVote extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAdminRecord extends string
        ? ReadonlyAccount<TAccountAdminRecord>
        : TAccountAdminRecord,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountVote extends string
        ? WritableAccount<TAccountVote>
        : TAccountVote,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type OpenVaultVoteInstructionData = {
  discriminator: ReadonlyUint8Array;
  snapshotRoot: ReadonlyUint8Array;
  totalWeight: bigint;
  votingPeriod: bigint;
};

export type OpenVaultVoteInstructionDataArgs = {
  snapshotRoot: ReadonlyUint8Array;
  totalWeight: number | bigint;
  votingPeriod: number | bigint;
};

export function getOpenVaultVoteInstructionDataEncoder(): FixedSizeEncoder<OpenVaultVoteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["snapshotRoot", fixEncoderSize(getBytesEncoder(), 32)],
      ["totalWeight", getU64Encoder()],
      ["votingPeriod", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: OPEN_VAULT_VOTE_DISCRIMINATOR }),
  );
}

export function getOpenVaultVoteInstructionDataDecoder(): FixedSizeDecoder<OpenVaultVoteInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["snapshotRoot", fixDecoderSize(getBytesDecoder(), 32)],
    ["totalWeight", getU64Decoder()],
    ["votingPeriod", getI64Decoder()],
  ]);
}

export function getOpenVaultVoteInstructionDataCodec(): FixedSizeCodec<
  OpenVaultVoteInstructionDataArgs,
  OpenVaultVoteInstructionData
> {
  return combineCodec(
    getOpenVaultVoteInstructionDataEncoder(),
    getOpenVaultVoteInstructionDataDecoder(),
  );
}

export type OpenVaultVoteAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountVault extends string = string,
  TAccountVote extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /** Admin gate — the signer must hold an `Admin` record. */
  adminRecord?: Address<TAccountAdminRecord>;
  vault: Address<TAccountVault>;
  vote: Address<TAccountVote>;
  systemProgram?: Address<TAccountSystemProgram>;
  snapshotRoot: OpenVaultVoteInstructionDataArgs["snapshotRoot"];
  totalWeight: OpenVaultVoteInstructionDataArgs["totalWeight"];
  votingPeriod: OpenVaultVoteInstructionDataArgs["votingPeriod"];
};

export async function getOpenVaultVoteInstructionAsync<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountVault extends string,
  TAccountVote extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: OpenVaultVoteAsyncInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountVault,
    TAccountVote,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  OpenVaultVoteInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountVault,
    TAccountVote,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
    vote: { value: input.vote ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.adminRecord.value) {
    accounts.adminRecord.value = await findAdminRecordPda({
      authority: expectAddress(accounts.authority.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.vote),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getOpenVaultVoteInstructionDataEncoder().encode(
      args as OpenVaultVoteInstructionDataArgs,
    ),
    programAddress,
  } as OpenVaultVoteInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountVault,
    TAccountVote,
    TAccountSystemProgram
  >);
}

export type OpenVaultVoteInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountVault extends string = string,
  TAccountVote extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /** Admin gate — the signer must hold an `Admin` record. */
  adminRecord: Address<TAccountAdminRecord>;
  vault: Address<TAccountVault>;
  vote: Address<TAccountVote>;
  systemProgram?: Address<TAccountSystemProgram>;
  snapshotRoot: OpenVaultVoteInstructionDataArgs["snapshotRoot"];
  totalWeight: OpenVaultVoteInstructionDataArgs["totalWeight"];
  votingPeriod: OpenVaultVoteInstructionDataArgs["votingPeriod"];
};

export function getOpenVaultVoteInstruction<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountVault extends string,
  TAccountVote extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: OpenVaultVoteInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountVault,
    TAccountVote,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): OpenVaultVoteInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAdminRecord,
  TAccountVault,
  TAccountVote,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
    vote: { value: input.vote ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.vote),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getOpenVaultVoteInstructionDataEncoder().encode(
      args as OpenVaultVoteInstructionDataArgs,
    ),
    programAddress,
  } as OpenVaultVoteInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountVault,
    TAccountVote,
    TAccountSystemProgram
  >);
}

export type ParsedOpenVaultVoteInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    /** Admin gate — the signer must hold an `Admin` record. */
    adminRecord: TAccountMetas[1];
    vault: TAccountMetas[2];
    vote: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: OpenVaultVoteInstructionData;
};

export function parseOpenVaultVoteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedOpenVaultVoteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      adminRecord: getNextAccount(),
      vault: getNextAccount(),
      vote: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getOpenVaultVoteInstructionDataDecoder().decode(instruction.data),
  };
}
