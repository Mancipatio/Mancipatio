/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  findNewPermissionsPda,
  findPlatformPda,
  findRecoveryPda,
} from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const EXECUTE_ISSUER_RECOVERY_DISCRIMINATOR = new Uint8Array([
  241, 43, 185, 210, 123, 153, 169, 48,
]);

export function getExecuteIssuerRecoveryDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    EXECUTE_ISSUER_RECOVERY_DISCRIMINATOR,
  );
}

export type ExecuteIssuerRecoveryInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountNewAuthority extends string | AccountMeta<string> = string,
  TAccountPlatform extends string | AccountMeta<string> = string,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TAccountRecovery extends string | AccountMeta<string> = string,
  TAccountProposer extends string | AccountMeta<string> = string,
  TAccountOldPermissions extends string | AccountMeta<string> = string,
  TAccountNewPermissions extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountNewAuthority extends string
        ? ReadonlySignerAccount<TAccountNewAuthority> &
            AccountSignerMeta<TAccountNewAuthority>
        : TAccountNewAuthority,
      TAccountPlatform extends string
        ? ReadonlyAccount<TAccountPlatform>
        : TAccountPlatform,
      TAccountIssuer extends string
        ? WritableAccount<TAccountIssuer>
        : TAccountIssuer,
      TAccountRecovery extends string
        ? WritableAccount<TAccountRecovery>
        : TAccountRecovery,
      TAccountProposer extends string
        ? WritableAccount<TAccountProposer>
        : TAccountProposer,
      TAccountOldPermissions extends string
        ? WritableAccount<TAccountOldPermissions>
        : TAccountOldPermissions,
      TAccountNewPermissions extends string
        ? WritableAccount<TAccountNewPermissions>
        : TAccountNewPermissions,
      ...TRemainingAccounts,
    ]
  >;

export type ExecuteIssuerRecoveryInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ExecuteIssuerRecoveryInstructionDataArgs = {};

export function getExecuteIssuerRecoveryInstructionDataEncoder(): FixedSizeEncoder<ExecuteIssuerRecoveryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: EXECUTE_ISSUER_RECOVERY_DISCRIMINATOR,
    }),
  );
}

export function getExecuteIssuerRecoveryInstructionDataDecoder(): FixedSizeDecoder<ExecuteIssuerRecoveryInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getExecuteIssuerRecoveryInstructionDataCodec(): FixedSizeCodec<
  ExecuteIssuerRecoveryInstructionDataArgs,
  ExecuteIssuerRecoveryInstructionData
> {
  return combineCodec(
    getExecuteIssuerRecoveryInstructionDataEncoder(),
    getExecuteIssuerRecoveryInstructionDataDecoder(),
  );
}

export type ExecuteIssuerRecoveryAsyncInput<
  TAccountNewAuthority extends string = string,
  TAccountPlatform extends string = string,
  TAccountIssuer extends string = string,
  TAccountRecovery extends string = string,
  TAccountProposer extends string = string,
  TAccountOldPermissions extends string = string,
  TAccountNewPermissions extends string = string,
> = {
  /**
   * The recovered key signs. Not `mut`: the old grant's and the recovery's
   * rent both go to `proposer`, so this key needs no balance.
   */
  newAuthority: TransactionSigner<TAccountNewAuthority>;
  platform?: Address<TAccountPlatform>;
  issuer: Address<TAccountIssuer>;
  recovery?: Address<TAccountRecovery>;
  proposer: Address<TAccountProposer>;
  /** The lost authority's grant, closed when present (rent to `proposer`). */
  oldPermissions: Address<TAccountOldPermissions>;
  /**
   * A leftover grant of the RECOVERED key, closed when present (rent to
   * `proposer`): a recovery never carries capabilities, so none may revive.
   */
  newPermissions?: Address<TAccountNewPermissions>;
};

export async function getExecuteIssuerRecoveryInstructionAsync<
  TAccountNewAuthority extends string,
  TAccountPlatform extends string,
  TAccountIssuer extends string,
  TAccountRecovery extends string,
  TAccountProposer extends string,
  TAccountOldPermissions extends string,
  TAccountNewPermissions extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ExecuteIssuerRecoveryAsyncInput<
    TAccountNewAuthority,
    TAccountPlatform,
    TAccountIssuer,
    TAccountRecovery,
    TAccountProposer,
    TAccountOldPermissions,
    TAccountNewPermissions
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ExecuteIssuerRecoveryInstruction<
    TProgramAddress,
    TAccountNewAuthority,
    TAccountPlatform,
    TAccountIssuer,
    TAccountRecovery,
    TAccountProposer,
    TAccountOldPermissions,
    TAccountNewPermissions
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    newAuthority: { value: input.newAuthority ?? null, isWritable: false },
    platform: { value: input.platform ?? null, isWritable: false },
    issuer: { value: input.issuer ?? null, isWritable: true },
    recovery: { value: input.recovery ?? null, isWritable: true },
    proposer: { value: input.proposer ?? null, isWritable: true },
    oldPermissions: { value: input.oldPermissions ?? null, isWritable: true },
    newPermissions: { value: input.newPermissions ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.platform.value) {
    accounts.platform.value = await findPlatformPda();
  }
  if (!accounts.recovery.value) {
    accounts.recovery.value = await findRecoveryPda({
      issuer: expectAddress(accounts.issuer.value),
    });
  }
  if (!accounts.newPermissions.value) {
    accounts.newPermissions.value = await findNewPermissionsPda({
      issuer: expectAddress(accounts.issuer.value),
      newAuthority: expectAddress(accounts.newAuthority.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.newAuthority),
      getAccountMeta(accounts.platform),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.recovery),
      getAccountMeta(accounts.proposer),
      getAccountMeta(accounts.oldPermissions),
      getAccountMeta(accounts.newPermissions),
    ],
    data: getExecuteIssuerRecoveryInstructionDataEncoder().encode({}),
    programAddress,
  } as ExecuteIssuerRecoveryInstruction<
    TProgramAddress,
    TAccountNewAuthority,
    TAccountPlatform,
    TAccountIssuer,
    TAccountRecovery,
    TAccountProposer,
    TAccountOldPermissions,
    TAccountNewPermissions
  >);
}

export type ExecuteIssuerRecoveryInput<
  TAccountNewAuthority extends string = string,
  TAccountPlatform extends string = string,
  TAccountIssuer extends string = string,
  TAccountRecovery extends string = string,
  TAccountProposer extends string = string,
  TAccountOldPermissions extends string = string,
  TAccountNewPermissions extends string = string,
> = {
  /**
   * The recovered key signs. Not `mut`: the old grant's and the recovery's
   * rent both go to `proposer`, so this key needs no balance.
   */
  newAuthority: TransactionSigner<TAccountNewAuthority>;
  platform: Address<TAccountPlatform>;
  issuer: Address<TAccountIssuer>;
  recovery: Address<TAccountRecovery>;
  proposer: Address<TAccountProposer>;
  /** The lost authority's grant, closed when present (rent to `proposer`). */
  oldPermissions: Address<TAccountOldPermissions>;
  /**
   * A leftover grant of the RECOVERED key, closed when present (rent to
   * `proposer`): a recovery never carries capabilities, so none may revive.
   */
  newPermissions: Address<TAccountNewPermissions>;
};

export function getExecuteIssuerRecoveryInstruction<
  TAccountNewAuthority extends string,
  TAccountPlatform extends string,
  TAccountIssuer extends string,
  TAccountRecovery extends string,
  TAccountProposer extends string,
  TAccountOldPermissions extends string,
  TAccountNewPermissions extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ExecuteIssuerRecoveryInput<
    TAccountNewAuthority,
    TAccountPlatform,
    TAccountIssuer,
    TAccountRecovery,
    TAccountProposer,
    TAccountOldPermissions,
    TAccountNewPermissions
  >,
  config?: { programAddress?: TProgramAddress },
): ExecuteIssuerRecoveryInstruction<
  TProgramAddress,
  TAccountNewAuthority,
  TAccountPlatform,
  TAccountIssuer,
  TAccountRecovery,
  TAccountProposer,
  TAccountOldPermissions,
  TAccountNewPermissions
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    newAuthority: { value: input.newAuthority ?? null, isWritable: false },
    platform: { value: input.platform ?? null, isWritable: false },
    issuer: { value: input.issuer ?? null, isWritable: true },
    recovery: { value: input.recovery ?? null, isWritable: true },
    proposer: { value: input.proposer ?? null, isWritable: true },
    oldPermissions: { value: input.oldPermissions ?? null, isWritable: true },
    newPermissions: { value: input.newPermissions ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.newAuthority),
      getAccountMeta(accounts.platform),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.recovery),
      getAccountMeta(accounts.proposer),
      getAccountMeta(accounts.oldPermissions),
      getAccountMeta(accounts.newPermissions),
    ],
    data: getExecuteIssuerRecoveryInstructionDataEncoder().encode({}),
    programAddress,
  } as ExecuteIssuerRecoveryInstruction<
    TProgramAddress,
    TAccountNewAuthority,
    TAccountPlatform,
    TAccountIssuer,
    TAccountRecovery,
    TAccountProposer,
    TAccountOldPermissions,
    TAccountNewPermissions
  >);
}

export type ParsedExecuteIssuerRecoveryInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * The recovered key signs. Not `mut`: the old grant's and the recovery's
     * rent both go to `proposer`, so this key needs no balance.
     */
    newAuthority: TAccountMetas[0];
    platform: TAccountMetas[1];
    issuer: TAccountMetas[2];
    recovery: TAccountMetas[3];
    proposer: TAccountMetas[4];
    /** The lost authority's grant, closed when present (rent to `proposer`). */
    oldPermissions: TAccountMetas[5];
    /**
     * A leftover grant of the RECOVERED key, closed when present (rent to
     * `proposer`): a recovery never carries capabilities, so none may revive.
     */
    newPermissions: TAccountMetas[6];
  };
  data: ExecuteIssuerRecoveryInstructionData;
};

export function parseExecuteIssuerRecoveryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedExecuteIssuerRecoveryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      newAuthority: getNextAccount(),
      platform: getNextAccount(),
      issuer: getNextAccount(),
      recovery: getNextAccount(),
      proposer: getNextAccount(),
      oldPermissions: getNextAccount(),
      newPermissions: getNextAccount(),
    },
    data: getExecuteIssuerRecoveryInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
