/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findPlatformPda, findRecoveryPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const PROPOSE_ISSUER_RECOVERY_DISCRIMINATOR = new Uint8Array([
  155, 16, 221, 45, 66, 204, 18, 249,
]);

export function getProposeIssuerRecoveryDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PROPOSE_ISSUER_RECOVERY_DISCRIMINATOR,
  );
}

export type ProposeIssuerRecoveryInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountSuperAdmin extends string | AccountMeta<string> = string,
  TAccountPlatform extends string | AccountMeta<string> = string,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TAccountRecovery extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSuperAdmin extends string
        ? WritableSignerAccount<TAccountSuperAdmin> &
            AccountSignerMeta<TAccountSuperAdmin>
        : TAccountSuperAdmin,
      TAccountPlatform extends string
        ? ReadonlyAccount<TAccountPlatform>
        : TAccountPlatform,
      TAccountIssuer extends string
        ? ReadonlyAccount<TAccountIssuer>
        : TAccountIssuer,
      TAccountRecovery extends string
        ? WritableAccount<TAccountRecovery>
        : TAccountRecovery,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ProposeIssuerRecoveryInstructionData = {
  discriminator: ReadonlyUint8Array;
  newAuthority: Address;
};

export type ProposeIssuerRecoveryInstructionDataArgs = {
  newAuthority: Address;
};

export function getProposeIssuerRecoveryInstructionDataEncoder(): FixedSizeEncoder<ProposeIssuerRecoveryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newAuthority", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: PROPOSE_ISSUER_RECOVERY_DISCRIMINATOR,
    }),
  );
}

export function getProposeIssuerRecoveryInstructionDataDecoder(): FixedSizeDecoder<ProposeIssuerRecoveryInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newAuthority", getAddressDecoder()],
  ]);
}

export function getProposeIssuerRecoveryInstructionDataCodec(): FixedSizeCodec<
  ProposeIssuerRecoveryInstructionDataArgs,
  ProposeIssuerRecoveryInstructionData
> {
  return combineCodec(
    getProposeIssuerRecoveryInstructionDataEncoder(),
    getProposeIssuerRecoveryInstructionDataDecoder(),
  );
}

export type ProposeIssuerRecoveryAsyncInput<
  TAccountSuperAdmin extends string = string,
  TAccountPlatform extends string = string,
  TAccountIssuer extends string = string,
  TAccountRecovery extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  superAdmin: TransactionSigner<TAccountSuperAdmin>;
  platform?: Address<TAccountPlatform>;
  issuer: Address<TAccountIssuer>;
  recovery?: Address<TAccountRecovery>;
  systemProgram?: Address<TAccountSystemProgram>;
  newAuthority: ProposeIssuerRecoveryInstructionDataArgs["newAuthority"];
};

export async function getProposeIssuerRecoveryInstructionAsync<
  TAccountSuperAdmin extends string,
  TAccountPlatform extends string,
  TAccountIssuer extends string,
  TAccountRecovery extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ProposeIssuerRecoveryAsyncInput<
    TAccountSuperAdmin,
    TAccountPlatform,
    TAccountIssuer,
    TAccountRecovery,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ProposeIssuerRecoveryInstruction<
    TProgramAddress,
    TAccountSuperAdmin,
    TAccountPlatform,
    TAccountIssuer,
    TAccountRecovery,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    superAdmin: { value: input.superAdmin ?? null, isWritable: true },
    platform: { value: input.platform ?? null, isWritable: false },
    issuer: { value: input.issuer ?? null, isWritable: false },
    recovery: { value: input.recovery ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.platform.value) {
    accounts.platform.value = await findPlatformPda();
  }
  if (!accounts.recovery.value) {
    accounts.recovery.value = await findRecoveryPda({
      issuer: expectAddress(accounts.issuer.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.superAdmin),
      getAccountMeta(accounts.platform),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.recovery),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getProposeIssuerRecoveryInstructionDataEncoder().encode(
      args as ProposeIssuerRecoveryInstructionDataArgs,
    ),
    programAddress,
  } as ProposeIssuerRecoveryInstruction<
    TProgramAddress,
    TAccountSuperAdmin,
    TAccountPlatform,
    TAccountIssuer,
    TAccountRecovery,
    TAccountSystemProgram
  >);
}

export type ProposeIssuerRecoveryInput<
  TAccountSuperAdmin extends string = string,
  TAccountPlatform extends string = string,
  TAccountIssuer extends string = string,
  TAccountRecovery extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  superAdmin: TransactionSigner<TAccountSuperAdmin>;
  platform: Address<TAccountPlatform>;
  issuer: Address<TAccountIssuer>;
  recovery: Address<TAccountRecovery>;
  systemProgram?: Address<TAccountSystemProgram>;
  newAuthority: ProposeIssuerRecoveryInstructionDataArgs["newAuthority"];
};

export function getProposeIssuerRecoveryInstruction<
  TAccountSuperAdmin extends string,
  TAccountPlatform extends string,
  TAccountIssuer extends string,
  TAccountRecovery extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ProposeIssuerRecoveryInput<
    TAccountSuperAdmin,
    TAccountPlatform,
    TAccountIssuer,
    TAccountRecovery,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ProposeIssuerRecoveryInstruction<
  TProgramAddress,
  TAccountSuperAdmin,
  TAccountPlatform,
  TAccountIssuer,
  TAccountRecovery,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    superAdmin: { value: input.superAdmin ?? null, isWritable: true },
    platform: { value: input.platform ?? null, isWritable: false },
    issuer: { value: input.issuer ?? null, isWritable: false },
    recovery: { value: input.recovery ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.superAdmin),
      getAccountMeta(accounts.platform),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.recovery),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getProposeIssuerRecoveryInstructionDataEncoder().encode(
      args as ProposeIssuerRecoveryInstructionDataArgs,
    ),
    programAddress,
  } as ProposeIssuerRecoveryInstruction<
    TProgramAddress,
    TAccountSuperAdmin,
    TAccountPlatform,
    TAccountIssuer,
    TAccountRecovery,
    TAccountSystemProgram
  >);
}

export type ParsedProposeIssuerRecoveryInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    superAdmin: TAccountMetas[0];
    platform: TAccountMetas[1];
    issuer: TAccountMetas[2];
    recovery: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: ProposeIssuerRecoveryInstructionData;
};

export function parseProposeIssuerRecoveryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProposeIssuerRecoveryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      superAdmin: getNextAccount(),
      platform: getNextAccount(),
      issuer: getNextAccount(),
      recovery: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getProposeIssuerRecoveryInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
