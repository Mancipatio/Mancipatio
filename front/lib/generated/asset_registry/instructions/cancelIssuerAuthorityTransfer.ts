/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findAcceptIssuerAuthorityTransferPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CANCEL_ISSUER_AUTHORITY_TRANSFER_DISCRIMINATOR = new Uint8Array([
  116, 70, 121, 72, 14, 114, 226, 159,
]);

export function getCancelIssuerAuthorityTransferDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_ISSUER_AUTHORITY_TRANSFER_DISCRIMINATOR,
  );
}

export type CancelIssuerAuthorityTransferInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TAccountTransfer extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountIssuer extends string
        ? ReadonlyAccount<TAccountIssuer>
        : TAccountIssuer,
      TAccountTransfer extends string
        ? WritableAccount<TAccountTransfer>
        : TAccountTransfer,
      ...TRemainingAccounts,
    ]
  >;

export type CancelIssuerAuthorityTransferInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CancelIssuerAuthorityTransferInstructionDataArgs = {};

export function getCancelIssuerAuthorityTransferInstructionDataEncoder(): FixedSizeEncoder<CancelIssuerAuthorityTransferInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CANCEL_ISSUER_AUTHORITY_TRANSFER_DISCRIMINATOR,
    }),
  );
}

export function getCancelIssuerAuthorityTransferInstructionDataDecoder(): FixedSizeDecoder<CancelIssuerAuthorityTransferInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCancelIssuerAuthorityTransferInstructionDataCodec(): FixedSizeCodec<
  CancelIssuerAuthorityTransferInstructionDataArgs,
  CancelIssuerAuthorityTransferInstructionData
> {
  return combineCodec(
    getCancelIssuerAuthorityTransferInstructionDataEncoder(),
    getCancelIssuerAuthorityTransferInstructionDataDecoder(),
  );
}

export type CancelIssuerAuthorityTransferAsyncInput<
  TAccountAuthority extends string = string,
  TAccountIssuer extends string = string,
  TAccountTransfer extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  issuer: Address<TAccountIssuer>;
  transfer?: Address<TAccountTransfer>;
};

export async function getCancelIssuerAuthorityTransferInstructionAsync<
  TAccountAuthority extends string,
  TAccountIssuer extends string,
  TAccountTransfer extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: CancelIssuerAuthorityTransferAsyncInput<
    TAccountAuthority,
    TAccountIssuer,
    TAccountTransfer
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CancelIssuerAuthorityTransferInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIssuer,
    TAccountTransfer
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    issuer: { value: input.issuer ?? null, isWritable: false },
    transfer: { value: input.transfer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.transfer.value) {
    accounts.transfer.value = await findAcceptIssuerAuthorityTransferPda({
      issuer: expectAddress(accounts.issuer.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.transfer),
    ],
    data: getCancelIssuerAuthorityTransferInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelIssuerAuthorityTransferInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIssuer,
    TAccountTransfer
  >);
}

export type CancelIssuerAuthorityTransferInput<
  TAccountAuthority extends string = string,
  TAccountIssuer extends string = string,
  TAccountTransfer extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  issuer: Address<TAccountIssuer>;
  transfer: Address<TAccountTransfer>;
};

export function getCancelIssuerAuthorityTransferInstruction<
  TAccountAuthority extends string,
  TAccountIssuer extends string,
  TAccountTransfer extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: CancelIssuerAuthorityTransferInput<
    TAccountAuthority,
    TAccountIssuer,
    TAccountTransfer
  >,
  config?: { programAddress?: TProgramAddress },
): CancelIssuerAuthorityTransferInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountIssuer,
  TAccountTransfer
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    issuer: { value: input.issuer ?? null, isWritable: false },
    transfer: { value: input.transfer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.transfer),
    ],
    data: getCancelIssuerAuthorityTransferInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelIssuerAuthorityTransferInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIssuer,
    TAccountTransfer
  >);
}

export type ParsedCancelIssuerAuthorityTransferInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    issuer: TAccountMetas[1];
    transfer: TAccountMetas[2];
  };
  data: CancelIssuerAuthorityTransferInstructionData;
};

export function parseCancelIssuerAuthorityTransferInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelIssuerAuthorityTransferInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      issuer: getNextAccount(),
      transfer: getNextAccount(),
    },
    data: getCancelIssuerAuthorityTransferInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
