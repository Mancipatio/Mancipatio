/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  findAdminRecordPda,
  findCloseDistributionEscrowMarkerPda,
  findDistributionPda,
  findEscrowPda,
  findPlanPda,
  findPlatformPda,
} from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CREATE_DISTRIBUTION_DISCRIMINATOR = new Uint8Array([
  237, 83, 162, 50, 49, 6, 28, 115,
]);

export function getCreateDistributionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_DISTRIBUTION_DISCRIMINATOR,
  );
}

export type CreateDistributionInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAdminRecord extends string | AccountMeta<string> = string,
  TAccountShareClass extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountPaymentMint extends string | AccountMeta<string> = string,
  TAccountDistribution extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountEscrowMarker extends string | AccountMeta<string> = string,
  TAccountFunder extends string | AccountMeta<string> = string,
  TAccountFunderPaymentAccount extends string | AccountMeta<string> = string,
  TAccountPaymentTokenProgram extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountPlan extends string | AccountMeta<string> = string,
  TAccountPlatform extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAdminRecord extends string
        ? ReadonlyAccount<TAccountAdminRecord>
        : TAccountAdminRecord,
      TAccountShareClass extends string
        ? ReadonlyAccount<TAccountShareClass>
        : TAccountShareClass,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountPaymentMint extends string
        ? ReadonlyAccount<TAccountPaymentMint>
        : TAccountPaymentMint,
      TAccountDistribution extends string
        ? WritableAccount<TAccountDistribution>
        : TAccountDistribution,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountEscrowMarker extends string
        ? WritableAccount<TAccountEscrowMarker>
        : TAccountEscrowMarker,
      TAccountFunder extends string
        ? ReadonlySignerAccount<TAccountFunder> &
            AccountSignerMeta<TAccountFunder>
        : TAccountFunder,
      TAccountFunderPaymentAccount extends string
        ? WritableAccount<TAccountFunderPaymentAccount>
        : TAccountFunderPaymentAccount,
      TAccountPaymentTokenProgram extends string
        ? ReadonlyAccount<TAccountPaymentTokenProgram>
        : TAccountPaymentTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountPlan extends string
        ? WritableAccount<TAccountPlan>
        : TAccountPlan,
      TAccountPlatform extends string
        ? ReadonlyAccount<TAccountPlatform>
        : TAccountPlatform,
      ...TRemainingAccounts,
    ]
  >;

export type CreateDistributionInstructionData = {
  discriminator: ReadonlyUint8Array;
  distributionId: bigint;
  totalAmount: bigint;
  snapshotSupply: bigint;
  batchRoot: ReadonlyUint8Array;
  batchCount: number;
};

export type CreateDistributionInstructionDataArgs = {
  distributionId: number | bigint;
  totalAmount: number | bigint;
  snapshotSupply: number | bigint;
  batchRoot: ReadonlyUint8Array;
  batchCount: number;
};

export function getCreateDistributionInstructionDataEncoder(): FixedSizeEncoder<CreateDistributionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["distributionId", getU64Encoder()],
      ["totalAmount", getU64Encoder()],
      ["snapshotSupply", getU64Encoder()],
      ["batchRoot", fixEncoderSize(getBytesEncoder(), 32)],
      ["batchCount", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_DISTRIBUTION_DISCRIMINATOR }),
  );
}

export function getCreateDistributionInstructionDataDecoder(): FixedSizeDecoder<CreateDistributionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["distributionId", getU64Decoder()],
    ["totalAmount", getU64Decoder()],
    ["snapshotSupply", getU64Decoder()],
    ["batchRoot", fixDecoderSize(getBytesDecoder(), 32)],
    ["batchCount", getU32Decoder()],
  ]);
}

export function getCreateDistributionInstructionDataCodec(): FixedSizeCodec<
  CreateDistributionInstructionDataArgs,
  CreateDistributionInstructionData
> {
  return combineCodec(
    getCreateDistributionInstructionDataEncoder(),
    getCreateDistributionInstructionDataDecoder(),
  );
}

export type CreateDistributionAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountShareClass extends string = string,
  TAccountMint extends string = string,
  TAccountPaymentMint extends string = string,
  TAccountDistribution extends string = string,
  TAccountEscrow extends string = string,
  TAccountEscrowMarker extends string = string,
  TAccountFunder extends string = string,
  TAccountFunderPaymentAccount extends string = string,
  TAccountPaymentTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountPlan extends string = string,
  TAccountPlatform extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /** Admin gate — only an admin may open distributions. */
  adminRecord?: Address<TAccountAdminRecord>;
  shareClass: Address<TAccountShareClass>;
  /** Share-class mint the distribution relates to (indexing only). */
  mint: Address<TAccountMint>;
  /** Payment mint being distributed (e.g. USDT). */
  paymentMint: Address<TAccountPaymentMint>;
  distribution?: Address<TAccountDistribution>;
  /** Escrow holding the payment tokens; authority is the `Distribution` PDA. */
  escrow?: Address<TAccountEscrow>;
  /**
   * Marks the distribution PDA as a platform escrow authority — the
   * transfer hook exempts escrow legs from receiver-KYC while this exists.
   * Closed by `close_distribution`.
   */
  escrowMarker?: Address<TAccountEscrowMarker>;
  /**
   * Funds the distribution — transfers exactly `total_amount` into the
   * escrow. Recorded on the `Distribution`; `close_distribution` refunds
   * the remainder (and the escrow's rent) to this wallet.
   */
  funder: TransactionSigner<TAccountFunder>;
  /** Funder's payment token account — debited `total_amount`. */
  funderPaymentAccount: Address<TAccountFunderPaymentAccount>;
  paymentTokenProgram: Address<TAccountPaymentTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  plan?: Address<TAccountPlan>;
  /**
   * Emergency-pause gate (read-only). Keep LAST among named accounts: old
   * account indices and the remaining-accounts hook tail keep their positions.
   */
  platform?: Address<TAccountPlatform>;
  distributionId: CreateDistributionInstructionDataArgs["distributionId"];
  totalAmount: CreateDistributionInstructionDataArgs["totalAmount"];
  snapshotSupply: CreateDistributionInstructionDataArgs["snapshotSupply"];
  batchRoot: CreateDistributionInstructionDataArgs["batchRoot"];
  batchCount: CreateDistributionInstructionDataArgs["batchCount"];
};

export async function getCreateDistributionInstructionAsync<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountShareClass extends string,
  TAccountMint extends string,
  TAccountPaymentMint extends string,
  TAccountDistribution extends string,
  TAccountEscrow extends string,
  TAccountEscrowMarker extends string,
  TAccountFunder extends string,
  TAccountFunderPaymentAccount extends string,
  TAccountPaymentTokenProgram extends string,
  TAccountSystemProgram extends string,
  TAccountPlan extends string,
  TAccountPlatform extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: CreateDistributionAsyncInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass,
    TAccountMint,
    TAccountPaymentMint,
    TAccountDistribution,
    TAccountEscrow,
    TAccountEscrowMarker,
    TAccountFunder,
    TAccountFunderPaymentAccount,
    TAccountPaymentTokenProgram,
    TAccountSystemProgram,
    TAccountPlan,
    TAccountPlatform
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateDistributionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass,
    TAccountMint,
    TAccountPaymentMint,
    TAccountDistribution,
    TAccountEscrow,
    TAccountEscrowMarker,
    TAccountFunder,
    TAccountFunderPaymentAccount,
    TAccountPaymentTokenProgram,
    TAccountSystemProgram,
    TAccountPlan,
    TAccountPlatform
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    paymentMint: { value: input.paymentMint ?? null, isWritable: false },
    distribution: { value: input.distribution ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    escrowMarker: { value: input.escrowMarker ?? null, isWritable: true },
    funder: { value: input.funder ?? null, isWritable: false },
    funderPaymentAccount: {
      value: input.funderPaymentAccount ?? null,
      isWritable: true,
    },
    paymentTokenProgram: {
      value: input.paymentTokenProgram ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    plan: { value: input.plan ?? null, isWritable: true },
    platform: { value: input.platform ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.adminRecord.value) {
    accounts.adminRecord.value = await findAdminRecordPda({
      authority: expectAddress(accounts.authority.value),
    });
  }
  if (!accounts.distribution.value) {
    accounts.distribution.value = await findDistributionPda({
      shareClass: expectAddress(accounts.shareClass.value),
      distributionId: expectSome(args.distributionId),
    });
  }
  if (!accounts.escrow.value) {
    accounts.escrow.value = await findEscrowPda({
      distribution: expectAddress(accounts.distribution.value),
    });
  }
  if (!accounts.escrowMarker.value) {
    accounts.escrowMarker.value = await findCloseDistributionEscrowMarkerPda({
      distribution: expectAddress(accounts.distribution.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.plan.value) {
    accounts.plan.value = await findPlanPda({
      distribution: expectAddress(accounts.distribution.value),
    });
  }
  if (!accounts.platform.value) {
    accounts.platform.value = await findPlatformPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.paymentMint),
      getAccountMeta(accounts.distribution),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.escrowMarker),
      getAccountMeta(accounts.funder),
      getAccountMeta(accounts.funderPaymentAccount),
      getAccountMeta(accounts.paymentTokenProgram),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.plan),
      getAccountMeta(accounts.platform),
    ],
    data: getCreateDistributionInstructionDataEncoder().encode(
      args as CreateDistributionInstructionDataArgs,
    ),
    programAddress,
  } as CreateDistributionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass,
    TAccountMint,
    TAccountPaymentMint,
    TAccountDistribution,
    TAccountEscrow,
    TAccountEscrowMarker,
    TAccountFunder,
    TAccountFunderPaymentAccount,
    TAccountPaymentTokenProgram,
    TAccountSystemProgram,
    TAccountPlan,
    TAccountPlatform
  >);
}

export type CreateDistributionInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountShareClass extends string = string,
  TAccountMint extends string = string,
  TAccountPaymentMint extends string = string,
  TAccountDistribution extends string = string,
  TAccountEscrow extends string = string,
  TAccountEscrowMarker extends string = string,
  TAccountFunder extends string = string,
  TAccountFunderPaymentAccount extends string = string,
  TAccountPaymentTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountPlan extends string = string,
  TAccountPlatform extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /** Admin gate — only an admin may open distributions. */
  adminRecord: Address<TAccountAdminRecord>;
  shareClass: Address<TAccountShareClass>;
  /** Share-class mint the distribution relates to (indexing only). */
  mint: Address<TAccountMint>;
  /** Payment mint being distributed (e.g. USDT). */
  paymentMint: Address<TAccountPaymentMint>;
  distribution: Address<TAccountDistribution>;
  /** Escrow holding the payment tokens; authority is the `Distribution` PDA. */
  escrow: Address<TAccountEscrow>;
  /**
   * Marks the distribution PDA as a platform escrow authority — the
   * transfer hook exempts escrow legs from receiver-KYC while this exists.
   * Closed by `close_distribution`.
   */
  escrowMarker: Address<TAccountEscrowMarker>;
  /**
   * Funds the distribution — transfers exactly `total_amount` into the
   * escrow. Recorded on the `Distribution`; `close_distribution` refunds
   * the remainder (and the escrow's rent) to this wallet.
   */
  funder: TransactionSigner<TAccountFunder>;
  /** Funder's payment token account — debited `total_amount`. */
  funderPaymentAccount: Address<TAccountFunderPaymentAccount>;
  paymentTokenProgram: Address<TAccountPaymentTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  plan: Address<TAccountPlan>;
  /**
   * Emergency-pause gate (read-only). Keep LAST among named accounts: old
   * account indices and the remaining-accounts hook tail keep their positions.
   */
  platform: Address<TAccountPlatform>;
  distributionId: CreateDistributionInstructionDataArgs["distributionId"];
  totalAmount: CreateDistributionInstructionDataArgs["totalAmount"];
  snapshotSupply: CreateDistributionInstructionDataArgs["snapshotSupply"];
  batchRoot: CreateDistributionInstructionDataArgs["batchRoot"];
  batchCount: CreateDistributionInstructionDataArgs["batchCount"];
};

export function getCreateDistributionInstruction<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountShareClass extends string,
  TAccountMint extends string,
  TAccountPaymentMint extends string,
  TAccountDistribution extends string,
  TAccountEscrow extends string,
  TAccountEscrowMarker extends string,
  TAccountFunder extends string,
  TAccountFunderPaymentAccount extends string,
  TAccountPaymentTokenProgram extends string,
  TAccountSystemProgram extends string,
  TAccountPlan extends string,
  TAccountPlatform extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: CreateDistributionInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass,
    TAccountMint,
    TAccountPaymentMint,
    TAccountDistribution,
    TAccountEscrow,
    TAccountEscrowMarker,
    TAccountFunder,
    TAccountFunderPaymentAccount,
    TAccountPaymentTokenProgram,
    TAccountSystemProgram,
    TAccountPlan,
    TAccountPlatform
  >,
  config?: { programAddress?: TProgramAddress },
): CreateDistributionInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAdminRecord,
  TAccountShareClass,
  TAccountMint,
  TAccountPaymentMint,
  TAccountDistribution,
  TAccountEscrow,
  TAccountEscrowMarker,
  TAccountFunder,
  TAccountFunderPaymentAccount,
  TAccountPaymentTokenProgram,
  TAccountSystemProgram,
  TAccountPlan,
  TAccountPlatform
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    paymentMint: { value: input.paymentMint ?? null, isWritable: false },
    distribution: { value: input.distribution ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    escrowMarker: { value: input.escrowMarker ?? null, isWritable: true },
    funder: { value: input.funder ?? null, isWritable: false },
    funderPaymentAccount: {
      value: input.funderPaymentAccount ?? null,
      isWritable: true,
    },
    paymentTokenProgram: {
      value: input.paymentTokenProgram ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    plan: { value: input.plan ?? null, isWritable: true },
    platform: { value: input.platform ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.paymentMint),
      getAccountMeta(accounts.distribution),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.escrowMarker),
      getAccountMeta(accounts.funder),
      getAccountMeta(accounts.funderPaymentAccount),
      getAccountMeta(accounts.paymentTokenProgram),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.plan),
      getAccountMeta(accounts.platform),
    ],
    data: getCreateDistributionInstructionDataEncoder().encode(
      args as CreateDistributionInstructionDataArgs,
    ),
    programAddress,
  } as CreateDistributionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass,
    TAccountMint,
    TAccountPaymentMint,
    TAccountDistribution,
    TAccountEscrow,
    TAccountEscrowMarker,
    TAccountFunder,
    TAccountFunderPaymentAccount,
    TAccountPaymentTokenProgram,
    TAccountSystemProgram,
    TAccountPlan,
    TAccountPlatform
  >);
}

export type ParsedCreateDistributionInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    /** Admin gate — only an admin may open distributions. */
    adminRecord: TAccountMetas[1];
    shareClass: TAccountMetas[2];
    /** Share-class mint the distribution relates to (indexing only). */
    mint: TAccountMetas[3];
    /** Payment mint being distributed (e.g. USDT). */
    paymentMint: TAccountMetas[4];
    distribution: TAccountMetas[5];
    /** Escrow holding the payment tokens; authority is the `Distribution` PDA. */
    escrow: TAccountMetas[6];
    /**
     * Marks the distribution PDA as a platform escrow authority — the
     * transfer hook exempts escrow legs from receiver-KYC while this exists.
     * Closed by `close_distribution`.
     */
    escrowMarker: TAccountMetas[7];
    /**
     * Funds the distribution — transfers exactly `total_amount` into the
     * escrow. Recorded on the `Distribution`; `close_distribution` refunds
     * the remainder (and the escrow's rent) to this wallet.
     */
    funder: TAccountMetas[8];
    /** Funder's payment token account — debited `total_amount`. */
    funderPaymentAccount: TAccountMetas[9];
    paymentTokenProgram: TAccountMetas[10];
    systemProgram: TAccountMetas[11];
    plan: TAccountMetas[12];
    /**
     * Emergency-pause gate (read-only). Keep LAST among named accounts: old
     * account indices and the remaining-accounts hook tail keep their positions.
     */
    platform: TAccountMetas[13];
  };
  data: CreateDistributionInstructionData;
};

export function parseCreateDistributionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateDistributionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 14) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      adminRecord: getNextAccount(),
      shareClass: getNextAccount(),
      mint: getNextAccount(),
      paymentMint: getNextAccount(),
      distribution: getNextAccount(),
      escrow: getNextAccount(),
      escrowMarker: getNextAccount(),
      funder: getNextAccount(),
      funderPaymentAccount: getNextAccount(),
      paymentTokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
      plan: getNextAccount(),
      platform: getNextAccount(),
    },
    data: getCreateDistributionInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
