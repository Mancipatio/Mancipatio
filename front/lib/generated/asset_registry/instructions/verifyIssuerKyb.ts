/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findPlatformPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const VERIFY_ISSUER_KYB_DISCRIMINATOR = new Uint8Array([
  140, 127, 102, 179, 39, 113, 234, 165,
]);

export function getVerifyIssuerKybDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VERIFY_ISSUER_KYB_DISCRIMINATOR,
  );
}

export type VerifyIssuerKybInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountPlatform extends string | AccountMeta<string> = string,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountPlatform extends string
        ? ReadonlyAccount<TAccountPlatform>
        : TAccountPlatform,
      TAccountIssuer extends string
        ? WritableAccount<TAccountIssuer>
        : TAccountIssuer,
      ...TRemainingAccounts,
    ]
  >;

export type VerifyIssuerKybInstructionData = {
  discriminator: ReadonlyUint8Array;
  approved: boolean;
};

export type VerifyIssuerKybInstructionDataArgs = { approved: boolean };

export function getVerifyIssuerKybInstructionDataEncoder(): FixedSizeEncoder<VerifyIssuerKybInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["approved", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: VERIFY_ISSUER_KYB_DISCRIMINATOR }),
  );
}

export function getVerifyIssuerKybInstructionDataDecoder(): FixedSizeDecoder<VerifyIssuerKybInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["approved", getBooleanDecoder()],
  ]);
}

export function getVerifyIssuerKybInstructionDataCodec(): FixedSizeCodec<
  VerifyIssuerKybInstructionDataArgs,
  VerifyIssuerKybInstructionData
> {
  return combineCodec(
    getVerifyIssuerKybInstructionDataEncoder(),
    getVerifyIssuerKybInstructionDataDecoder(),
  );
}

export type VerifyIssuerKybAsyncInput<
  TAccountAdmin extends string = string,
  TAccountPlatform extends string = string,
  TAccountIssuer extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  platform?: Address<TAccountPlatform>;
  issuer: Address<TAccountIssuer>;
  approved: VerifyIssuerKybInstructionDataArgs["approved"];
};

export async function getVerifyIssuerKybInstructionAsync<
  TAccountAdmin extends string,
  TAccountPlatform extends string,
  TAccountIssuer extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: VerifyIssuerKybAsyncInput<
    TAccountAdmin,
    TAccountPlatform,
    TAccountIssuer
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  VerifyIssuerKybInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountPlatform,
    TAccountIssuer
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    platform: { value: input.platform ?? null, isWritable: false },
    issuer: { value: input.issuer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.platform.value) {
    accounts.platform.value = await findPlatformPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.platform),
      getAccountMeta(accounts.issuer),
    ],
    data: getVerifyIssuerKybInstructionDataEncoder().encode(
      args as VerifyIssuerKybInstructionDataArgs,
    ),
    programAddress,
  } as VerifyIssuerKybInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountPlatform,
    TAccountIssuer
  >);
}

export type VerifyIssuerKybInput<
  TAccountAdmin extends string = string,
  TAccountPlatform extends string = string,
  TAccountIssuer extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  platform: Address<TAccountPlatform>;
  issuer: Address<TAccountIssuer>;
  approved: VerifyIssuerKybInstructionDataArgs["approved"];
};

export function getVerifyIssuerKybInstruction<
  TAccountAdmin extends string,
  TAccountPlatform extends string,
  TAccountIssuer extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: VerifyIssuerKybInput<TAccountAdmin, TAccountPlatform, TAccountIssuer>,
  config?: { programAddress?: TProgramAddress },
): VerifyIssuerKybInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountPlatform,
  TAccountIssuer
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    platform: { value: input.platform ?? null, isWritable: false },
    issuer: { value: input.issuer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.platform),
      getAccountMeta(accounts.issuer),
    ],
    data: getVerifyIssuerKybInstructionDataEncoder().encode(
      args as VerifyIssuerKybInstructionDataArgs,
    ),
    programAddress,
  } as VerifyIssuerKybInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountPlatform,
    TAccountIssuer
  >);
}

export type ParsedVerifyIssuerKybInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    platform: TAccountMetas[1];
    issuer: TAccountMetas[2];
  };
  data: VerifyIssuerKybInstructionData;
};

export function parseVerifyIssuerKybInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedVerifyIssuerKybInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      platform: getNextAccount(),
      issuer: getNextAccount(),
    },
    data: getVerifyIssuerKybInstructionDataDecoder().decode(instruction.data),
  };
}
