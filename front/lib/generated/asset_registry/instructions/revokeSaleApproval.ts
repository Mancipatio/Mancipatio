/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findAdminRecordPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const REVOKE_SALE_APPROVAL_DISCRIMINATOR = new Uint8Array([
  123, 227, 105, 220, 92, 2, 48, 39,
]);

export function getRevokeSaleApprovalDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REVOKE_SALE_APPROVAL_DISCRIMINATOR,
  );
}

export type RevokeSaleApprovalInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAdminRecord extends string | AccountMeta<string> = string,
  TAccountSaleApproval extends string | AccountMeta<string> = string,
  TAccountApprovedBy extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAdminRecord extends string
        ? ReadonlyAccount<TAccountAdminRecord>
        : TAccountAdminRecord,
      TAccountSaleApproval extends string
        ? WritableAccount<TAccountSaleApproval>
        : TAccountSaleApproval,
      TAccountApprovedBy extends string
        ? WritableAccount<TAccountApprovedBy>
        : TAccountApprovedBy,
      ...TRemainingAccounts,
    ]
  >;

export type RevokeSaleApprovalInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RevokeSaleApprovalInstructionDataArgs = {};

export function getRevokeSaleApprovalInstructionDataEncoder(): FixedSizeEncoder<RevokeSaleApprovalInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: REVOKE_SALE_APPROVAL_DISCRIMINATOR,
    }),
  );
}

export function getRevokeSaleApprovalInstructionDataDecoder(): FixedSizeDecoder<RevokeSaleApprovalInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRevokeSaleApprovalInstructionDataCodec(): FixedSizeCodec<
  RevokeSaleApprovalInstructionDataArgs,
  RevokeSaleApprovalInstructionData
> {
  return combineCodec(
    getRevokeSaleApprovalInstructionDataEncoder(),
    getRevokeSaleApprovalInstructionDataDecoder(),
  );
}

export type RevokeSaleApprovalAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountSaleApproval extends string = string,
  TAccountApprovedBy extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /** Admin gate: any active Admin may revoke, not only the approver. */
  adminRecord?: Address<TAccountAdminRecord>;
  saleApproval: Address<TAccountSaleApproval>;
  /** The approving Admin; receives the approval's rent, whoever revokes. */
  approvedBy: Address<TAccountApprovedBy>;
};

export async function getRevokeSaleApprovalInstructionAsync<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountSaleApproval extends string,
  TAccountApprovedBy extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: RevokeSaleApprovalAsyncInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountSaleApproval,
    TAccountApprovedBy
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RevokeSaleApprovalInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountSaleApproval,
    TAccountApprovedBy
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    saleApproval: { value: input.saleApproval ?? null, isWritable: true },
    approvedBy: { value: input.approvedBy ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.adminRecord.value) {
    accounts.adminRecord.value = await findAdminRecordPda({
      authority: expectAddress(accounts.authority.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.saleApproval),
      getAccountMeta(accounts.approvedBy),
    ],
    data: getRevokeSaleApprovalInstructionDataEncoder().encode({}),
    programAddress,
  } as RevokeSaleApprovalInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountSaleApproval,
    TAccountApprovedBy
  >);
}

export type RevokeSaleApprovalInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountSaleApproval extends string = string,
  TAccountApprovedBy extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /** Admin gate: any active Admin may revoke, not only the approver. */
  adminRecord: Address<TAccountAdminRecord>;
  saleApproval: Address<TAccountSaleApproval>;
  /** The approving Admin; receives the approval's rent, whoever revokes. */
  approvedBy: Address<TAccountApprovedBy>;
};

export function getRevokeSaleApprovalInstruction<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountSaleApproval extends string,
  TAccountApprovedBy extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: RevokeSaleApprovalInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountSaleApproval,
    TAccountApprovedBy
  >,
  config?: { programAddress?: TProgramAddress },
): RevokeSaleApprovalInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAdminRecord,
  TAccountSaleApproval,
  TAccountApprovedBy
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    saleApproval: { value: input.saleApproval ?? null, isWritable: true },
    approvedBy: { value: input.approvedBy ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.saleApproval),
      getAccountMeta(accounts.approvedBy),
    ],
    data: getRevokeSaleApprovalInstructionDataEncoder().encode({}),
    programAddress,
  } as RevokeSaleApprovalInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountSaleApproval,
    TAccountApprovedBy
  >);
}

export type ParsedRevokeSaleApprovalInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    /** Admin gate: any active Admin may revoke, not only the approver. */
    adminRecord: TAccountMetas[1];
    saleApproval: TAccountMetas[2];
    /** The approving Admin; receives the approval's rent, whoever revokes. */
    approvedBy: TAccountMetas[3];
  };
  data: RevokeSaleApprovalInstructionData;
};

export function parseRevokeSaleApprovalInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRevokeSaleApprovalInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      adminRecord: getNextAccount(),
      saleApproval: getNextAccount(),
      approvedBy: getNextAccount(),
    },
    data: getRevokeSaleApprovalInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
