/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  findCreateKycRegistryAdminRecordPda,
  findKycRegistryPda,
} from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CREATE_KYC_REGISTRY_DISCRIMINATOR = new Uint8Array([
  126, 191, 211, 24, 100, 225, 72, 161,
]);

export function getCreateKycRegistryDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_KYC_REGISTRY_DISCRIMINATOR,
  );
}

export type CreateKycRegistryInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAdminAuthority extends string | AccountMeta<string> = string,
  TAccountAdminRecord extends string | AccountMeta<string> = string,
  TAccountKycRegistry extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAdminAuthority extends string
        ? ReadonlySignerAccount<TAccountAdminAuthority> &
            AccountSignerMeta<TAccountAdminAuthority>
        : TAccountAdminAuthority,
      TAccountAdminRecord extends string
        ? ReadonlyAccount<TAccountAdminRecord>
        : TAccountAdminRecord,
      TAccountKycRegistry extends string
        ? WritableAccount<TAccountKycRegistry>
        : TAccountKycRegistry,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateKycRegistryInstructionData = {
  discriminator: ReadonlyUint8Array;
  approvedJurisdictions: ReadonlyUint8Array;
  blockedJurisdictions: ReadonlyUint8Array;
};

export type CreateKycRegistryInstructionDataArgs = {
  approvedJurisdictions: ReadonlyUint8Array;
  blockedJurisdictions: ReadonlyUint8Array;
};

export function getCreateKycRegistryInstructionDataEncoder(): FixedSizeEncoder<CreateKycRegistryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["approvedJurisdictions", fixEncoderSize(getBytesEncoder(), 128)],
      ["blockedJurisdictions", fixEncoderSize(getBytesEncoder(), 128)],
    ]),
    (value) => ({ ...value, discriminator: CREATE_KYC_REGISTRY_DISCRIMINATOR }),
  );
}

export function getCreateKycRegistryInstructionDataDecoder(): FixedSizeDecoder<CreateKycRegistryInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["approvedJurisdictions", fixDecoderSize(getBytesDecoder(), 128)],
    ["blockedJurisdictions", fixDecoderSize(getBytesDecoder(), 128)],
  ]);
}

export function getCreateKycRegistryInstructionDataCodec(): FixedSizeCodec<
  CreateKycRegistryInstructionDataArgs,
  CreateKycRegistryInstructionData
> {
  return combineCodec(
    getCreateKycRegistryInstructionDataEncoder(),
    getCreateKycRegistryInstructionDataDecoder(),
  );
}

export type CreateKycRegistryAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAdminAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountKycRegistry extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * The KYC provider the registry belongs to — pays for and owns it, and is
   * the only signer `approve_holder` / `revoke_holder` accept afterwards,
   * until rotated (`propose_kyc_registry_authority` /
   * `accept_kyc_registry_authority`). The registry address stays derived
   * from THIS creating key forever, so this key can never create a second
   * registry, even after rotating the first one away.
   */
  authority: TransactionSigner<TAccountAuthority>;
  /**
   * Platform admin co-signer. Registries are the root of trust for every
   * `KycGated` mint, so one cannot be conjured permissionlessly. The
   * co-signature gates only the CREATION of the address: afterwards the
   * current registry authority alone rotates the registry (to any key,
   * including an admin key) and replaces its jurisdiction bitmaps, with no
   * admin co-signature, admin override or pause flag. The registry
   * authority gets no admin powers from this record.
   */
  adminAuthority: TransactionSigner<TAccountAdminAuthority>;
  /** Admin gate — `admin_authority` must hold an `Admin` record. */
  adminRecord?: Address<TAccountAdminRecord>;
  kycRegistry?: Address<TAccountKycRegistry>;
  systemProgram?: Address<TAccountSystemProgram>;
  approvedJurisdictions: CreateKycRegistryInstructionDataArgs["approvedJurisdictions"];
  blockedJurisdictions: CreateKycRegistryInstructionDataArgs["blockedJurisdictions"];
};

export async function getCreateKycRegistryInstructionAsync<
  TAccountAuthority extends string,
  TAccountAdminAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountKycRegistry extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: CreateKycRegistryAsyncInput<
    TAccountAuthority,
    TAccountAdminAuthority,
    TAccountAdminRecord,
    TAccountKycRegistry,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateKycRegistryInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminAuthority,
    TAccountAdminRecord,
    TAccountKycRegistry,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    adminAuthority: { value: input.adminAuthority ?? null, isWritable: false },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    kycRegistry: { value: input.kycRegistry ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.adminRecord.value) {
    accounts.adminRecord.value = await findCreateKycRegistryAdminRecordPda({
      adminAuthority: expectAddress(accounts.adminAuthority.value),
    });
  }
  if (!accounts.kycRegistry.value) {
    accounts.kycRegistry.value = await findKycRegistryPda({
      authority: expectAddress(accounts.authority.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminAuthority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.kycRegistry),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateKycRegistryInstructionDataEncoder().encode(
      args as CreateKycRegistryInstructionDataArgs,
    ),
    programAddress,
  } as CreateKycRegistryInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminAuthority,
    TAccountAdminRecord,
    TAccountKycRegistry,
    TAccountSystemProgram
  >);
}

export type CreateKycRegistryInput<
  TAccountAuthority extends string = string,
  TAccountAdminAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountKycRegistry extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * The KYC provider the registry belongs to — pays for and owns it, and is
   * the only signer `approve_holder` / `revoke_holder` accept afterwards,
   * until rotated (`propose_kyc_registry_authority` /
   * `accept_kyc_registry_authority`). The registry address stays derived
   * from THIS creating key forever, so this key can never create a second
   * registry, even after rotating the first one away.
   */
  authority: TransactionSigner<TAccountAuthority>;
  /**
   * Platform admin co-signer. Registries are the root of trust for every
   * `KycGated` mint, so one cannot be conjured permissionlessly. The
   * co-signature gates only the CREATION of the address: afterwards the
   * current registry authority alone rotates the registry (to any key,
   * including an admin key) and replaces its jurisdiction bitmaps, with no
   * admin co-signature, admin override or pause flag. The registry
   * authority gets no admin powers from this record.
   */
  adminAuthority: TransactionSigner<TAccountAdminAuthority>;
  /** Admin gate — `admin_authority` must hold an `Admin` record. */
  adminRecord: Address<TAccountAdminRecord>;
  kycRegistry: Address<TAccountKycRegistry>;
  systemProgram?: Address<TAccountSystemProgram>;
  approvedJurisdictions: CreateKycRegistryInstructionDataArgs["approvedJurisdictions"];
  blockedJurisdictions: CreateKycRegistryInstructionDataArgs["blockedJurisdictions"];
};

export function getCreateKycRegistryInstruction<
  TAccountAuthority extends string,
  TAccountAdminAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountKycRegistry extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: CreateKycRegistryInput<
    TAccountAuthority,
    TAccountAdminAuthority,
    TAccountAdminRecord,
    TAccountKycRegistry,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateKycRegistryInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAdminAuthority,
  TAccountAdminRecord,
  TAccountKycRegistry,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    adminAuthority: { value: input.adminAuthority ?? null, isWritable: false },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    kycRegistry: { value: input.kycRegistry ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminAuthority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.kycRegistry),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateKycRegistryInstructionDataEncoder().encode(
      args as CreateKycRegistryInstructionDataArgs,
    ),
    programAddress,
  } as CreateKycRegistryInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminAuthority,
    TAccountAdminRecord,
    TAccountKycRegistry,
    TAccountSystemProgram
  >);
}

export type ParsedCreateKycRegistryInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * The KYC provider the registry belongs to — pays for and owns it, and is
     * the only signer `approve_holder` / `revoke_holder` accept afterwards,
     * until rotated (`propose_kyc_registry_authority` /
     * `accept_kyc_registry_authority`). The registry address stays derived
     * from THIS creating key forever, so this key can never create a second
     * registry, even after rotating the first one away.
     */
    authority: TAccountMetas[0];
    /**
     * Platform admin co-signer. Registries are the root of trust for every
     * `KycGated` mint, so one cannot be conjured permissionlessly. The
     * co-signature gates only the CREATION of the address: afterwards the
     * current registry authority alone rotates the registry (to any key,
     * including an admin key) and replaces its jurisdiction bitmaps, with no
     * admin co-signature, admin override or pause flag. The registry
     * authority gets no admin powers from this record.
     */
    adminAuthority: TAccountMetas[1];
    /** Admin gate — `admin_authority` must hold an `Admin` record. */
    adminRecord: TAccountMetas[2];
    kycRegistry: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: CreateKycRegistryInstructionData;
};

export function parseCreateKycRegistryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateKycRegistryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      adminAuthority: getNextAccount(),
      adminRecord: getNextAccount(),
      kycRegistry: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateKycRegistryInstructionDataDecoder().decode(instruction.data),
  };
}
