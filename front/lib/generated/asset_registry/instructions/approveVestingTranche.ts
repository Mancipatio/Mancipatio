/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const APPROVE_VESTING_TRANCHE_DISCRIMINATOR = new Uint8Array([
  125, 33, 100, 87, 255, 61, 74, 178,
]);

export function getApproveVestingTrancheDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPROVE_VESTING_TRANCHE_DISCRIMINATOR,
  );
}

export type ApproveVestingTrancheInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSeries extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSeries extends string
        ? WritableAccount<TAccountSeries>
        : TAccountSeries,
      ...TRemainingAccounts,
    ]
  >;

export type ApproveVestingTrancheInstructionData = {
  discriminator: ReadonlyUint8Array;
  trancheIndex: number;
};

export type ApproveVestingTrancheInstructionDataArgs = { trancheIndex: number };

export function getApproveVestingTrancheInstructionDataEncoder(): FixedSizeEncoder<ApproveVestingTrancheInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["trancheIndex", getU16Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: APPROVE_VESTING_TRANCHE_DISCRIMINATOR,
    }),
  );
}

export function getApproveVestingTrancheInstructionDataDecoder(): FixedSizeDecoder<ApproveVestingTrancheInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["trancheIndex", getU16Decoder()],
  ]);
}

export function getApproveVestingTrancheInstructionDataCodec(): FixedSizeCodec<
  ApproveVestingTrancheInstructionDataArgs,
  ApproveVestingTrancheInstructionData
> {
  return combineCodec(
    getApproveVestingTrancheInstructionDataEncoder(),
    getApproveVestingTrancheInstructionDataDecoder(),
  );
}

export type ApproveVestingTrancheInput<
  TAccountAuthority extends string = string,
  TAccountSeries extends string = string,
> = {
  /** The client — series authority. */
  authority: TransactionSigner<TAccountAuthority>;
  series: Address<TAccountSeries>;
  trancheIndex: ApproveVestingTrancheInstructionDataArgs["trancheIndex"];
};

export function getApproveVestingTrancheInstruction<
  TAccountAuthority extends string,
  TAccountSeries extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ApproveVestingTrancheInput<TAccountAuthority, TAccountSeries>,
  config?: { programAddress?: TProgramAddress },
): ApproveVestingTrancheInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountSeries
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    series: { value: input.series ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.series),
    ],
    data: getApproveVestingTrancheInstructionDataEncoder().encode(
      args as ApproveVestingTrancheInstructionDataArgs,
    ),
    programAddress,
  } as ApproveVestingTrancheInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountSeries
  >);
}

export type ParsedApproveVestingTrancheInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The client — series authority. */
    authority: TAccountMetas[0];
    series: TAccountMetas[1];
  };
  data: ApproveVestingTrancheInstructionData;
};

export function parseApproveVestingTrancheInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedApproveVestingTrancheInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), series: getNextAccount() },
    data: getApproveVestingTrancheInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
