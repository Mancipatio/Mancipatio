/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findIssuerPda, findPlatformPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const REGISTER_ISSUER_DISCRIMINATOR = new Uint8Array([
  145, 117, 52, 59, 189, 27, 127, 18,
]);

export function getRegisterIssuerDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTER_ISSUER_DISCRIMINATOR,
  );
}

export type RegisterIssuerInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountPlatform extends string | AccountMeta<string> = string,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountPlatform extends string
        ? WritableAccount<TAccountPlatform>
        : TAccountPlatform,
      TAccountIssuer extends string
        ? WritableAccount<TAccountIssuer>
        : TAccountIssuer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterIssuerInstructionData = {
  discriminator: ReadonlyUint8Array;
  legalEntityId: ReadonlyUint8Array;
  jurisdiction: number;
  kybDocHash: ReadonlyUint8Array;
};

export type RegisterIssuerInstructionDataArgs = {
  legalEntityId: ReadonlyUint8Array;
  jurisdiction: number;
  kybDocHash: ReadonlyUint8Array;
};

export function getRegisterIssuerInstructionDataEncoder(): FixedSizeEncoder<RegisterIssuerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["legalEntityId", fixEncoderSize(getBytesEncoder(), 32)],
      ["jurisdiction", getU16Encoder()],
      ["kybDocHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: REGISTER_ISSUER_DISCRIMINATOR }),
  );
}

export function getRegisterIssuerInstructionDataDecoder(): FixedSizeDecoder<RegisterIssuerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["legalEntityId", fixDecoderSize(getBytesDecoder(), 32)],
    ["jurisdiction", getU16Decoder()],
    ["kybDocHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getRegisterIssuerInstructionDataCodec(): FixedSizeCodec<
  RegisterIssuerInstructionDataArgs,
  RegisterIssuerInstructionData
> {
  return combineCodec(
    getRegisterIssuerInstructionDataEncoder(),
    getRegisterIssuerInstructionDataDecoder(),
  );
}

export type RegisterIssuerAsyncInput<
  TAccountAuthority extends string = string,
  TAccountPlatform extends string = string,
  TAccountIssuer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  platform?: Address<TAccountPlatform>;
  issuer?: Address<TAccountIssuer>;
  systemProgram?: Address<TAccountSystemProgram>;
  legalEntityId: RegisterIssuerInstructionDataArgs["legalEntityId"];
  jurisdiction: RegisterIssuerInstructionDataArgs["jurisdiction"];
  kybDocHash: RegisterIssuerInstructionDataArgs["kybDocHash"];
};

export async function getRegisterIssuerInstructionAsync<
  TAccountAuthority extends string,
  TAccountPlatform extends string,
  TAccountIssuer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: RegisterIssuerAsyncInput<
    TAccountAuthority,
    TAccountPlatform,
    TAccountIssuer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RegisterIssuerInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPlatform,
    TAccountIssuer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    platform: { value: input.platform ?? null, isWritable: true },
    issuer: { value: input.issuer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.platform.value) {
    accounts.platform.value = await findPlatformPda();
  }
  if (!accounts.issuer.value) {
    accounts.issuer.value = await findIssuerPda({
      legalEntityId: expectSome(args.legalEntityId),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.platform),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRegisterIssuerInstructionDataEncoder().encode(
      args as RegisterIssuerInstructionDataArgs,
    ),
    programAddress,
  } as RegisterIssuerInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPlatform,
    TAccountIssuer,
    TAccountSystemProgram
  >);
}

export type RegisterIssuerInput<
  TAccountAuthority extends string = string,
  TAccountPlatform extends string = string,
  TAccountIssuer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  platform: Address<TAccountPlatform>;
  issuer: Address<TAccountIssuer>;
  systemProgram?: Address<TAccountSystemProgram>;
  legalEntityId: RegisterIssuerInstructionDataArgs["legalEntityId"];
  jurisdiction: RegisterIssuerInstructionDataArgs["jurisdiction"];
  kybDocHash: RegisterIssuerInstructionDataArgs["kybDocHash"];
};

export function getRegisterIssuerInstruction<
  TAccountAuthority extends string,
  TAccountPlatform extends string,
  TAccountIssuer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: RegisterIssuerInput<
    TAccountAuthority,
    TAccountPlatform,
    TAccountIssuer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RegisterIssuerInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountPlatform,
  TAccountIssuer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    platform: { value: input.platform ?? null, isWritable: true },
    issuer: { value: input.issuer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.platform),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRegisterIssuerInstructionDataEncoder().encode(
      args as RegisterIssuerInstructionDataArgs,
    ),
    programAddress,
  } as RegisterIssuerInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPlatform,
    TAccountIssuer,
    TAccountSystemProgram
  >);
}

export type ParsedRegisterIssuerInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    platform: TAccountMetas[1];
    issuer: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: RegisterIssuerInstructionData;
};

export function parseRegisterIssuerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegisterIssuerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      platform: getNextAccount(),
      issuer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterIssuerInstructionDataDecoder().decode(instruction.data),
  };
}
