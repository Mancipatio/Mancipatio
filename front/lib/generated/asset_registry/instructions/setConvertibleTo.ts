/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SET_CONVERTIBLE_TO_DISCRIMINATOR = new Uint8Array([
  40, 25, 145, 110, 215, 255, 50, 147,
]);

export function getSetConvertibleToDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_CONVERTIBLE_TO_DISCRIMINATOR,
  );
}

export type SetConvertibleToInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAdminRecord extends string | AccountMeta<string> = string,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TAccountAsset extends string | AccountMeta<string> = string,
  TAccountShareClass extends string | AccountMeta<string> = string,
  TAccountTargetShareClass extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAdminRecord extends string
        ? ReadonlyAccount<TAccountAdminRecord>
        : TAccountAdminRecord,
      TAccountIssuer extends string
        ? ReadonlyAccount<TAccountIssuer>
        : TAccountIssuer,
      TAccountAsset extends string
        ? ReadonlyAccount<TAccountAsset>
        : TAccountAsset,
      TAccountShareClass extends string
        ? WritableAccount<TAccountShareClass>
        : TAccountShareClass,
      TAccountTargetShareClass extends string
        ? ReadonlyAccount<TAccountTargetShareClass>
        : TAccountTargetShareClass,
      ...TRemainingAccounts,
    ]
  >;

export type SetConvertibleToInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type SetConvertibleToInstructionDataArgs = {};

export function getSetConvertibleToInstructionDataEncoder(): FixedSizeEncoder<SetConvertibleToInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SET_CONVERTIBLE_TO_DISCRIMINATOR }),
  );
}

export function getSetConvertibleToInstructionDataDecoder(): FixedSizeDecoder<SetConvertibleToInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSetConvertibleToInstructionDataCodec(): FixedSizeCodec<
  SetConvertibleToInstructionDataArgs,
  SetConvertibleToInstructionData
> {
  return combineCodec(
    getSetConvertibleToInstructionDataEncoder(),
    getSetConvertibleToInstructionDataDecoder(),
  );
}

export type SetConvertibleToInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountIssuer extends string = string,
  TAccountAsset extends string = string,
  TAccountShareClass extends string = string,
  TAccountTargetShareClass extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /** Global Admin or issuer-local CONVERSION capability; signer is also the issuer. */
  adminRecord: Address<TAccountAdminRecord>;
  issuer: Address<TAccountIssuer>;
  asset: Address<TAccountAsset>;
  /** The share class whose conversion target is being set. */
  shareClass: Address<TAccountShareClass>;
  /**
   * The share class this one converts into. Pass it to set the target;
   * omit it to clear (`convertible_to = None`). Must be an existing share
   * class of the SAME asset and must not be `share_class` itself.
   */
  targetShareClass?: Address<TAccountTargetShareClass>;
};

export function getSetConvertibleToInstruction<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountIssuer extends string,
  TAccountAsset extends string,
  TAccountShareClass extends string,
  TAccountTargetShareClass extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: SetConvertibleToInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountIssuer,
    TAccountAsset,
    TAccountShareClass,
    TAccountTargetShareClass
  >,
  config?: { programAddress?: TProgramAddress },
): SetConvertibleToInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAdminRecord,
  TAccountIssuer,
  TAccountAsset,
  TAccountShareClass,
  TAccountTargetShareClass
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    issuer: { value: input.issuer ?? null, isWritable: false },
    asset: { value: input.asset ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: true },
    targetShareClass: {
      value: input.targetShareClass ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.asset),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.targetShareClass),
    ],
    data: getSetConvertibleToInstructionDataEncoder().encode({}),
    programAddress,
  } as SetConvertibleToInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountIssuer,
    TAccountAsset,
    TAccountShareClass,
    TAccountTargetShareClass
  >);
}

export type ParsedSetConvertibleToInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    /** Global Admin or issuer-local CONVERSION capability; signer is also the issuer. */
    adminRecord: TAccountMetas[1];
    issuer: TAccountMetas[2];
    asset: TAccountMetas[3];
    /** The share class whose conversion target is being set. */
    shareClass: TAccountMetas[4];
    /**
     * The share class this one converts into. Pass it to set the target;
     * omit it to clear (`convertible_to = None`). Must be an existing share
     * class of the SAME asset and must not be `share_class` itself.
     */
    targetShareClass?: TAccountMetas[5] | undefined;
  };
  data: SetConvertibleToInstructionData;
};

export function parseSetConvertibleToInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetConvertibleToInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === ASSET_REGISTRY_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      adminRecord: getNextAccount(),
      issuer: getNextAccount(),
      asset: getNextAccount(),
      shareClass: getNextAccount(),
      targetShareClass: getNextOptionalAccount(),
    },
    data: getSetConvertibleToInstructionDataDecoder().decode(instruction.data),
  };
}
