/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const FREEZE_VAULT_DISCRIMINATOR = new Uint8Array([
  144, 211, 63, 236, 97, 31, 170, 175,
]);

export function getFreezeVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FREEZE_VAULT_DISCRIMINATOR,
  );
}

export type FreezeVaultInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      ...TRemainingAccounts,
    ]
  >;

export type FreezeVaultInstructionData = { discriminator: ReadonlyUint8Array };

export type FreezeVaultInstructionDataArgs = {};

export function getFreezeVaultInstructionDataEncoder(): FixedSizeEncoder<FreezeVaultInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: FREEZE_VAULT_DISCRIMINATOR }),
  );
}

export function getFreezeVaultInstructionDataDecoder(): FixedSizeDecoder<FreezeVaultInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getFreezeVaultInstructionDataCodec(): FixedSizeCodec<
  FreezeVaultInstructionDataArgs,
  FreezeVaultInstructionData
> {
  return combineCodec(
    getFreezeVaultInstructionDataEncoder(),
    getFreezeVaultInstructionDataDecoder(),
  );
}

export type FreezeVaultInput<TAccountVault extends string = string> = {
  vault: Address<TAccountVault>;
};

export function getFreezeVaultInstruction<
  TAccountVault extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: FreezeVaultInput<TAccountVault>,
  config?: { programAddress?: TProgramAddress },
): FreezeVaultInstruction<TProgramAddress, TAccountVault> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.vault)],
    data: getFreezeVaultInstructionDataEncoder().encode({}),
    programAddress,
  } as FreezeVaultInstruction<TProgramAddress, TAccountVault>);
}

export type ParsedFreezeVaultInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vault: TAccountMetas[0];
  };
  data: FreezeVaultInstructionData;
};

export function parseFreezeVaultInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFreezeVaultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { vault: getNextAccount() },
    data: getFreezeVaultInstructionDataDecoder().decode(instruction.data),
  };
}
