/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findPlatformPda, findSuperAdminRecordPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const REMOVE_ADMIN_DISCRIMINATOR = new Uint8Array([
  74, 202, 71, 106, 252, 31, 72, 183,
]);

export function getRemoveAdminDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REMOVE_ADMIN_DISCRIMINATOR,
  );
}

export type RemoveAdminInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountSuperAdmin extends string | AccountMeta<string> = string,
  TAccountPlatform extends string | AccountMeta<string> = string,
  TAccountAdminRecord extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSuperAdmin extends string
        ? WritableSignerAccount<TAccountSuperAdmin> &
            AccountSignerMeta<TAccountSuperAdmin>
        : TAccountSuperAdmin,
      TAccountPlatform extends string
        ? ReadonlyAccount<TAccountPlatform>
        : TAccountPlatform,
      TAccountAdminRecord extends string
        ? WritableAccount<TAccountAdminRecord>
        : TAccountAdminRecord,
      ...TRemainingAccounts,
    ]
  >;

export type RemoveAdminInstructionData = {
  discriminator: ReadonlyUint8Array;
  admin: Address;
};

export type RemoveAdminInstructionDataArgs = { admin: Address };

export function getRemoveAdminInstructionDataEncoder(): FixedSizeEncoder<RemoveAdminInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["admin", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: REMOVE_ADMIN_DISCRIMINATOR }),
  );
}

export function getRemoveAdminInstructionDataDecoder(): FixedSizeDecoder<RemoveAdminInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["admin", getAddressDecoder()],
  ]);
}

export function getRemoveAdminInstructionDataCodec(): FixedSizeCodec<
  RemoveAdminInstructionDataArgs,
  RemoveAdminInstructionData
> {
  return combineCodec(
    getRemoveAdminInstructionDataEncoder(),
    getRemoveAdminInstructionDataDecoder(),
  );
}

export type RemoveAdminAsyncInput<
  TAccountSuperAdmin extends string = string,
  TAccountPlatform extends string = string,
  TAccountAdminRecord extends string = string,
> = {
  superAdmin: TransactionSigner<TAccountSuperAdmin>;
  platform?: Address<TAccountPlatform>;
  adminRecord?: Address<TAccountAdminRecord>;
  admin: RemoveAdminInstructionDataArgs["admin"];
};

export async function getRemoveAdminInstructionAsync<
  TAccountSuperAdmin extends string,
  TAccountPlatform extends string,
  TAccountAdminRecord extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: RemoveAdminAsyncInput<
    TAccountSuperAdmin,
    TAccountPlatform,
    TAccountAdminRecord
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RemoveAdminInstruction<
    TProgramAddress,
    TAccountSuperAdmin,
    TAccountPlatform,
    TAccountAdminRecord
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    superAdmin: { value: input.superAdmin ?? null, isWritable: true },
    platform: { value: input.platform ?? null, isWritable: false },
    adminRecord: { value: input.adminRecord ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.platform.value) {
    accounts.platform.value = await findPlatformPda();
  }
  if (!accounts.adminRecord.value) {
    accounts.adminRecord.value = await findSuperAdminRecordPda({
      admin: expectSome(args.admin),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.superAdmin),
      getAccountMeta(accounts.platform),
      getAccountMeta(accounts.adminRecord),
    ],
    data: getRemoveAdminInstructionDataEncoder().encode(
      args as RemoveAdminInstructionDataArgs,
    ),
    programAddress,
  } as RemoveAdminInstruction<
    TProgramAddress,
    TAccountSuperAdmin,
    TAccountPlatform,
    TAccountAdminRecord
  >);
}

export type RemoveAdminInput<
  TAccountSuperAdmin extends string = string,
  TAccountPlatform extends string = string,
  TAccountAdminRecord extends string = string,
> = {
  superAdmin: TransactionSigner<TAccountSuperAdmin>;
  platform: Address<TAccountPlatform>;
  adminRecord: Address<TAccountAdminRecord>;
  admin: RemoveAdminInstructionDataArgs["admin"];
};

export function getRemoveAdminInstruction<
  TAccountSuperAdmin extends string,
  TAccountPlatform extends string,
  TAccountAdminRecord extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: RemoveAdminInput<
    TAccountSuperAdmin,
    TAccountPlatform,
    TAccountAdminRecord
  >,
  config?: { programAddress?: TProgramAddress },
): RemoveAdminInstruction<
  TProgramAddress,
  TAccountSuperAdmin,
  TAccountPlatform,
  TAccountAdminRecord
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    superAdmin: { value: input.superAdmin ?? null, isWritable: true },
    platform: { value: input.platform ?? null, isWritable: false },
    adminRecord: { value: input.adminRecord ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.superAdmin),
      getAccountMeta(accounts.platform),
      getAccountMeta(accounts.adminRecord),
    ],
    data: getRemoveAdminInstructionDataEncoder().encode(
      args as RemoveAdminInstructionDataArgs,
    ),
    programAddress,
  } as RemoveAdminInstruction<
    TProgramAddress,
    TAccountSuperAdmin,
    TAccountPlatform,
    TAccountAdminRecord
  >);
}

export type ParsedRemoveAdminInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    superAdmin: TAccountMetas[0];
    platform: TAccountMetas[1];
    adminRecord: TAccountMetas[2];
  };
  data: RemoveAdminInstructionData;
};

export function parseRemoveAdminInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRemoveAdminInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      superAdmin: getNextAccount(),
      platform: getNextAccount(),
      adminRecord: getNextAccount(),
    },
    data: getRemoveAdminInstructionDataDecoder().decode(instruction.data),
  };
}
