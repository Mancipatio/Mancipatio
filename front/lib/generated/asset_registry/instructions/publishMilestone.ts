/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findAdminRecordPda, findMilestonePda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const PUBLISH_MILESTONE_DISCRIMINATOR = new Uint8Array([
  224, 187, 210, 241, 161, 63, 149, 203,
]);

export function getPublishMilestoneDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PUBLISH_MILESTONE_DISCRIMINATOR,
  );
}

export type PublishMilestoneInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAdminRecord extends string | AccountMeta<string> = string,
  TAccountRightsIssuance extends string | AccountMeta<string> = string,
  TAccountMilestone extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAdminRecord extends string
        ? ReadonlyAccount<TAccountAdminRecord>
        : TAccountAdminRecord,
      TAccountRightsIssuance extends string
        ? WritableAccount<TAccountRightsIssuance>
        : TAccountRightsIssuance,
      TAccountMilestone extends string
        ? WritableAccount<TAccountMilestone>
        : TAccountMilestone,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PublishMilestoneInstructionData = {
  discriminator: ReadonlyUint8Array;
  index: number;
  merkleRoot: ReadonlyUint8Array;
  amountPool: bigint;
  unlockTs: bigint;
};

export type PublishMilestoneInstructionDataArgs = {
  index: number;
  merkleRoot: ReadonlyUint8Array;
  amountPool: number | bigint;
  unlockTs: number | bigint;
};

export function getPublishMilestoneInstructionDataEncoder(): FixedSizeEncoder<PublishMilestoneInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["index", getU16Encoder()],
      ["merkleRoot", fixEncoderSize(getBytesEncoder(), 32)],
      ["amountPool", getU64Encoder()],
      ["unlockTs", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PUBLISH_MILESTONE_DISCRIMINATOR }),
  );
}

export function getPublishMilestoneInstructionDataDecoder(): FixedSizeDecoder<PublishMilestoneInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["index", getU16Decoder()],
    ["merkleRoot", fixDecoderSize(getBytesDecoder(), 32)],
    ["amountPool", getU64Decoder()],
    ["unlockTs", getI64Decoder()],
  ]);
}

export function getPublishMilestoneInstructionDataCodec(): FixedSizeCodec<
  PublishMilestoneInstructionDataArgs,
  PublishMilestoneInstructionData
> {
  return combineCodec(
    getPublishMilestoneInstructionDataEncoder(),
    getPublishMilestoneInstructionDataDecoder(),
  );
}

export type PublishMilestoneAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountRightsIssuance extends string = string,
  TAccountMilestone extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /** Admin gate. */
  adminRecord?: Address<TAccountAdminRecord>;
  rightsIssuance: Address<TAccountRightsIssuance>;
  milestone?: Address<TAccountMilestone>;
  systemProgram?: Address<TAccountSystemProgram>;
  index: PublishMilestoneInstructionDataArgs["index"];
  merkleRoot: PublishMilestoneInstructionDataArgs["merkleRoot"];
  amountPool: PublishMilestoneInstructionDataArgs["amountPool"];
  unlockTs: PublishMilestoneInstructionDataArgs["unlockTs"];
};

export async function getPublishMilestoneInstructionAsync<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountRightsIssuance extends string,
  TAccountMilestone extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: PublishMilestoneAsyncInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountRightsIssuance,
    TAccountMilestone,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PublishMilestoneInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountRightsIssuance,
    TAccountMilestone,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    rightsIssuance: { value: input.rightsIssuance ?? null, isWritable: true },
    milestone: { value: input.milestone ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.adminRecord.value) {
    accounts.adminRecord.value = await findAdminRecordPda({
      authority: expectAddress(accounts.authority.value),
    });
  }
  if (!accounts.milestone.value) {
    accounts.milestone.value = await findMilestonePda({
      rightsIssuance: expectAddress(accounts.rightsIssuance.value),
      index: expectSome(args.index),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.rightsIssuance),
      getAccountMeta(accounts.milestone),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getPublishMilestoneInstructionDataEncoder().encode(
      args as PublishMilestoneInstructionDataArgs,
    ),
    programAddress,
  } as PublishMilestoneInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountRightsIssuance,
    TAccountMilestone,
    TAccountSystemProgram
  >);
}

export type PublishMilestoneInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountRightsIssuance extends string = string,
  TAccountMilestone extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /** Admin gate. */
  adminRecord: Address<TAccountAdminRecord>;
  rightsIssuance: Address<TAccountRightsIssuance>;
  milestone: Address<TAccountMilestone>;
  systemProgram?: Address<TAccountSystemProgram>;
  index: PublishMilestoneInstructionDataArgs["index"];
  merkleRoot: PublishMilestoneInstructionDataArgs["merkleRoot"];
  amountPool: PublishMilestoneInstructionDataArgs["amountPool"];
  unlockTs: PublishMilestoneInstructionDataArgs["unlockTs"];
};

export function getPublishMilestoneInstruction<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountRightsIssuance extends string,
  TAccountMilestone extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: PublishMilestoneInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountRightsIssuance,
    TAccountMilestone,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PublishMilestoneInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAdminRecord,
  TAccountRightsIssuance,
  TAccountMilestone,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    rightsIssuance: { value: input.rightsIssuance ?? null, isWritable: true },
    milestone: { value: input.milestone ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.rightsIssuance),
      getAccountMeta(accounts.milestone),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getPublishMilestoneInstructionDataEncoder().encode(
      args as PublishMilestoneInstructionDataArgs,
    ),
    programAddress,
  } as PublishMilestoneInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountRightsIssuance,
    TAccountMilestone,
    TAccountSystemProgram
  >);
}

export type ParsedPublishMilestoneInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    /** Admin gate. */
    adminRecord: TAccountMetas[1];
    rightsIssuance: TAccountMetas[2];
    milestone: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: PublishMilestoneInstructionData;
};

export function parsePublishMilestoneInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPublishMilestoneInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      adminRecord: getNextAccount(),
      rightsIssuance: getNextAccount(),
      milestone: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getPublishMilestoneInstructionDataDecoder().decode(instruction.data),
  };
}
