/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findAdminRecordPda, findBatchPda, findPlanPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const DISTRIBUTE_BATCH_DISCRIMINATOR = new Uint8Array([
  75, 80, 138, 222, 195, 82, 91, 34,
]);

export function getDistributeBatchDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DISTRIBUTE_BATCH_DISCRIMINATOR,
  );
}

export type DistributeBatchInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAdminRecord extends string | AccountMeta<string> = string,
  TAccountDistribution extends string | AccountMeta<string> = string,
  TAccountPaymentMint extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountPaymentTokenProgram extends string | AccountMeta<string> = string,
  TAccountPlan extends string | AccountMeta<string> = string,
  TAccountBatch extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAdminRecord extends string
        ? ReadonlyAccount<TAccountAdminRecord>
        : TAccountAdminRecord,
      TAccountDistribution extends string
        ? WritableAccount<TAccountDistribution>
        : TAccountDistribution,
      TAccountPaymentMint extends string
        ? ReadonlyAccount<TAccountPaymentMint>
        : TAccountPaymentMint,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountPaymentTokenProgram extends string
        ? ReadonlyAccount<TAccountPaymentTokenProgram>
        : TAccountPaymentTokenProgram,
      TAccountPlan extends string
        ? ReadonlyAccount<TAccountPlan>
        : TAccountPlan,
      TAccountBatch extends string
        ? WritableAccount<TAccountBatch>
        : TAccountBatch,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DistributeBatchInstructionData = {
  discriminator: ReadonlyUint8Array;
  distributionId: bigint;
  batchId: number;
  amounts: Array<bigint>;
  proof: Array<ReadonlyUint8Array>;
};

export type DistributeBatchInstructionDataArgs = {
  distributionId: number | bigint;
  batchId: number;
  amounts: Array<number | bigint>;
  proof: Array<ReadonlyUint8Array>;
};

export function getDistributeBatchInstructionDataEncoder(): Encoder<DistributeBatchInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["distributionId", getU64Encoder()],
      ["batchId", getU32Encoder()],
      ["amounts", getArrayEncoder(getU64Encoder())],
      ["proof", getArrayEncoder(fixEncoderSize(getBytesEncoder(), 32))],
    ]),
    (value) => ({ ...value, discriminator: DISTRIBUTE_BATCH_DISCRIMINATOR }),
  );
}

export function getDistributeBatchInstructionDataDecoder(): Decoder<DistributeBatchInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["distributionId", getU64Decoder()],
    ["batchId", getU32Decoder()],
    ["amounts", getArrayDecoder(getU64Decoder())],
    ["proof", getArrayDecoder(fixDecoderSize(getBytesDecoder(), 32))],
  ]);
}

export function getDistributeBatchInstructionDataCodec(): Codec<
  DistributeBatchInstructionDataArgs,
  DistributeBatchInstructionData
> {
  return combineCodec(
    getDistributeBatchInstructionDataEncoder(),
    getDistributeBatchInstructionDataDecoder(),
  );
}

export type DistributeBatchAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountDistribution extends string = string,
  TAccountPaymentMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountPaymentTokenProgram extends string = string,
  TAccountPlan extends string = string,
  TAccountBatch extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  adminRecord?: Address<TAccountAdminRecord>;
  distribution: Address<TAccountDistribution>;
  paymentMint: Address<TAccountPaymentMint>;
  /** receipt replay may arrive after close_distribution closed this account. */
  escrow: Address<TAccountEscrow>;
  paymentTokenProgram: Address<TAccountPaymentTokenProgram>;
  plan?: Address<TAccountPlan>;
  batch?: Address<TAccountBatch>;
  systemProgram?: Address<TAccountSystemProgram>;
  distributionId: DistributeBatchInstructionDataArgs["distributionId"];
  batchId: DistributeBatchInstructionDataArgs["batchId"];
  amounts: DistributeBatchInstructionDataArgs["amounts"];
  proof: DistributeBatchInstructionDataArgs["proof"];
};

export async function getDistributeBatchInstructionAsync<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountDistribution extends string,
  TAccountPaymentMint extends string,
  TAccountEscrow extends string,
  TAccountPaymentTokenProgram extends string,
  TAccountPlan extends string,
  TAccountBatch extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: DistributeBatchAsyncInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountDistribution,
    TAccountPaymentMint,
    TAccountEscrow,
    TAccountPaymentTokenProgram,
    TAccountPlan,
    TAccountBatch,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DistributeBatchInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountDistribution,
    TAccountPaymentMint,
    TAccountEscrow,
    TAccountPaymentTokenProgram,
    TAccountPlan,
    TAccountBatch,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    distribution: { value: input.distribution ?? null, isWritable: true },
    paymentMint: { value: input.paymentMint ?? null, isWritable: false },
    escrow: { value: input.escrow ?? null, isWritable: true },
    paymentTokenProgram: {
      value: input.paymentTokenProgram ?? null,
      isWritable: false,
    },
    plan: { value: input.plan ?? null, isWritable: false },
    batch: { value: input.batch ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.adminRecord.value) {
    accounts.adminRecord.value = await findAdminRecordPda({
      authority: expectAddress(accounts.authority.value),
    });
  }
  if (!accounts.plan.value) {
    accounts.plan.value = await findPlanPda({
      distribution: expectAddress(accounts.distribution.value),
    });
  }
  if (!accounts.batch.value) {
    accounts.batch.value = await findBatchPda({
      distribution: expectAddress(accounts.distribution.value),
      batchId: expectSome(args.batchId),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.distribution),
      getAccountMeta(accounts.paymentMint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.paymentTokenProgram),
      getAccountMeta(accounts.plan),
      getAccountMeta(accounts.batch),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getDistributeBatchInstructionDataEncoder().encode(
      args as DistributeBatchInstructionDataArgs,
    ),
    programAddress,
  } as DistributeBatchInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountDistribution,
    TAccountPaymentMint,
    TAccountEscrow,
    TAccountPaymentTokenProgram,
    TAccountPlan,
    TAccountBatch,
    TAccountSystemProgram
  >);
}

export type DistributeBatchInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountDistribution extends string = string,
  TAccountPaymentMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountPaymentTokenProgram extends string = string,
  TAccountPlan extends string = string,
  TAccountBatch extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  adminRecord: Address<TAccountAdminRecord>;
  distribution: Address<TAccountDistribution>;
  paymentMint: Address<TAccountPaymentMint>;
  /** receipt replay may arrive after close_distribution closed this account. */
  escrow: Address<TAccountEscrow>;
  paymentTokenProgram: Address<TAccountPaymentTokenProgram>;
  plan: Address<TAccountPlan>;
  batch: Address<TAccountBatch>;
  systemProgram?: Address<TAccountSystemProgram>;
  distributionId: DistributeBatchInstructionDataArgs["distributionId"];
  batchId: DistributeBatchInstructionDataArgs["batchId"];
  amounts: DistributeBatchInstructionDataArgs["amounts"];
  proof: DistributeBatchInstructionDataArgs["proof"];
};

export function getDistributeBatchInstruction<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountDistribution extends string,
  TAccountPaymentMint extends string,
  TAccountEscrow extends string,
  TAccountPaymentTokenProgram extends string,
  TAccountPlan extends string,
  TAccountBatch extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: DistributeBatchInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountDistribution,
    TAccountPaymentMint,
    TAccountEscrow,
    TAccountPaymentTokenProgram,
    TAccountPlan,
    TAccountBatch,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): DistributeBatchInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAdminRecord,
  TAccountDistribution,
  TAccountPaymentMint,
  TAccountEscrow,
  TAccountPaymentTokenProgram,
  TAccountPlan,
  TAccountBatch,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    distribution: { value: input.distribution ?? null, isWritable: true },
    paymentMint: { value: input.paymentMint ?? null, isWritable: false },
    escrow: { value: input.escrow ?? null, isWritable: true },
    paymentTokenProgram: {
      value: input.paymentTokenProgram ?? null,
      isWritable: false,
    },
    plan: { value: input.plan ?? null, isWritable: false },
    batch: { value: input.batch ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.distribution),
      getAccountMeta(accounts.paymentMint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.paymentTokenProgram),
      getAccountMeta(accounts.plan),
      getAccountMeta(accounts.batch),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getDistributeBatchInstructionDataEncoder().encode(
      args as DistributeBatchInstructionDataArgs,
    ),
    programAddress,
  } as DistributeBatchInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountDistribution,
    TAccountPaymentMint,
    TAccountEscrow,
    TAccountPaymentTokenProgram,
    TAccountPlan,
    TAccountBatch,
    TAccountSystemProgram
  >);
}

export type ParsedDistributeBatchInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    adminRecord: TAccountMetas[1];
    distribution: TAccountMetas[2];
    paymentMint: TAccountMetas[3];
    /** receipt replay may arrive after close_distribution closed this account. */
    escrow: TAccountMetas[4];
    paymentTokenProgram: TAccountMetas[5];
    plan: TAccountMetas[6];
    batch: TAccountMetas[7];
    systemProgram: TAccountMetas[8];
  };
  data: DistributeBatchInstructionData;
};

export function parseDistributeBatchInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDistributeBatchInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      adminRecord: getNextAccount(),
      distribution: getNextAccount(),
      paymentMint: getNextAccount(),
      escrow: getNextAccount(),
      paymentTokenProgram: getNextAccount(),
      plan: getNextAccount(),
      batch: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getDistributeBatchInstructionDataDecoder().decode(instruction.data),
  };
}
