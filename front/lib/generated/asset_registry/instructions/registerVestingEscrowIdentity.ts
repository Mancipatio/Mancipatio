/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findCreateVestingSeriesIdentityPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const REGISTER_VESTING_ESCROW_IDENTITY_DISCRIMINATOR = new Uint8Array([
  30, 96, 200, 199, 207, 176, 153, 183,
]);

export function getRegisterVestingEscrowIdentityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTER_VESTING_ESCROW_IDENTITY_DISCRIMINATOR,
  );
}

export type RegisterVestingEscrowIdentityInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSeries extends string | AccountMeta<string> = string,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSeries extends string
        ? ReadonlyAccount<TAccountSeries>
        : TAccountSeries,
      TAccountIdentity extends string
        ? WritableAccount<TAccountIdentity>
        : TAccountIdentity,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterVestingEscrowIdentityInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RegisterVestingEscrowIdentityInstructionDataArgs = {};

export function getRegisterVestingEscrowIdentityInstructionDataEncoder(): FixedSizeEncoder<RegisterVestingEscrowIdentityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: REGISTER_VESTING_ESCROW_IDENTITY_DISCRIMINATOR,
    }),
  );
}

export function getRegisterVestingEscrowIdentityInstructionDataDecoder(): FixedSizeDecoder<RegisterVestingEscrowIdentityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRegisterVestingEscrowIdentityInstructionDataCodec(): FixedSizeCodec<
  RegisterVestingEscrowIdentityInstructionDataArgs,
  RegisterVestingEscrowIdentityInstructionData
> {
  return combineCodec(
    getRegisterVestingEscrowIdentityInstructionDataEncoder(),
    getRegisterVestingEscrowIdentityInstructionDataDecoder(),
  );
}

export type RegisterVestingEscrowIdentityAsyncInput<
  TAccountPayer extends string = string,
  TAccountSeries extends string = string,
  TAccountIdentity extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  series: Address<TAccountSeries>;
  identity?: Address<TAccountIdentity>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getRegisterVestingEscrowIdentityInstructionAsync<
  TAccountPayer extends string,
  TAccountSeries extends string,
  TAccountIdentity extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: RegisterVestingEscrowIdentityAsyncInput<
    TAccountPayer,
    TAccountSeries,
    TAccountIdentity,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RegisterVestingEscrowIdentityInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountSeries,
    TAccountIdentity,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    series: { value: input.series ?? null, isWritable: false },
    identity: { value: input.identity ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.identity.value) {
    accounts.identity.value = await findCreateVestingSeriesIdentityPda({
      series: expectAddress(accounts.series.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.series),
      getAccountMeta(accounts.identity),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRegisterVestingEscrowIdentityInstructionDataEncoder().encode({}),
    programAddress,
  } as RegisterVestingEscrowIdentityInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountSeries,
    TAccountIdentity,
    TAccountSystemProgram
  >);
}

export type RegisterVestingEscrowIdentityInput<
  TAccountPayer extends string = string,
  TAccountSeries extends string = string,
  TAccountIdentity extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  series: Address<TAccountSeries>;
  identity: Address<TAccountIdentity>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getRegisterVestingEscrowIdentityInstruction<
  TAccountPayer extends string,
  TAccountSeries extends string,
  TAccountIdentity extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: RegisterVestingEscrowIdentityInput<
    TAccountPayer,
    TAccountSeries,
    TAccountIdentity,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RegisterVestingEscrowIdentityInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountSeries,
  TAccountIdentity,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    series: { value: input.series ?? null, isWritable: false },
    identity: { value: input.identity ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.series),
      getAccountMeta(accounts.identity),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRegisterVestingEscrowIdentityInstructionDataEncoder().encode({}),
    programAddress,
  } as RegisterVestingEscrowIdentityInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountSeries,
    TAccountIdentity,
    TAccountSystemProgram
  >);
}

export type ParsedRegisterVestingEscrowIdentityInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    series: TAccountMetas[1];
    identity: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: RegisterVestingEscrowIdentityInstructionData;
};

export function parseRegisterVestingEscrowIdentityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegisterVestingEscrowIdentityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      series: getNextAccount(),
      identity: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterVestingEscrowIdentityInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
