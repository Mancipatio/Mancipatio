/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findPlatformPda, findRecoveryPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CANCEL_ISSUER_RECOVERY_DISCRIMINATOR = new Uint8Array([
  78, 73, 145, 14, 21, 174, 194, 147,
]);

export function getCancelIssuerRecoveryDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_ISSUER_RECOVERY_DISCRIMINATOR,
  );
}

export type CancelIssuerRecoveryInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountCanceller extends string | AccountMeta<string> = string,
  TAccountPlatform extends string | AccountMeta<string> = string,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TAccountRecovery extends string | AccountMeta<string> = string,
  TAccountProposer extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCanceller extends string
        ? ReadonlySignerAccount<TAccountCanceller> &
            AccountSignerMeta<TAccountCanceller>
        : TAccountCanceller,
      TAccountPlatform extends string
        ? ReadonlyAccount<TAccountPlatform>
        : TAccountPlatform,
      TAccountIssuer extends string
        ? ReadonlyAccount<TAccountIssuer>
        : TAccountIssuer,
      TAccountRecovery extends string
        ? WritableAccount<TAccountRecovery>
        : TAccountRecovery,
      TAccountProposer extends string
        ? WritableAccount<TAccountProposer>
        : TAccountProposer,
      ...TRemainingAccounts,
    ]
  >;

export type CancelIssuerRecoveryInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CancelIssuerRecoveryInstructionDataArgs = {};

export function getCancelIssuerRecoveryInstructionDataEncoder(): FixedSizeEncoder<CancelIssuerRecoveryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CANCEL_ISSUER_RECOVERY_DISCRIMINATOR,
    }),
  );
}

export function getCancelIssuerRecoveryInstructionDataDecoder(): FixedSizeDecoder<CancelIssuerRecoveryInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCancelIssuerRecoveryInstructionDataCodec(): FixedSizeCodec<
  CancelIssuerRecoveryInstructionDataArgs,
  CancelIssuerRecoveryInstructionData
> {
  return combineCodec(
    getCancelIssuerRecoveryInstructionDataEncoder(),
    getCancelIssuerRecoveryInstructionDataDecoder(),
  );
}

export type CancelIssuerRecoveryAsyncInput<
  TAccountCanceller extends string = string,
  TAccountPlatform extends string = string,
  TAccountIssuer extends string = string,
  TAccountRecovery extends string = string,
  TAccountProposer extends string = string,
> = {
  /**
   * The live issuer authority or the live super admin. Not `mut`: it may be
   * the same key as `proposer`.
   */
  canceller: TransactionSigner<TAccountCanceller>;
  platform?: Address<TAccountPlatform>;
  issuer: Address<TAccountIssuer>;
  recovery?: Address<TAccountRecovery>;
  proposer: Address<TAccountProposer>;
};

export async function getCancelIssuerRecoveryInstructionAsync<
  TAccountCanceller extends string,
  TAccountPlatform extends string,
  TAccountIssuer extends string,
  TAccountRecovery extends string,
  TAccountProposer extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: CancelIssuerRecoveryAsyncInput<
    TAccountCanceller,
    TAccountPlatform,
    TAccountIssuer,
    TAccountRecovery,
    TAccountProposer
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CancelIssuerRecoveryInstruction<
    TProgramAddress,
    TAccountCanceller,
    TAccountPlatform,
    TAccountIssuer,
    TAccountRecovery,
    TAccountProposer
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    canceller: { value: input.canceller ?? null, isWritable: false },
    platform: { value: input.platform ?? null, isWritable: false },
    issuer: { value: input.issuer ?? null, isWritable: false },
    recovery: { value: input.recovery ?? null, isWritable: true },
    proposer: { value: input.proposer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.platform.value) {
    accounts.platform.value = await findPlatformPda();
  }
  if (!accounts.recovery.value) {
    accounts.recovery.value = await findRecoveryPda({
      issuer: expectAddress(accounts.issuer.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.canceller),
      getAccountMeta(accounts.platform),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.recovery),
      getAccountMeta(accounts.proposer),
    ],
    data: getCancelIssuerRecoveryInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelIssuerRecoveryInstruction<
    TProgramAddress,
    TAccountCanceller,
    TAccountPlatform,
    TAccountIssuer,
    TAccountRecovery,
    TAccountProposer
  >);
}

export type CancelIssuerRecoveryInput<
  TAccountCanceller extends string = string,
  TAccountPlatform extends string = string,
  TAccountIssuer extends string = string,
  TAccountRecovery extends string = string,
  TAccountProposer extends string = string,
> = {
  /**
   * The live issuer authority or the live super admin. Not `mut`: it may be
   * the same key as `proposer`.
   */
  canceller: TransactionSigner<TAccountCanceller>;
  platform: Address<TAccountPlatform>;
  issuer: Address<TAccountIssuer>;
  recovery: Address<TAccountRecovery>;
  proposer: Address<TAccountProposer>;
};

export function getCancelIssuerRecoveryInstruction<
  TAccountCanceller extends string,
  TAccountPlatform extends string,
  TAccountIssuer extends string,
  TAccountRecovery extends string,
  TAccountProposer extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: CancelIssuerRecoveryInput<
    TAccountCanceller,
    TAccountPlatform,
    TAccountIssuer,
    TAccountRecovery,
    TAccountProposer
  >,
  config?: { programAddress?: TProgramAddress },
): CancelIssuerRecoveryInstruction<
  TProgramAddress,
  TAccountCanceller,
  TAccountPlatform,
  TAccountIssuer,
  TAccountRecovery,
  TAccountProposer
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    canceller: { value: input.canceller ?? null, isWritable: false },
    platform: { value: input.platform ?? null, isWritable: false },
    issuer: { value: input.issuer ?? null, isWritable: false },
    recovery: { value: input.recovery ?? null, isWritable: true },
    proposer: { value: input.proposer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.canceller),
      getAccountMeta(accounts.platform),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.recovery),
      getAccountMeta(accounts.proposer),
    ],
    data: getCancelIssuerRecoveryInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelIssuerRecoveryInstruction<
    TProgramAddress,
    TAccountCanceller,
    TAccountPlatform,
    TAccountIssuer,
    TAccountRecovery,
    TAccountProposer
  >);
}

export type ParsedCancelIssuerRecoveryInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * The live issuer authority or the live super admin. Not `mut`: it may be
     * the same key as `proposer`.
     */
    canceller: TAccountMetas[0];
    platform: TAccountMetas[1];
    issuer: TAccountMetas[2];
    recovery: TAccountMetas[3];
    proposer: TAccountMetas[4];
  };
  data: CancelIssuerRecoveryInstructionData;
};

export function parseCancelIssuerRecoveryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelIssuerRecoveryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      canceller: getNextAccount(),
      platform: getNextAccount(),
      issuer: getNextAccount(),
      recovery: getNextAccount(),
      proposer: getNextAccount(),
    },
    data: getCancelIssuerRecoveryInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
