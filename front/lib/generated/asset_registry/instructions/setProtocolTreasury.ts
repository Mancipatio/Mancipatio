/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findPlatformPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SET_PROTOCOL_TREASURY_DISCRIMINATOR = new Uint8Array([
  70, 185, 238, 193, 38, 214, 189, 7,
]);

export function getSetProtocolTreasuryDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_PROTOCOL_TREASURY_DISCRIMINATOR,
  );
}

export type SetProtocolTreasuryInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountSuperAdmin extends string | AccountMeta<string> = string,
  TAccountPlatform extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSuperAdmin extends string
        ? ReadonlySignerAccount<TAccountSuperAdmin> &
            AccountSignerMeta<TAccountSuperAdmin>
        : TAccountSuperAdmin,
      TAccountPlatform extends string
        ? WritableAccount<TAccountPlatform>
        : TAccountPlatform,
      ...TRemainingAccounts,
    ]
  >;

export type SetProtocolTreasuryInstructionData = {
  discriminator: ReadonlyUint8Array;
  newTreasury: Address;
};

export type SetProtocolTreasuryInstructionDataArgs = { newTreasury: Address };

export function getSetProtocolTreasuryInstructionDataEncoder(): FixedSizeEncoder<SetProtocolTreasuryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newTreasury", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: SET_PROTOCOL_TREASURY_DISCRIMINATOR,
    }),
  );
}

export function getSetProtocolTreasuryInstructionDataDecoder(): FixedSizeDecoder<SetProtocolTreasuryInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newTreasury", getAddressDecoder()],
  ]);
}

export function getSetProtocolTreasuryInstructionDataCodec(): FixedSizeCodec<
  SetProtocolTreasuryInstructionDataArgs,
  SetProtocolTreasuryInstructionData
> {
  return combineCodec(
    getSetProtocolTreasuryInstructionDataEncoder(),
    getSetProtocolTreasuryInstructionDataDecoder(),
  );
}

export type SetProtocolTreasuryAsyncInput<
  TAccountSuperAdmin extends string = string,
  TAccountPlatform extends string = string,
> = {
  superAdmin: TransactionSigner<TAccountSuperAdmin>;
  platform?: Address<TAccountPlatform>;
  newTreasury: SetProtocolTreasuryInstructionDataArgs["newTreasury"];
};

export async function getSetProtocolTreasuryInstructionAsync<
  TAccountSuperAdmin extends string,
  TAccountPlatform extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: SetProtocolTreasuryAsyncInput<TAccountSuperAdmin, TAccountPlatform>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetProtocolTreasuryInstruction<
    TProgramAddress,
    TAccountSuperAdmin,
    TAccountPlatform
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    superAdmin: { value: input.superAdmin ?? null, isWritable: false },
    platform: { value: input.platform ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.platform.value) {
    accounts.platform.value = await findPlatformPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.superAdmin),
      getAccountMeta(accounts.platform),
    ],
    data: getSetProtocolTreasuryInstructionDataEncoder().encode(
      args as SetProtocolTreasuryInstructionDataArgs,
    ),
    programAddress,
  } as SetProtocolTreasuryInstruction<
    TProgramAddress,
    TAccountSuperAdmin,
    TAccountPlatform
  >);
}

export type SetProtocolTreasuryInput<
  TAccountSuperAdmin extends string = string,
  TAccountPlatform extends string = string,
> = {
  superAdmin: TransactionSigner<TAccountSuperAdmin>;
  platform: Address<TAccountPlatform>;
  newTreasury: SetProtocolTreasuryInstructionDataArgs["newTreasury"];
};

export function getSetProtocolTreasuryInstruction<
  TAccountSuperAdmin extends string,
  TAccountPlatform extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: SetProtocolTreasuryInput<TAccountSuperAdmin, TAccountPlatform>,
  config?: { programAddress?: TProgramAddress },
): SetProtocolTreasuryInstruction<
  TProgramAddress,
  TAccountSuperAdmin,
  TAccountPlatform
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    superAdmin: { value: input.superAdmin ?? null, isWritable: false },
    platform: { value: input.platform ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.superAdmin),
      getAccountMeta(accounts.platform),
    ],
    data: getSetProtocolTreasuryInstructionDataEncoder().encode(
      args as SetProtocolTreasuryInstructionDataArgs,
    ),
    programAddress,
  } as SetProtocolTreasuryInstruction<
    TProgramAddress,
    TAccountSuperAdmin,
    TAccountPlatform
  >);
}

export type ParsedSetProtocolTreasuryInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    superAdmin: TAccountMetas[0];
    platform: TAccountMetas[1];
  };
  data: SetProtocolTreasuryInstructionData;
};

export function parseSetProtocolTreasuryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetProtocolTreasuryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { superAdmin: getNextAccount(), platform: getNextAccount() },
    data: getSetProtocolTreasuryInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
