/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findPositionPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const RECOVER_VESTING_POSITION_DISCRIMINATOR = new Uint8Array([
  249, 219, 131, 89, 195, 149, 225, 189,
]);

export function getRecoverVestingPositionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RECOVER_VESTING_POSITION_DISCRIMINATOR,
  );
}

export type RecoverVestingPositionInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSeries extends string | AccountMeta<string> = string,
  TAccountPosition extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSeries extends string
        ? ReadonlyAccount<TAccountSeries>
        : TAccountSeries,
      TAccountPosition extends string
        ? WritableAccount<TAccountPosition>
        : TAccountPosition,
      ...TRemainingAccounts,
    ]
  >;

export type RecoverVestingPositionInstructionData = {
  discriminator: ReadonlyUint8Array;
  positionIndex: number;
  newWallet: Address;
};

export type RecoverVestingPositionInstructionDataArgs = {
  positionIndex: number;
  newWallet: Address;
};

export function getRecoverVestingPositionInstructionDataEncoder(): FixedSizeEncoder<RecoverVestingPositionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["positionIndex", getU32Encoder()],
      ["newWallet", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: RECOVER_VESTING_POSITION_DISCRIMINATOR,
    }),
  );
}

export function getRecoverVestingPositionInstructionDataDecoder(): FixedSizeDecoder<RecoverVestingPositionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["positionIndex", getU32Decoder()],
    ["newWallet", getAddressDecoder()],
  ]);
}

export function getRecoverVestingPositionInstructionDataCodec(): FixedSizeCodec<
  RecoverVestingPositionInstructionDataArgs,
  RecoverVestingPositionInstructionData
> {
  return combineCodec(
    getRecoverVestingPositionInstructionDataEncoder(),
    getRecoverVestingPositionInstructionDataDecoder(),
  );
}

export type RecoverVestingPositionAsyncInput<
  TAccountAuthority extends string = string,
  TAccountSeries extends string = string,
  TAccountPosition extends string = string,
> = {
  /**
   * The client — series authority. Recovery authority sits with the
   * CLIENT, not with Mancipatio (spec §11.1.10); confirming that the
   * replacement wallet belongs to the same person is the client's own
   * off-chain verification process.
   */
  authority: TransactionSigner<TAccountAuthority>;
  series: Address<TAccountSeries>;
  position?: Address<TAccountPosition>;
  positionIndex: RecoverVestingPositionInstructionDataArgs["positionIndex"];
  newWallet: RecoverVestingPositionInstructionDataArgs["newWallet"];
};

export async function getRecoverVestingPositionInstructionAsync<
  TAccountAuthority extends string,
  TAccountSeries extends string,
  TAccountPosition extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: RecoverVestingPositionAsyncInput<
    TAccountAuthority,
    TAccountSeries,
    TAccountPosition
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RecoverVestingPositionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountSeries,
    TAccountPosition
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    series: { value: input.series ?? null, isWritable: false },
    position: { value: input.position ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.position.value) {
    accounts.position.value = await findPositionPda({
      series: expectAddress(accounts.series.value),
      positionIndex: expectSome(args.positionIndex),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.series),
      getAccountMeta(accounts.position),
    ],
    data: getRecoverVestingPositionInstructionDataEncoder().encode(
      args as RecoverVestingPositionInstructionDataArgs,
    ),
    programAddress,
  } as RecoverVestingPositionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountSeries,
    TAccountPosition
  >);
}

export type RecoverVestingPositionInput<
  TAccountAuthority extends string = string,
  TAccountSeries extends string = string,
  TAccountPosition extends string = string,
> = {
  /**
   * The client — series authority. Recovery authority sits with the
   * CLIENT, not with Mancipatio (spec §11.1.10); confirming that the
   * replacement wallet belongs to the same person is the client's own
   * off-chain verification process.
   */
  authority: TransactionSigner<TAccountAuthority>;
  series: Address<TAccountSeries>;
  position: Address<TAccountPosition>;
  positionIndex: RecoverVestingPositionInstructionDataArgs["positionIndex"];
  newWallet: RecoverVestingPositionInstructionDataArgs["newWallet"];
};

export function getRecoverVestingPositionInstruction<
  TAccountAuthority extends string,
  TAccountSeries extends string,
  TAccountPosition extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: RecoverVestingPositionInput<
    TAccountAuthority,
    TAccountSeries,
    TAccountPosition
  >,
  config?: { programAddress?: TProgramAddress },
): RecoverVestingPositionInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountSeries,
  TAccountPosition
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    series: { value: input.series ?? null, isWritable: false },
    position: { value: input.position ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.series),
      getAccountMeta(accounts.position),
    ],
    data: getRecoverVestingPositionInstructionDataEncoder().encode(
      args as RecoverVestingPositionInstructionDataArgs,
    ),
    programAddress,
  } as RecoverVestingPositionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountSeries,
    TAccountPosition
  >);
}

export type ParsedRecoverVestingPositionInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * The client — series authority. Recovery authority sits with the
     * CLIENT, not with Mancipatio (spec §11.1.10); confirming that the
     * replacement wallet belongs to the same person is the client's own
     * off-chain verification process.
     */
    authority: TAccountMetas[0];
    series: TAccountMetas[1];
    position: TAccountMetas[2];
  };
  data: RecoverVestingPositionInstructionData;
};

export function parseRecoverVestingPositionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRecoverVestingPositionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      series: getNextAccount(),
      position: getNextAccount(),
    },
    data: getRecoverVestingPositionInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
