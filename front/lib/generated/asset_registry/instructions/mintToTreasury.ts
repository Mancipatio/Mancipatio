/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findMintPda, findPlatformPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const MINT_TO_TREASURY_DISCRIMINATOR = new Uint8Array([
  11, 33, 44, 19, 40, 162, 76, 2,
]);

export function getMintToTreasuryDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    MINT_TO_TREASURY_DISCRIMINATOR,
  );
}

export type MintToTreasuryInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAdminRecord extends string | AccountMeta<string> = string,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TAccountAsset extends string | AccountMeta<string> = string,
  TAccountShareClass extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountDestination extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountPlatform extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAdminRecord extends string
        ? ReadonlyAccount<TAccountAdminRecord>
        : TAccountAdminRecord,
      TAccountIssuer extends string
        ? ReadonlyAccount<TAccountIssuer>
        : TAccountIssuer,
      TAccountAsset extends string
        ? ReadonlyAccount<TAccountAsset>
        : TAccountAsset,
      TAccountShareClass extends string
        ? WritableAccount<TAccountShareClass>
        : TAccountShareClass,
      TAccountMint extends string
        ? WritableAccount<TAccountMint>
        : TAccountMint,
      TAccountDestination extends string
        ? WritableAccount<TAccountDestination>
        : TAccountDestination,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountPlatform extends string
        ? ReadonlyAccount<TAccountPlatform>
        : TAccountPlatform,
      ...TRemainingAccounts,
    ]
  >;

export type MintToTreasuryInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type MintToTreasuryInstructionDataArgs = { amount: number | bigint };

export function getMintToTreasuryInstructionDataEncoder(): FixedSizeEncoder<MintToTreasuryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: MINT_TO_TREASURY_DISCRIMINATOR }),
  );
}

export function getMintToTreasuryInstructionDataDecoder(): FixedSizeDecoder<MintToTreasuryInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getMintToTreasuryInstructionDataCodec(): FixedSizeCodec<
  MintToTreasuryInstructionDataArgs,
  MintToTreasuryInstructionData
> {
  return combineCodec(
    getMintToTreasuryInstructionDataEncoder(),
    getMintToTreasuryInstructionDataDecoder(),
  );
}

export type MintToTreasuryAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountIssuer extends string = string,
  TAccountAsset extends string = string,
  TAccountShareClass extends string = string,
  TAccountMint extends string = string,
  TAccountDestination extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountPlatform extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /** Global Admin or issuer-local MINT capability; the signer must also be the issuer. */
  adminRecord: Address<TAccountAdminRecord>;
  issuer: Address<TAccountIssuer>;
  asset: Address<TAccountAsset>;
  shareClass: Address<TAccountShareClass>;
  mint?: Address<TAccountMint>;
  /**
   * Destination token account — receives the freshly minted units. Bound in
   * the handler: owned by the signing issuer authority (the treasury) or the
   * escrow of an `Active` burn-only `CustodyVault` / a `RightsIssuance` of
   * this very mint (proven by deserializing that account, not by its owner
   * program — and, for a vault, by its exit shape).
   */
  destination: Address<TAccountDestination>;
  tokenProgram?: Address<TAccountTokenProgram>;
  /**
   * Emergency-pause gate (read-only). Keep LAST among named accounts: old
   * account indices and the remaining-accounts hook tail keep their positions.
   */
  platform?: Address<TAccountPlatform>;
  amount: MintToTreasuryInstructionDataArgs["amount"];
};

export async function getMintToTreasuryInstructionAsync<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountIssuer extends string,
  TAccountAsset extends string,
  TAccountShareClass extends string,
  TAccountMint extends string,
  TAccountDestination extends string,
  TAccountTokenProgram extends string,
  TAccountPlatform extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: MintToTreasuryAsyncInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountIssuer,
    TAccountAsset,
    TAccountShareClass,
    TAccountMint,
    TAccountDestination,
    TAccountTokenProgram,
    TAccountPlatform
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  MintToTreasuryInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountIssuer,
    TAccountAsset,
    TAccountShareClass,
    TAccountMint,
    TAccountDestination,
    TAccountTokenProgram,
    TAccountPlatform
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    issuer: { value: input.issuer ?? null, isWritable: false },
    asset: { value: input.asset ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: true },
    destination: { value: input.destination ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    platform: { value: input.platform ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.mint.value) {
    accounts.mint.value = await findMintPda({
      shareClass: expectAddress(accounts.shareClass.value),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.platform.value) {
    accounts.platform.value = await findPlatformPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.asset),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.destination),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.platform),
    ],
    data: getMintToTreasuryInstructionDataEncoder().encode(
      args as MintToTreasuryInstructionDataArgs,
    ),
    programAddress,
  } as MintToTreasuryInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountIssuer,
    TAccountAsset,
    TAccountShareClass,
    TAccountMint,
    TAccountDestination,
    TAccountTokenProgram,
    TAccountPlatform
  >);
}

export type MintToTreasuryInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountIssuer extends string = string,
  TAccountAsset extends string = string,
  TAccountShareClass extends string = string,
  TAccountMint extends string = string,
  TAccountDestination extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountPlatform extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /** Global Admin or issuer-local MINT capability; the signer must also be the issuer. */
  adminRecord: Address<TAccountAdminRecord>;
  issuer: Address<TAccountIssuer>;
  asset: Address<TAccountAsset>;
  shareClass: Address<TAccountShareClass>;
  mint: Address<TAccountMint>;
  /**
   * Destination token account — receives the freshly minted units. Bound in
   * the handler: owned by the signing issuer authority (the treasury) or the
   * escrow of an `Active` burn-only `CustodyVault` / a `RightsIssuance` of
   * this very mint (proven by deserializing that account, not by its owner
   * program — and, for a vault, by its exit shape).
   */
  destination: Address<TAccountDestination>;
  tokenProgram?: Address<TAccountTokenProgram>;
  /**
   * Emergency-pause gate (read-only). Keep LAST among named accounts: old
   * account indices and the remaining-accounts hook tail keep their positions.
   */
  platform: Address<TAccountPlatform>;
  amount: MintToTreasuryInstructionDataArgs["amount"];
};

export function getMintToTreasuryInstruction<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountIssuer extends string,
  TAccountAsset extends string,
  TAccountShareClass extends string,
  TAccountMint extends string,
  TAccountDestination extends string,
  TAccountTokenProgram extends string,
  TAccountPlatform extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: MintToTreasuryInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountIssuer,
    TAccountAsset,
    TAccountShareClass,
    TAccountMint,
    TAccountDestination,
    TAccountTokenProgram,
    TAccountPlatform
  >,
  config?: { programAddress?: TProgramAddress },
): MintToTreasuryInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAdminRecord,
  TAccountIssuer,
  TAccountAsset,
  TAccountShareClass,
  TAccountMint,
  TAccountDestination,
  TAccountTokenProgram,
  TAccountPlatform
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    issuer: { value: input.issuer ?? null, isWritable: false },
    asset: { value: input.asset ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: true },
    destination: { value: input.destination ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    platform: { value: input.platform ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.asset),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.destination),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.platform),
    ],
    data: getMintToTreasuryInstructionDataEncoder().encode(
      args as MintToTreasuryInstructionDataArgs,
    ),
    programAddress,
  } as MintToTreasuryInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountIssuer,
    TAccountAsset,
    TAccountShareClass,
    TAccountMint,
    TAccountDestination,
    TAccountTokenProgram,
    TAccountPlatform
  >);
}

export type ParsedMintToTreasuryInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    /** Global Admin or issuer-local MINT capability; the signer must also be the issuer. */
    adminRecord: TAccountMetas[1];
    issuer: TAccountMetas[2];
    asset: TAccountMetas[3];
    shareClass: TAccountMetas[4];
    mint: TAccountMetas[5];
    /**
     * Destination token account — receives the freshly minted units. Bound in
     * the handler: owned by the signing issuer authority (the treasury) or the
     * escrow of an `Active` burn-only `CustodyVault` / a `RightsIssuance` of
     * this very mint (proven by deserializing that account, not by its owner
     * program — and, for a vault, by its exit shape).
     */
    destination: TAccountMetas[6];
    tokenProgram: TAccountMetas[7];
    /**
     * Emergency-pause gate (read-only). Keep LAST among named accounts: old
     * account indices and the remaining-accounts hook tail keep their positions.
     */
    platform: TAccountMetas[8];
  };
  data: MintToTreasuryInstructionData;
};

export function parseMintToTreasuryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedMintToTreasuryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      adminRecord: getNextAccount(),
      issuer: getNextAccount(),
      asset: getNextAccount(),
      shareClass: getNextAccount(),
      mint: getNextAccount(),
      destination: getNextAccount(),
      tokenProgram: getNextAccount(),
      platform: getNextAccount(),
    },
    data: getMintToTreasuryInstructionDataDecoder().decode(instruction.data),
  };
}
