/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findIdentityPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const REGISTER_RIGHTS_ESCROW_IDENTITY_DISCRIMINATOR = new Uint8Array([
  153, 1, 95, 147, 162, 8, 109, 85,
]);

export function getRegisterRightsEscrowIdentityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTER_RIGHTS_ESCROW_IDENTITY_DISCRIMINATOR,
  );
}

export type RegisterRightsEscrowIdentityInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountRightsIssuance extends string | AccountMeta<string> = string,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountRightsIssuance extends string
        ? ReadonlyAccount<TAccountRightsIssuance>
        : TAccountRightsIssuance,
      TAccountIdentity extends string
        ? WritableAccount<TAccountIdentity>
        : TAccountIdentity,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterRightsEscrowIdentityInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RegisterRightsEscrowIdentityInstructionDataArgs = {};

export function getRegisterRightsEscrowIdentityInstructionDataEncoder(): FixedSizeEncoder<RegisterRightsEscrowIdentityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: REGISTER_RIGHTS_ESCROW_IDENTITY_DISCRIMINATOR,
    }),
  );
}

export function getRegisterRightsEscrowIdentityInstructionDataDecoder(): FixedSizeDecoder<RegisterRightsEscrowIdentityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRegisterRightsEscrowIdentityInstructionDataCodec(): FixedSizeCodec<
  RegisterRightsEscrowIdentityInstructionDataArgs,
  RegisterRightsEscrowIdentityInstructionData
> {
  return combineCodec(
    getRegisterRightsEscrowIdentityInstructionDataEncoder(),
    getRegisterRightsEscrowIdentityInstructionDataDecoder(),
  );
}

export type RegisterRightsEscrowIdentityAsyncInput<
  TAccountPayer extends string = string,
  TAccountRightsIssuance extends string = string,
  TAccountIdentity extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  rightsIssuance: Address<TAccountRightsIssuance>;
  identity?: Address<TAccountIdentity>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getRegisterRightsEscrowIdentityInstructionAsync<
  TAccountPayer extends string,
  TAccountRightsIssuance extends string,
  TAccountIdentity extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: RegisterRightsEscrowIdentityAsyncInput<
    TAccountPayer,
    TAccountRightsIssuance,
    TAccountIdentity,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RegisterRightsEscrowIdentityInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountRightsIssuance,
    TAccountIdentity,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    rightsIssuance: { value: input.rightsIssuance ?? null, isWritable: false },
    identity: { value: input.identity ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.identity.value) {
    accounts.identity.value = await findIdentityPda({
      rightsIssuance: expectAddress(accounts.rightsIssuance.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.rightsIssuance),
      getAccountMeta(accounts.identity),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRegisterRightsEscrowIdentityInstructionDataEncoder().encode({}),
    programAddress,
  } as RegisterRightsEscrowIdentityInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountRightsIssuance,
    TAccountIdentity,
    TAccountSystemProgram
  >);
}

export type RegisterRightsEscrowIdentityInput<
  TAccountPayer extends string = string,
  TAccountRightsIssuance extends string = string,
  TAccountIdentity extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  rightsIssuance: Address<TAccountRightsIssuance>;
  identity: Address<TAccountIdentity>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getRegisterRightsEscrowIdentityInstruction<
  TAccountPayer extends string,
  TAccountRightsIssuance extends string,
  TAccountIdentity extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: RegisterRightsEscrowIdentityInput<
    TAccountPayer,
    TAccountRightsIssuance,
    TAccountIdentity,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RegisterRightsEscrowIdentityInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountRightsIssuance,
  TAccountIdentity,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    rightsIssuance: { value: input.rightsIssuance ?? null, isWritable: false },
    identity: { value: input.identity ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.rightsIssuance),
      getAccountMeta(accounts.identity),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRegisterRightsEscrowIdentityInstructionDataEncoder().encode({}),
    programAddress,
  } as RegisterRightsEscrowIdentityInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountRightsIssuance,
    TAccountIdentity,
    TAccountSystemProgram
  >);
}

export type ParsedRegisterRightsEscrowIdentityInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    rightsIssuance: TAccountMetas[1];
    identity: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: RegisterRightsEscrowIdentityInstructionData;
};

export function parseRegisterRightsEscrowIdentityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegisterRightsEscrowIdentityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      rightsIssuance: getNextAccount(),
      identity: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterRightsEscrowIdentityInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
