/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findOpenCustodyVaultEscrowMarkerPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const RETURN_CUSTODY_VAULT_DISCRIMINATOR = new Uint8Array([
  140, 140, 141, 209, 76, 4, 88, 14,
]);

export function getReturnCustodyVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RETURN_CUSTODY_VAULT_DISCRIMINATOR,
  );
}

export type ReturnCustodyVaultInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountShareClass extends string | AccountMeta<string> = string,
  TAccountCustodyVault extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountBeneficiaryTokenAccount extends string | AccountMeta<string> = string,
  TAccountEscrowMarker extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountAuthorityAdminRecord extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountShareClass extends string
        ? ReadonlyAccount<TAccountShareClass>
        : TAccountShareClass,
      TAccountCustodyVault extends string
        ? WritableAccount<TAccountCustodyVault>
        : TAccountCustodyVault,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountBeneficiaryTokenAccount extends string
        ? WritableAccount<TAccountBeneficiaryTokenAccount>
        : TAccountBeneficiaryTokenAccount,
      TAccountEscrowMarker extends string
        ? WritableAccount<TAccountEscrowMarker>
        : TAccountEscrowMarker,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountAuthorityAdminRecord extends string
        ? ReadonlyAccount<TAccountAuthorityAdminRecord>
        : TAccountAuthorityAdminRecord,
      ...TRemainingAccounts,
    ]
  >;

export type ReturnCustodyVaultInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ReturnCustodyVaultInstructionDataArgs = {};

export function getReturnCustodyVaultInstructionDataEncoder(): FixedSizeEncoder<ReturnCustodyVaultInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: RETURN_CUSTODY_VAULT_DISCRIMINATOR,
    }),
  );
}

export function getReturnCustodyVaultInstructionDataDecoder(): FixedSizeDecoder<ReturnCustodyVaultInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getReturnCustodyVaultInstructionDataCodec(): FixedSizeCodec<
  ReturnCustodyVaultInstructionDataArgs,
  ReturnCustodyVaultInstructionData
> {
  return combineCodec(
    getReturnCustodyVaultInstructionDataEncoder(),
    getReturnCustodyVaultInstructionDataDecoder(),
  );
}

export type ReturnCustodyVaultAsyncInput<
  TAccountSigner extends string = string,
  TAccountShareClass extends string = string,
  TAccountCustodyVault extends string = string,
  TAccountMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountBeneficiaryTokenAccount extends string = string,
  TAccountEscrowMarker extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountAuthorityAdminRecord extends string = string,
> = {
  /**
   * Vault authority with a live Admin role at any time (`Active`/`Triggered`); anyone
   * once a positive `deadline` has passed (`deadline == 0` — the only
   * non-positive value `open_custody_vault` accepts — disables the
   * permissionless path). Mut: receives the closed escrow marker's rent.
   */
  signer: TransactionSigner<TAccountSigner>;
  shareClass: Address<TAccountShareClass>;
  custodyVault: Address<TAccountCustodyVault>;
  mint: Address<TAccountMint>;
  escrow: Address<TAccountEscrow>;
  /** The beneficiary receives the escrowed tokens back here. */
  beneficiaryTokenAccount: Address<TAccountBeneficiaryTokenAccount>;
  /**
   * Escrow marker for the vault PDA. Closed by the handler (rent → signer)
   * ONLY when the escrow is fully drained — a return that had to withhold an
   * un-releasable surplus leaves the vault (and this marker) alive so the
   * remainder still has an exit. Closed manually rather than with
   * `close = signer` for exactly that reason.
   */
  escrowMarker?: Address<TAccountEscrowMarker>;
  tokenProgram?: Address<TAccountTokenProgram>;
  authorityAdminRecord: Address<TAccountAuthorityAdminRecord>;
};

export async function getReturnCustodyVaultInstructionAsync<
  TAccountSigner extends string,
  TAccountShareClass extends string,
  TAccountCustodyVault extends string,
  TAccountMint extends string,
  TAccountEscrow extends string,
  TAccountBeneficiaryTokenAccount extends string,
  TAccountEscrowMarker extends string,
  TAccountTokenProgram extends string,
  TAccountAuthorityAdminRecord extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ReturnCustodyVaultAsyncInput<
    TAccountSigner,
    TAccountShareClass,
    TAccountCustodyVault,
    TAccountMint,
    TAccountEscrow,
    TAccountBeneficiaryTokenAccount,
    TAccountEscrowMarker,
    TAccountTokenProgram,
    TAccountAuthorityAdminRecord
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ReturnCustodyVaultInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountShareClass,
    TAccountCustodyVault,
    TAccountMint,
    TAccountEscrow,
    TAccountBeneficiaryTokenAccount,
    TAccountEscrowMarker,
    TAccountTokenProgram,
    TAccountAuthorityAdminRecord
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    shareClass: { value: input.shareClass ?? null, isWritable: false },
    custodyVault: { value: input.custodyVault ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    escrow: { value: input.escrow ?? null, isWritable: true },
    beneficiaryTokenAccount: {
      value: input.beneficiaryTokenAccount ?? null,
      isWritable: true,
    },
    escrowMarker: { value: input.escrowMarker ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    authorityAdminRecord: {
      value: input.authorityAdminRecord ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.escrowMarker.value) {
    accounts.escrowMarker.value = await findOpenCustodyVaultEscrowMarkerPda({
      custodyVault: expectAddress(accounts.custodyVault.value),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.custodyVault),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.beneficiaryTokenAccount),
      getAccountMeta(accounts.escrowMarker),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.authorityAdminRecord),
    ],
    data: getReturnCustodyVaultInstructionDataEncoder().encode({}),
    programAddress,
  } as ReturnCustodyVaultInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountShareClass,
    TAccountCustodyVault,
    TAccountMint,
    TAccountEscrow,
    TAccountBeneficiaryTokenAccount,
    TAccountEscrowMarker,
    TAccountTokenProgram,
    TAccountAuthorityAdminRecord
  >);
}

export type ReturnCustodyVaultInput<
  TAccountSigner extends string = string,
  TAccountShareClass extends string = string,
  TAccountCustodyVault extends string = string,
  TAccountMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountBeneficiaryTokenAccount extends string = string,
  TAccountEscrowMarker extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountAuthorityAdminRecord extends string = string,
> = {
  /**
   * Vault authority with a live Admin role at any time (`Active`/`Triggered`); anyone
   * once a positive `deadline` has passed (`deadline == 0` — the only
   * non-positive value `open_custody_vault` accepts — disables the
   * permissionless path). Mut: receives the closed escrow marker's rent.
   */
  signer: TransactionSigner<TAccountSigner>;
  shareClass: Address<TAccountShareClass>;
  custodyVault: Address<TAccountCustodyVault>;
  mint: Address<TAccountMint>;
  escrow: Address<TAccountEscrow>;
  /** The beneficiary receives the escrowed tokens back here. */
  beneficiaryTokenAccount: Address<TAccountBeneficiaryTokenAccount>;
  /**
   * Escrow marker for the vault PDA. Closed by the handler (rent → signer)
   * ONLY when the escrow is fully drained — a return that had to withhold an
   * un-releasable surplus leaves the vault (and this marker) alive so the
   * remainder still has an exit. Closed manually rather than with
   * `close = signer` for exactly that reason.
   */
  escrowMarker: Address<TAccountEscrowMarker>;
  tokenProgram?: Address<TAccountTokenProgram>;
  authorityAdminRecord: Address<TAccountAuthorityAdminRecord>;
};

export function getReturnCustodyVaultInstruction<
  TAccountSigner extends string,
  TAccountShareClass extends string,
  TAccountCustodyVault extends string,
  TAccountMint extends string,
  TAccountEscrow extends string,
  TAccountBeneficiaryTokenAccount extends string,
  TAccountEscrowMarker extends string,
  TAccountTokenProgram extends string,
  TAccountAuthorityAdminRecord extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ReturnCustodyVaultInput<
    TAccountSigner,
    TAccountShareClass,
    TAccountCustodyVault,
    TAccountMint,
    TAccountEscrow,
    TAccountBeneficiaryTokenAccount,
    TAccountEscrowMarker,
    TAccountTokenProgram,
    TAccountAuthorityAdminRecord
  >,
  config?: { programAddress?: TProgramAddress },
): ReturnCustodyVaultInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountShareClass,
  TAccountCustodyVault,
  TAccountMint,
  TAccountEscrow,
  TAccountBeneficiaryTokenAccount,
  TAccountEscrowMarker,
  TAccountTokenProgram,
  TAccountAuthorityAdminRecord
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    shareClass: { value: input.shareClass ?? null, isWritable: false },
    custodyVault: { value: input.custodyVault ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    escrow: { value: input.escrow ?? null, isWritable: true },
    beneficiaryTokenAccount: {
      value: input.beneficiaryTokenAccount ?? null,
      isWritable: true,
    },
    escrowMarker: { value: input.escrowMarker ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    authorityAdminRecord: {
      value: input.authorityAdminRecord ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.custodyVault),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.beneficiaryTokenAccount),
      getAccountMeta(accounts.escrowMarker),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.authorityAdminRecord),
    ],
    data: getReturnCustodyVaultInstructionDataEncoder().encode({}),
    programAddress,
  } as ReturnCustodyVaultInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountShareClass,
    TAccountCustodyVault,
    TAccountMint,
    TAccountEscrow,
    TAccountBeneficiaryTokenAccount,
    TAccountEscrowMarker,
    TAccountTokenProgram,
    TAccountAuthorityAdminRecord
  >);
}

export type ParsedReturnCustodyVaultInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Vault authority with a live Admin role at any time (`Active`/`Triggered`); anyone
     * once a positive `deadline` has passed (`deadline == 0` — the only
     * non-positive value `open_custody_vault` accepts — disables the
     * permissionless path). Mut: receives the closed escrow marker's rent.
     */
    signer: TAccountMetas[0];
    shareClass: TAccountMetas[1];
    custodyVault: TAccountMetas[2];
    mint: TAccountMetas[3];
    escrow: TAccountMetas[4];
    /** The beneficiary receives the escrowed tokens back here. */
    beneficiaryTokenAccount: TAccountMetas[5];
    /**
     * Escrow marker for the vault PDA. Closed by the handler (rent → signer)
     * ONLY when the escrow is fully drained — a return that had to withhold an
     * un-releasable surplus leaves the vault (and this marker) alive so the
     * remainder still has an exit. Closed manually rather than with
     * `close = signer` for exactly that reason.
     */
    escrowMarker: TAccountMetas[6];
    tokenProgram: TAccountMetas[7];
    authorityAdminRecord: TAccountMetas[8];
  };
  data: ReturnCustodyVaultInstructionData;
};

export function parseReturnCustodyVaultInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedReturnCustodyVaultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      shareClass: getNextAccount(),
      custodyVault: getNextAccount(),
      mint: getNextAccount(),
      escrow: getNextAccount(),
      beneficiaryTokenAccount: getNextAccount(),
      escrowMarker: getNextAccount(),
      tokenProgram: getNextAccount(),
      authorityAdminRecord: getNextAccount(),
    },
    data: getReturnCustodyVaultInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
