/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findAcceptIssuerAuthorityTransferPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const PROPOSE_ISSUER_AUTHORITY_DISCRIMINATOR = new Uint8Array([
  39, 132, 136, 42, 232, 76, 223, 142,
]);

export function getProposeIssuerAuthorityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PROPOSE_ISSUER_AUTHORITY_DISCRIMINATOR,
  );
}

export type ProposeIssuerAuthorityInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TAccountTransfer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountIssuer extends string
        ? ReadonlyAccount<TAccountIssuer>
        : TAccountIssuer,
      TAccountTransfer extends string
        ? WritableAccount<TAccountTransfer>
        : TAccountTransfer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ProposeIssuerAuthorityInstructionData = {
  discriminator: ReadonlyUint8Array;
  newAuthority: Address;
};

export type ProposeIssuerAuthorityInstructionDataArgs = {
  newAuthority: Address;
};

export function getProposeIssuerAuthorityInstructionDataEncoder(): FixedSizeEncoder<ProposeIssuerAuthorityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newAuthority", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: PROPOSE_ISSUER_AUTHORITY_DISCRIMINATOR,
    }),
  );
}

export function getProposeIssuerAuthorityInstructionDataDecoder(): FixedSizeDecoder<ProposeIssuerAuthorityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newAuthority", getAddressDecoder()],
  ]);
}

export function getProposeIssuerAuthorityInstructionDataCodec(): FixedSizeCodec<
  ProposeIssuerAuthorityInstructionDataArgs,
  ProposeIssuerAuthorityInstructionData
> {
  return combineCodec(
    getProposeIssuerAuthorityInstructionDataEncoder(),
    getProposeIssuerAuthorityInstructionDataDecoder(),
  );
}

export type ProposeIssuerAuthorityAsyncInput<
  TAccountAuthority extends string = string,
  TAccountIssuer extends string = string,
  TAccountTransfer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  issuer: Address<TAccountIssuer>;
  transfer?: Address<TAccountTransfer>;
  systemProgram?: Address<TAccountSystemProgram>;
  newAuthority: ProposeIssuerAuthorityInstructionDataArgs["newAuthority"];
};

export async function getProposeIssuerAuthorityInstructionAsync<
  TAccountAuthority extends string,
  TAccountIssuer extends string,
  TAccountTransfer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ProposeIssuerAuthorityAsyncInput<
    TAccountAuthority,
    TAccountIssuer,
    TAccountTransfer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ProposeIssuerAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIssuer,
    TAccountTransfer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    issuer: { value: input.issuer ?? null, isWritable: false },
    transfer: { value: input.transfer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.transfer.value) {
    accounts.transfer.value = await findAcceptIssuerAuthorityTransferPda({
      issuer: expectAddress(accounts.issuer.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.transfer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getProposeIssuerAuthorityInstructionDataEncoder().encode(
      args as ProposeIssuerAuthorityInstructionDataArgs,
    ),
    programAddress,
  } as ProposeIssuerAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIssuer,
    TAccountTransfer,
    TAccountSystemProgram
  >);
}

export type ProposeIssuerAuthorityInput<
  TAccountAuthority extends string = string,
  TAccountIssuer extends string = string,
  TAccountTransfer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  issuer: Address<TAccountIssuer>;
  transfer: Address<TAccountTransfer>;
  systemProgram?: Address<TAccountSystemProgram>;
  newAuthority: ProposeIssuerAuthorityInstructionDataArgs["newAuthority"];
};

export function getProposeIssuerAuthorityInstruction<
  TAccountAuthority extends string,
  TAccountIssuer extends string,
  TAccountTransfer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ProposeIssuerAuthorityInput<
    TAccountAuthority,
    TAccountIssuer,
    TAccountTransfer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ProposeIssuerAuthorityInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountIssuer,
  TAccountTransfer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    issuer: { value: input.issuer ?? null, isWritable: false },
    transfer: { value: input.transfer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.issuer),
      getAccountMeta(accounts.transfer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getProposeIssuerAuthorityInstructionDataEncoder().encode(
      args as ProposeIssuerAuthorityInstructionDataArgs,
    ),
    programAddress,
  } as ProposeIssuerAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIssuer,
    TAccountTransfer,
    TAccountSystemProgram
  >);
}

export type ParsedProposeIssuerAuthorityInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    issuer: TAccountMetas[1];
    transfer: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: ProposeIssuerAuthorityInstructionData;
};

export function parseProposeIssuerAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProposeIssuerAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      issuer: getNextAccount(),
      transfer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getProposeIssuerAuthorityInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
