/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findAdminRecordPda, findHolderEscrowMarkerPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CLAWBACK_BLOCKLISTED_HOLDER_DISCRIMINATOR = new Uint8Array([
  79, 167, 196, 210, 61, 140, 75, 221,
]);

export function getClawbackBlocklistedHolderDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAWBACK_BLOCKLISTED_HOLDER_DISCRIMINATOR,
  );
}

export type ClawbackBlocklistedHolderInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAdminRecord extends string | AccountMeta<string> = string,
  TAccountShareClass extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountHolderShareAccount extends string | AccountMeta<string> = string,
  TAccountHolderEscrowMarker extends string | AccountMeta<string> = string,
  TAccountBlockEntry extends string | AccountMeta<string> = string,
  TAccountDestination extends string | AccountMeta<string> = string,
  TAccountCustodyVault extends string | AccountMeta<string> = string,
  TAccountHookConfig extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAdminRecord extends string
        ? ReadonlyAccount<TAccountAdminRecord>
        : TAccountAdminRecord,
      TAccountShareClass extends string
        ? ReadonlyAccount<TAccountShareClass>
        : TAccountShareClass,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountHolderShareAccount extends string
        ? WritableAccount<TAccountHolderShareAccount>
        : TAccountHolderShareAccount,
      TAccountHolderEscrowMarker extends string
        ? ReadonlyAccount<TAccountHolderEscrowMarker>
        : TAccountHolderEscrowMarker,
      TAccountBlockEntry extends string
        ? ReadonlyAccount<TAccountBlockEntry>
        : TAccountBlockEntry,
      TAccountDestination extends string
        ? WritableAccount<TAccountDestination>
        : TAccountDestination,
      TAccountCustodyVault extends string
        ? ReadonlyAccount<TAccountCustodyVault>
        : TAccountCustodyVault,
      TAccountHookConfig extends string
        ? ReadonlyAccount<TAccountHookConfig>
        : TAccountHookConfig,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ClawbackBlocklistedHolderInstructionData = {
  discriminator: ReadonlyUint8Array;
  holder: Address;
  amount: bigint;
};

export type ClawbackBlocklistedHolderInstructionDataArgs = {
  holder: Address;
  amount: number | bigint;
};

export function getClawbackBlocklistedHolderInstructionDataEncoder(): FixedSizeEncoder<ClawbackBlocklistedHolderInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["holder", getAddressEncoder()],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: CLAWBACK_BLOCKLISTED_HOLDER_DISCRIMINATOR,
    }),
  );
}

export function getClawbackBlocklistedHolderInstructionDataDecoder(): FixedSizeDecoder<ClawbackBlocklistedHolderInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["holder", getAddressDecoder()],
    ["amount", getU64Decoder()],
  ]);
}

export function getClawbackBlocklistedHolderInstructionDataCodec(): FixedSizeCodec<
  ClawbackBlocklistedHolderInstructionDataArgs,
  ClawbackBlocklistedHolderInstructionData
> {
  return combineCodec(
    getClawbackBlocklistedHolderInstructionDataEncoder(),
    getClawbackBlocklistedHolderInstructionDataDecoder(),
  );
}

export type ClawbackBlocklistedHolderAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountShareClass extends string = string,
  TAccountMint extends string = string,
  TAccountHolderShareAccount extends string = string,
  TAccountHolderEscrowMarker extends string = string,
  TAccountBlockEntry extends string = string,
  TAccountDestination extends string = string,
  TAccountCustodyVault extends string = string,
  TAccountHookConfig extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /**
   * Admin gate — the signer must hold an `Admin` record. This is the second
   * key; the first is the BlocklistAuthority that created `block_entry`.
   */
  adminRecord?: Address<TAccountAdminRecord>;
  shareClass: Address<TAccountShareClass>;
  mint: Address<TAccountMint>;
  /**
   * Any token account of this mint owned by `holder` — the clawback source.
   * The hook independently re-derives its `BlockEntry` from this account's
   * owner, so the evidence chain is `source.owner == holder ==
   * BlockEntry.wallet`.
   */
  holderShareAccount: Address<TAccountHolderShareAccount>;
  /**
   * Proof that `holder` is an ordinary wallet and not one of this program's
   * own escrows (`EscrowMarker` / `EscrowIdentity` at this address). The
   * BlocklistAuthority can block ANY pubkey, escrow PDAs included, so even
   * with both keys a program escrow cannot be targeted — see
   * `clawback_from_holder` for the full reasoning.
   */
  holderEscrowMarker?: Address<TAccountHolderEscrowMarker>;
  /**
   * The holder's transfer-hook `BlockEntry` PDA `["blocked", holder]` —
   * the first key. Only the hook's `BlocklistAuthority` can create it.
   * discriminator, wallet) checked by `util::require_blocklisted`.
   */
  blockEntry?: Address<TAccountBlockEntry>;
  /**
   * Destination — the escrow of `custody_vault` below (burn-only quarantine
   * of THIS share class).
   */
  destination: Address<TAccountDestination>;
  /**
   * The quarantine vault — identical constraints to `clawback_from_holder`:
   * a typed, Active `RedemptionQueue` + `BurnAndAttest` `CustodyVault` of
   * this class whose every exit burns.
   */
  custodyVault: Address<TAccountCustodyVault>;
  /**
   * The mint's `TransferHookConfig` PDA (`["hook_cfg", mint]` under the
   * hook), in ANY mode — it tells the handler whether the mint is KycGated.
   * checks it is hook-owned and names this mint and share class.
   */
  hookConfig?: Address<TAccountHookConfig>;
  tokenProgram?: Address<TAccountTokenProgram>;
  holder: ClawbackBlocklistedHolderInstructionDataArgs["holder"];
  amount: ClawbackBlocklistedHolderInstructionDataArgs["amount"];
};

export async function getClawbackBlocklistedHolderInstructionAsync<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountShareClass extends string,
  TAccountMint extends string,
  TAccountHolderShareAccount extends string,
  TAccountHolderEscrowMarker extends string,
  TAccountBlockEntry extends string,
  TAccountDestination extends string,
  TAccountCustodyVault extends string,
  TAccountHookConfig extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ClawbackBlocklistedHolderAsyncInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass,
    TAccountMint,
    TAccountHolderShareAccount,
    TAccountHolderEscrowMarker,
    TAccountBlockEntry,
    TAccountDestination,
    TAccountCustodyVault,
    TAccountHookConfig,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ClawbackBlocklistedHolderInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass,
    TAccountMint,
    TAccountHolderShareAccount,
    TAccountHolderEscrowMarker,
    TAccountBlockEntry,
    TAccountDestination,
    TAccountCustodyVault,
    TAccountHookConfig,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    holderShareAccount: {
      value: input.holderShareAccount ?? null,
      isWritable: true,
    },
    holderEscrowMarker: {
      value: input.holderEscrowMarker ?? null,
      isWritable: false,
    },
    blockEntry: { value: input.blockEntry ?? null, isWritable: false },
    destination: { value: input.destination ?? null, isWritable: true },
    custodyVault: { value: input.custodyVault ?? null, isWritable: false },
    hookConfig: { value: input.hookConfig ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.adminRecord.value) {
    accounts.adminRecord.value = await findAdminRecordPda({
      authority: expectAddress(accounts.authority.value),
    });
  }
  if (!accounts.holderEscrowMarker.value) {
    accounts.holderEscrowMarker.value = await findHolderEscrowMarkerPda({
      holder: expectSome(args.holder),
    });
  }
  if (!accounts.blockEntry.value) {
    accounts.blockEntry.value = await getProgramDerivedAddress({
      programAddress:
        "GBDyesyTr266LqKeFq95r1DeigRyHpfw6ACWdjENHAPy" as Address<"GBDyesyTr266LqKeFq95r1DeigRyHpfw6ACWdjENHAPy">,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([98, 108, 111, 99, 107, 101, 100]),
        ),
        getAddressEncoder().encode(expectSome(args.holder)),
      ],
    });
  }
  if (!accounts.hookConfig.value) {
    accounts.hookConfig.value = await getProgramDerivedAddress({
      programAddress:
        "GBDyesyTr266LqKeFq95r1DeigRyHpfw6ACWdjENHAPy" as Address<"GBDyesyTr266LqKeFq95r1DeigRyHpfw6ACWdjENHAPy">,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([104, 111, 111, 107, 95, 99, 102, 103]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.mint.value)),
      ],
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.holderShareAccount),
      getAccountMeta(accounts.holderEscrowMarker),
      getAccountMeta(accounts.blockEntry),
      getAccountMeta(accounts.destination),
      getAccountMeta(accounts.custodyVault),
      getAccountMeta(accounts.hookConfig),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getClawbackBlocklistedHolderInstructionDataEncoder().encode(
      args as ClawbackBlocklistedHolderInstructionDataArgs,
    ),
    programAddress,
  } as ClawbackBlocklistedHolderInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass,
    TAccountMint,
    TAccountHolderShareAccount,
    TAccountHolderEscrowMarker,
    TAccountBlockEntry,
    TAccountDestination,
    TAccountCustodyVault,
    TAccountHookConfig,
    TAccountTokenProgram
  >);
}

export type ClawbackBlocklistedHolderInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountShareClass extends string = string,
  TAccountMint extends string = string,
  TAccountHolderShareAccount extends string = string,
  TAccountHolderEscrowMarker extends string = string,
  TAccountBlockEntry extends string = string,
  TAccountDestination extends string = string,
  TAccountCustodyVault extends string = string,
  TAccountHookConfig extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /**
   * Admin gate — the signer must hold an `Admin` record. This is the second
   * key; the first is the BlocklistAuthority that created `block_entry`.
   */
  adminRecord: Address<TAccountAdminRecord>;
  shareClass: Address<TAccountShareClass>;
  mint: Address<TAccountMint>;
  /**
   * Any token account of this mint owned by `holder` — the clawback source.
   * The hook independently re-derives its `BlockEntry` from this account's
   * owner, so the evidence chain is `source.owner == holder ==
   * BlockEntry.wallet`.
   */
  holderShareAccount: Address<TAccountHolderShareAccount>;
  /**
   * Proof that `holder` is an ordinary wallet and not one of this program's
   * own escrows (`EscrowMarker` / `EscrowIdentity` at this address). The
   * BlocklistAuthority can block ANY pubkey, escrow PDAs included, so even
   * with both keys a program escrow cannot be targeted — see
   * `clawback_from_holder` for the full reasoning.
   */
  holderEscrowMarker: Address<TAccountHolderEscrowMarker>;
  /**
   * The holder's transfer-hook `BlockEntry` PDA `["blocked", holder]` —
   * the first key. Only the hook's `BlocklistAuthority` can create it.
   * discriminator, wallet) checked by `util::require_blocklisted`.
   */
  blockEntry: Address<TAccountBlockEntry>;
  /**
   * Destination — the escrow of `custody_vault` below (burn-only quarantine
   * of THIS share class).
   */
  destination: Address<TAccountDestination>;
  /**
   * The quarantine vault — identical constraints to `clawback_from_holder`:
   * a typed, Active `RedemptionQueue` + `BurnAndAttest` `CustodyVault` of
   * this class whose every exit burns.
   */
  custodyVault: Address<TAccountCustodyVault>;
  /**
   * The mint's `TransferHookConfig` PDA (`["hook_cfg", mint]` under the
   * hook), in ANY mode — it tells the handler whether the mint is KycGated.
   * checks it is hook-owned and names this mint and share class.
   */
  hookConfig: Address<TAccountHookConfig>;
  tokenProgram?: Address<TAccountTokenProgram>;
  holder: ClawbackBlocklistedHolderInstructionDataArgs["holder"];
  amount: ClawbackBlocklistedHolderInstructionDataArgs["amount"];
};

export function getClawbackBlocklistedHolderInstruction<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountShareClass extends string,
  TAccountMint extends string,
  TAccountHolderShareAccount extends string,
  TAccountHolderEscrowMarker extends string,
  TAccountBlockEntry extends string,
  TAccountDestination extends string,
  TAccountCustodyVault extends string,
  TAccountHookConfig extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ClawbackBlocklistedHolderInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass,
    TAccountMint,
    TAccountHolderShareAccount,
    TAccountHolderEscrowMarker,
    TAccountBlockEntry,
    TAccountDestination,
    TAccountCustodyVault,
    TAccountHookConfig,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ClawbackBlocklistedHolderInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAdminRecord,
  TAccountShareClass,
  TAccountMint,
  TAccountHolderShareAccount,
  TAccountHolderEscrowMarker,
  TAccountBlockEntry,
  TAccountDestination,
  TAccountCustodyVault,
  TAccountHookConfig,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    holderShareAccount: {
      value: input.holderShareAccount ?? null,
      isWritable: true,
    },
    holderEscrowMarker: {
      value: input.holderEscrowMarker ?? null,
      isWritable: false,
    },
    blockEntry: { value: input.blockEntry ?? null, isWritable: false },
    destination: { value: input.destination ?? null, isWritable: true },
    custodyVault: { value: input.custodyVault ?? null, isWritable: false },
    hookConfig: { value: input.hookConfig ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.holderShareAccount),
      getAccountMeta(accounts.holderEscrowMarker),
      getAccountMeta(accounts.blockEntry),
      getAccountMeta(accounts.destination),
      getAccountMeta(accounts.custodyVault),
      getAccountMeta(accounts.hookConfig),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getClawbackBlocklistedHolderInstructionDataEncoder().encode(
      args as ClawbackBlocklistedHolderInstructionDataArgs,
    ),
    programAddress,
  } as ClawbackBlocklistedHolderInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass,
    TAccountMint,
    TAccountHolderShareAccount,
    TAccountHolderEscrowMarker,
    TAccountBlockEntry,
    TAccountDestination,
    TAccountCustodyVault,
    TAccountHookConfig,
    TAccountTokenProgram
  >);
}

export type ParsedClawbackBlocklistedHolderInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    /**
     * Admin gate — the signer must hold an `Admin` record. This is the second
     * key; the first is the BlocklistAuthority that created `block_entry`.
     */
    adminRecord: TAccountMetas[1];
    shareClass: TAccountMetas[2];
    mint: TAccountMetas[3];
    /**
     * Any token account of this mint owned by `holder` — the clawback source.
     * The hook independently re-derives its `BlockEntry` from this account's
     * owner, so the evidence chain is `source.owner == holder ==
     * BlockEntry.wallet`.
     */
    holderShareAccount: TAccountMetas[4];
    /**
     * Proof that `holder` is an ordinary wallet and not one of this program's
     * own escrows (`EscrowMarker` / `EscrowIdentity` at this address). The
     * BlocklistAuthority can block ANY pubkey, escrow PDAs included, so even
     * with both keys a program escrow cannot be targeted — see
     * `clawback_from_holder` for the full reasoning.
     */
    holderEscrowMarker: TAccountMetas[5];
    /**
     * The holder's transfer-hook `BlockEntry` PDA `["blocked", holder]` —
     * the first key. Only the hook's `BlocklistAuthority` can create it.
     * discriminator, wallet) checked by `util::require_blocklisted`.
     */
    blockEntry: TAccountMetas[6];
    /**
     * Destination — the escrow of `custody_vault` below (burn-only quarantine
     * of THIS share class).
     */
    destination: TAccountMetas[7];
    /**
     * The quarantine vault — identical constraints to `clawback_from_holder`:
     * a typed, Active `RedemptionQueue` + `BurnAndAttest` `CustodyVault` of
     * this class whose every exit burns.
     */
    custodyVault: TAccountMetas[8];
    /**
     * The mint's `TransferHookConfig` PDA (`["hook_cfg", mint]` under the
     * hook), in ANY mode — it tells the handler whether the mint is KycGated.
     * checks it is hook-owned and names this mint and share class.
     */
    hookConfig: TAccountMetas[9];
    tokenProgram: TAccountMetas[10];
  };
  data: ClawbackBlocklistedHolderInstructionData;
};

export function parseClawbackBlocklistedHolderInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClawbackBlocklistedHolderInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 11) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      adminRecord: getNextAccount(),
      shareClass: getNextAccount(),
      mint: getNextAccount(),
      holderShareAccount: getNextAccount(),
      holderEscrowMarker: getNextAccount(),
      blockEntry: getNextAccount(),
      destination: getNextAccount(),
      custodyVault: getNextAccount(),
      hookConfig: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getClawbackBlocklistedHolderInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
