/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findAdminRecordPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const LOCK_SUPPLY_DISCRIMINATOR = new Uint8Array([
  69, 31, 184, 123, 116, 65, 61, 37,
]);

export function getLockSupplyDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(LOCK_SUPPLY_DISCRIMINATOR);
}

export type LockSupplyInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAdminRecord extends string | AccountMeta<string> = string,
  TAccountShareClass extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAdminRecord extends string
        ? ReadonlyAccount<TAccountAdminRecord>
        : TAccountAdminRecord,
      TAccountShareClass extends string
        ? WritableAccount<TAccountShareClass>
        : TAccountShareClass,
      ...TRemainingAccounts,
    ]
  >;

export type LockSupplyInstructionData = { discriminator: ReadonlyUint8Array };

export type LockSupplyInstructionDataArgs = {};

export function getLockSupplyInstructionDataEncoder(): FixedSizeEncoder<LockSupplyInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: LOCK_SUPPLY_DISCRIMINATOR }),
  );
}

export function getLockSupplyInstructionDataDecoder(): FixedSizeDecoder<LockSupplyInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getLockSupplyInstructionDataCodec(): FixedSizeCodec<
  LockSupplyInstructionDataArgs,
  LockSupplyInstructionData
> {
  return combineCodec(
    getLockSupplyInstructionDataEncoder(),
    getLockSupplyInstructionDataDecoder(),
  );
}

export type LockSupplyAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountShareClass extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /** Admin gate — only an admin may lock a share class's supply. */
  adminRecord?: Address<TAccountAdminRecord>;
  shareClass: Address<TAccountShareClass>;
};

export async function getLockSupplyInstructionAsync<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountShareClass extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: LockSupplyAsyncInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  LockSupplyInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.adminRecord.value) {
    accounts.adminRecord.value = await findAdminRecordPda({
      authority: expectAddress(accounts.authority.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.shareClass),
    ],
    data: getLockSupplyInstructionDataEncoder().encode({}),
    programAddress,
  } as LockSupplyInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass
  >);
}

export type LockSupplyInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountShareClass extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /** Admin gate — only an admin may lock a share class's supply. */
  adminRecord: Address<TAccountAdminRecord>;
  shareClass: Address<TAccountShareClass>;
};

export function getLockSupplyInstruction<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountShareClass extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: LockSupplyInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass
  >,
  config?: { programAddress?: TProgramAddress },
): LockSupplyInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAdminRecord,
  TAccountShareClass
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.shareClass),
    ],
    data: getLockSupplyInstructionDataEncoder().encode({}),
    programAddress,
  } as LockSupplyInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountShareClass
  >);
}

export type ParsedLockSupplyInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    /** Admin gate — only an admin may lock a share class's supply. */
    adminRecord: TAccountMetas[1];
    shareClass: TAccountMetas[2];
  };
  data: LockSupplyInstructionData;
};

export function parseLockSupplyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedLockSupplyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      adminRecord: getNextAccount(),
      shareClass: getNextAccount(),
    },
    data: getLockSupplyInstructionDataDecoder().decode(instruction.data),
  };
}
