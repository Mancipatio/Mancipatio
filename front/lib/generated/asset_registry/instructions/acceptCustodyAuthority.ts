/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  findNewAdminRecordPda,
  findPlatformPda,
  findTransferPda,
} from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const ACCEPT_CUSTODY_AUTHORITY_DISCRIMINATOR = new Uint8Array([
  174, 229, 47, 38, 148, 225, 135, 204,
]);

export function getAcceptCustodyAuthorityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ACCEPT_CUSTODY_AUTHORITY_DISCRIMINATOR,
  );
}

export type AcceptCustodyAuthorityInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountNewAuthority extends string | AccountMeta<string> = string,
  TAccountPlatform extends string | AccountMeta<string> = string,
  TAccountCustodyVault extends string | AccountMeta<string> = string,
  TAccountNewAdminRecord extends string | AccountMeta<string> = string,
  TAccountTransfer extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountNewAuthority extends string
        ? WritableSignerAccount<TAccountNewAuthority> &
            AccountSignerMeta<TAccountNewAuthority>
        : TAccountNewAuthority,
      TAccountPlatform extends string
        ? ReadonlyAccount<TAccountPlatform>
        : TAccountPlatform,
      TAccountCustodyVault extends string
        ? WritableAccount<TAccountCustodyVault>
        : TAccountCustodyVault,
      TAccountNewAdminRecord extends string
        ? ReadonlyAccount<TAccountNewAdminRecord>
        : TAccountNewAdminRecord,
      TAccountTransfer extends string
        ? WritableAccount<TAccountTransfer>
        : TAccountTransfer,
      ...TRemainingAccounts,
    ]
  >;

export type AcceptCustodyAuthorityInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AcceptCustodyAuthorityInstructionDataArgs = {};

export function getAcceptCustodyAuthorityInstructionDataEncoder(): FixedSizeEncoder<AcceptCustodyAuthorityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: ACCEPT_CUSTODY_AUTHORITY_DISCRIMINATOR,
    }),
  );
}

export function getAcceptCustodyAuthorityInstructionDataDecoder(): FixedSizeDecoder<AcceptCustodyAuthorityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAcceptCustodyAuthorityInstructionDataCodec(): FixedSizeCodec<
  AcceptCustodyAuthorityInstructionDataArgs,
  AcceptCustodyAuthorityInstructionData
> {
  return combineCodec(
    getAcceptCustodyAuthorityInstructionDataEncoder(),
    getAcceptCustodyAuthorityInstructionDataDecoder(),
  );
}

export type AcceptCustodyAuthorityAsyncInput<
  TAccountNewAuthority extends string = string,
  TAccountPlatform extends string = string,
  TAccountCustodyVault extends string = string,
  TAccountNewAdminRecord extends string = string,
  TAccountTransfer extends string = string,
> = {
  newAuthority: TransactionSigner<TAccountNewAuthority>;
  platform?: Address<TAccountPlatform>;
  custodyVault: Address<TAccountCustodyVault>;
  newAdminRecord?: Address<TAccountNewAdminRecord>;
  transfer?: Address<TAccountTransfer>;
};

export async function getAcceptCustodyAuthorityInstructionAsync<
  TAccountNewAuthority extends string,
  TAccountPlatform extends string,
  TAccountCustodyVault extends string,
  TAccountNewAdminRecord extends string,
  TAccountTransfer extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: AcceptCustodyAuthorityAsyncInput<
    TAccountNewAuthority,
    TAccountPlatform,
    TAccountCustodyVault,
    TAccountNewAdminRecord,
    TAccountTransfer
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AcceptCustodyAuthorityInstruction<
    TProgramAddress,
    TAccountNewAuthority,
    TAccountPlatform,
    TAccountCustodyVault,
    TAccountNewAdminRecord,
    TAccountTransfer
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    newAuthority: { value: input.newAuthority ?? null, isWritable: true },
    platform: { value: input.platform ?? null, isWritable: false },
    custodyVault: { value: input.custodyVault ?? null, isWritable: true },
    newAdminRecord: { value: input.newAdminRecord ?? null, isWritable: false },
    transfer: { value: input.transfer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.platform.value) {
    accounts.platform.value = await findPlatformPda();
  }
  if (!accounts.newAdminRecord.value) {
    accounts.newAdminRecord.value = await findNewAdminRecordPda({
      newAuthority: expectAddress(accounts.newAuthority.value),
    });
  }
  if (!accounts.transfer.value) {
    accounts.transfer.value = await findTransferPda({
      custodyVault: expectAddress(accounts.custodyVault.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.newAuthority),
      getAccountMeta(accounts.platform),
      getAccountMeta(accounts.custodyVault),
      getAccountMeta(accounts.newAdminRecord),
      getAccountMeta(accounts.transfer),
    ],
    data: getAcceptCustodyAuthorityInstructionDataEncoder().encode({}),
    programAddress,
  } as AcceptCustodyAuthorityInstruction<
    TProgramAddress,
    TAccountNewAuthority,
    TAccountPlatform,
    TAccountCustodyVault,
    TAccountNewAdminRecord,
    TAccountTransfer
  >);
}

export type AcceptCustodyAuthorityInput<
  TAccountNewAuthority extends string = string,
  TAccountPlatform extends string = string,
  TAccountCustodyVault extends string = string,
  TAccountNewAdminRecord extends string = string,
  TAccountTransfer extends string = string,
> = {
  newAuthority: TransactionSigner<TAccountNewAuthority>;
  platform: Address<TAccountPlatform>;
  custodyVault: Address<TAccountCustodyVault>;
  newAdminRecord: Address<TAccountNewAdminRecord>;
  transfer: Address<TAccountTransfer>;
};

export function getAcceptCustodyAuthorityInstruction<
  TAccountNewAuthority extends string,
  TAccountPlatform extends string,
  TAccountCustodyVault extends string,
  TAccountNewAdminRecord extends string,
  TAccountTransfer extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: AcceptCustodyAuthorityInput<
    TAccountNewAuthority,
    TAccountPlatform,
    TAccountCustodyVault,
    TAccountNewAdminRecord,
    TAccountTransfer
  >,
  config?: { programAddress?: TProgramAddress },
): AcceptCustodyAuthorityInstruction<
  TProgramAddress,
  TAccountNewAuthority,
  TAccountPlatform,
  TAccountCustodyVault,
  TAccountNewAdminRecord,
  TAccountTransfer
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    newAuthority: { value: input.newAuthority ?? null, isWritable: true },
    platform: { value: input.platform ?? null, isWritable: false },
    custodyVault: { value: input.custodyVault ?? null, isWritable: true },
    newAdminRecord: { value: input.newAdminRecord ?? null, isWritable: false },
    transfer: { value: input.transfer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.newAuthority),
      getAccountMeta(accounts.platform),
      getAccountMeta(accounts.custodyVault),
      getAccountMeta(accounts.newAdminRecord),
      getAccountMeta(accounts.transfer),
    ],
    data: getAcceptCustodyAuthorityInstructionDataEncoder().encode({}),
    programAddress,
  } as AcceptCustodyAuthorityInstruction<
    TProgramAddress,
    TAccountNewAuthority,
    TAccountPlatform,
    TAccountCustodyVault,
    TAccountNewAdminRecord,
    TAccountTransfer
  >);
}

export type ParsedAcceptCustodyAuthorityInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    newAuthority: TAccountMetas[0];
    platform: TAccountMetas[1];
    custodyVault: TAccountMetas[2];
    newAdminRecord: TAccountMetas[3];
    transfer: TAccountMetas[4];
  };
  data: AcceptCustodyAuthorityInstructionData;
};

export function parseAcceptCustodyAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAcceptCustodyAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      newAuthority: getNextAccount(),
      platform: getNextAccount(),
      custodyVault: getNextAccount(),
      newAdminRecord: getNextAccount(),
      transfer: getNextAccount(),
    },
    data: getAcceptCustodyAuthorityInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
