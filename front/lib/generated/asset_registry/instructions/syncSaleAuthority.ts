/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SYNC_SALE_AUTHORITY_DISCRIMINATOR = new Uint8Array([
  135, 153, 185, 21, 112, 64, 166, 184,
]);

export function getSyncSaleAuthorityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SYNC_SALE_AUTHORITY_DISCRIMINATOR,
  );
}

export type SyncSaleAuthorityInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountSale extends string | AccountMeta<string> = string,
  TAccountShareClass extends string | AccountMeta<string> = string,
  TAccountAsset extends string | AccountMeta<string> = string,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSale extends string
        ? WritableAccount<TAccountSale>
        : TAccountSale,
      TAccountShareClass extends string
        ? ReadonlyAccount<TAccountShareClass>
        : TAccountShareClass,
      TAccountAsset extends string
        ? ReadonlyAccount<TAccountAsset>
        : TAccountAsset,
      TAccountIssuer extends string
        ? ReadonlyAccount<TAccountIssuer>
        : TAccountIssuer,
      ...TRemainingAccounts,
    ]
  >;

export type SyncSaleAuthorityInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type SyncSaleAuthorityInstructionDataArgs = {};

export function getSyncSaleAuthorityInstructionDataEncoder(): FixedSizeEncoder<SyncSaleAuthorityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SYNC_SALE_AUTHORITY_DISCRIMINATOR }),
  );
}

export function getSyncSaleAuthorityInstructionDataDecoder(): FixedSizeDecoder<SyncSaleAuthorityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSyncSaleAuthorityInstructionDataCodec(): FixedSizeCodec<
  SyncSaleAuthorityInstructionDataArgs,
  SyncSaleAuthorityInstructionData
> {
  return combineCodec(
    getSyncSaleAuthorityInstructionDataEncoder(),
    getSyncSaleAuthorityInstructionDataDecoder(),
  );
}

export type SyncSaleAuthorityInput<
  TAccountSale extends string = string,
  TAccountShareClass extends string = string,
  TAccountAsset extends string = string,
  TAccountIssuer extends string = string,
> = {
  sale: Address<TAccountSale>;
  /** only `asset` (byte 8) is read. */
  shareClass: Address<TAccountShareClass>;
  /** only `issuer` (byte 8) is read. */
  asset: Address<TAccountAsset>;
  issuer: Address<TAccountIssuer>;
};

export function getSyncSaleAuthorityInstruction<
  TAccountSale extends string,
  TAccountShareClass extends string,
  TAccountAsset extends string,
  TAccountIssuer extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: SyncSaleAuthorityInput<
    TAccountSale,
    TAccountShareClass,
    TAccountAsset,
    TAccountIssuer
  >,
  config?: { programAddress?: TProgramAddress },
): SyncSaleAuthorityInstruction<
  TProgramAddress,
  TAccountSale,
  TAccountShareClass,
  TAccountAsset,
  TAccountIssuer
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    sale: { value: input.sale ?? null, isWritable: true },
    shareClass: { value: input.shareClass ?? null, isWritable: false },
    asset: { value: input.asset ?? null, isWritable: false },
    issuer: { value: input.issuer ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.sale),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.asset),
      getAccountMeta(accounts.issuer),
    ],
    data: getSyncSaleAuthorityInstructionDataEncoder().encode({}),
    programAddress,
  } as SyncSaleAuthorityInstruction<
    TProgramAddress,
    TAccountSale,
    TAccountShareClass,
    TAccountAsset,
    TAccountIssuer
  >);
}

export type ParsedSyncSaleAuthorityInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    sale: TAccountMetas[0];
    /** only `asset` (byte 8) is read. */
    shareClass: TAccountMetas[1];
    /** only `issuer` (byte 8) is read. */
    asset: TAccountMetas[2];
    issuer: TAccountMetas[3];
  };
  data: SyncSaleAuthorityInstructionData;
};

export function parseSyncSaleAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSyncSaleAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      sale: getNextAccount(),
      shareClass: getNextAccount(),
      asset: getNextAccount(),
      issuer: getNextAccount(),
    },
    data: getSyncSaleAuthorityInstructionDataDecoder().decode(instruction.data),
  };
}
