/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findPositionPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const PUSH_VESTED_DISCRIMINATOR = new Uint8Array([
  188, 139, 156, 15, 34, 90, 42, 40,
]);

export function getPushVestedDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(PUSH_VESTED_DISCRIMINATOR);
}

export type PushVestedInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSeries extends string | AccountMeta<string> = string,
  TAccountPosition extends string | AccountMeta<string> = string,
  TAccountTokenMint extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountRecipientTokenAccount extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSeries extends string
        ? WritableAccount<TAccountSeries>
        : TAccountSeries,
      TAccountPosition extends string
        ? WritableAccount<TAccountPosition>
        : TAccountPosition,
      TAccountTokenMint extends string
        ? ReadonlyAccount<TAccountTokenMint>
        : TAccountTokenMint,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountRecipientTokenAccount extends string
        ? WritableAccount<TAccountRecipientTokenAccount>
        : TAccountRecipientTokenAccount,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PushVestedInstructionData = {
  discriminator: ReadonlyUint8Array;
  positionIndex: number;
};

export type PushVestedInstructionDataArgs = { positionIndex: number };

export function getPushVestedInstructionDataEncoder(): FixedSizeEncoder<PushVestedInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["positionIndex", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PUSH_VESTED_DISCRIMINATOR }),
  );
}

export function getPushVestedInstructionDataDecoder(): FixedSizeDecoder<PushVestedInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["positionIndex", getU32Decoder()],
  ]);
}

export function getPushVestedInstructionDataCodec(): FixedSizeCodec<
  PushVestedInstructionDataArgs,
  PushVestedInstructionData
> {
  return combineCodec(
    getPushVestedInstructionDataEncoder(),
    getPushVestedInstructionDataDecoder(),
  );
}

export type PushVestedAsyncInput<
  TAccountPayer extends string = string,
  TAccountSeries extends string = string,
  TAccountPosition extends string = string,
  TAccountTokenMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountRecipientTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /**
   * Anyone — pays for the delivery. Permissionless by design: in Push mode
   * the contract releases on schedule without depending on the client, the
   * recipient, or Mancipatio (spec §11.1.8, §11.1.11).
   */
  payer: TransactionSigner<TAccountPayer>;
  series: Address<TAccountSeries>;
  position?: Address<TAccountPosition>;
  tokenMint: Address<TAccountTokenMint>;
  escrow: Address<TAccountEscrow>;
  /**
   * The recipient's token account — owned by the position's CURRENT wallet
   * (recovery re-points delivery).
   */
  recipientTokenAccount: Address<TAccountRecipientTokenAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
  positionIndex: PushVestedInstructionDataArgs["positionIndex"];
};

export async function getPushVestedInstructionAsync<
  TAccountPayer extends string,
  TAccountSeries extends string,
  TAccountPosition extends string,
  TAccountTokenMint extends string,
  TAccountEscrow extends string,
  TAccountRecipientTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: PushVestedAsyncInput<
    TAccountPayer,
    TAccountSeries,
    TAccountPosition,
    TAccountTokenMint,
    TAccountEscrow,
    TAccountRecipientTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PushVestedInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountSeries,
    TAccountPosition,
    TAccountTokenMint,
    TAccountEscrow,
    TAccountRecipientTokenAccount,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    series: { value: input.series ?? null, isWritable: true },
    position: { value: input.position ?? null, isWritable: true },
    tokenMint: { value: input.tokenMint ?? null, isWritable: false },
    escrow: { value: input.escrow ?? null, isWritable: true },
    recipientTokenAccount: {
      value: input.recipientTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.position.value) {
    accounts.position.value = await findPositionPda({
      series: expectAddress(accounts.series.value),
      positionIndex: expectSome(args.positionIndex),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.series),
      getAccountMeta(accounts.position),
      getAccountMeta(accounts.tokenMint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.recipientTokenAccount),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getPushVestedInstructionDataEncoder().encode(
      args as PushVestedInstructionDataArgs,
    ),
    programAddress,
  } as PushVestedInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountSeries,
    TAccountPosition,
    TAccountTokenMint,
    TAccountEscrow,
    TAccountRecipientTokenAccount,
    TAccountTokenProgram
  >);
}

export type PushVestedInput<
  TAccountPayer extends string = string,
  TAccountSeries extends string = string,
  TAccountPosition extends string = string,
  TAccountTokenMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountRecipientTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /**
   * Anyone — pays for the delivery. Permissionless by design: in Push mode
   * the contract releases on schedule without depending on the client, the
   * recipient, or Mancipatio (spec §11.1.8, §11.1.11).
   */
  payer: TransactionSigner<TAccountPayer>;
  series: Address<TAccountSeries>;
  position: Address<TAccountPosition>;
  tokenMint: Address<TAccountTokenMint>;
  escrow: Address<TAccountEscrow>;
  /**
   * The recipient's token account — owned by the position's CURRENT wallet
   * (recovery re-points delivery).
   */
  recipientTokenAccount: Address<TAccountRecipientTokenAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
  positionIndex: PushVestedInstructionDataArgs["positionIndex"];
};

export function getPushVestedInstruction<
  TAccountPayer extends string,
  TAccountSeries extends string,
  TAccountPosition extends string,
  TAccountTokenMint extends string,
  TAccountEscrow extends string,
  TAccountRecipientTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: PushVestedInput<
    TAccountPayer,
    TAccountSeries,
    TAccountPosition,
    TAccountTokenMint,
    TAccountEscrow,
    TAccountRecipientTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PushVestedInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountSeries,
  TAccountPosition,
  TAccountTokenMint,
  TAccountEscrow,
  TAccountRecipientTokenAccount,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    series: { value: input.series ?? null, isWritable: true },
    position: { value: input.position ?? null, isWritable: true },
    tokenMint: { value: input.tokenMint ?? null, isWritable: false },
    escrow: { value: input.escrow ?? null, isWritable: true },
    recipientTokenAccount: {
      value: input.recipientTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.series),
      getAccountMeta(accounts.position),
      getAccountMeta(accounts.tokenMint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.recipientTokenAccount),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getPushVestedInstructionDataEncoder().encode(
      args as PushVestedInstructionDataArgs,
    ),
    programAddress,
  } as PushVestedInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountSeries,
    TAccountPosition,
    TAccountTokenMint,
    TAccountEscrow,
    TAccountRecipientTokenAccount,
    TAccountTokenProgram
  >);
}

export type ParsedPushVestedInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Anyone — pays for the delivery. Permissionless by design: in Push mode
     * the contract releases on schedule without depending on the client, the
     * recipient, or Mancipatio (spec §11.1.8, §11.1.11).
     */
    payer: TAccountMetas[0];
    series: TAccountMetas[1];
    position: TAccountMetas[2];
    tokenMint: TAccountMetas[3];
    escrow: TAccountMetas[4];
    /**
     * The recipient's token account — owned by the position's CURRENT wallet
     * (recovery re-points delivery).
     */
    recipientTokenAccount: TAccountMetas[5];
    tokenProgram: TAccountMetas[6];
  };
  data: PushVestedInstructionData;
};

export function parsePushVestedInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPushVestedInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      series: getNextAccount(),
      position: getNextAccount(),
      tokenMint: getNextAccount(),
      escrow: getNextAccount(),
      recipientTokenAccount: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getPushVestedInstructionDataDecoder().decode(instruction.data),
  };
}
