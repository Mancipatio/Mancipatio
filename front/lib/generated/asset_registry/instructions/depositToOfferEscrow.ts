/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findPlatformPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const DEPOSIT_TO_OFFER_ESCROW_DISCRIMINATOR = new Uint8Array([
  114, 64, 222, 144, 169, 162, 7, 76,
]);

export function getDepositToOfferEscrowDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DEPOSIT_TO_OFFER_ESCROW_DISCRIMINATOR,
  );
}

export type DepositToOfferEscrowInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMaker extends string | AccountMeta<string> = string,
  TAccountOffer extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountMakerShareAccount extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountPlatform extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMaker extends string
        ? ReadonlySignerAccount<TAccountMaker> &
            AccountSignerMeta<TAccountMaker>
        : TAccountMaker,
      TAccountOffer extends string
        ? WritableAccount<TAccountOffer>
        : TAccountOffer,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountMakerShareAccount extends string
        ? WritableAccount<TAccountMakerShareAccount>
        : TAccountMakerShareAccount,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountPlatform extends string
        ? ReadonlyAccount<TAccountPlatform>
        : TAccountPlatform,
      ...TRemainingAccounts,
    ]
  >;

export type DepositToOfferEscrowInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type DepositToOfferEscrowInstructionDataArgs = {
  amount: number | bigint;
};

export function getDepositToOfferEscrowInstructionDataEncoder(): FixedSizeEncoder<DepositToOfferEscrowInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: DEPOSIT_TO_OFFER_ESCROW_DISCRIMINATOR,
    }),
  );
}

export function getDepositToOfferEscrowInstructionDataDecoder(): FixedSizeDecoder<DepositToOfferEscrowInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getDepositToOfferEscrowInstructionDataCodec(): FixedSizeCodec<
  DepositToOfferEscrowInstructionDataArgs,
  DepositToOfferEscrowInstructionData
> {
  return combineCodec(
    getDepositToOfferEscrowInstructionDataEncoder(),
    getDepositToOfferEscrowInstructionDataDecoder(),
  );
}

export type DepositToOfferEscrowAsyncInput<
  TAccountMaker extends string = string,
  TAccountOffer extends string = string,
  TAccountMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountMakerShareAccount extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountPlatform extends string = string,
> = {
  /**
   * The offer's maker — the only party whose units this ledger may record,
   * and the only party the refund paths pay out to.
   */
  maker: TransactionSigner<TAccountMaker>;
  offer: Address<TAccountOffer>;
  mint: Address<TAccountMint>;
  escrow: Address<TAccountEscrow>;
  /** The maker's share-class token account — debited exactly `amount`. */
  makerShareAccount: Address<TAccountMakerShareAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
  /**
   * Emergency-pause gate (read-only). Keep LAST among named accounts: old
   * account indices and the remaining-accounts hook tail keep their positions.
   */
  platform?: Address<TAccountPlatform>;
  amount: DepositToOfferEscrowInstructionDataArgs["amount"];
};

export async function getDepositToOfferEscrowInstructionAsync<
  TAccountMaker extends string,
  TAccountOffer extends string,
  TAccountMint extends string,
  TAccountEscrow extends string,
  TAccountMakerShareAccount extends string,
  TAccountTokenProgram extends string,
  TAccountPlatform extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: DepositToOfferEscrowAsyncInput<
    TAccountMaker,
    TAccountOffer,
    TAccountMint,
    TAccountEscrow,
    TAccountMakerShareAccount,
    TAccountTokenProgram,
    TAccountPlatform
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DepositToOfferEscrowInstruction<
    TProgramAddress,
    TAccountMaker,
    TAccountOffer,
    TAccountMint,
    TAccountEscrow,
    TAccountMakerShareAccount,
    TAccountTokenProgram,
    TAccountPlatform
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    maker: { value: input.maker ?? null, isWritable: false },
    offer: { value: input.offer ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    escrow: { value: input.escrow ?? null, isWritable: true },
    makerShareAccount: {
      value: input.makerShareAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    platform: { value: input.platform ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.platform.value) {
    accounts.platform.value = await findPlatformPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.maker),
      getAccountMeta(accounts.offer),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.makerShareAccount),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.platform),
    ],
    data: getDepositToOfferEscrowInstructionDataEncoder().encode(
      args as DepositToOfferEscrowInstructionDataArgs,
    ),
    programAddress,
  } as DepositToOfferEscrowInstruction<
    TProgramAddress,
    TAccountMaker,
    TAccountOffer,
    TAccountMint,
    TAccountEscrow,
    TAccountMakerShareAccount,
    TAccountTokenProgram,
    TAccountPlatform
  >);
}

export type DepositToOfferEscrowInput<
  TAccountMaker extends string = string,
  TAccountOffer extends string = string,
  TAccountMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountMakerShareAccount extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountPlatform extends string = string,
> = {
  /**
   * The offer's maker — the only party whose units this ledger may record,
   * and the only party the refund paths pay out to.
   */
  maker: TransactionSigner<TAccountMaker>;
  offer: Address<TAccountOffer>;
  mint: Address<TAccountMint>;
  escrow: Address<TAccountEscrow>;
  /** The maker's share-class token account — debited exactly `amount`. */
  makerShareAccount: Address<TAccountMakerShareAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
  /**
   * Emergency-pause gate (read-only). Keep LAST among named accounts: old
   * account indices and the remaining-accounts hook tail keep their positions.
   */
  platform: Address<TAccountPlatform>;
  amount: DepositToOfferEscrowInstructionDataArgs["amount"];
};

export function getDepositToOfferEscrowInstruction<
  TAccountMaker extends string,
  TAccountOffer extends string,
  TAccountMint extends string,
  TAccountEscrow extends string,
  TAccountMakerShareAccount extends string,
  TAccountTokenProgram extends string,
  TAccountPlatform extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: DepositToOfferEscrowInput<
    TAccountMaker,
    TAccountOffer,
    TAccountMint,
    TAccountEscrow,
    TAccountMakerShareAccount,
    TAccountTokenProgram,
    TAccountPlatform
  >,
  config?: { programAddress?: TProgramAddress },
): DepositToOfferEscrowInstruction<
  TProgramAddress,
  TAccountMaker,
  TAccountOffer,
  TAccountMint,
  TAccountEscrow,
  TAccountMakerShareAccount,
  TAccountTokenProgram,
  TAccountPlatform
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    maker: { value: input.maker ?? null, isWritable: false },
    offer: { value: input.offer ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    escrow: { value: input.escrow ?? null, isWritable: true },
    makerShareAccount: {
      value: input.makerShareAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    platform: { value: input.platform ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.maker),
      getAccountMeta(accounts.offer),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.makerShareAccount),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.platform),
    ],
    data: getDepositToOfferEscrowInstructionDataEncoder().encode(
      args as DepositToOfferEscrowInstructionDataArgs,
    ),
    programAddress,
  } as DepositToOfferEscrowInstruction<
    TProgramAddress,
    TAccountMaker,
    TAccountOffer,
    TAccountMint,
    TAccountEscrow,
    TAccountMakerShareAccount,
    TAccountTokenProgram,
    TAccountPlatform
  >);
}

export type ParsedDepositToOfferEscrowInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * The offer's maker — the only party whose units this ledger may record,
     * and the only party the refund paths pay out to.
     */
    maker: TAccountMetas[0];
    offer: TAccountMetas[1];
    mint: TAccountMetas[2];
    escrow: TAccountMetas[3];
    /** The maker's share-class token account — debited exactly `amount`. */
    makerShareAccount: TAccountMetas[4];
    tokenProgram: TAccountMetas[5];
    /**
     * Emergency-pause gate (read-only). Keep LAST among named accounts: old
     * account indices and the remaining-accounts hook tail keep their positions.
     */
    platform: TAccountMetas[6];
  };
  data: DepositToOfferEscrowInstructionData;
};

export function parseDepositToOfferEscrowInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDepositToOfferEscrowInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      maker: getNextAccount(),
      offer: getNextAccount(),
      mint: getNextAccount(),
      escrow: getNextAccount(),
      makerShareAccount: getNextAccount(),
      tokenProgram: getNextAccount(),
      platform: getNextAccount(),
    },
    data: getDepositToOfferEscrowInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
