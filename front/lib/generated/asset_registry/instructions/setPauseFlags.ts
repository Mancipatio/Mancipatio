/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findAdminRecordPda, findPlatformPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const SET_PAUSE_FLAGS_DISCRIMINATOR = new Uint8Array([
  205, 167, 85, 237, 144, 202, 248, 175,
]);

export function getSetPauseFlagsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_PAUSE_FLAGS_DISCRIMINATOR,
  );
}

export type SetPauseFlagsInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAdminRecord extends string | AccountMeta<string> = string,
  TAccountPlatform extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAdminRecord extends string
        ? ReadonlyAccount<TAccountAdminRecord>
        : TAccountAdminRecord,
      TAccountPlatform extends string
        ? WritableAccount<TAccountPlatform>
        : TAccountPlatform,
      ...TRemainingAccounts,
    ]
  >;

export type SetPauseFlagsInstructionData = {
  discriminator: ReadonlyUint8Array;
  setMask: number;
  clearMask: number;
};

export type SetPauseFlagsInstructionDataArgs = {
  setMask: number;
  clearMask: number;
};

export function getSetPauseFlagsInstructionDataEncoder(): FixedSizeEncoder<SetPauseFlagsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["setMask", getU8Encoder()],
      ["clearMask", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_PAUSE_FLAGS_DISCRIMINATOR }),
  );
}

export function getSetPauseFlagsInstructionDataDecoder(): FixedSizeDecoder<SetPauseFlagsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["setMask", getU8Decoder()],
    ["clearMask", getU8Decoder()],
  ]);
}

export function getSetPauseFlagsInstructionDataCodec(): FixedSizeCodec<
  SetPauseFlagsInstructionDataArgs,
  SetPauseFlagsInstructionData
> {
  return combineCodec(
    getSetPauseFlagsInstructionDataEncoder(),
    getSetPauseFlagsInstructionDataDecoder(),
  );
}

export type SetPauseFlagsAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountPlatform extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /**
   * `util::is_active_admin` unless `authority == platform.admin` (a super
   * admin without an Admin record is tolerated).
   */
  adminRecord?: Address<TAccountAdminRecord>;
  platform?: Address<TAccountPlatform>;
  setMask: SetPauseFlagsInstructionDataArgs["setMask"];
  clearMask: SetPauseFlagsInstructionDataArgs["clearMask"];
};

export async function getSetPauseFlagsInstructionAsync<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountPlatform extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: SetPauseFlagsAsyncInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountPlatform
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetPauseFlagsInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountPlatform
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    platform: { value: input.platform ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.adminRecord.value) {
    accounts.adminRecord.value = await findAdminRecordPda({
      authority: expectAddress(accounts.authority.value),
    });
  }
  if (!accounts.platform.value) {
    accounts.platform.value = await findPlatformPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.platform),
    ],
    data: getSetPauseFlagsInstructionDataEncoder().encode(
      args as SetPauseFlagsInstructionDataArgs,
    ),
    programAddress,
  } as SetPauseFlagsInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountPlatform
  >);
}

export type SetPauseFlagsInput<
  TAccountAuthority extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountPlatform extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  /**
   * `util::is_active_admin` unless `authority == platform.admin` (a super
   * admin without an Admin record is tolerated).
   */
  adminRecord: Address<TAccountAdminRecord>;
  platform: Address<TAccountPlatform>;
  setMask: SetPauseFlagsInstructionDataArgs["setMask"];
  clearMask: SetPauseFlagsInstructionDataArgs["clearMask"];
};

export function getSetPauseFlagsInstruction<
  TAccountAuthority extends string,
  TAccountAdminRecord extends string,
  TAccountPlatform extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: SetPauseFlagsInput<
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountPlatform
  >,
  config?: { programAddress?: TProgramAddress },
): SetPauseFlagsInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAdminRecord,
  TAccountPlatform
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    platform: { value: input.platform ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.adminRecord),
      getAccountMeta(accounts.platform),
    ],
    data: getSetPauseFlagsInstructionDataEncoder().encode(
      args as SetPauseFlagsInstructionDataArgs,
    ),
    programAddress,
  } as SetPauseFlagsInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAdminRecord,
    TAccountPlatform
  >);
}

export type ParsedSetPauseFlagsInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    /**
     * `util::is_active_admin` unless `authority == platform.admin` (a super
     * admin without an Admin record is tolerated).
     */
    adminRecord: TAccountMetas[1];
    platform: TAccountMetas[2];
  };
  data: SetPauseFlagsInstructionData;
};

export function parseSetPauseFlagsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetPauseFlagsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      adminRecord: getNextAccount(),
      platform: getNextAccount(),
    },
    data: getSetPauseFlagsInstructionDataDecoder().decode(instruction.data),
  };
}
