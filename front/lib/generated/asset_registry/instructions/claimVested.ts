/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findPositionPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CLAIM_VESTED_DISCRIMINATOR = new Uint8Array([
  208, 190, 166, 114, 203, 225, 140, 208,
]);

export function getClaimVestedDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAIM_VESTED_DISCRIMINATOR,
  );
}

export type ClaimVestedInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountRecipient extends string | AccountMeta<string> = string,
  TAccountSeries extends string | AccountMeta<string> = string,
  TAccountPosition extends string | AccountMeta<string> = string,
  TAccountTokenMint extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountRecipientTokenAccount extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRecipient extends string
        ? ReadonlySignerAccount<TAccountRecipient> &
            AccountSignerMeta<TAccountRecipient>
        : TAccountRecipient,
      TAccountSeries extends string
        ? WritableAccount<TAccountSeries>
        : TAccountSeries,
      TAccountPosition extends string
        ? WritableAccount<TAccountPosition>
        : TAccountPosition,
      TAccountTokenMint extends string
        ? ReadonlyAccount<TAccountTokenMint>
        : TAccountTokenMint,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountRecipientTokenAccount extends string
        ? WritableAccount<TAccountRecipientTokenAccount>
        : TAccountRecipientTokenAccount,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ClaimVestedInstructionData = {
  discriminator: ReadonlyUint8Array;
  positionIndex: number;
};

export type ClaimVestedInstructionDataArgs = { positionIndex: number };

export function getClaimVestedInstructionDataEncoder(): FixedSizeEncoder<ClaimVestedInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["positionIndex", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CLAIM_VESTED_DISCRIMINATOR }),
  );
}

export function getClaimVestedInstructionDataDecoder(): FixedSizeDecoder<ClaimVestedInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["positionIndex", getU32Decoder()],
  ]);
}

export function getClaimVestedInstructionDataCodec(): FixedSizeCodec<
  ClaimVestedInstructionDataArgs,
  ClaimVestedInstructionData
> {
  return combineCodec(
    getClaimVestedInstructionDataEncoder(),
    getClaimVestedInstructionDataDecoder(),
  );
}

export type ClaimVestedAsyncInput<
  TAccountRecipient extends string = string,
  TAccountSeries extends string = string,
  TAccountPosition extends string = string,
  TAccountTokenMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountRecipientTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** The recipient — must be the wallet currently recorded on the position. */
  recipient: TransactionSigner<TAccountRecipient>;
  series: Address<TAccountSeries>;
  position?: Address<TAccountPosition>;
  tokenMint: Address<TAccountTokenMint>;
  escrow: Address<TAccountEscrow>;
  /** The recipient's token account — must be owned by the position's wallet. */
  recipientTokenAccount: Address<TAccountRecipientTokenAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
  positionIndex: ClaimVestedInstructionDataArgs["positionIndex"];
};

export async function getClaimVestedInstructionAsync<
  TAccountRecipient extends string,
  TAccountSeries extends string,
  TAccountPosition extends string,
  TAccountTokenMint extends string,
  TAccountEscrow extends string,
  TAccountRecipientTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ClaimVestedAsyncInput<
    TAccountRecipient,
    TAccountSeries,
    TAccountPosition,
    TAccountTokenMint,
    TAccountEscrow,
    TAccountRecipientTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ClaimVestedInstruction<
    TProgramAddress,
    TAccountRecipient,
    TAccountSeries,
    TAccountPosition,
    TAccountTokenMint,
    TAccountEscrow,
    TAccountRecipientTokenAccount,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    recipient: { value: input.recipient ?? null, isWritable: false },
    series: { value: input.series ?? null, isWritable: true },
    position: { value: input.position ?? null, isWritable: true },
    tokenMint: { value: input.tokenMint ?? null, isWritable: false },
    escrow: { value: input.escrow ?? null, isWritable: true },
    recipientTokenAccount: {
      value: input.recipientTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.position.value) {
    accounts.position.value = await findPositionPda({
      series: expectAddress(accounts.series.value),
      positionIndex: expectSome(args.positionIndex),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.recipient),
      getAccountMeta(accounts.series),
      getAccountMeta(accounts.position),
      getAccountMeta(accounts.tokenMint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.recipientTokenAccount),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getClaimVestedInstructionDataEncoder().encode(
      args as ClaimVestedInstructionDataArgs,
    ),
    programAddress,
  } as ClaimVestedInstruction<
    TProgramAddress,
    TAccountRecipient,
    TAccountSeries,
    TAccountPosition,
    TAccountTokenMint,
    TAccountEscrow,
    TAccountRecipientTokenAccount,
    TAccountTokenProgram
  >);
}

export type ClaimVestedInput<
  TAccountRecipient extends string = string,
  TAccountSeries extends string = string,
  TAccountPosition extends string = string,
  TAccountTokenMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountRecipientTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** The recipient — must be the wallet currently recorded on the position. */
  recipient: TransactionSigner<TAccountRecipient>;
  series: Address<TAccountSeries>;
  position: Address<TAccountPosition>;
  tokenMint: Address<TAccountTokenMint>;
  escrow: Address<TAccountEscrow>;
  /** The recipient's token account — must be owned by the position's wallet. */
  recipientTokenAccount: Address<TAccountRecipientTokenAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
  positionIndex: ClaimVestedInstructionDataArgs["positionIndex"];
};

export function getClaimVestedInstruction<
  TAccountRecipient extends string,
  TAccountSeries extends string,
  TAccountPosition extends string,
  TAccountTokenMint extends string,
  TAccountEscrow extends string,
  TAccountRecipientTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ClaimVestedInput<
    TAccountRecipient,
    TAccountSeries,
    TAccountPosition,
    TAccountTokenMint,
    TAccountEscrow,
    TAccountRecipientTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ClaimVestedInstruction<
  TProgramAddress,
  TAccountRecipient,
  TAccountSeries,
  TAccountPosition,
  TAccountTokenMint,
  TAccountEscrow,
  TAccountRecipientTokenAccount,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    recipient: { value: input.recipient ?? null, isWritable: false },
    series: { value: input.series ?? null, isWritable: true },
    position: { value: input.position ?? null, isWritable: true },
    tokenMint: { value: input.tokenMint ?? null, isWritable: false },
    escrow: { value: input.escrow ?? null, isWritable: true },
    recipientTokenAccount: {
      value: input.recipientTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.recipient),
      getAccountMeta(accounts.series),
      getAccountMeta(accounts.position),
      getAccountMeta(accounts.tokenMint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.recipientTokenAccount),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getClaimVestedInstructionDataEncoder().encode(
      args as ClaimVestedInstructionDataArgs,
    ),
    programAddress,
  } as ClaimVestedInstruction<
    TProgramAddress,
    TAccountRecipient,
    TAccountSeries,
    TAccountPosition,
    TAccountTokenMint,
    TAccountEscrow,
    TAccountRecipientTokenAccount,
    TAccountTokenProgram
  >);
}

export type ParsedClaimVestedInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The recipient — must be the wallet currently recorded on the position. */
    recipient: TAccountMetas[0];
    series: TAccountMetas[1];
    position: TAccountMetas[2];
    tokenMint: TAccountMetas[3];
    escrow: TAccountMetas[4];
    /** The recipient's token account — must be owned by the position's wallet. */
    recipientTokenAccount: TAccountMetas[5];
    tokenProgram: TAccountMetas[6];
  };
  data: ClaimVestedInstructionData;
};

export function parseClaimVestedInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClaimVestedInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      recipient: getNextAccount(),
      series: getNextAccount(),
      position: getNextAccount(),
      tokenMint: getNextAccount(),
      escrow: getNextAccount(),
      recipientTokenAccount: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getClaimVestedInstructionDataDecoder().decode(instruction.data),
  };
}
