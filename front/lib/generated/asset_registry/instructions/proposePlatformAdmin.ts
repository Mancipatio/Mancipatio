/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findAcceptPlatformAdminTransferPda, findPlatformPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const PROPOSE_PLATFORM_ADMIN_DISCRIMINATOR = new Uint8Array([
  26, 187, 5, 155, 78, 207, 193, 4,
]);

export function getProposePlatformAdminDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PROPOSE_PLATFORM_ADMIN_DISCRIMINATOR,
  );
}

export type ProposePlatformAdminInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountPlatform extends string | AccountMeta<string> = string,
  TAccountTransfer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountPlatform extends string
        ? ReadonlyAccount<TAccountPlatform>
        : TAccountPlatform,
      TAccountTransfer extends string
        ? WritableAccount<TAccountTransfer>
        : TAccountTransfer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ProposePlatformAdminInstructionData = {
  discriminator: ReadonlyUint8Array;
  newAdmin: Address;
};

export type ProposePlatformAdminInstructionDataArgs = { newAdmin: Address };

export function getProposePlatformAdminInstructionDataEncoder(): FixedSizeEncoder<ProposePlatformAdminInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newAdmin", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: PROPOSE_PLATFORM_ADMIN_DISCRIMINATOR,
    }),
  );
}

export function getProposePlatformAdminInstructionDataDecoder(): FixedSizeDecoder<ProposePlatformAdminInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newAdmin", getAddressDecoder()],
  ]);
}

export function getProposePlatformAdminInstructionDataCodec(): FixedSizeCodec<
  ProposePlatformAdminInstructionDataArgs,
  ProposePlatformAdminInstructionData
> {
  return combineCodec(
    getProposePlatformAdminInstructionDataEncoder(),
    getProposePlatformAdminInstructionDataDecoder(),
  );
}

export type ProposePlatformAdminAsyncInput<
  TAccountAuthority extends string = string,
  TAccountPlatform extends string = string,
  TAccountTransfer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  platform?: Address<TAccountPlatform>;
  transfer?: Address<TAccountTransfer>;
  systemProgram?: Address<TAccountSystemProgram>;
  newAdmin: ProposePlatformAdminInstructionDataArgs["newAdmin"];
};

export async function getProposePlatformAdminInstructionAsync<
  TAccountAuthority extends string,
  TAccountPlatform extends string,
  TAccountTransfer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ProposePlatformAdminAsyncInput<
    TAccountAuthority,
    TAccountPlatform,
    TAccountTransfer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ProposePlatformAdminInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPlatform,
    TAccountTransfer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    platform: { value: input.platform ?? null, isWritable: false },
    transfer: { value: input.transfer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.platform.value) {
    accounts.platform.value = await findPlatformPda();
  }
  if (!accounts.transfer.value) {
    accounts.transfer.value = await findAcceptPlatformAdminTransferPda({
      platform: expectAddress(accounts.platform.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.platform),
      getAccountMeta(accounts.transfer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getProposePlatformAdminInstructionDataEncoder().encode(
      args as ProposePlatformAdminInstructionDataArgs,
    ),
    programAddress,
  } as ProposePlatformAdminInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPlatform,
    TAccountTransfer,
    TAccountSystemProgram
  >);
}

export type ProposePlatformAdminInput<
  TAccountAuthority extends string = string,
  TAccountPlatform extends string = string,
  TAccountTransfer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  platform: Address<TAccountPlatform>;
  transfer: Address<TAccountTransfer>;
  systemProgram?: Address<TAccountSystemProgram>;
  newAdmin: ProposePlatformAdminInstructionDataArgs["newAdmin"];
};

export function getProposePlatformAdminInstruction<
  TAccountAuthority extends string,
  TAccountPlatform extends string,
  TAccountTransfer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ProposePlatformAdminInput<
    TAccountAuthority,
    TAccountPlatform,
    TAccountTransfer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ProposePlatformAdminInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountPlatform,
  TAccountTransfer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    platform: { value: input.platform ?? null, isWritable: false },
    transfer: { value: input.transfer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.platform),
      getAccountMeta(accounts.transfer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getProposePlatformAdminInstructionDataEncoder().encode(
      args as ProposePlatformAdminInstructionDataArgs,
    ),
    programAddress,
  } as ProposePlatformAdminInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPlatform,
    TAccountTransfer,
    TAccountSystemProgram
  >);
}

export type ParsedProposePlatformAdminInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    platform: TAccountMetas[1];
    transfer: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: ProposePlatformAdminInstructionData;
};

export function parseProposePlatformAdminInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProposePlatformAdminInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      platform: getNextAccount(),
      transfer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getProposePlatformAdminInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
