/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findEscrowMarkerPda, findPlatformPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const TAKE_OFFER_DISCRIMINATOR = new Uint8Array([
  128, 156, 242, 207, 237, 192, 103, 240,
]);

export function getTakeOfferDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(TAKE_OFFER_DISCRIMINATOR);
}

export type TakeOfferInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountTaker extends string | AccountMeta<string> = string,
  TAccountOffer extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountTakerShareAccount extends string | AccountMeta<string> = string,
  TAccountPaymentMint extends string | AccountMeta<string> = string,
  TAccountTakerPaymentAccount extends string | AccountMeta<string> = string,
  TAccountMakerPaymentAccount extends string | AccountMeta<string> = string,
  TAccountEscrowMarker extends string | AccountMeta<string> = string,
  TAccountShareTokenProgram extends string | AccountMeta<string> = string,
  TAccountPaymentTokenProgram extends string | AccountMeta<string> = string,
  TAccountPlatform extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTaker extends string
        ? WritableSignerAccount<TAccountTaker> &
            AccountSignerMeta<TAccountTaker>
        : TAccountTaker,
      TAccountOffer extends string
        ? WritableAccount<TAccountOffer>
        : TAccountOffer,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountTakerShareAccount extends string
        ? WritableAccount<TAccountTakerShareAccount>
        : TAccountTakerShareAccount,
      TAccountPaymentMint extends string
        ? ReadonlyAccount<TAccountPaymentMint>
        : TAccountPaymentMint,
      TAccountTakerPaymentAccount extends string
        ? WritableAccount<TAccountTakerPaymentAccount>
        : TAccountTakerPaymentAccount,
      TAccountMakerPaymentAccount extends string
        ? WritableAccount<TAccountMakerPaymentAccount>
        : TAccountMakerPaymentAccount,
      TAccountEscrowMarker extends string
        ? WritableAccount<TAccountEscrowMarker>
        : TAccountEscrowMarker,
      TAccountShareTokenProgram extends string
        ? ReadonlyAccount<TAccountShareTokenProgram>
        : TAccountShareTokenProgram,
      TAccountPaymentTokenProgram extends string
        ? ReadonlyAccount<TAccountPaymentTokenProgram>
        : TAccountPaymentTokenProgram,
      TAccountPlatform extends string
        ? ReadonlyAccount<TAccountPlatform>
        : TAccountPlatform,
      ...TRemainingAccounts,
    ]
  >;

export type TakeOfferInstructionData = { discriminator: ReadonlyUint8Array };

export type TakeOfferInstructionDataArgs = {};

export function getTakeOfferInstructionDataEncoder(): FixedSizeEncoder<TakeOfferInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: TAKE_OFFER_DISCRIMINATOR }),
  );
}

export function getTakeOfferInstructionDataDecoder(): FixedSizeDecoder<TakeOfferInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getTakeOfferInstructionDataCodec(): FixedSizeCodec<
  TakeOfferInstructionDataArgs,
  TakeOfferInstructionData
> {
  return combineCodec(
    getTakeOfferInstructionDataEncoder(),
    getTakeOfferInstructionDataDecoder(),
  );
}

export type TakeOfferAsyncInput<
  TAccountTaker extends string = string,
  TAccountOffer extends string = string,
  TAccountMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountTakerShareAccount extends string = string,
  TAccountPaymentMint extends string = string,
  TAccountTakerPaymentAccount extends string = string,
  TAccountMakerPaymentAccount extends string = string,
  TAccountEscrowMarker extends string = string,
  TAccountShareTokenProgram extends string = string,
  TAccountPaymentTokenProgram extends string = string,
  TAccountPlatform extends string = string,
> = {
  taker: TransactionSigner<TAccountTaker>;
  offer: Address<TAccountOffer>;
  mint: Address<TAccountMint>;
  escrow: Address<TAccountEscrow>;
  /**
   * Taker receives the share units here. Bound to the taker signer so the
   * receiver of the escrowed security is the party whose KYC is checked below
   * — shares can never be redirected to an arbitrary third-party account
   * (mirrors `expire_offer` / `deposit_otc_asset`).
   */
  takerShareAccount: Address<TAccountTakerShareAccount>;
  paymentMint: Address<TAccountPaymentMint>;
  /** Taker pays from here. */
  takerPaymentAccount: Address<TAccountTakerPaymentAccount>;
  /** Maker receives the payment here. */
  makerPaymentAccount: Address<TAccountMakerPaymentAccount>;
  /**
   * Escrow marker for the offer PDA — closed here (rent → taker);
   * take is a terminal path.
   */
  escrowMarker?: Address<TAccountEscrowMarker>;
  shareTokenProgram: Address<TAccountShareTokenProgram>;
  paymentTokenProgram: Address<TAccountPaymentTokenProgram>;
  /**
   * Emergency-pause gate (read-only). Keep LAST among named accounts: old
   * account indices and the remaining-accounts hook tail keep their positions.
   */
  platform?: Address<TAccountPlatform>;
};

export async function getTakeOfferInstructionAsync<
  TAccountTaker extends string,
  TAccountOffer extends string,
  TAccountMint extends string,
  TAccountEscrow extends string,
  TAccountTakerShareAccount extends string,
  TAccountPaymentMint extends string,
  TAccountTakerPaymentAccount extends string,
  TAccountMakerPaymentAccount extends string,
  TAccountEscrowMarker extends string,
  TAccountShareTokenProgram extends string,
  TAccountPaymentTokenProgram extends string,
  TAccountPlatform extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: TakeOfferAsyncInput<
    TAccountTaker,
    TAccountOffer,
    TAccountMint,
    TAccountEscrow,
    TAccountTakerShareAccount,
    TAccountPaymentMint,
    TAccountTakerPaymentAccount,
    TAccountMakerPaymentAccount,
    TAccountEscrowMarker,
    TAccountShareTokenProgram,
    TAccountPaymentTokenProgram,
    TAccountPlatform
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  TakeOfferInstruction<
    TProgramAddress,
    TAccountTaker,
    TAccountOffer,
    TAccountMint,
    TAccountEscrow,
    TAccountTakerShareAccount,
    TAccountPaymentMint,
    TAccountTakerPaymentAccount,
    TAccountMakerPaymentAccount,
    TAccountEscrowMarker,
    TAccountShareTokenProgram,
    TAccountPaymentTokenProgram,
    TAccountPlatform
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    taker: { value: input.taker ?? null, isWritable: true },
    offer: { value: input.offer ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    escrow: { value: input.escrow ?? null, isWritable: true },
    takerShareAccount: {
      value: input.takerShareAccount ?? null,
      isWritable: true,
    },
    paymentMint: { value: input.paymentMint ?? null, isWritable: false },
    takerPaymentAccount: {
      value: input.takerPaymentAccount ?? null,
      isWritable: true,
    },
    makerPaymentAccount: {
      value: input.makerPaymentAccount ?? null,
      isWritable: true,
    },
    escrowMarker: { value: input.escrowMarker ?? null, isWritable: true },
    shareTokenProgram: {
      value: input.shareTokenProgram ?? null,
      isWritable: false,
    },
    paymentTokenProgram: {
      value: input.paymentTokenProgram ?? null,
      isWritable: false,
    },
    platform: { value: input.platform ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.escrowMarker.value) {
    accounts.escrowMarker.value = await findEscrowMarkerPda({
      offer: expectAddress(accounts.offer.value),
    });
  }
  if (!accounts.platform.value) {
    accounts.platform.value = await findPlatformPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.taker),
      getAccountMeta(accounts.offer),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.takerShareAccount),
      getAccountMeta(accounts.paymentMint),
      getAccountMeta(accounts.takerPaymentAccount),
      getAccountMeta(accounts.makerPaymentAccount),
      getAccountMeta(accounts.escrowMarker),
      getAccountMeta(accounts.shareTokenProgram),
      getAccountMeta(accounts.paymentTokenProgram),
      getAccountMeta(accounts.platform),
    ],
    data: getTakeOfferInstructionDataEncoder().encode({}),
    programAddress,
  } as TakeOfferInstruction<
    TProgramAddress,
    TAccountTaker,
    TAccountOffer,
    TAccountMint,
    TAccountEscrow,
    TAccountTakerShareAccount,
    TAccountPaymentMint,
    TAccountTakerPaymentAccount,
    TAccountMakerPaymentAccount,
    TAccountEscrowMarker,
    TAccountShareTokenProgram,
    TAccountPaymentTokenProgram,
    TAccountPlatform
  >);
}

export type TakeOfferInput<
  TAccountTaker extends string = string,
  TAccountOffer extends string = string,
  TAccountMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountTakerShareAccount extends string = string,
  TAccountPaymentMint extends string = string,
  TAccountTakerPaymentAccount extends string = string,
  TAccountMakerPaymentAccount extends string = string,
  TAccountEscrowMarker extends string = string,
  TAccountShareTokenProgram extends string = string,
  TAccountPaymentTokenProgram extends string = string,
  TAccountPlatform extends string = string,
> = {
  taker: TransactionSigner<TAccountTaker>;
  offer: Address<TAccountOffer>;
  mint: Address<TAccountMint>;
  escrow: Address<TAccountEscrow>;
  /**
   * Taker receives the share units here. Bound to the taker signer so the
   * receiver of the escrowed security is the party whose KYC is checked below
   * — shares can never be redirected to an arbitrary third-party account
   * (mirrors `expire_offer` / `deposit_otc_asset`).
   */
  takerShareAccount: Address<TAccountTakerShareAccount>;
  paymentMint: Address<TAccountPaymentMint>;
  /** Taker pays from here. */
  takerPaymentAccount: Address<TAccountTakerPaymentAccount>;
  /** Maker receives the payment here. */
  makerPaymentAccount: Address<TAccountMakerPaymentAccount>;
  /**
   * Escrow marker for the offer PDA — closed here (rent → taker);
   * take is a terminal path.
   */
  escrowMarker: Address<TAccountEscrowMarker>;
  shareTokenProgram: Address<TAccountShareTokenProgram>;
  paymentTokenProgram: Address<TAccountPaymentTokenProgram>;
  /**
   * Emergency-pause gate (read-only). Keep LAST among named accounts: old
   * account indices and the remaining-accounts hook tail keep their positions.
   */
  platform: Address<TAccountPlatform>;
};

export function getTakeOfferInstruction<
  TAccountTaker extends string,
  TAccountOffer extends string,
  TAccountMint extends string,
  TAccountEscrow extends string,
  TAccountTakerShareAccount extends string,
  TAccountPaymentMint extends string,
  TAccountTakerPaymentAccount extends string,
  TAccountMakerPaymentAccount extends string,
  TAccountEscrowMarker extends string,
  TAccountShareTokenProgram extends string,
  TAccountPaymentTokenProgram extends string,
  TAccountPlatform extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: TakeOfferInput<
    TAccountTaker,
    TAccountOffer,
    TAccountMint,
    TAccountEscrow,
    TAccountTakerShareAccount,
    TAccountPaymentMint,
    TAccountTakerPaymentAccount,
    TAccountMakerPaymentAccount,
    TAccountEscrowMarker,
    TAccountShareTokenProgram,
    TAccountPaymentTokenProgram,
    TAccountPlatform
  >,
  config?: { programAddress?: TProgramAddress },
): TakeOfferInstruction<
  TProgramAddress,
  TAccountTaker,
  TAccountOffer,
  TAccountMint,
  TAccountEscrow,
  TAccountTakerShareAccount,
  TAccountPaymentMint,
  TAccountTakerPaymentAccount,
  TAccountMakerPaymentAccount,
  TAccountEscrowMarker,
  TAccountShareTokenProgram,
  TAccountPaymentTokenProgram,
  TAccountPlatform
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    taker: { value: input.taker ?? null, isWritable: true },
    offer: { value: input.offer ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    escrow: { value: input.escrow ?? null, isWritable: true },
    takerShareAccount: {
      value: input.takerShareAccount ?? null,
      isWritable: true,
    },
    paymentMint: { value: input.paymentMint ?? null, isWritable: false },
    takerPaymentAccount: {
      value: input.takerPaymentAccount ?? null,
      isWritable: true,
    },
    makerPaymentAccount: {
      value: input.makerPaymentAccount ?? null,
      isWritable: true,
    },
    escrowMarker: { value: input.escrowMarker ?? null, isWritable: true },
    shareTokenProgram: {
      value: input.shareTokenProgram ?? null,
      isWritable: false,
    },
    paymentTokenProgram: {
      value: input.paymentTokenProgram ?? null,
      isWritable: false,
    },
    platform: { value: input.platform ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.taker),
      getAccountMeta(accounts.offer),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.takerShareAccount),
      getAccountMeta(accounts.paymentMint),
      getAccountMeta(accounts.takerPaymentAccount),
      getAccountMeta(accounts.makerPaymentAccount),
      getAccountMeta(accounts.escrowMarker),
      getAccountMeta(accounts.shareTokenProgram),
      getAccountMeta(accounts.paymentTokenProgram),
      getAccountMeta(accounts.platform),
    ],
    data: getTakeOfferInstructionDataEncoder().encode({}),
    programAddress,
  } as TakeOfferInstruction<
    TProgramAddress,
    TAccountTaker,
    TAccountOffer,
    TAccountMint,
    TAccountEscrow,
    TAccountTakerShareAccount,
    TAccountPaymentMint,
    TAccountTakerPaymentAccount,
    TAccountMakerPaymentAccount,
    TAccountEscrowMarker,
    TAccountShareTokenProgram,
    TAccountPaymentTokenProgram,
    TAccountPlatform
  >);
}

export type ParsedTakeOfferInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    taker: TAccountMetas[0];
    offer: TAccountMetas[1];
    mint: TAccountMetas[2];
    escrow: TAccountMetas[3];
    /**
     * Taker receives the share units here. Bound to the taker signer so the
     * receiver of the escrowed security is the party whose KYC is checked below
     * — shares can never be redirected to an arbitrary third-party account
     * (mirrors `expire_offer` / `deposit_otc_asset`).
     */
    takerShareAccount: TAccountMetas[4];
    paymentMint: TAccountMetas[5];
    /** Taker pays from here. */
    takerPaymentAccount: TAccountMetas[6];
    /** Maker receives the payment here. */
    makerPaymentAccount: TAccountMetas[7];
    /**
     * Escrow marker for the offer PDA — closed here (rent → taker);
     * take is a terminal path.
     */
    escrowMarker: TAccountMetas[8];
    shareTokenProgram: TAccountMetas[9];
    paymentTokenProgram: TAccountMetas[10];
    /**
     * Emergency-pause gate (read-only). Keep LAST among named accounts: old
     * account indices and the remaining-accounts hook tail keep their positions.
     */
    platform: TAccountMetas[11];
  };
  data: TakeOfferInstructionData;
};

export function parseTakeOfferInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedTakeOfferInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 12) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      taker: getNextAccount(),
      offer: getNextAccount(),
      mint: getNextAccount(),
      escrow: getNextAccount(),
      takerShareAccount: getNextAccount(),
      paymentMint: getNextAccount(),
      takerPaymentAccount: getNextAccount(),
      makerPaymentAccount: getNextAccount(),
      escrowMarker: getNextAccount(),
      shareTokenProgram: getNextAccount(),
      paymentTokenProgram: getNextAccount(),
      platform: getNextAccount(),
    },
    data: getTakeOfferInstructionDataDecoder().decode(instruction.data),
  };
}
