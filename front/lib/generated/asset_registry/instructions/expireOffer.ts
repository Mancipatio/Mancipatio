/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findEscrowMarkerPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const EXPIRE_OFFER_DISCRIMINATOR = new Uint8Array([
  161, 133, 120, 51, 0, 201, 224, 248,
]);

export function getExpireOfferDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    EXPIRE_OFFER_DISCRIMINATOR,
  );
}

export type ExpireOfferInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountOffer extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountMakerShareAccount extends string | AccountMeta<string> = string,
  TAccountEscrowMarker extends string | AccountMeta<string> = string,
  TAccountShareTokenProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountOffer extends string
        ? WritableAccount<TAccountOffer>
        : TAccountOffer,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountMakerShareAccount extends string
        ? WritableAccount<TAccountMakerShareAccount>
        : TAccountMakerShareAccount,
      TAccountEscrowMarker extends string
        ? WritableAccount<TAccountEscrowMarker>
        : TAccountEscrowMarker,
      TAccountShareTokenProgram extends string
        ? ReadonlyAccount<TAccountShareTokenProgram>
        : TAccountShareTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ExpireOfferInstructionData = { discriminator: ReadonlyUint8Array };

export type ExpireOfferInstructionDataArgs = {};

export function getExpireOfferInstructionDataEncoder(): FixedSizeEncoder<ExpireOfferInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: EXPIRE_OFFER_DISCRIMINATOR }),
  );
}

export function getExpireOfferInstructionDataDecoder(): FixedSizeDecoder<ExpireOfferInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getExpireOfferInstructionDataCodec(): FixedSizeCodec<
  ExpireOfferInstructionDataArgs,
  ExpireOfferInstructionData
> {
  return combineCodec(
    getExpireOfferInstructionDataEncoder(),
    getExpireOfferInstructionDataDecoder(),
  );
}

export type ExpireOfferAsyncInput<
  TAccountPayer extends string = string,
  TAccountOffer extends string = string,
  TAccountMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountMakerShareAccount extends string = string,
  TAccountEscrowMarker extends string = string,
  TAccountShareTokenProgram extends string = string,
> = {
  /**
   * Permissionless after `expires_at` — any signer may pay the fee.
   * Mut: receives the closed escrow marker's rent.
   */
  payer: TransactionSigner<TAccountPayer>;
  offer: Address<TAccountOffer>;
  mint: Address<TAccountMint>;
  escrow: Address<TAccountEscrow>;
  /** The maker reclaims the escrowed share units here. */
  makerShareAccount: Address<TAccountMakerShareAccount>;
  /**
   * Escrow marker for the offer PDA — closed here (rent → payer);
   * expiry is a terminal path.
   */
  escrowMarker?: Address<TAccountEscrowMarker>;
  shareTokenProgram: Address<TAccountShareTokenProgram>;
};

export async function getExpireOfferInstructionAsync<
  TAccountPayer extends string,
  TAccountOffer extends string,
  TAccountMint extends string,
  TAccountEscrow extends string,
  TAccountMakerShareAccount extends string,
  TAccountEscrowMarker extends string,
  TAccountShareTokenProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ExpireOfferAsyncInput<
    TAccountPayer,
    TAccountOffer,
    TAccountMint,
    TAccountEscrow,
    TAccountMakerShareAccount,
    TAccountEscrowMarker,
    TAccountShareTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ExpireOfferInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountOffer,
    TAccountMint,
    TAccountEscrow,
    TAccountMakerShareAccount,
    TAccountEscrowMarker,
    TAccountShareTokenProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    offer: { value: input.offer ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    escrow: { value: input.escrow ?? null, isWritable: true },
    makerShareAccount: {
      value: input.makerShareAccount ?? null,
      isWritable: true,
    },
    escrowMarker: { value: input.escrowMarker ?? null, isWritable: true },
    shareTokenProgram: {
      value: input.shareTokenProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.escrowMarker.value) {
    accounts.escrowMarker.value = await findEscrowMarkerPda({
      offer: expectAddress(accounts.offer.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.offer),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.makerShareAccount),
      getAccountMeta(accounts.escrowMarker),
      getAccountMeta(accounts.shareTokenProgram),
    ],
    data: getExpireOfferInstructionDataEncoder().encode({}),
    programAddress,
  } as ExpireOfferInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountOffer,
    TAccountMint,
    TAccountEscrow,
    TAccountMakerShareAccount,
    TAccountEscrowMarker,
    TAccountShareTokenProgram
  >);
}

export type ExpireOfferInput<
  TAccountPayer extends string = string,
  TAccountOffer extends string = string,
  TAccountMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountMakerShareAccount extends string = string,
  TAccountEscrowMarker extends string = string,
  TAccountShareTokenProgram extends string = string,
> = {
  /**
   * Permissionless after `expires_at` — any signer may pay the fee.
   * Mut: receives the closed escrow marker's rent.
   */
  payer: TransactionSigner<TAccountPayer>;
  offer: Address<TAccountOffer>;
  mint: Address<TAccountMint>;
  escrow: Address<TAccountEscrow>;
  /** The maker reclaims the escrowed share units here. */
  makerShareAccount: Address<TAccountMakerShareAccount>;
  /**
   * Escrow marker for the offer PDA — closed here (rent → payer);
   * expiry is a terminal path.
   */
  escrowMarker: Address<TAccountEscrowMarker>;
  shareTokenProgram: Address<TAccountShareTokenProgram>;
};

export function getExpireOfferInstruction<
  TAccountPayer extends string,
  TAccountOffer extends string,
  TAccountMint extends string,
  TAccountEscrow extends string,
  TAccountMakerShareAccount extends string,
  TAccountEscrowMarker extends string,
  TAccountShareTokenProgram extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: ExpireOfferInput<
    TAccountPayer,
    TAccountOffer,
    TAccountMint,
    TAccountEscrow,
    TAccountMakerShareAccount,
    TAccountEscrowMarker,
    TAccountShareTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ExpireOfferInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountOffer,
  TAccountMint,
  TAccountEscrow,
  TAccountMakerShareAccount,
  TAccountEscrowMarker,
  TAccountShareTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    offer: { value: input.offer ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    escrow: { value: input.escrow ?? null, isWritable: true },
    makerShareAccount: {
      value: input.makerShareAccount ?? null,
      isWritable: true,
    },
    escrowMarker: { value: input.escrowMarker ?? null, isWritable: true },
    shareTokenProgram: {
      value: input.shareTokenProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.offer),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.makerShareAccount),
      getAccountMeta(accounts.escrowMarker),
      getAccountMeta(accounts.shareTokenProgram),
    ],
    data: getExpireOfferInstructionDataEncoder().encode({}),
    programAddress,
  } as ExpireOfferInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountOffer,
    TAccountMint,
    TAccountEscrow,
    TAccountMakerShareAccount,
    TAccountEscrowMarker,
    TAccountShareTokenProgram
  >);
}

export type ParsedExpireOfferInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Permissionless after `expires_at` — any signer may pay the fee.
     * Mut: receives the closed escrow marker's rent.
     */
    payer: TAccountMetas[0];
    offer: TAccountMetas[1];
    mint: TAccountMetas[2];
    escrow: TAccountMetas[3];
    /** The maker reclaims the escrowed share units here. */
    makerShareAccount: TAccountMetas[4];
    /**
     * Escrow marker for the offer PDA — closed here (rent → payer);
     * expiry is a terminal path.
     */
    escrowMarker: TAccountMetas[5];
    shareTokenProgram: TAccountMetas[6];
  };
  data: ExpireOfferInstructionData;
};

export function parseExpireOfferInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedExpireOfferInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      offer: getNextAccount(),
      mint: getNextAccount(),
      escrow: getNextAccount(),
      makerShareAccount: getNextAccount(),
      escrowMarker: getNextAccount(),
      shareTokenProgram: getNextAccount(),
    },
    data: getExpireOfferInstructionDataDecoder().decode(instruction.data),
  };
}
