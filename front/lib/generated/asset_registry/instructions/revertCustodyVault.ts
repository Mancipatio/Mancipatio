/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findOpenCustodyVaultEscrowMarkerPda } from "../pdas";
import { ASSET_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const REVERT_CUSTODY_VAULT_DISCRIMINATOR = new Uint8Array([
  21, 190, 66, 255, 156, 110, 2, 55,
]);

export function getRevertCustodyVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REVERT_CUSTODY_VAULT_DISCRIMINATOR,
  );
}

export type RevertCustodyVaultInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountShareClass extends string | AccountMeta<string> = string,
  TAccountCustodyVault extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountEscrowMarker extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountAuthorityAdminRecord extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountShareClass extends string
        ? WritableAccount<TAccountShareClass>
        : TAccountShareClass,
      TAccountCustodyVault extends string
        ? WritableAccount<TAccountCustodyVault>
        : TAccountCustodyVault,
      TAccountMint extends string
        ? WritableAccount<TAccountMint>
        : TAccountMint,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountEscrowMarker extends string
        ? WritableAccount<TAccountEscrowMarker>
        : TAccountEscrowMarker,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountAuthorityAdminRecord extends string
        ? ReadonlyAccount<TAccountAuthorityAdminRecord>
        : TAccountAuthorityAdminRecord,
      ...TRemainingAccounts,
    ]
  >;

export type RevertCustodyVaultInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RevertCustodyVaultInstructionDataArgs = {};

export function getRevertCustodyVaultInstructionDataEncoder(): FixedSizeEncoder<RevertCustodyVaultInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: REVERT_CUSTODY_VAULT_DISCRIMINATOR,
    }),
  );
}

export function getRevertCustodyVaultInstructionDataDecoder(): FixedSizeDecoder<RevertCustodyVaultInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRevertCustodyVaultInstructionDataCodec(): FixedSizeCodec<
  RevertCustodyVaultInstructionDataArgs,
  RevertCustodyVaultInstructionData
> {
  return combineCodec(
    getRevertCustodyVaultInstructionDataEncoder(),
    getRevertCustodyVaultInstructionDataDecoder(),
  );
}

export type RevertCustodyVaultAsyncInput<
  TAccountPayer extends string = string,
  TAccountShareClass extends string = string,
  TAccountCustodyVault extends string = string,
  TAccountMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountEscrowMarker extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountAuthorityAdminRecord extends string = string,
> = {
  /**
   * The vault `authority`, or — once a POSITIVE `deadline` has passed —
   * anyone (`deadline == 0` disables the permissionless path, mirroring
   * `return_custody_vault`). Mut: receives the closed escrow marker's rent.
   */
  payer: TransactionSigner<TAccountPayer>;
  shareClass: Address<TAccountShareClass>;
  custodyVault: Address<TAccountCustodyVault>;
  mint: Address<TAccountMint>;
  escrow: Address<TAccountEscrow>;
  /**
   * Escrow marker for the vault PDA — closed here (rent → payer);
   * revert is a terminal path.
   */
  escrowMarker?: Address<TAccountEscrowMarker>;
  tokenProgram?: Address<TAccountTokenProgram>;
  authorityAdminRecord: Address<TAccountAuthorityAdminRecord>;
};

export async function getRevertCustodyVaultInstructionAsync<
  TAccountPayer extends string,
  TAccountShareClass extends string,
  TAccountCustodyVault extends string,
  TAccountMint extends string,
  TAccountEscrow extends string,
  TAccountEscrowMarker extends string,
  TAccountTokenProgram extends string,
  TAccountAuthorityAdminRecord extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: RevertCustodyVaultAsyncInput<
    TAccountPayer,
    TAccountShareClass,
    TAccountCustodyVault,
    TAccountMint,
    TAccountEscrow,
    TAccountEscrowMarker,
    TAccountTokenProgram,
    TAccountAuthorityAdminRecord
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RevertCustodyVaultInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountShareClass,
    TAccountCustodyVault,
    TAccountMint,
    TAccountEscrow,
    TAccountEscrowMarker,
    TAccountTokenProgram,
    TAccountAuthorityAdminRecord
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    shareClass: { value: input.shareClass ?? null, isWritable: true },
    custodyVault: { value: input.custodyVault ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    escrowMarker: { value: input.escrowMarker ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    authorityAdminRecord: {
      value: input.authorityAdminRecord ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.escrowMarker.value) {
    accounts.escrowMarker.value = await findOpenCustodyVaultEscrowMarkerPda({
      custodyVault: expectAddress(accounts.custodyVault.value),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.custodyVault),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.escrowMarker),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.authorityAdminRecord),
    ],
    data: getRevertCustodyVaultInstructionDataEncoder().encode({}),
    programAddress,
  } as RevertCustodyVaultInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountShareClass,
    TAccountCustodyVault,
    TAccountMint,
    TAccountEscrow,
    TAccountEscrowMarker,
    TAccountTokenProgram,
    TAccountAuthorityAdminRecord
  >);
}

export type RevertCustodyVaultInput<
  TAccountPayer extends string = string,
  TAccountShareClass extends string = string,
  TAccountCustodyVault extends string = string,
  TAccountMint extends string = string,
  TAccountEscrow extends string = string,
  TAccountEscrowMarker extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountAuthorityAdminRecord extends string = string,
> = {
  /**
   * The vault `authority`, or — once a POSITIVE `deadline` has passed —
   * anyone (`deadline == 0` disables the permissionless path, mirroring
   * `return_custody_vault`). Mut: receives the closed escrow marker's rent.
   */
  payer: TransactionSigner<TAccountPayer>;
  shareClass: Address<TAccountShareClass>;
  custodyVault: Address<TAccountCustodyVault>;
  mint: Address<TAccountMint>;
  escrow: Address<TAccountEscrow>;
  /**
   * Escrow marker for the vault PDA — closed here (rent → payer);
   * revert is a terminal path.
   */
  escrowMarker: Address<TAccountEscrowMarker>;
  tokenProgram?: Address<TAccountTokenProgram>;
  authorityAdminRecord: Address<TAccountAuthorityAdminRecord>;
};

export function getRevertCustodyVaultInstruction<
  TAccountPayer extends string,
  TAccountShareClass extends string,
  TAccountCustodyVault extends string,
  TAccountMint extends string,
  TAccountEscrow extends string,
  TAccountEscrowMarker extends string,
  TAccountTokenProgram extends string,
  TAccountAuthorityAdminRecord extends string,
  TProgramAddress extends Address = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
>(
  input: RevertCustodyVaultInput<
    TAccountPayer,
    TAccountShareClass,
    TAccountCustodyVault,
    TAccountMint,
    TAccountEscrow,
    TAccountEscrowMarker,
    TAccountTokenProgram,
    TAccountAuthorityAdminRecord
  >,
  config?: { programAddress?: TProgramAddress },
): RevertCustodyVaultInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountShareClass,
  TAccountCustodyVault,
  TAccountMint,
  TAccountEscrow,
  TAccountEscrowMarker,
  TAccountTokenProgram,
  TAccountAuthorityAdminRecord
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ASSET_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    shareClass: { value: input.shareClass ?? null, isWritable: true },
    custodyVault: { value: input.custodyVault ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    escrowMarker: { value: input.escrowMarker ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    authorityAdminRecord: {
      value: input.authorityAdminRecord ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.custodyVault),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.escrowMarker),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.authorityAdminRecord),
    ],
    data: getRevertCustodyVaultInstructionDataEncoder().encode({}),
    programAddress,
  } as RevertCustodyVaultInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountShareClass,
    TAccountCustodyVault,
    TAccountMint,
    TAccountEscrow,
    TAccountEscrowMarker,
    TAccountTokenProgram,
    TAccountAuthorityAdminRecord
  >);
}

export type ParsedRevertCustodyVaultInstruction<
  TProgram extends string = typeof ASSET_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * The vault `authority`, or — once a POSITIVE `deadline` has passed —
     * anyone (`deadline == 0` disables the permissionless path, mirroring
     * `return_custody_vault`). Mut: receives the closed escrow marker's rent.
     */
    payer: TAccountMetas[0];
    shareClass: TAccountMetas[1];
    custodyVault: TAccountMetas[2];
    mint: TAccountMetas[3];
    escrow: TAccountMetas[4];
    /**
     * Escrow marker for the vault PDA — closed here (rent → payer);
     * revert is a terminal path.
     */
    escrowMarker: TAccountMetas[5];
    tokenProgram: TAccountMetas[6];
    authorityAdminRecord: TAccountMetas[7];
  };
  data: RevertCustodyVaultInstructionData;
};

export function parseRevertCustodyVaultInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRevertCustodyVaultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      shareClass: getNextAccount(),
      custodyVault: getNextAccount(),
      mint: getNextAccount(),
      escrow: getNextAccount(),
      escrowMarker: getNextAccount(),
      tokenProgram: getNextAccount(),
      authorityAdminRecord: getNextAccount(),
    },
    data: getRevertCustodyVaultInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
