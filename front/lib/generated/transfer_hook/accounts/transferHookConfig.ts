/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getRestrictionModeDecoder,
  getRestrictionModeEncoder,
  type RestrictionMode,
  type RestrictionModeArgs,
} from "../types";

export const TRANSFER_HOOK_CONFIG_DISCRIMINATOR = new Uint8Array([
  168, 122, 191, 221, 204, 201, 149, 88,
]);

export function getTransferHookConfigDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TRANSFER_HOOK_CONFIG_DISCRIMINATOR,
  );
}

export type TransferHookConfig = {
  discriminator: ReadonlyUint8Array;
  mint: Address;
  shareClass: Address;
  /** Blocklist registry consulted on every transfer (sanctions / court order). */
  blocklist: Address;
  restrictionMode: RestrictionMode;
  /** Required when `restriction_mode == KycGated`; otherwise `None`. */
  kycRegistry: Option<Address>;
  version: number;
  bump: number;
};

export type TransferHookConfigArgs = {
  mint: Address;
  shareClass: Address;
  /** Blocklist registry consulted on every transfer (sanctions / court order). */
  blocklist: Address;
  restrictionMode: RestrictionModeArgs;
  /** Required when `restriction_mode == KycGated`; otherwise `None`. */
  kycRegistry: OptionOrNullable<Address>;
  version: number;
  bump: number;
};

/** Gets the encoder for {@link TransferHookConfigArgs} account data. */
export function getTransferHookConfigEncoder(): Encoder<TransferHookConfigArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["mint", getAddressEncoder()],
      ["shareClass", getAddressEncoder()],
      ["blocklist", getAddressEncoder()],
      ["restrictionMode", getRestrictionModeEncoder()],
      ["kycRegistry", getOptionEncoder(getAddressEncoder())],
      ["version", getU8Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: TRANSFER_HOOK_CONFIG_DISCRIMINATOR,
    }),
  );
}

/** Gets the decoder for {@link TransferHookConfig} account data. */
export function getTransferHookConfigDecoder(): Decoder<TransferHookConfig> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["mint", getAddressDecoder()],
    ["shareClass", getAddressDecoder()],
    ["blocklist", getAddressDecoder()],
    ["restrictionMode", getRestrictionModeDecoder()],
    ["kycRegistry", getOptionDecoder(getAddressDecoder())],
    ["version", getU8Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link TransferHookConfig} account data. */
export function getTransferHookConfigCodec(): Codec<
  TransferHookConfigArgs,
  TransferHookConfig
> {
  return combineCodec(
    getTransferHookConfigEncoder(),
    getTransferHookConfigDecoder(),
  );
}

export function decodeTransferHookConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<TransferHookConfig, TAddress>;
export function decodeTransferHookConfig<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<TransferHookConfig, TAddress>;
export function decodeTransferHookConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<TransferHookConfig, TAddress>
  | MaybeAccount<TransferHookConfig, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getTransferHookConfigDecoder(),
  );
}

export async function fetchTransferHookConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<TransferHookConfig, TAddress>> {
  const maybeAccount = await fetchMaybeTransferHookConfig(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeTransferHookConfig<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<TransferHookConfig, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeTransferHookConfig(maybeAccount);
}

export async function fetchAllTransferHookConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<TransferHookConfig>[]> {
  const maybeAccounts = await fetchAllMaybeTransferHookConfig(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeTransferHookConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<TransferHookConfig>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeTransferHookConfig(maybeAccount),
  );
}
