/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  findBlocklistAuthorityPda,
  findConfigPda,
  findExtraAccountMetaListPda,
} from "../pdas";
import { TRANSFER_HOOK_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";
import {
  getRestrictionModeDecoder,
  getRestrictionModeEncoder,
  type RestrictionMode,
  type RestrictionModeArgs,
} from "../types";

export const UPDATE_TRANSFER_HOOK_CONFIG_DISCRIMINATOR = new Uint8Array([
  143, 24, 119, 189, 12, 223, 226, 26,
]);

export function getUpdateTransferHookConfigDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_TRANSFER_HOOK_CONFIG_DISCRIMINATOR,
  );
}

export type UpdateTransferHookConfigInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountBlocklistAuthority extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountExtraAccountMetaList extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountBlocklistAuthority extends string
        ? ReadonlyAccount<TAccountBlocklistAuthority>
        : TAccountBlocklistAuthority,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountExtraAccountMetaList extends string
        ? WritableAccount<TAccountExtraAccountMetaList>
        : TAccountExtraAccountMetaList,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateTransferHookConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  restrictionMode: RestrictionMode;
  kycRegistry: Option<Address>;
};

export type UpdateTransferHookConfigInstructionDataArgs = {
  restrictionMode: RestrictionModeArgs;
  kycRegistry: OptionOrNullable<Address>;
};

export function getUpdateTransferHookConfigInstructionDataEncoder(): Encoder<UpdateTransferHookConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["restrictionMode", getRestrictionModeEncoder()],
      ["kycRegistry", getOptionEncoder(getAddressEncoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_TRANSFER_HOOK_CONFIG_DISCRIMINATOR,
    }),
  );
}

export function getUpdateTransferHookConfigInstructionDataDecoder(): Decoder<UpdateTransferHookConfigInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["restrictionMode", getRestrictionModeDecoder()],
    ["kycRegistry", getOptionDecoder(getAddressDecoder())],
  ]);
}

export function getUpdateTransferHookConfigInstructionDataCodec(): Codec<
  UpdateTransferHookConfigInstructionDataArgs,
  UpdateTransferHookConfigInstructionData
> {
  return combineCodec(
    getUpdateTransferHookConfigInstructionDataEncoder(),
    getUpdateTransferHookConfigInstructionDataDecoder(),
  );
}

export type UpdateTransferHookConfigAsyncInput<
  TAccountAuthority extends string = string,
  TAccountBlocklistAuthority extends string = string,
  TAccountMint extends string = string,
  TAccountConfig extends string = string,
  TAccountExtraAccountMetaList extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * Must be the `BlocklistAuthority.authority` (Mancipatio admin multisig);
   * pays the rent top-up when the meta list grows.
   */
  authority: TransactionSigner<TAccountAuthority>;
  blocklistAuthority?: Address<TAccountBlocklistAuthority>;
  mint: Address<TAccountMint>;
  config?: Address<TAccountConfig>;
  /** program, i.e. initialized); resized and rewritten in the handler. */
  extraAccountMetaList?: Address<TAccountExtraAccountMetaList>;
  systemProgram?: Address<TAccountSystemProgram>;
  restrictionMode: UpdateTransferHookConfigInstructionDataArgs["restrictionMode"];
  kycRegistry: UpdateTransferHookConfigInstructionDataArgs["kycRegistry"];
};

export async function getUpdateTransferHookConfigInstructionAsync<
  TAccountAuthority extends string,
  TAccountBlocklistAuthority extends string,
  TAccountMint extends string,
  TAccountConfig extends string,
  TAccountExtraAccountMetaList extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: UpdateTransferHookConfigAsyncInput<
    TAccountAuthority,
    TAccountBlocklistAuthority,
    TAccountMint,
    TAccountConfig,
    TAccountExtraAccountMetaList,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateTransferHookConfigInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountBlocklistAuthority,
    TAccountMint,
    TAccountConfig,
    TAccountExtraAccountMetaList,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    blocklistAuthority: {
      value: input.blocklistAuthority ?? null,
      isWritable: false,
    },
    mint: { value: input.mint ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
    extraAccountMetaList: {
      value: input.extraAccountMetaList ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.blocklistAuthority.value) {
    accounts.blocklistAuthority.value = await findBlocklistAuthorityPda();
  }
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({
      mint: expectAddress(accounts.mint.value),
    });
  }
  if (!accounts.extraAccountMetaList.value) {
    accounts.extraAccountMetaList.value = await findExtraAccountMetaListPda({
      mint: expectAddress(accounts.mint.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.blocklistAuthority),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.extraAccountMetaList),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getUpdateTransferHookConfigInstructionDataEncoder().encode(
      args as UpdateTransferHookConfigInstructionDataArgs,
    ),
    programAddress,
  } as UpdateTransferHookConfigInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountBlocklistAuthority,
    TAccountMint,
    TAccountConfig,
    TAccountExtraAccountMetaList,
    TAccountSystemProgram
  >);
}

export type UpdateTransferHookConfigInput<
  TAccountAuthority extends string = string,
  TAccountBlocklistAuthority extends string = string,
  TAccountMint extends string = string,
  TAccountConfig extends string = string,
  TAccountExtraAccountMetaList extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * Must be the `BlocklistAuthority.authority` (Mancipatio admin multisig);
   * pays the rent top-up when the meta list grows.
   */
  authority: TransactionSigner<TAccountAuthority>;
  blocklistAuthority: Address<TAccountBlocklistAuthority>;
  mint: Address<TAccountMint>;
  config: Address<TAccountConfig>;
  /** program, i.e. initialized); resized and rewritten in the handler. */
  extraAccountMetaList: Address<TAccountExtraAccountMetaList>;
  systemProgram?: Address<TAccountSystemProgram>;
  restrictionMode: UpdateTransferHookConfigInstructionDataArgs["restrictionMode"];
  kycRegistry: UpdateTransferHookConfigInstructionDataArgs["kycRegistry"];
};

export function getUpdateTransferHookConfigInstruction<
  TAccountAuthority extends string,
  TAccountBlocklistAuthority extends string,
  TAccountMint extends string,
  TAccountConfig extends string,
  TAccountExtraAccountMetaList extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: UpdateTransferHookConfigInput<
    TAccountAuthority,
    TAccountBlocklistAuthority,
    TAccountMint,
    TAccountConfig,
    TAccountExtraAccountMetaList,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateTransferHookConfigInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountBlocklistAuthority,
  TAccountMint,
  TAccountConfig,
  TAccountExtraAccountMetaList,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    blocklistAuthority: {
      value: input.blocklistAuthority ?? null,
      isWritable: false,
    },
    mint: { value: input.mint ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
    extraAccountMetaList: {
      value: input.extraAccountMetaList ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.blocklistAuthority),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.extraAccountMetaList),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getUpdateTransferHookConfigInstructionDataEncoder().encode(
      args as UpdateTransferHookConfigInstructionDataArgs,
    ),
    programAddress,
  } as UpdateTransferHookConfigInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountBlocklistAuthority,
    TAccountMint,
    TAccountConfig,
    TAccountExtraAccountMetaList,
    TAccountSystemProgram
  >);
}

export type ParsedUpdateTransferHookConfigInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Must be the `BlocklistAuthority.authority` (Mancipatio admin multisig);
     * pays the rent top-up when the meta list grows.
     */
    authority: TAccountMetas[0];
    blocklistAuthority: TAccountMetas[1];
    mint: TAccountMetas[2];
    config: TAccountMetas[3];
    /** program, i.e. initialized); resized and rewritten in the handler. */
    extraAccountMetaList: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: UpdateTransferHookConfigInstructionData;
};

export function parseUpdateTransferHookConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateTransferHookConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      blocklistAuthority: getNextAccount(),
      mint: getNextAccount(),
      config: getNextAccount(),
      extraAccountMetaList: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getUpdateTransferHookConfigInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
