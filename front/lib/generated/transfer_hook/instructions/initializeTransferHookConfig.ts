/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findConfigPda } from "../pdas";
import { TRANSFER_HOOK_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";
import {
  getRestrictionModeDecoder,
  getRestrictionModeEncoder,
  type RestrictionMode,
  type RestrictionModeArgs,
} from "../types";

export const INITIALIZE_TRANSFER_HOOK_CONFIG_DISCRIMINATOR = new Uint8Array([
  169, 224, 202, 10, 67, 62, 216, 135,
]);

export function getInitializeTransferHookConfigDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_TRANSFER_HOOK_CONFIG_DISCRIMINATOR,
  );
}

export type InitializeTransferHookConfigInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountShareClass extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountShareClass extends string
        ? ReadonlySignerAccount<TAccountShareClass> &
            AccountSignerMeta<TAccountShareClass>
        : TAccountShareClass,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeTransferHookConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  blocklist: Address;
  restrictionMode: RestrictionMode;
  kycRegistry: Option<Address>;
};

export type InitializeTransferHookConfigInstructionDataArgs = {
  blocklist: Address;
  restrictionMode: RestrictionModeArgs;
  kycRegistry: OptionOrNullable<Address>;
};

export function getInitializeTransferHookConfigInstructionDataEncoder(): Encoder<InitializeTransferHookConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["blocklist", getAddressEncoder()],
      ["restrictionMode", getRestrictionModeEncoder()],
      ["kycRegistry", getOptionEncoder(getAddressEncoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_TRANSFER_HOOK_CONFIG_DISCRIMINATOR,
    }),
  );
}

export function getInitializeTransferHookConfigInstructionDataDecoder(): Decoder<InitializeTransferHookConfigInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["blocklist", getAddressDecoder()],
    ["restrictionMode", getRestrictionModeDecoder()],
    ["kycRegistry", getOptionDecoder(getAddressDecoder())],
  ]);
}

export function getInitializeTransferHookConfigInstructionDataCodec(): Codec<
  InitializeTransferHookConfigInstructionDataArgs,
  InitializeTransferHookConfigInstructionData
> {
  return combineCodec(
    getInitializeTransferHookConfigInstructionDataEncoder(),
    getInitializeTransferHookConfigInstructionDataDecoder(),
  );
}

export type InitializeTransferHookConfigAsyncInput<
  TAccountAuthority extends string = string,
  TAccountMint extends string = string,
  TAccountShareClass extends string = string,
  TAccountConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  mint: Address<TAccountMint>;
  /**
   * The `asset_registry`-owned `ShareClass` account for this mint, as a
   * co-signer. A `ShareClass` is a PDA — it has no private key — so this
   * signature can only come from the asset_registry program's own CPI
   * (`invoke_signed` with the ShareClass seeds). The discriminator check
   * rejects registry-owned-but-keypair-signable shells created via system
   * `allocate`+`assign` (their data is all zeros).
   */
  shareClass: TransactionSigner<TAccountShareClass>;
  config?: Address<TAccountConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  blocklist: InitializeTransferHookConfigInstructionDataArgs["blocklist"];
  restrictionMode: InitializeTransferHookConfigInstructionDataArgs["restrictionMode"];
  kycRegistry: InitializeTransferHookConfigInstructionDataArgs["kycRegistry"];
};

export async function getInitializeTransferHookConfigInstructionAsync<
  TAccountAuthority extends string,
  TAccountMint extends string,
  TAccountShareClass extends string,
  TAccountConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: InitializeTransferHookConfigAsyncInput<
    TAccountAuthority,
    TAccountMint,
    TAccountShareClass,
    TAccountConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeTransferHookConfigInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountMint,
    TAccountShareClass,
    TAccountConfig,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({
      mint: expectAddress(accounts.mint.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeTransferHookConfigInstructionDataEncoder().encode(
      args as InitializeTransferHookConfigInstructionDataArgs,
    ),
    programAddress,
  } as InitializeTransferHookConfigInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountMint,
    TAccountShareClass,
    TAccountConfig,
    TAccountSystemProgram
  >);
}

export type InitializeTransferHookConfigInput<
  TAccountAuthority extends string = string,
  TAccountMint extends string = string,
  TAccountShareClass extends string = string,
  TAccountConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  mint: Address<TAccountMint>;
  /**
   * The `asset_registry`-owned `ShareClass` account for this mint, as a
   * co-signer. A `ShareClass` is a PDA — it has no private key — so this
   * signature can only come from the asset_registry program's own CPI
   * (`invoke_signed` with the ShareClass seeds). The discriminator check
   * rejects registry-owned-but-keypair-signable shells created via system
   * `allocate`+`assign` (their data is all zeros).
   */
  shareClass: TransactionSigner<TAccountShareClass>;
  config: Address<TAccountConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  blocklist: InitializeTransferHookConfigInstructionDataArgs["blocklist"];
  restrictionMode: InitializeTransferHookConfigInstructionDataArgs["restrictionMode"];
  kycRegistry: InitializeTransferHookConfigInstructionDataArgs["kycRegistry"];
};

export function getInitializeTransferHookConfigInstruction<
  TAccountAuthority extends string,
  TAccountMint extends string,
  TAccountShareClass extends string,
  TAccountConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: InitializeTransferHookConfigInput<
    TAccountAuthority,
    TAccountMint,
    TAccountShareClass,
    TAccountConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeTransferHookConfigInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountMint,
  TAccountShareClass,
  TAccountConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    shareClass: { value: input.shareClass ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.shareClass),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeTransferHookConfigInstructionDataEncoder().encode(
      args as InitializeTransferHookConfigInstructionDataArgs,
    ),
    programAddress,
  } as InitializeTransferHookConfigInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountMint,
    TAccountShareClass,
    TAccountConfig,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeTransferHookConfigInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    mint: TAccountMetas[1];
    /**
     * The `asset_registry`-owned `ShareClass` account for this mint, as a
     * co-signer. A `ShareClass` is a PDA — it has no private key — so this
     * signature can only come from the asset_registry program's own CPI
     * (`invoke_signed` with the ShareClass seeds). The discriminator check
     * rejects registry-owned-but-keypair-signable shells created via system
     * `allocate`+`assign` (their data is all zeros).
     */
    shareClass: TAccountMetas[2];
    config: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: InitializeTransferHookConfigInstructionData;
};

export function parseInitializeTransferHookConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeTransferHookConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      mint: getNextAccount(),
      shareClass: getNextAccount(),
      config: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeTransferHookConfigInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
