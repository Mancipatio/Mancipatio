/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findBlocklistAuthorityPda, findTransferPda } from "../pdas";
import { TRANSFER_HOOK_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const PROPOSE_BLOCKLIST_AUTHORITY_DISCRIMINATOR = new Uint8Array([
  127, 135, 141, 118, 167, 128, 150, 173,
]);

export function getProposeBlocklistAuthorityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PROPOSE_BLOCKLIST_AUTHORITY_DISCRIMINATOR,
  );
}

export type ProposeBlocklistAuthorityInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountBlocklistAuthority extends string | AccountMeta<string> = string,
  TAccountTransfer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountBlocklistAuthority extends string
        ? ReadonlyAccount<TAccountBlocklistAuthority>
        : TAccountBlocklistAuthority,
      TAccountTransfer extends string
        ? WritableAccount<TAccountTransfer>
        : TAccountTransfer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ProposeBlocklistAuthorityInstructionData = {
  discriminator: ReadonlyUint8Array;
  newAuthority: Address;
};

export type ProposeBlocklistAuthorityInstructionDataArgs = {
  newAuthority: Address;
};

export function getProposeBlocklistAuthorityInstructionDataEncoder(): FixedSizeEncoder<ProposeBlocklistAuthorityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newAuthority", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: PROPOSE_BLOCKLIST_AUTHORITY_DISCRIMINATOR,
    }),
  );
}

export function getProposeBlocklistAuthorityInstructionDataDecoder(): FixedSizeDecoder<ProposeBlocklistAuthorityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newAuthority", getAddressDecoder()],
  ]);
}

export function getProposeBlocklistAuthorityInstructionDataCodec(): FixedSizeCodec<
  ProposeBlocklistAuthorityInstructionDataArgs,
  ProposeBlocklistAuthorityInstructionData
> {
  return combineCodec(
    getProposeBlocklistAuthorityInstructionDataEncoder(),
    getProposeBlocklistAuthorityInstructionDataDecoder(),
  );
}

export type ProposeBlocklistAuthorityAsyncInput<
  TAccountAuthority extends string = string,
  TAccountBlocklistAuthority extends string = string,
  TAccountTransfer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  blocklistAuthority?: Address<TAccountBlocklistAuthority>;
  transfer?: Address<TAccountTransfer>;
  systemProgram?: Address<TAccountSystemProgram>;
  newAuthority: ProposeBlocklistAuthorityInstructionDataArgs["newAuthority"];
};

export async function getProposeBlocklistAuthorityInstructionAsync<
  TAccountAuthority extends string,
  TAccountBlocklistAuthority extends string,
  TAccountTransfer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: ProposeBlocklistAuthorityAsyncInput<
    TAccountAuthority,
    TAccountBlocklistAuthority,
    TAccountTransfer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ProposeBlocklistAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountBlocklistAuthority,
    TAccountTransfer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    blocklistAuthority: {
      value: input.blocklistAuthority ?? null,
      isWritable: false,
    },
    transfer: { value: input.transfer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.blocklistAuthority.value) {
    accounts.blocklistAuthority.value = await findBlocklistAuthorityPda();
  }
  if (!accounts.transfer.value) {
    accounts.transfer.value = await findTransferPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.blocklistAuthority),
      getAccountMeta(accounts.transfer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getProposeBlocklistAuthorityInstructionDataEncoder().encode(
      args as ProposeBlocklistAuthorityInstructionDataArgs,
    ),
    programAddress,
  } as ProposeBlocklistAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountBlocklistAuthority,
    TAccountTransfer,
    TAccountSystemProgram
  >);
}

export type ProposeBlocklistAuthorityInput<
  TAccountAuthority extends string = string,
  TAccountBlocklistAuthority extends string = string,
  TAccountTransfer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  blocklistAuthority: Address<TAccountBlocklistAuthority>;
  transfer: Address<TAccountTransfer>;
  systemProgram?: Address<TAccountSystemProgram>;
  newAuthority: ProposeBlocklistAuthorityInstructionDataArgs["newAuthority"];
};

export function getProposeBlocklistAuthorityInstruction<
  TAccountAuthority extends string,
  TAccountBlocklistAuthority extends string,
  TAccountTransfer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: ProposeBlocklistAuthorityInput<
    TAccountAuthority,
    TAccountBlocklistAuthority,
    TAccountTransfer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ProposeBlocklistAuthorityInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountBlocklistAuthority,
  TAccountTransfer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    blocklistAuthority: {
      value: input.blocklistAuthority ?? null,
      isWritable: false,
    },
    transfer: { value: input.transfer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.blocklistAuthority),
      getAccountMeta(accounts.transfer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getProposeBlocklistAuthorityInstructionDataEncoder().encode(
      args as ProposeBlocklistAuthorityInstructionDataArgs,
    ),
    programAddress,
  } as ProposeBlocklistAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountBlocklistAuthority,
    TAccountTransfer,
    TAccountSystemProgram
  >);
}

export type ParsedProposeBlocklistAuthorityInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    blocklistAuthority: TAccountMetas[1];
    transfer: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: ProposeBlocklistAuthorityInstructionData;
};

export function parseProposeBlocklistAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProposeBlocklistAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      blocklistAuthority: getNextAccount(),
      transfer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getProposeBlocklistAuthorityInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
