/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findBlockEntryPda, findBlocklistAuthorityPda } from "../pdas";
import { TRANSFER_HOOK_PROGRAM_ADDRESS } from "../programs";
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const REMOVE_FROM_BLOCKLIST_DISCRIMINATOR = new Uint8Array([
  132, 125, 30, 120, 139, 22, 210, 90,
]);

export function getRemoveFromBlocklistDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REMOVE_FROM_BLOCKLIST_DISCRIMINATOR,
  );
}

export type RemoveFromBlocklistInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountBlocklistAuthority extends string | AccountMeta<string> = string,
  TAccountBlockEntry extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountBlocklistAuthority extends string
        ? ReadonlyAccount<TAccountBlocklistAuthority>
        : TAccountBlocklistAuthority,
      TAccountBlockEntry extends string
        ? WritableAccount<TAccountBlockEntry>
        : TAccountBlockEntry,
      ...TRemainingAccounts,
    ]
  >;

export type RemoveFromBlocklistInstructionData = {
  discriminator: ReadonlyUint8Array;
  wallet: Address;
};

export type RemoveFromBlocklistInstructionDataArgs = { wallet: Address };

export function getRemoveFromBlocklistInstructionDataEncoder(): FixedSizeEncoder<RemoveFromBlocklistInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["wallet", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: REMOVE_FROM_BLOCKLIST_DISCRIMINATOR,
    }),
  );
}

export function getRemoveFromBlocklistInstructionDataDecoder(): FixedSizeDecoder<RemoveFromBlocklistInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["wallet", getAddressDecoder()],
  ]);
}

export function getRemoveFromBlocklistInstructionDataCodec(): FixedSizeCodec<
  RemoveFromBlocklistInstructionDataArgs,
  RemoveFromBlocklistInstructionData
> {
  return combineCodec(
    getRemoveFromBlocklistInstructionDataEncoder(),
    getRemoveFromBlocklistInstructionDataDecoder(),
  );
}

export type RemoveFromBlocklistAsyncInput<
  TAccountAuthority extends string = string,
  TAccountBlocklistAuthority extends string = string,
  TAccountBlockEntry extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  blocklistAuthority?: Address<TAccountBlocklistAuthority>;
  blockEntry?: Address<TAccountBlockEntry>;
  wallet: RemoveFromBlocklistInstructionDataArgs["wallet"];
};

export async function getRemoveFromBlocklistInstructionAsync<
  TAccountAuthority extends string,
  TAccountBlocklistAuthority extends string,
  TAccountBlockEntry extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: RemoveFromBlocklistAsyncInput<
    TAccountAuthority,
    TAccountBlocklistAuthority,
    TAccountBlockEntry
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RemoveFromBlocklistInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountBlocklistAuthority,
    TAccountBlockEntry
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    blocklistAuthority: {
      value: input.blocklistAuthority ?? null,
      isWritable: false,
    },
    blockEntry: { value: input.blockEntry ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.blocklistAuthority.value) {
    accounts.blocklistAuthority.value = await findBlocklistAuthorityPda();
  }
  if (!accounts.blockEntry.value) {
    accounts.blockEntry.value = await findBlockEntryPda({
      wallet: expectSome(args.wallet),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.blocklistAuthority),
      getAccountMeta(accounts.blockEntry),
    ],
    data: getRemoveFromBlocklistInstructionDataEncoder().encode(
      args as RemoveFromBlocklistInstructionDataArgs,
    ),
    programAddress,
  } as RemoveFromBlocklistInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountBlocklistAuthority,
    TAccountBlockEntry
  >);
}

export type RemoveFromBlocklistInput<
  TAccountAuthority extends string = string,
  TAccountBlocklistAuthority extends string = string,
  TAccountBlockEntry extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  blocklistAuthority: Address<TAccountBlocklistAuthority>;
  blockEntry: Address<TAccountBlockEntry>;
  wallet: RemoveFromBlocklistInstructionDataArgs["wallet"];
};

export function getRemoveFromBlocklistInstruction<
  TAccountAuthority extends string,
  TAccountBlocklistAuthority extends string,
  TAccountBlockEntry extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: RemoveFromBlocklistInput<
    TAccountAuthority,
    TAccountBlocklistAuthority,
    TAccountBlockEntry
  >,
  config?: { programAddress?: TProgramAddress },
): RemoveFromBlocklistInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountBlocklistAuthority,
  TAccountBlockEntry
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    blocklistAuthority: {
      value: input.blocklistAuthority ?? null,
      isWritable: false,
    },
    blockEntry: { value: input.blockEntry ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.blocklistAuthority),
      getAccountMeta(accounts.blockEntry),
    ],
    data: getRemoveFromBlocklistInstructionDataEncoder().encode(
      args as RemoveFromBlocklistInstructionDataArgs,
    ),
    programAddress,
  } as RemoveFromBlocklistInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountBlocklistAuthority,
    TAccountBlockEntry
  >);
}

export type ParsedRemoveFromBlocklistInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    blocklistAuthority: TAccountMetas[1];
    blockEntry: TAccountMetas[2];
  };
  data: RemoveFromBlocklistInstructionData;
};

export function parseRemoveFromBlocklistInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRemoveFromBlocklistInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      blocklistAuthority: getNextAccount(),
      blockEntry: getNextAccount(),
    },
    data: getRemoveFromBlocklistInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
