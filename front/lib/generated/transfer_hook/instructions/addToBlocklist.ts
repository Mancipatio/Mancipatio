/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findBlockEntryPda, findBlocklistAuthorityPda } from "../pdas";
import { TRANSFER_HOOK_PROGRAM_ADDRESS } from "../programs";
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const ADD_TO_BLOCKLIST_DISCRIMINATOR = new Uint8Array([
  201, 138, 75, 216, 252, 201, 26, 106,
]);

export function getAddToBlocklistDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADD_TO_BLOCKLIST_DISCRIMINATOR,
  );
}

export type AddToBlocklistInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountBlocklistAuthority extends string | AccountMeta<string> = string,
  TAccountBlockEntry extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountBlocklistAuthority extends string
        ? ReadonlyAccount<TAccountBlocklistAuthority>
        : TAccountBlocklistAuthority,
      TAccountBlockEntry extends string
        ? WritableAccount<TAccountBlockEntry>
        : TAccountBlockEntry,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AddToBlocklistInstructionData = {
  discriminator: ReadonlyUint8Array;
  wallet: Address;
};

export type AddToBlocklistInstructionDataArgs = { wallet: Address };

export function getAddToBlocklistInstructionDataEncoder(): FixedSizeEncoder<AddToBlocklistInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["wallet", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: ADD_TO_BLOCKLIST_DISCRIMINATOR }),
  );
}

export function getAddToBlocklistInstructionDataDecoder(): FixedSizeDecoder<AddToBlocklistInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["wallet", getAddressDecoder()],
  ]);
}

export function getAddToBlocklistInstructionDataCodec(): FixedSizeCodec<
  AddToBlocklistInstructionDataArgs,
  AddToBlocklistInstructionData
> {
  return combineCodec(
    getAddToBlocklistInstructionDataEncoder(),
    getAddToBlocklistInstructionDataDecoder(),
  );
}

export type AddToBlocklistAsyncInput<
  TAccountAuthority extends string = string,
  TAccountBlocklistAuthority extends string = string,
  TAccountBlockEntry extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  blocklistAuthority?: Address<TAccountBlocklistAuthority>;
  blockEntry?: Address<TAccountBlockEntry>;
  systemProgram?: Address<TAccountSystemProgram>;
  wallet: AddToBlocklistInstructionDataArgs["wallet"];
};

export async function getAddToBlocklistInstructionAsync<
  TAccountAuthority extends string,
  TAccountBlocklistAuthority extends string,
  TAccountBlockEntry extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: AddToBlocklistAsyncInput<
    TAccountAuthority,
    TAccountBlocklistAuthority,
    TAccountBlockEntry,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AddToBlocklistInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountBlocklistAuthority,
    TAccountBlockEntry,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    blocklistAuthority: {
      value: input.blocklistAuthority ?? null,
      isWritable: false,
    },
    blockEntry: { value: input.blockEntry ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.blocklistAuthority.value) {
    accounts.blocklistAuthority.value = await findBlocklistAuthorityPda();
  }
  if (!accounts.blockEntry.value) {
    accounts.blockEntry.value = await findBlockEntryPda({
      wallet: expectSome(args.wallet),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.blocklistAuthority),
      getAccountMeta(accounts.blockEntry),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAddToBlocklistInstructionDataEncoder().encode(
      args as AddToBlocklistInstructionDataArgs,
    ),
    programAddress,
  } as AddToBlocklistInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountBlocklistAuthority,
    TAccountBlockEntry,
    TAccountSystemProgram
  >);
}

export type AddToBlocklistInput<
  TAccountAuthority extends string = string,
  TAccountBlocklistAuthority extends string = string,
  TAccountBlockEntry extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  blocklistAuthority: Address<TAccountBlocklistAuthority>;
  blockEntry: Address<TAccountBlockEntry>;
  systemProgram?: Address<TAccountSystemProgram>;
  wallet: AddToBlocklistInstructionDataArgs["wallet"];
};

export function getAddToBlocklistInstruction<
  TAccountAuthority extends string,
  TAccountBlocklistAuthority extends string,
  TAccountBlockEntry extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: AddToBlocklistInput<
    TAccountAuthority,
    TAccountBlocklistAuthority,
    TAccountBlockEntry,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AddToBlocklistInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountBlocklistAuthority,
  TAccountBlockEntry,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    blocklistAuthority: {
      value: input.blocklistAuthority ?? null,
      isWritable: false,
    },
    blockEntry: { value: input.blockEntry ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.blocklistAuthority),
      getAccountMeta(accounts.blockEntry),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAddToBlocklistInstructionDataEncoder().encode(
      args as AddToBlocklistInstructionDataArgs,
    ),
    programAddress,
  } as AddToBlocklistInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountBlocklistAuthority,
    TAccountBlockEntry,
    TAccountSystemProgram
  >);
}

export type ParsedAddToBlocklistInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    blocklistAuthority: TAccountMetas[1];
    blockEntry: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: AddToBlocklistInstructionData;
};

export function parseAddToBlocklistInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAddToBlocklistInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      blocklistAuthority: getNextAccount(),
      blockEntry: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAddToBlocklistInstructionDataDecoder().decode(instruction.data),
  };
}
